% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{flag_reference_errors}
\alias{flag_reference_errors}
\title{Flag mislabeled sequences in the reference database}
\usage{
flag_reference_errors(
  raw_df,
  mislabel_threshold = 0.02,
  return_all = FALSE,
  min_coverage = NULL,
  singleton_match_threshold = 0.98,
  verified_clean = NULL
)
}
\arguments{
\item{raw_df}{Data frame of pairwise match scores, as returned by
\code{build_sequence_matrix()}.  Must contain columns \code{id_x}, \code{id_y},
\code{species.x}, \code{species.y}, and \code{p_match} (raw scores on the 0-100 or 0-1
scale used consistently within the matrix).}

\item{mislabel_threshold}{Numeric scalar (default \code{0.02}).  A reference
sequence is flagged when its median within-species match score minus its
maximum cross-species match score is below negative \code{mislabel_threshold}.
The default 0.02 means a sequence is flagged if its best foreign match is
within 2 percentage points of its typical self-match.  Adjust based on
reference quality: lower values are stricter, flagging more sequences;
higher values are more permissive, suitable for noisier markers.}

\item{return_all}{Logical (default \code{FALSE}).  If \code{TRUE}, returns all
sequences including those flagged \code{"clean"}.}

\item{min_coverage}{Numeric or \code{NULL} (default \code{NULL}). See
\code{.compute_reference_qc_stats()}'s own documentation of this same
parameter -- excludes a pair from every statistic below when its
\code{coverage} (real per-pair alignment overlap, from \code{build_sequence_matrix()})
falls below this threshold, catching e.g. two same-length sequences
that cover different, non-overlapping parts of the gene.}

\item{singleton_match_threshold}{Numeric scalar in (0, 1] (default \code{0.98}).
A singleton (no within-species neighbours) is flagged
\code{"unverified_singleton_high_match"} when its best cross-species match
exceeds this value. 98\% identity is the conventional barcode gap
threshold for many markers; for ITS (fungi), where within-species
variation is higher, consider raising to \code{0.99}. Previously a hardcoded
literal with a comment suggesting it be raised for ITS but no actual way
for a caller to do so -- now a real parameter, with the exact same
default and comparison (\code{>}) as before.}

\item{verified_clean}{Character vector of \code{id_x} accessions (default
\code{NULL}), or \code{NULL} to disable. Forces these accessions to \code{error_type = "clean"} regardless of what the within-reference-set heuristic above
computes for them. Exists because this heuristic is known to over-flag
(a 2026-08-08 audit against \code{TaxaMatch::evaluate_reference_accessions()}
-- a BLAST-based check against a broad, independent database -- found
0 of 40 randomly-sampled real \code{"likely_mislabeled"} flags on one real
dataset were confirmed as genuine mislabels; most were same-submission-
batch artifacts or tight-congener false positives this function's
narrow same-reference-set comparison cannot distinguish from a real
error). \code{train_likelihood_model()} calls this function unconditionally
on every training run; by default (\code{mislabel_behavior = "flag"}, see
that function's own docs) it no longer removes anything on this
heuristic's say-so, but if a caller explicitly opts into
\code{mislabel_behavior = "remove"}, an accession independently confirmed
clean by a stronger check would otherwise be re-flagged and re-removed
on every retrain without this parameter. Supply the accessions
\code{TaxaMatch::evaluate_reference_accessions()}
verdicts \code{"congruent"} (or \code{"insufficient_independent_evidence"}, if
you choose to trust that too) here to keep them in training under
\code{"remove"} mode. Does not
change \code{"clean"}-when-return_all rows, and does not force the OPPOSITE
direction (an accession this heuristic calls \code{"clean"} is never forced
to \code{"likely_mislabeled"} -- this parameter only rescues, never removes).}
}
\value{
A data frame with one row per unique \code{id_x} and columns:
\describe{
\item{\code{id_x}}{Sequence identifier.}
\item{\code{species_x}}{Species label assigned to the sequence.}
\item{\code{median_self_match}}{Median match score to within-species sequences.}
\item{\code{max_foreign_match}}{Best match score to any cross-species sequence.}
\item{\code{n_self_neighbors}}{Number of within-species neighbours.}
\item{\code{integrity_gap}}{\code{median_self_match - max_foreign_match}.}
\item{\code{error_type}}{\code{"likely_mislabeled"}, \code{"unverified_singleton_high_match"},
or \code{"clean"}.}
}
}
\description{
Examines a pairwise distance matrix (output of \code{build_sequence_matrix()})
and flags sequences whose best match is to a \emph{different} species than their
own label -- a pattern consistent with mislabeling or contamination.
}
\note{
For a fully runnable, non-\verb{\dontrun{}} demonstration, see
\code{inst/review_function_inputs.R} Section 2 in the package source.
}
\section{Scope}{

This function applies to DNA sequence reference databases only.  For
acoustic and image data, users bring pre-classified candidate outputs
(classifier confidence scores); mislabel detection via score distributions
is not applicable in that context.

Two error types are returned:
\itemize{
\item \strong{\code{"likely_mislabeled"}} -- the sequence's median within-species match is
lower than its best cross-species match by more than \code{mislabel_threshold}.
\item \strong{\code{"unverified_singleton_high_match"}} -- the sequence has no within-species
neighbours yet matches a foreign species at >= 0.98; it may be mislabeled or
simply the only representative of its species.
}
}

\examples{
\dontrun{
ref_matrix <- build_sequence_matrix(reference_df,
                                     rank_system = c("family", "genus", "species"))
flagged <- flag_reference_errors(ref_matrix, mislabel_threshold = 0.02)
table(flagged$error_type)
}

}
\seealso{
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}}, \code{\link[=train_likelihood_model]{train_likelihood_model()}}
}
