% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_recordings.R
\name{fetch_reference_recordings}
\alias{fetch_reference_recordings}
\title{Fetch Reference Recordings from Xeno-canto}
\usage{
fetch_reference_recordings(
  species,
  quality = c("A", "B"),
  type = NULL,
  max_per_species = 50L,
  api_key = Sys.getenv("XC_API_KEY"),
  download = FALSE,
  download_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{species}{Character vector. Scientific names to query
(e.g. \code{c("Turdus migratorius", "Setophaga petechia")}). Each name is
queried separately. Genus-only queries are supported.}

\item{quality}{Character vector. Xeno-canto quality grades to include:
\code{"A"} (best) through \code{"E"} (worst). Default \code{c("A", "B")}.}

\item{type}{Character. Recording type filter, e.g. \code{"song"}, \code{"call"},
\code{"alarm call"}. Default \code{NULL} (no filter). Xeno-canto type tags are
free-text; common values are \code{"song"} and \code{"call"}.}

\item{max_per_species}{Integer. Maximum recordings to return per species
after quality-sorting (A before B, etc.). Default \code{50L}.}

\item{api_key}{Character. Xeno-canto API v3 key. Required. Defaults to the
\code{XC_API_KEY} environment variable. Every registered XC member with a
verified email address has one; look yours up at
\verb{https://xeno-canto.org/account}.}

\item{download}{Logical. If \code{TRUE}, download audio files to \code{download_dir}.
Default \code{FALSE} (metadata only).}

\item{download_dir}{Character. Directory for downloaded audio. Default
\code{NULL} creates \verb{xeno-canto-recordings/} inside \code{tempdir()}. Ignored when
\code{download = FALSE}.}

\item{verbose}{Logical. Print per-species progress messages. Default \code{TRUE}.}
}
\value{
A data frame with one row per recording, containing:
\describe{
\item{\code{recording_id}}{Xeno-canto identifier, e.g. \code{"XC123456"}.}
\item{\code{species}}{Scientific binomial (\code{genus} + species epithet).}
\item{\code{genus}}{Genus name.}
\item{\code{common_name}}{English common name.}
\item{\code{quality}}{Quality grade (\code{A}–\code{E}).}
\item{\code{type}}{Recording type tag (e.g. \code{"song"}).}
\item{\code{country}}{Country where recording was made.}
\item{\code{location}}{Locality description.}
\item{\code{lat}, \code{lng}}{Coordinates (numeric; \code{NA} if absent).}
\item{\code{duration_s}}{Duration in seconds (parsed from \code{"m:ss"}).}
\item{\code{date}}{Recording date (\code{"YYYY-MM-DD"}).}
\item{\code{license}}{Creative Commons license URL.}
\item{\code{file_url}}{Direct download URL for the audio file.}
\item{\code{also_species}}{Background species in the recording (comma-separated).}
\item{\code{local_path}}{Local file path if \code{download = TRUE}; \code{NA} otherwise.}
}
The \code{"xc_query"} attribute stores the original \code{species} argument.
}
\description{
Queries the Xeno-canto API v3 for bird sound recordings matching a list
of species. Returns a metadata table suitable for building a reference set
to train a TaxaLikely acoustic likelihood model. Optionally downloads audio
files for local BirdNET-Analyzer processing.
}
\details{
\strong{Xeno-canto API v3:} Queries the Xeno-canto API v3
(\url{https://xeno-canto.org/api/3/recordings}). An API key is required —
register at \url{https://xeno-canto.org/account} and store your key in the
\code{XC_API_KEY} environment variable (e.g., in \verb{~/.Renviron}). The API
returns up to 500 recordings per page; pagination is handled automatically.

\strong{Reference training workflow:}
\enumerate{
\item Call \code{fetch_reference_recordings()} to get metadata + download URLs.
\item Download audio files (\code{download = TRUE}) or use the \code{file_url} column.
\item Run BirdNET-Analyzer on the downloaded recordings.
\item Read BirdNET output with \code{\link[TaxaMatch:read_birdnet_output]{TaxaMatch::read_birdnet_output()}}.
\item Label each detection as H1/H2/H3 by joining BirdNET's identified species
back to the Xeno-canto ground-truth label (via \code{source_file} /
\code{local_path}): H1 = correct species, H2 = wrong species (same genus or
close relative in your species list), H3 = wrong genus.
\item Train with \code{\link[=train_likelihood_model]{train_likelihood_model()}}.
}

\strong{Background species:} The \code{also_species} column lists other species
audible in the recording. Consider filtering these out from your training
set — a BirdNET detection of a background species is not a false positive
in the way you want H2 to represent.

\strong{Polite API use:} A 0.5-second delay is inserted between paginated
requests. Xeno-canto asks that automated clients not overwhelm the API.
For large species lists, run during off-peak hours.
}
\examples{
\dontrun{
# Fetch high-quality song recordings for two species (metadata only)
recs <- fetch_reference_recordings(
  species         = c("Turdus migratorius", "Setophaga petechia"),
  quality         = c("A", "B"),
  type            = "song",
  max_per_species = 30L
)
head(recs[, c("recording_id", "species", "quality", "duration_s", "file_url")])

# Download audio for model training
recs <- fetch_reference_recordings(
  species      = "Turdus migratorius",
  quality      = "A",
  download     = TRUE,
  download_dir = "reference_audio/"
)
# Run BirdNET-Analyzer on reference_audio/, then:
# TaxaMatch::read_birdnet_output("birdnet_results/")
}
}
\seealso{
\code{\link[=train_likelihood_model]{train_likelihood_model()}},
\link[TaxaMatch:read_birdnet_output]{TaxaMatch::read_birdnet_output()}
}
