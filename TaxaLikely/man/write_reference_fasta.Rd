% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_fasta.R
\name{write_reference_fasta}
\alias{write_reference_fasta}
\title{Write a Reference Data Frame to FASTA Format}
\usage{
write_reference_fasta(
  reference_df,
  file,
  taxonomy_file = NULL,
  rank_system = NULL
)
}
\arguments{
\item{reference_df}{Data frame with columns \code{composite_id}, \code{sequence}, and
at least one taxonomy column from \code{rank_system}.
Output of \code{\link[=fetch_reference_sequences]{fetch_reference_sequences()}}, \code{\link[=read_reference_fasta]{read_reference_fasta()}}, or
\code{\link[=read_crabs_output]{read_crabs_output()}}.}

\item{file}{Character. Output path for the FASTA file
(e.g., \code{"reference.fasta"}).}

\item{taxonomy_file}{Character or \code{NULL}.
If not NULL, write a companion 2-column taxonomy TSV to this path.
The file can be passed to \code{\link[=read_reference_fasta]{read_reference_fasta()}} as \code{taxonomy_file}.}

\item{rank_system}{Character vector of taxonomy columns to include, coarse
to fine. Defaults to all taxonomy-like columns found in \code{reference_df}
(everything except \code{composite_id} and \code{sequence}).}
}
\value{
Invisibly returns \code{reference_df}. Called for its side-effect of
writing files.
}
\description{
Exports a \code{reference_df} (from \code{\link[=fetch_reference_sequences]{fetch_reference_sequences()}} or
\code{\link[=read_reference_fasta]{read_reference_fasta()}}) to a FASTA file compatible with BLAST, CRABS,
Obitools, and other external tools. Optionally writes a companion taxonomy
TSV in the same positional format accepted by
\code{\link[=read_reference_fasta]{read_reference_fasta()}}, making the export fully round-trippable.
}
\details{
The FASTA header format is:
\code{>{composite_id} {rank1_value} {rank2_value} ...}
with taxonomy values joined by single spaces (missing ranks omitted).
This is the most widely compatible header format for downstream tools.

The optional taxonomy TSV is a 2-column tab-delimited file (no header):
column 1 = \code{composite_id}, column 2 = semicolon-separated taxonomy values
in prefix-style format (e.g. \verb{f__Fundulidae;g__Fundulus;s__Fundulus heteroclitus}).
Prefix-style is used rather than positional format because positional parsing
assumes a full 7-rank hierarchy from kingdom; any subset of ranks would
map to the wrong positions. This file can be passed directly to
\code{\link[=read_reference_fasta]{read_reference_fasta()}} as \code{taxonomy_file} to reload the reference.
}
\examples{
\dontrun{
ref <- fetch_reference_sequences(
  taxa        = "Fundulus",
  barcode_term = "MiFishU"
)
write_reference_fasta(ref, "fundulus_mifish.fasta",
                      taxonomy_file = "fundulus_mifish_taxonomy.tsv")

# Round-trip: reload the same reference
ref2 <- read_reference_fasta("fundulus_mifish.fasta",
                              taxonomy_file = "fundulus_mifish_taxonomy.tsv",
                              rank_system   = c("family", "genus", "species"))
}

}
\seealso{
\code{\link[=read_reference_fasta]{read_reference_fasta()}}, \code{\link[=fetch_reference_sequences]{fetch_reference_sequences()}},
\code{\link[=read_crabs_output]{read_crabs_output()}}, \code{\link[=build_site_reference]{build_site_reference()}}
}
