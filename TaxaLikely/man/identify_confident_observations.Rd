% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_query_noise.R
\name{identify_confident_observations}
\alias{identify_confident_observations}
\title{Identify confident, non-circular observations for query-noise calibration}
\usage{
identify_confident_observations(
  match_df,
  priors,
  plausibility_threshold = 0.001
)
}
\arguments{
\item{match_df}{Data frame. Canonical match object (e.g. \code{match_obj_restored})
with \code{observation_id}, \code{genus}, and a score column (\code{score_original},
\code{score}, or \code{p_match}, checked in that order).}

\item{priors}{Data frame. Occurrence-based priors (e.g. \code{TaxaExpect} output)
with \code{taxon_name}, \code{taxon_name_rank}, and \code{theta_mean}.}

\item{plausibility_threshold}{Numeric (default \code{1e-3}). Minimum \code{theta_mean}
for a species to count as "locally plausible" in a genus. Values are
typically well-separated from background/floor priors (often ~1e-6); the
default is a conservative gap above that floor.}
}
\value{
Data frame: one row per confident \code{observation_id}, with all
\code{match_df} columns plus \code{confident_genus} (the genus that qualified) and
\code{confident_species} (its single plausible species, from \code{priors}).
}
\description{
Finds genera where exactly one species clears a local occurrence-plausibility
threshold in \code{priors} (e.g. \code{TaxaExpect} output), then returns the best-scoring
row per \code{observation_id} for every \code{match_df} observation whose genus is one of
those. Because the single-plausible-species call comes from independent
occurrence/range data -- not from \code{match_df}'s own scores or the likelihood
model being calibrated -- these observations can be treated as (very likely)
correct species identifications without circularity.
}
\examples{
match_df <- data.frame(
  observation_id = paste0("Q", 1:5),
  genus          = "Genusone",
  score_original = c(95, 96, 94, 97, 93)
)
priors <- data.frame(
  taxon_name      = "Genusone speciesa",
  taxon_name_rank = "species",
  theta_mean      = 0.5
)
identify_confident_observations(match_df, priors)

}
\seealso{
\code{\link[=calibrate_query_noise]{calibrate_query_noise()}}
}
