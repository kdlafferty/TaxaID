% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate_coverage_filter}
\alias{calibrate_coverage_filter}
\title{Calibrate a coverage filter for reference pairs}
\usage{
calibrate_coverage_filter(
  ref_pairs,
  rank_system = NULL,
  thresholds = seq(0, 0.99, by = 0.05)
)
}
\arguments{
\item{ref_pairs}{Data frame.  Output of \code{\link[=build_sequence_matrix]{build_sequence_matrix()}}.  Must
contain columns \code{coverage}, \code{p_match}, \code{id_x}, and paired \verb{\{rank\}.x} /
\verb{\{rank\}.y} columns for H1/H2 classification.}

\item{rank_system}{Character vector of rank names coarse-to-fine
(e.g., \code{c("genus", "species")}), used to identify the finest rank column
for H1 vs H2/H3 classification.  Default \code{NULL} auto-detects from paired
\code{.x}/\code{.y} columns via \link[TaxaTools:standard_ranks]{TaxaTools::standard_ranks}.}

\item{thresholds}{Numeric vector of coverage thresholds to evaluate.
Default \code{seq(0, 0.99, by = 0.05)}.  Each value is a minimum: pairs with
\code{coverage >= threshold} are retained.}
}
\value{
A data frame with one row per threshold and columns:
\describe{
\item{\code{threshold}}{Coverage threshold evaluated.}
\item{\code{n_queries}}{Unique \code{id_x} values with at least one surviving pair.}
\item{\code{breadth}}{\code{n_queries / total_queries}, range 0 to 1.}
\item{\code{h1_pairs}}{Count of H1 pairs retained at this threshold.}
\item{\code{h2_pairs}}{Count of H2/H3 pairs retained at this threshold.}
\item{\code{h1_retention}}{\code{h1_pairs / total_h1_unfiltered}, range 0 to 1.}
\item{\code{h2_retention}}{\code{h2_pairs / total_h2_unfiltered}, range 0 to 1.}
\item{\code{youden_j}}{\verb{h1_retention − h2_retention}.  Maximised at the
Pareto-optimal threshold.  \code{NA} when H1/H2 classification is
unavailable.}
\item{\code{discrimination}}{\code{h1_retention / max(h2_retention, 1e-9)}.
Ratio form; \code{NA} when unavailable.}
\item{\code{mean_h1_score}}{Mean \code{p_match} of retained H1 pairs; \code{NA} when
no H1 pairs survive or H1/H2 classification is unavailable.}
}
}
\description{
Sweeps a grid of coverage thresholds over the pairwise reference dataset
produced by \code{\link[=build_sequence_matrix]{build_sequence_matrix()}} and
returns per-threshold metrics quantifying the trade-off between retaining
query breadth and discriminating H1 (within-species) pairs from H2/H3
(cross-species) noise.
}
\section{Motivation}{

Match scores (\code{p_match}) measure how similar two sequences or audio clips
are, but they do not capture \emph{how much} of each observation contributed to
that score.  A 99\% DNA identity computed from a 50 bp fragment of a 600 bp
barcode is far less reliable than the same identity computed from a 580 bp
overlap — yet both produce the same \code{p_match} value.  Similarly, a high
BirdNET confidence from a rain-soaked recording of a distant bird carries
less information than the same score from a quiet, close-range recording.

The \code{coverage} column added by \code{\link[=build_sequence_matrix]{build_sequence_matrix()}} (pairwise alignment
overlap fraction, 0--1) places sequences on a common scale.  Removing low-coverage
pairs before model training filters out low-information observations that can
blur the H1/H2 boundary and inflate model variance.

The threshold that maximises Youden's J is Pareto-optimal: it retains the
most H1 information while discarding the most H2/H3 noise.
}

\section{Metrics returned}{

\describe{
\item{\code{breadth}}{Fraction of unique queries (\code{id_x}) that still have at
least one pair surviving the filter.  Decreasing breadth means some
observations are completely excluded from training.}
\item{\code{h1_retention}}{Fraction of within-species (H1) pairs retained
relative to the unfiltered dataset.  Analogous to sensitivity in ROC
analysis.}
\item{\code{h2_retention}}{Fraction of cross-species (H2/H3) pairs retained.
Analogous to (1 − specificity).}
\item{\code{youden_j}}{Youden's J statistic: \verb{h1_retention − h2_retention}.
Bounded from -1 to 1; equals 0 at the no-filter baseline and is
maximised at the Pareto-optimal threshold.  Treats H1 retention and
H2/H3 exclusion symmetrically and has no divide-by-zero edge case.
\emph{This is the primary criterion for threshold selection.}}
\item{\code{discrimination}}{Ratio \code{h1_retention / h2_retention} (epsilon-guarded
at 1e-9).  The multiplicative form of the same signal; useful for
log-scale plots.  Equals 1.0 at the no-filter baseline; rises above 1.0
when H1 pairs carry systematically higher coverage than H2/H3 pairs.}
\item{\code{mean_h1_score}}{Mean \code{p_match} among retained H1 pairs.  Should
be stable or increase slightly as low-coverage (and typically
lower-scoring) H1 pairs are removed.}
}
}

\section{Acoustic data note}{

Xeno-canto quality grades are properties of the \emph{recording}, not the
individual detection pair.  Every pair produced from the same recording —
both H1 and H2/H3 — carries the same \code{coverage} value.  Filtering by
coverage therefore removes entire recordings and excludes H1 and H2/H3
pairs in equal proportion.  For acoustic reference data, \code{youden_j} and
\code{discrimination} will be near-flat across thresholds.  Use \code{breadth} and
\code{mean_h1_score} as the primary guides to decide which quality grades to
include.  The function detects categorical coverage (≤ 10 unique values)
and emits a message in this case.
}

\examples{
\dontrun{
ref_matrix <- build_sequence_matrix(reference_df)

cal <- calibrate_coverage_filter(ref_matrix)

# Select the Pareto-optimal threshold (maximises Youden's J)
best <- cal[which.max(cal$youden_j), ]
cat("Optimal threshold:", best$threshold,
    "| J =", round(best$youden_j, 3),
    "| breadth =", round(best$breadth, 3), "\n")

# Visualise the breadth vs discrimination trade-off
plot(cal$breadth, cal$youden_j, type = "b",
     xlab = "Breadth (fraction of queries retained)",
     ylab = "Youden's J (H1 retention - H2 retention)",
     main = "Coverage filter calibration")
abline(v = best$breadth, lty = 2, col = "red")

# Apply the chosen threshold before training
ref_filtered <- ref_matrix[ref_matrix$coverage >= best$threshold, ]
model <- train_likelihood_model(ref_filtered)
}

}
\seealso{
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}},
\code{\link[=coverage_threshold]{coverage_threshold()}}, \code{\link[=train_likelihood_model]{train_likelihood_model()}}, \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}
}
