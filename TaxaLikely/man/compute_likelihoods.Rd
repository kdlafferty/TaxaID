% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_likelihoods.R
\name{compute_likelihoods}
\alias{compute_likelihoods}
\title{Compute likelihoods from a match object (unified pipeline wrapper)}
\usage{
compute_likelihoods(
  match_df,
  score_type,
  model_params = NULL,
  rank_system = NULL,
  include_unreferenced_family = FALSE,
  score_col = "score_original",
  score_sharpness = 0.1,
  ratio_threshold = 0.01,
  min_match_threshold = 0.5,
  n_sims = 0L,
  min_coverage = NULL,
  verbose = FALSE
)
}
\arguments{
\item{match_df}{Data frame. Canonical match object from
\code{TaxaMatch::standardize_match_data()} or equivalent. Must contain
\code{observation_id}, \code{taxon_name}, \code{taxon_name_rank}, and
taxonomy columns. A \code{score_original} column is required unless
\code{score_type = "none"}.}

\item{score_type}{Character scalar. One of \code{"none"},
\code{"probability"}, \code{"similarity_softmax"}, or
\code{"similarity"}.  See \code{\link[=assign_scores]{assign_scores()}} for full descriptions.}

\item{model_params}{Object of class \code{"taxa_model_params"} from
\code{\link[=train_likelihood_model]{train_likelihood_model()}}.  Required when
\code{score_type = "similarity"}; ignored otherwise.}

\item{rank_system}{Character vector of rank names coarse to fine.
\code{NULL} (default) auto-detects from \code{match_df}.}

\item{include_unreferenced_family}{Logical (default \code{FALSE}). Passed
to \code{\link[=unreferenced_candidates]{unreferenced_candidates()}}. See that function for guidance.}

\item{score_col}{Character (default \code{"score_original"}). Passed to
\code{\link[=assign_scores]{assign_scores()}}.}

\item{score_sharpness}{Positive numeric (default \code{0.1}). Passed to
\code{\link[=assign_scores]{assign_scores()}}; applies to \code{score_type = "similarity_softmax"}
only.}

\item{ratio_threshold}{Numeric (default \code{0.01}). Passed to
\code{\link[=model_likelihoods]{model_likelihoods()}}; applies to \code{score_type = "similarity"} only.}

\item{min_match_threshold}{Numeric (default \code{0.50}). Passed to
\code{\link[=model_likelihoods]{model_likelihoods()}}; applies to \code{score_type = "similarity"} only.}

\item{n_sims}{Integer (default \code{0L}). Monte Carlo simulations. Passed
to \code{\link[=model_likelihoods]{model_likelihoods()}}; applies to \code{score_type = "similarity"}
only.}

\item{min_coverage}{Numeric or \code{NULL}. Coverage pre-filter. Passed to
\code{\link[=model_likelihoods]{model_likelihoods()}}; applies to \code{score_type = "similarity"} only.}

\item{verbose}{Logical (default \code{FALSE}). Passed to
\code{\link[=model_likelihoods]{model_likelihoods()}}.}
}
\value{
A named list:
\describe{
\item{\code{$likelihoods}}{Data frame with one row per
\code{observation_id} x hypothesis, containing
\code{observation_id}, \code{taxon_name}, \code{taxon_name_rank},
\code{hypothesis_type}, \code{score_likelihood},
\code{score_likelihood_mean}, \code{score_likelihood_sd}, and
\code{score_method}.  Rows with \code{NA} \code{taxon_name} are
excluded except for \code{"unreferenced_family"} rows (which have
\code{taxon_name = NA} by design).}
\item{\code{$unresolved}}{Rows from \code{match_df} for queries
that produced no usable likelihoods. Empty data frame if none.
Non-empty only when \code{score_type = "similarity"}.}
}
}
\description{
Orchestrates the three-step unified likelihood pipeline:
\code{\link[=unreferenced_candidates]{unreferenced_candidates()}} → \code{\link[=assign_scores]{assign_scores()}} → \code{\link[=model_likelihoods]{model_likelihoods()}}
(similarity pathway only).
}
\details{
This is the recommended high-level entry point for new workflows.  For the
bivariate-normal similarity pathway it is equivalent to
\code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}.  For other data types it replaces
\code{expand_consensus_candidates()} with a consistent interface.
}
\examples{
\dontrun{
# No-score pathway (morphology or upranked consensus)
result <- compute_likelihoods(match_df, score_type = "none")
head(result$likelihoods)

# Probability pathway (BirdNET or iNaturalist CV)
result <- compute_likelihoods(match_df, score_type = "probability")

# Similarity pathway (eDNA BLAST + trained model)
result <- compute_likelihoods(
  match_df, score_type = "similarity",
  model_params = model,
  rank_system  = c("family", "genus", "species"),
  n_sims       = 200L
)
}

}
\seealso{
\code{\link[=unreferenced_candidates]{unreferenced_candidates()}}, \code{\link[=assign_scores]{assign_scores()}},
\code{\link[=model_likelihoods]{model_likelihoods()}}, \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}
}
