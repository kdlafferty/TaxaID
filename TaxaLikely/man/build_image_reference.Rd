% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_image.R
\name{build_image_reference}
\alias{build_image_reference}
\title{Build an Image-Classifier Reference Dataset for Likelihood Model Training}
\usage{
build_image_reference(
  image_df,
  images_meta,
  rank_system = c("genus", "species"),
  min_confidence = 0
)
}
\arguments{
\item{image_df}{Data frame. Output of \code{\link[TaxaMatch:read_animl_output]{TaxaMatch::read_animl_output()}},
\code{\link[TaxaMatch:read_inaturalist_cv_output]{TaxaMatch::read_inaturalist_cv_output()}}, or any classifier reader with
\code{top_n >= 2} recommended.  Required columns: \code{observation_id}, \code{score},
\code{species}, \code{genus}, \code{source_file}.}

\item{images_meta}{Data frame. Ground-truth species labels for reference
images.  Required columns: \code{image_path} (path to the image file — the
file stem is used as the join key) plus any columns named in
\code{rank_system} (e.g., \code{species}, \code{genus}).  Optional: \code{testid} (model
stratification category), \code{quality} (numeric 0--1 image quality score
used as \code{coverage} when \code{image_df} lacks a \code{coverage} column).}

\item{rank_system}{Character vector of rank names coarse-to-fine.  Default
\code{c("genus", "species")}.  Must be present in both \code{image_df} and
\code{images_meta}.}

\item{min_confidence}{Numeric. Drop classifier detections below this
threshold before building pairs.  Default \code{0} (keep all).}
}
\value{
A data frame with one row per (image x classifier detection) pair,
containing:
\describe{
\item{\code{id_x}}{\code{observation_id} of the reference image (the query).}
\item{\code{id_y}}{Synthetic unique identifier for the detection
(\code{"{observation_id}_det{rank}"}).}
\item{\code{p_match}}{Classifier confidence (0--1).}
\item{\verb{\{rank\}.x}}{Ground-truth taxonomy from \code{images_meta}.}
\item{\verb{\{rank\}.y}}{Classifier-detected taxonomy from \code{image_df}.}
\item{\code{coverage}}{Numeric image quality on a 0 to 1 scale.  Sourced
from \code{image_df$coverage} (bounding box area fraction) if present,
otherwise from \code{images_meta$quality}, otherwise \code{NA}.  Use
\code{\link[=coverage_threshold]{coverage_threshold()}} or \code{\link[=calibrate_coverage_filter]{calibrate_coverage_filter()}} to select
which images to include before calling \code{\link[=train_likelihood_model]{train_likelihood_model()}}.}
\item{\code{testid}}{Image category from \code{images_meta$testid}; \code{NA} if
absent.  Filter on this column to train type-specific models.}
\item{\code{source_file}}{CSV filename from \code{image_df} for traceability.}
}
}
\description{
Joins image classifier detections to ground-truth species labels and
produces a pairwise training dataset in the same format as
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}} and \code{\link[=build_acoustic_reference]{build_acoustic_reference()}}, ready for
\code{\link[=train_likelihood_model]{train_likelihood_model()}}.
}
\details{
Each reference image becomes a query (\code{id_x}).  Each classifier
detection for that image becomes a candidate match (\code{id_y}).
Detections where the classifier found the correct species produce H1
(within-species) rows; detections of wrong species produce H2/H3
(cross-species) rows.  The match score is classifier confidence (0--1).

\strong{Analogies across data types:}\tabular{lllll}{
   Data type \tab Reference unit \tab Join key \tab Coverage \tab testid \cr
   DNA \tab sequence \tab accession \tab alignment \tab barcode marker \cr
   Acoustic \tab audio file \tab file stem \tab recording quality \tab recording type \cr
   Image \tab image file \tab file stem \tab bbox area \tab classifier / image type \cr
}


\strong{Controlling for image type:} The \code{testid} column in \code{images_meta}
(optional) stores a category for model stratification — e.g.,
\code{"camera_trap"} vs \code{"hand_held"}, or classifier name.
Train a separate model per type, exactly as you would train a separate model
per barcode marker in eDNA or per recording type in acoustics.

\strong{Coverage:} If \code{image_df} includes a \code{coverage} column (produced by
\code{\link[TaxaMatch:read_animl_output]{TaxaMatch::read_animl_output()}} when \code{bbox_cols} is supplied), that value
is passed through directly to the output.  Coverage represents the
fractional bounding-box area of the image: a small value (distant or
partial animal) signals weaker classification evidence.  Where \code{coverage}
is absent from \code{image_df} but \code{images_meta$quality} is present (0--1
numeric), that quality value is used instead.  If neither is available,
\code{coverage} is \code{NA}.

\strong{Gap computation:} Supply \code{top_n >= 2} to the reader function
(\code{\link[TaxaMatch:read_animl_output]{TaxaMatch::read_animl_output()}}, etc.) so each image has at least two
candidates.  The gap (top-1 minus top-2 confidence) is the key
discriminator in \code{\link[=train_likelihood_model]{train_likelihood_model()}}.

\strong{Join key:} \code{observation_id} from \code{image_df} (the image filename stem as
produced by the reader functions) is matched against
\code{tools::file_path_sans_ext(basename(image_path))} from \code{images_meta}.
Ensure image filenames are unique across your reference collection.

\strong{Output format compatibility:} The \code{.x}/\code{.y} suffix convention and
\code{p_match} column name match the output of \code{\link[=build_sequence_matrix]{build_sequence_matrix()}} and
\code{\link[=build_acoustic_reference]{build_acoustic_reference()}}, so \code{\link[=train_likelihood_model]{train_likelihood_model()}} and
\code{\link[=flag_reference_errors]{flag_reference_errors()}} accept this data frame directly.

\strong{Non-wildlife detections:} Remove \code{"empty"}, \code{"human"}, \code{"vehicle"} rows
from \code{image_df} before calling this function; they produce uninformative
H2/H3 pairs.  See \code{\link[TaxaMatch:read_animl_output]{TaxaMatch::read_animl_output()}} for details.
}
\examples{
\dontrun{
# 1. Prepare ground-truth reference images
# Each row = one reference image + true species label
images_meta <- data.frame(
  image_path = c("ref_images/img_deer_001.jpg",
                 "ref_images/img_deer_002.jpg",
                 "ref_images/img_rabbit_001.jpg"),
  species    = c("Odocoileus virginianus", "Odocoileus virginianus",
                 "Sylvilagus floridanus"),
  genus      = c("Odocoileus", "Odocoileus", "Sylvilagus"),
  testid     = "camera_trap",
  stringsAsFactors = FALSE
)

# 2. Run Animl on reference images (or any other classifier)
animl_df <- TaxaMatch::read_animl_output(
  "animl_ref_results/",
  min_confidence = 0.1,
  top_n          = 3L,
  bbox_cols      = c(w = "bbox_w", h = "bbox_h")
) |>
  subset(!species \%in\% c("empty", "human", "vehicle"))

# 3. Build pairwise training dataset
ref_pairs <- build_image_reference(
  image_df    = animl_df,
  images_meta = images_meta,
  rank_system = c("genus", "species")
)

# 4. Train model
model_camera <- train_likelihood_model(
  raw_df      = ref_pairs,
  rank_system = c("genus", "species")
)
}
}
\seealso{
\code{\link[TaxaMatch:read_animl_output]{TaxaMatch::read_animl_output()}},
\link[TaxaMatch:read_inaturalist_cv_output]{TaxaMatch::read_inaturalist_cv_output()},
\code{\link[=build_acoustic_reference]{build_acoustic_reference()}}, \code{\link[=train_likelihood_model]{train_likelihood_model()}}
}
