% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_sequence.R
\name{check_cross_genus_sampling_noise}
\alias{check_cross_genus_sampling_noise}
\title{How Much Does the Random Cross-Genus Draw Move the Estimate?}
\usage{
check_cross_genus_sampling_noise(
  reference_df,
  rank_system = NULL,
  max_dist = 0.25,
  min_seq_len = 100L,
  max_seq_len = 2000L,
  filter_unnamed = TRUE,
  max_seqs_per_taxon = NULL,
  barcode_term = NULL,
  max_foreign_reps_per_genus = 20L,
  n_replicates = 5L
)
}
\arguments{
\item{reference_df, rank_system, max_dist, min_seq_len, max_seq_len, filter_unnamed, max_seqs_per_taxon, barcode_term, max_foreign_reps_per_genus}{Forwarded to \code{build_sequence_matrix(by_genus = TRUE, ...)}, identically
for every replicate.}

\item{n_replicates}{Integer (default \code{5L}). How many independent random
representative draws to compare. Each draw consumes the caller's RNG
state (\code{sample()}, same convention as \code{max_seqs_per_taxon}) -- do NOT
call \code{set.seed()} between replicates, or every "replicate" would be
identical.}
}
\value{
A list:
\describe{
\item{\code{replicates}}{Data frame, one row per replicate:
\code{replicate}, \code{n_cross_genus_pairs}, \code{mean_p_match},
\code{median_p_match}, \code{sd_p_match}.}
\item{\code{summary}}{Named list: the range (min, max) and coefficient of
variation (sd/mean) of \code{mean_p_match} across replicates -- the
single number worth looking at first.}
}
}
\description{
\code{build_sequence_matrix(by_genus = TRUE)} represents each genus by ONE
randomly-drawn sequence when building the cross-genus sample that feeds H3
(and H2's pooled fallback). This re-runs that whole draw-and-align step
\code{n_replicates} times and reports how much the resulting cross-genus pair
distribution moves across replicates -- a transparency report, like
\code{TaxaExpect::kernel_budget_sensitivity()}'s own per-group budget table, not a
pass/fail gate. There is no universal "good enough" spread to check
against; the point is to make the spread visible so you can judge whether
it is small relative to what H3 actually does (a coarse, already-pooled
fallback likelihood shift for genuinely unreferenced species -- not a
species-specific parameter).
}
\section{Cost}{

Each replicate re-runs the FULL \code{by_genus = TRUE} pipeline (including the
within-genus alignments, not just the cheap cross-genus step) -- simpler
and safer than threading a special reduced-recompute path through
\code{build_sequence_matrix()}'s internals, at the cost of some real redundant
work. Because \code{by_genus = TRUE} already made the whole pipeline
substantially cheaper than one whole-set alignment (see
\code{build_sequence_matrix()}'s own \verb{@section Per-genus alignment}),
\code{n_replicates} repeats of it can still cost meaningfully LESS than a single
whole-set run would have -- but this is still real, additive NCBI-free
compute cost, not free; run it once when adopting \code{by_genus = TRUE} for a
new marker, not on every training run.
}

\seealso{
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}}, \code{TaxaExpect::kernel_budget_sensitivity()}
}
