% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{interpret_model}
\alias{interpret_model}
\title{Summarise a trained likelihood model}
\usage{
interpret_model(model_params, print_report = TRUE)
}
\arguments{
\item{model_params}{Object of class \code{"taxa_model_params"} returned by
\code{\link[=train_likelihood_model]{train_likelihood_model()}}.}

\item{print_report}{Logical (default \code{TRUE}).  If \code{FALSE}, suppresses
console output.}
}
\value{
An invisible named list with elements:
\describe{
\item{\code{hypothesis_baselines}}{Data frame: expected match \% and expected
gap \% for H1, H2, H3. The gap is the difference between the best
match and the runner-up. H2/H3 have gap near 0 because when the true
species or genus is absent, no candidate has a clear advantage.}
\item{\code{global_h1}}{Data frame: global mean score, gap, and SD.}
\item{\code{hierarchy}}{Data frame: per-rank counts and averages.}
\item{\code{species_thresholds}}{Data frame: per-species expected match \%, gap
\%, and status classification.}
\item{\code{raw_sigma}}{The 2x2 covariance matrix from the model.}
}
}
\description{
Converts the internal logit-space parameters of a \code{"taxa_model_params"}
object back to interpretable percentages and prints a formatted report.
Returns the summary tables invisibly for programmatic access.
}
\examples{
\dontrun{
model <- train_likelihood_model(ref_matrix,
                                rank_system = c("family", "genus", "species"))
summary_df <- interpret_model(model)
}

}
\seealso{
\code{\link[=train_likelihood_model]{train_likelihood_model()}}, \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}
}
