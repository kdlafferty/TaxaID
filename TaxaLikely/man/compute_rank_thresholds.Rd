% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support_curves.R
\name{compute_rank_thresholds}
\alias{compute_rank_thresholds}
\title{Derive marker-specific rank_thresholds via per-rank Youden's J}
\usage{
compute_rank_thresholds(
  seq_matrix,
  rank_system = NULL,
  prior_weight = 10,
  threshold_grid = seq(0, 100, by = 1)
)
}
\arguments{
\item{seq_matrix}{Data frame of pairwise match scores, as returned by
\code{build_sequence_matrix()}, for the SAME marker/reference database
\code{score_consensus()} will be scoring against. Every pair must have known
taxonomy on both sides (species/genus/family), which is exactly what
\code{build_sequence_matrix()} already produces.}

\item{rank_system}{Character vector of rank names, coarse to fine, e.g.
\code{c("family", "genus", "species")}. Default \code{NULL} auto-detects from
\code{.x}-suffixed columns in \code{seq_matrix} (same convention as
\code{train_likelihood_model()}).}

\item{prior_weight}{Numeric. Empirical Bayes shrinkage weight for the
per-genus/per-family curves (default \code{10.0}, matching
\code{train_likelihood_model()}'s own default).}

\item{threshold_grid}{Numeric vector of percent-identity thresholds to
search over. Default \code{seq(0, 100, by = 1)}.}
}
\value{
A named numeric vector, a subset of \code{c(species=, genus=, family=)}
depending on what \code{rank_system}/\code{seq_matrix} make computable -- directly
usable as \code{TaxaAssign::score_consensus(rank_thresholds = ...)}. Errors
if nothing is computable at all (fewer than 2 usable rank levels).
}
\description{
A real, exported, marker-agnostic version of
\code{diagnostics/score_floor_roc_sweep.R}'s per-rank Youden's J logic: given a
\code{build_sequence_matrix()}-style pairwise distance matrix for YOUR marker
and reference database, returns the percent-identity threshold at each
taxonomic rank (species/genus/family) that maximizes true-positive minus
false-positive rate for that rank's own, correctly-matched positive/
negative classes -- species tier: TP = within-species, FP = congeneric;
genus tier: TP = same-genus, FP = confamilial; family tier: TP =
same-family, FP = cross-family. Reuses the identical genus-/family-
equal-weighted, Empirical-Bayes-shrunk pooling machinery
\code{train_likelihood_model()} stores in \code{model_params$Confusion_Risk_Curves} (see
\code{.compute_rank_score_curves()}), so the two stay consistent with each
other rather than duplicating the correction independently.
}
\details{
Exists specifically because \code{TaxaAssign::score_consensus()}'s
\code{rank_thresholds} has no safe universal default (see that function's own
documentation) -- this is the "derive your own from real reference data"
option that error message points at.
}
\examples{
\dontrun{
ref_matrix <- build_sequence_matrix(reference_df,
  rank_system = c("family", "genus", "species")
)
rt <- compute_rank_thresholds(ref_matrix,
  rank_system = c("family", "genus", "species")
)
TaxaAssign::score_consensus(match_df, rank_thresholds = rt)
}

}
\seealso{
\code{\link[=train_likelihood_model]{train_likelihood_model()}}, \code{\link[=build_sequence_matrix]{build_sequence_matrix()}}
}
