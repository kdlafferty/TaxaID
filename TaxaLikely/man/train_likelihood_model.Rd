% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{train_likelihood_model}
\alias{train_likelihood_model}
\title{Train the hierarchical likelihood model on reference-vs-reference scores}
\usage{
train_likelihood_model(
  raw_df,
  rank_system = NULL,
  score_bounds = NULL,
  min_observed_sigma = NULL,
  prior_weight = 10,
  use_hierarchy = TRUE,
  anchor_perfect = TRUE,
  logit_epsilon = 1e-04,
  max_gap_ceiling = NULL,
  score_transform = "logit",
  min_pair_coverage = 0.8,
  shrinkage = c("empirical_bayes", "fixed")
)
}
\arguments{
\item{raw_df}{Data frame of pairwise match scores, as returned by
\code{build_sequence_matrix()}.  Passed through \code{.prep_training_data()}.}

\item{rank_system}{Character vector of rank names, \strong{coarse to fine}
(e.g., \code{c("family", "genus", "species")}). Default \code{NULL} auto-detects
from the \code{.x}-suffixed columns in \code{raw_df}.}

\item{score_bounds}{Optional \code{c(min, max)} for score normalization.}

\item{min_observed_sigma}{Numeric or \code{NULL} (default).  Floor on observed
within-species score variance.  Prevents overfitting on species with very
low variance (e.g., a species with two nearly identical reference
sequences).  \code{NULL} resolves to \code{1.0} for \code{score_transform = "logit"}
(in logit space, 1.0 corresponds to meaningful within-species variation)
or the equivalent rescaled value for \code{"sqrt_mismatch"} -- see
\code{score_transform} below.}

\item{prior_weight}{Numeric.  Equivalent pseudo-sample size for Empirical
Bayes shrinkage toward the global mean.  Controls how strongly
species-specific estimates are regularized.  A value of 10 means each
species estimate is pulled toward the global mean as if 10 additional
observations at the global mean had been added.  Higher values produce
more conservative (less species-specific) estimates; lower values trust
per-species data more but risk overfitting for species with few
references.  Default \code{10.0}.}

\item{use_hierarchy}{Logical (default \code{TRUE}).  If \code{TRUE} and \code{lme4} is
available, fits random intercepts per rank level to stabilize estimates
across the taxonomic hierarchy.}

\item{anchor_perfect}{Logical (default \code{TRUE}).  If \code{TRUE}, injects
synthetic perfect-match observations into the H1 training data to
prevent the perfection penalty (see section below).}

\item{logit_epsilon}{Numeric.  Logit-clipping value (default \code{1e-4}).
Used only when \code{score_transform = "logit"}.}

\item{max_gap_ceiling}{Numeric or \code{NULL} (default).  Gap cap.  \code{NULL}
resolves to \code{5.0} for \code{score_transform = "logit"} (roughly the gap
between 99.3\% and 50\% identity, in logit units) or \code{0.6234} for
\code{"sqrt_mismatch"} (the same 99.3\%-vs-50\% reference gap on that scale) --
prevents extreme outliers from dominating model estimates either way.}

\item{score_transform}{Character, \code{"logit"} (default) or \code{"sqrt_mismatch"}.
The scale H1/H2/H3 are all modeled on -- must be shared across all three
hypotheses, since they are compared via density ratios at the same
observed point (a valid likelihood ratio requires one consistent scale;
mixing transforms across hypotheses would need an explicit
change-of-variables correction that this package does not implement).
\code{"logit"} (\code{log(p/(1-p))}) is a standard scale. \code{"sqrt_mismatch"}
(\code{-sqrt(1-p)}) exists because real 12S congener data
showed \code{"logit"} gives a genuinely backwards answer for one of the
package's more novel claims: whether a genus's species are hard to tell
apart (small, consistent divergence) or easy to tell apart (larger,
more variable divergence). On the raw match-proportion scale, tight
genera show lower congener-score variance, as expected -- but under
\code{"logit"}, this can reverse sign, because nearly all real barcode
matches sit close to 100\% identity, exactly where logit's derivative
diverges fastest. \code{"sqrt_mismatch"} treats divergence as a rare-event
count (few mismatches out of many aligned bases -- the regime real data
is actually in), for which square-root is the classical
variance-stabilizing transform; it recovers the correct qualitative
ordering where \code{"logit"} does not. See \code{evaluate_likelihoods()}'s own
documentation for the full empirical derivation, including why
several other standard transforms (probit, complementary log-log) were
checked and rejected. \code{"sqrt_mismatch"} is bounded to \verb{[-1, 0]} rather
than unbounded like \code{"logit"} -- a Gaussian fit to it is technically an
approximation for that reason, but a mild one in practice, since real
observations concentrate near 0 (good matches), far from the -1
boundary. \strong{Not yet ported to this scale}: \code{calibrate_query_noise()}
and \code{evaluate_likelihoods()}'s \code{evidence_col}/\code{min_coverage} mechanisms
all assume \code{"logit"} internally and will error rather than silently
produce wrong numbers if combined with a \code{"sqrt_mismatch"}-trained model.}

\item{min_pair_coverage}{Numeric in (0, 1] or \code{NULL} (default \code{0.8}).  The
alignment-coverage floor a reference-vs-reference pair must meet before it
may DEFINE a reference's best foreign match, best congener match, or best
conspecific match (\code{coverage} column from \code{\link[=build_sequence_matrix]{build_sequence_matrix()}}).
Must equal the coverage floor the match object was built under --
\code{TaxaMatch::blast_sequences(min_query_coverage = 80)} is \code{0.8} here --
so that training and inference see the same pair population;
\code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}'s check against the match object is
one-directional -- it warns only when the match object admits LOWER
coverage than this model's own floor (a 0.01 tolerance), never the
other way around.  This is NOT a data filter: no pair is removed from the
data and no species is ever dropped (a reference with no qualifying
conspecific pair falls back to its best one).  \code{NULL} disables the
floor; a \code{raw_df} without a \code{coverage} column skips it with a message.
See the "Alignment-coverage floor" section.}

\item{shrinkage}{\code{"empirical_bayes"} (default) or \code{"fixed"}.  How each
species' H1 mean score and mean gap are shrunk toward the global mean.
\code{"fixed"} is the original \code{N / (N + prior_weight)} weight.
\code{"empirical_bayes"} estimates the real between-species variance of the
means (\code{tau^2}, method of moments, per dimension) and weights each
species by \code{tau^2 / (tau^2 + sigma^2 / N)}: no signal beyond noise
collapses every species to the global mean, real signal lets
well-referenced species keep more of their own.  The estimate is
reported in \code{Stats$tau2_score}/\code{Stats$tau2_gap} and the per-species
weights in \code{H1_Lookup$shrink_w_score}/\code{shrink_w_gap}.  Variance
shrinkage always uses the fixed weight.  Needs at least 3 species;
otherwise falls back to \code{"fixed"} with a message.}
}
\value{
A named list (class \code{"taxa_model_params"}) with slots:
\describe{
\item{\code{H1_Lookup}}{Data frame with per-species parameters: \code{lookup_key},
\code{rank}, \code{mu_score}, \code{mu_gap}, \code{sigma_score}, \code{n_obs_species} (number of
within-species reference sequences trained on -- used by
\code{evaluate_likelihoods()} to weight the Monte Carlo uncertainty of the
trained mean; see that function's own documentation).}
\item{\code{H1_Global_Mu}}{Named numeric vector: \code{score_logit}, \code{gap_logit}.}
\item{\code{H1_Sigma}}{2x2 covariance matrix for the global H1 distribution.}
\item{\code{Score_Transform}}{Character, \code{"logit"} or \code{"sqrt_mismatch"} --
the scale this model's H1/H2/H3 parameters were fit on. Read
automatically by \code{evaluate_likelihoods()}; callers should not need
to track or re-supply it.}
\item{\code{H2}}{List with \code{delta} and \code{sigma} for the missing-species
hypothesis (pooled across all genera).}
\item{\code{H3}}{List with \code{delta} and \code{sigma} for the missing-genus
hypothesis.}
\item{\code{H2_Lookup}}{Data frame (or \code{NULL}) with per-genus \code{H2} delta and
variance estimates: \code{genus}, \code{n_pairs}, \code{delta_shrunk}, \code{var_shrunk}.
See "Per-genus delta shrinkage" section below.}
\item{\code{Confusion_Risk_Curves}}{List (or \code{NULL}) of genus-/family-equal-
weighted, Empirical-Bayes-shrunk per-rank score curves, read by
\code{evaluate_likelihoods()} to compute its \code{species_confusion_risk}/
\code{genus_confusion_risk}/\code{family_confusion_risk} output columns -- a
model-independent, score-only diagnostic of the risk that the raw
match score is equally well explained by a confusable congener/
confamilial/cross-family relative at the rank a hypothesis resolved
to, distinct from this model's own trained bivariate-normal
likelihood. \code{NULL} when \code{rank_system} has fewer than 2 levels or the
required taxonomy columns aren't usable (\code{species_confusion_risk}/
\code{genus_confusion_risk}/\code{family_confusion_risk} are then simply \code{NA},
never an error). See
\code{.compute_rank_score_curves()}'s own documentation for the exact
structure, and \code{compute_rank_thresholds()} for the sibling function
that derives \code{TaxaAssign::score_consensus()}'s \code{rank_thresholds}
from the same underlying curves.}
\item{\code{Stats}}{List of diagnostics:
\describe{
\item{\code{AIC_Score}}{From the \code{lme4} hierarchy fit, when it succeeded;
\code{NA} otherwise.}
\item{\code{n_species}}{Number of species with at least one H1
(within-species) training pair.}
\item{\code{n_singletons}}{Number of reference sequences with no
within-species neighbour, trained via the \code{"Singleton"} pathway
(see \code{.prep_training_data()}).}
\item{\code{n_anchors}}{Number of synthetic perfect-match pseudo-
observations injected via \code{anchor_perfect}; \code{0} when disabled.}
\item{\code{n_h1_pooled}}{Total sequences behind the global H1 mean
(informational only; NOT used as the Monte Carlo uncertainty
fallback -- see \code{prior_weight} below).}
\item{\code{n_h2_pooled}}{Foreign-match count behind the pooled global
\code{H2} delta, \code{NA} when too few foreign matches existed to estimate
one (also informational only).}
\item{\code{prior_weight}}{The same value passed to this call -- what
\code{evaluate_likelihoods()} actually uses as the equivalent sample
size for candidates with no species/genus-specific reference data
at all: the true pooled sample size (\code{n_h1_pooled}/\code{n_h2_pooled})
reflects how well the GLOBAL average is known, not how
confidently that average applies to a candidate we have zero
direct data for, so using it directly would understate
uncertainty exactly where it should be largest.}
\item{\code{shrinkage}}{\code{"empirical_bayes"} or \code{"fixed"} -- which
shrinkage-weight formula was actually used for the per-species H1
means (see the \code{shrinkage} parameter).}
\item{\code{tau2_score}, \code{tau2_gap}}{Method-of-moments estimates of the
between-species variance of the H1 score/gap means (used only
under \code{shrinkage = "empirical_bayes"}; both \code{NA} under
\code{"fixed"}). These carry NO standard error: with K non-singleton
species, the sampling SD of the between-species variance estimate
is roughly \code{var * sqrt(2/(K-1))}, so a true tau/sigma up to about
0.2-0.3 is statistically indistinguishable from 0 at K ~= 40 and
gets truncated to exactly 0. A value of \code{0} therefore means "not
detectable at this species count," not "genuinely absent."}
\item{\code{min_pair_coverage}}{The coverage floor actually applied (see
the \code{min_pair_coverage} parameter), or \code{NA_real_} when \code{raw_df}
carried no \code{coverage} column and the floor was skipped entirely.}
\item{\code{n_self_fallback}}{Number of references whose best conspecific
pair fell below \code{min_pair_coverage}, so they fell back to their
best conspecific pair regardless -- a reference is never dropped
from training by the floor.}
\item{\code{n_foreign_unqualified}}{Number of references with no
qualifying foreign pair clearing \code{min_pair_coverage}, routed to
the noise-floor global rather than an unfiltered (and
potentially coverage-inflated) maximum.}
\item{\code{mlr_violations}}{Character vector of species names failing
the monotone-likelihood-ratio check against their genus's H2
alternative (see \code{.check_score_ratio_monotonicity()}); empty
(length 0) when none violate it.}
\item{\code{max_ceiling_z}, \code{max_ceiling_z_species}}{The largest
floored-sigma distance, across every trained species, between a
perfect match and that species' own fitted score mean, and which
species it belongs to.}
\item{\code{h1_bimodality}}{The result of running \code{.bimodality_check()}
on this model's own real H1 training scores (raw
percent identity, real within-species pairs only -- computed
BEFORE any \code{anchor_perfect} pseudo-observations are appended, so
a synthetic anchor spike can never manufacture a false second
mode). A list with \code{n}, \code{bic_1}, \code{bic_2}, \code{delta_bic}, \code{weights},
\code{means}, \code{sds}, \code{quantum} (the estimated
identity-per-mismatch spacing, or \code{NA} when the scores don't
look discretely comb-shaped), \code{flag}, and \code{explanation} -- see
that function's own docs and \code{calibrate_query_noise()}'s
"Bimodality diagnostic" and "Percent identity is discrete, not
continuous" \verb{@section}s for the full mechanism and thresholds.
This function
only RECORDS the result on the model, it does not warn -- see
\verb{@section H1 bimodality (recorded, not warned)} below.}
}
}
}
}
\description{
Fits an Empirical Bayes model using pairwise within-species match scores
(from \code{build_sequence_matrix()}) to learn species-specific score
distributions.  Per-species parameters are shrunk toward a global mean,
with shrinkage strength inversely proportional to the number of observations
for that species.  Optionally uses \code{lme4} random intercepts per taxonomic
rank for more robust estimation.
}
\note{
For a fully runnable, non-\verb{\dontrun{}} demonstration (including how
\code{reference_df} is derived), see \code{inst/review_function_inputs.R}
Section 2 in the package source.
}
\section{Model structure}{

For each candidate hypothesis the model uses a 2-dimensional multivariate
normal distribution over \verb{(score_logit, gap_logit)}:
\itemize{
\item \strong{H1 (known species):} species-specific mean with shrinkage; global
covariance matrix.
\item \strong{H2 (missing species):} H1 mean shifted left by \code{H2$delta} --
approximately what a sister species looks like. \code{H2$delta} is a single
value pooled across every genus in the training set; where a genus has
real congener pairs in the reference, \code{H2_Lookup} provides a
genus-specific delta (Empirical Bayes shrinkage toward \code{H2$delta},
identical in form to the per-species shrinkage below) that
\code{evaluate_likelihoods()} prefers when available. See the "Per-genus
delta shrinkage" section below.
\item \strong{H3 (missing genus):} H1 mean shifted further left by \code{H3$delta}.
}
}

\section{Marker validation scope}{

The continuous bivariate-normal form above (as opposed to a discrete
"100\% match or nothing" rule) was empirically validated against real
PtConception data for the 12S and 18S markers only (see the "Framing B
verdict" project notes) -- 100\% rules were shown unreliable at the
congeneric level, and the bivariate-normal form captured the real score
distribution well for those two markers specifically. Nothing in this
function checks that the same form fits a different marker (e.g. COI,
16S, cytb, rbcL, matK, trnL -- all now trainable here via
\code{trim_to_amplicon()}) before fitting it. Run
the \code{seq_matrix_score_distribution.R} diagnostic (TaxaID_dev repository) on \code{build_sequence_matrix()}
output for a new marker before trusting this model there -- it checks
logit-normality and the H1/H2 spike ratio, the two properties this
function's Gaussian form assumes hold.
}

\section{Per-genus delta shrinkage}{

The pooled, global \code{H2$delta} treats every genus identically, which is a
poor approximation for genera whose real congeneric divergence is far from
the training-set average -- most consequentially for cryptic species
complexes, where true divergence is much smaller than the pooled estimate
and the pooled delta will overstate confidence in the known-species
hypothesis. Where a genus has at least one real congener pair in the
reference database, \code{H2_Lookup} stores a genus-specific delta shrunk
toward the pooled value by \code{n_pairs / (n_pairs + prior_weight)} (the same
Empirical Bayes form used for per-species means). A genus with only one
referenced species (no congener pairs at all) gets no \code{H2_Lookup} row and
falls back to the pooled \code{H2$delta} unchanged -- there is no way to
estimate local divergence from a single species, and none is invented.
This says nothing about whether an unreferenced congener is plausible in
the first place (that is a prior-side question, answered by
\code{TaxaExpect} from occurrence/richness data, not by this function) --
only about how far its likelihood should be shifted from a known match
\emph{if} one exists. Nor does it address the case where a query's true
relatives are absent from the reference for reasons unrelated to sequence
divergence (e.g. an unreferenced acoustic or visual mimic in the acoustic
and image pathways, where evidence similarity need not track phylogeny at
all) -- see \code{inst/TaxaLikely_supplemental_methods.md} for a fuller
discussion of that limitation.
}

\section{Pseudo-data anchoring}{

When \code{anchor_perfect = TRUE}, synthetic "perfect match" observations are
injected into the H1 training data before fitting the global mean.  This
softens the \strong{perfection penalty} -- a pathology where a 100\% match
receives a \emph{lower} likelihood than the training mean (e.g., 98.5\%) because
the Gaussian density peaks at the mean -- by nudging the \emph{global} (pooled)
mean upward, diluted by however much real training data exists. It does
\strong{not} force any individual species' own \code{H1_Lookup$mu_score} to the
ceiling (the anchor pseudo-rows are excluded from per-species estimation
before shrinkage happens) and does \strong{not}, on its own, guarantee a
monotonically increasing density as match quality approaches 100\% for any
given species -- see the "Non-monotonic score->likelihood shape" section
below for what property this model actually needs (and checks
automatically), and why a peaked-below-100\%-density is not itself a
problem. The number of anchor points is 10\% of the H1 training rows
(minimum 5), weighted enough to nudge the pooled mean without overwhelming
real data.

This is not a workaround standing in for "a real Bayesian model" -- it IS
one, via the standard prior-as-pseudo-observations construction for
conjugate Normal estimation: adding \code{n0} synthetic rows at a target value
is mathematically equivalent to placing a \code{Normal(target, sigma^2/n0)}
prior on the mean and computing the closed-form posterior mean, the exact
same \code{w = N/(N+prior_weight)} Empirical Bayes shrinkage form already used
throughout this model for per-species/per-genus estimates (see
\code{prior_weight} above). Implementing it as injected rows, rather than a
second, separately-coded prior-density formula, keeps this correction on
the same estimation machinery as everything else in the model instead of
adding a parallel mechanism to keep in sync. It is also deliberately NOT a
tight/strong prior: capped at 10\% of real H1 rows (min 5) specifically so
it nudges the pooled global mean rather than dominating it, and it is
never applied to any individual species' own shrunk estimate -- only the
pooled global mean is nudged, and only diluted by however much real
training data exists (a species with abundant real data is essentially
unaffected).
}

\section{Non-monotonic score->likelihood shape}{

A trained species' H1 density is a Gaussian (or bivariate normal, jointly
with gap) on the transformed score, and a Gaussian is unimodal by
construction -- it peaks at that species' own fitted \code{mu_score}, not
necessarily at the transformed value of a perfect (100\%) match. Since
\code{mu_score} is estimated from real reference-pair comparisons (which are
essentially never \emph{exactly} 100\% identical even for the correct species,
due to real intraspecific variation and sequencing/assembly noise) and
real query-time scores run measurably below the training-only mean on top
of that (\code{calibrate_query_noise()}'s own real-data finding: trained means
commonly sit 0.6-1.2 percentage points above real production query
medians), it is normal and expected for a species' H1 density to peak at
an intermediate score and decline, rather than increase, as score
approaches 100\%. \strong{This is not itself a sign of overfitting or a
bug} -- it is the correct behavior of a well-specified Gaussian estimating
a quantity whose true mean is genuinely below the ceiling.

What actually matters for a Bayesian classifier is not where the H1
density's own peak sits, but whether the \strong{H1-vs-H2 likelihood
ratio} stays monotone at the ceiling -- i.e. whether a better score is
ever \emph{weaker}, rather than stronger or equal, evidence for the
known-species hypothesis relative to the missing-species alternative. Two
Gaussians with different means and different variances can, in principle,
cross twice, so this is checked directly (not assumed) at the end of
training via an internal \code{.check_score_ratio_monotonicity()}, using the
same inference-time sigma floor \code{evaluate_likelihoods()} applies. A
\code{warning()} is raised if any species' ratio turns over before the ceiling,
or if any species' perfect-match point sits implausibly far (more than 2
floored standard deviations) from its own fitted mean -- both symptoms are
recorded in the returned object's \code{Stats} list (\code{mlr_violations} and
\code{max_ceiling_z} respectively) for inspection.

On real production models (Great Lakes, Mugu; all trained with
\code{score_transform = "sqrt_mismatch"}), this was checked empirically across
~2,000 real species and found negligible: the likelihood ratio was
strictly increasing all the way to a perfect match in every case, and the
worst observed distance from a species' fitted mean to the ceiling was
0.85 floored standard deviations (0\% exceeded 1 SD). The one severe case
found (median ~2.5 floored SDs, some species effectively at the boundary)
was a stale, orphaned model object still trained with
\code{score_transform = "logit"} -- under \code{logit}, the transformed value of a
perfect match is not a fixed point determined by real data but is set by
\code{logit_epsilon} (a numerical-hygiene constant), and the default sits only
about one order of magnitude away from a value that would make every
perfect match implausible under every species' own fitted distribution.
\code{score_transform = "sqrt_mismatch"} does not have this fragility (a
perfect match maps to exactly \code{0}, the finite edge of that transform's own
range, with no free parameter) and is the more robust choice for any new
training run near this boundary.
}

\section{Alignment-coverage floor on pair selection}{

The H1 gap feature is \code{score - max_foreign_score}, and \code{max_foreign_score}
is taken over every cross-species pair a reference has.  A pairwise
alignment between a short deposit and an unrelated sequence can be 100\%
identical over a few percent of the sequence, and on real GreatLakes 12S
data (2,750 references) that was the "best foreign match" for 86\% of
references (median coverage 4.6\%): 89\% of references trained with
\code{gap <= 0}, the global expected gap was negative, and at inference -- where
every BLAST candidate has query coverage >= 80 -- a perfect match with a
real positive gap read as atypical while a 96\% match with a negative gap
read as typical.  728 of 885 ASVs were H1 near-ties and a Lamar-confirmed
grass carp detection lost to bighead carp at equal priors.

\code{min_pair_coverage} restricts which pair may define the foreign, congener
and conspecific maxima to pairs at or above the inference-side floor.
It is train/inference distribution matching for one feature, not a data
quality filter: it removes no pair and drops no species.  Measured on the
same data (foreign side only): global expected gap -0.010 -> +0.122,
near-ties 728 -> 295, and the top H1 candidate agrees with a unique best
BLAST hit (>= 99.5\% identity) in 244/326 cases vs 178/326.
}

\section{No built-in reference-quality screening}{

This function does NOT screen \code{raw_df} for mislabeled/contaminated
reference accessions before fitting -- it trains on exactly what it's
given. An internal, free, within-reference-set heuristic for this purpose
measured only roughly 6.7\% precise against
\code{TaxaMatch::evaluate_reference_accessions()}'s BLAST-based screen (0 of 40
randomly-sampled flags confirmed as genuine mislabels on one real
dataset), and a TaxaMatch mechanism
(\code{TaxaMatch::corroborate_references_locally()} +
\code{TaxaMatch::evaluate_reference_accessions()}) already does the same job
more precisely, for the same zero/low NCBI cost, directly on \code{reference_df}
before it ever reaches \code{build_sequence_matrix()}. Screen there, exclude
any accession \code{evaluate_reference_accessions()}/\code{score_reference_labels()}
resolves to \code{reference_action == "remove"} from \code{reference_df} (or from
\code{raw_df} by \code{id_x}/\code{id_y}) before calling this function -- there is
no parameter here to pass a bad-accession list through.
}

\section{H1 bimodality (recorded, not warned)}{

\code{Stats$h1_bimodality} runs the same two-component-normal-vs-BIC check
\code{calibrate_query_noise()} uses (see that function's own "Bimodality
diagnostic" and "Percent identity is discrete, not continuous" sections
for the full method, the thresholds, and why a bimodality-coefficient
test was rejected instead) against this model's OWN real H1 training
scores, so a saved \code{lik_model} carries the evidence regardless of
whether/when \code{calibrate_query_noise()} is ever called on it. \strong{Percent
identity is discrete, not continuous} -- confirmed as a real false
positive on a PtConception 12S production run:
\code{Stats$h1_bimodality} reported weights 0.323/0.677, means 98.3/100, sds
3.26/0.0202 -- the "second component" was just the perfect-match spike,
a STRUCTURAL feature of every reference-based dataset (every reference
matching itself, or an identical duplicate deposit, scores at or near
100\% identity), not evidence of a second population on its own.
\code{.bimodality_check()} estimates a quantum directly from the data and
smooths over it before fitting, requires the minority component to hold
real mass, and requires the two fitted means to be separated by more
than a few quanta -- see that function's own docs for the exact
thresholds and mechanism. \strong{This model's OWN H1 training scores are a
structurally different dataset from \code{calibrate_query_noise()}'s
confident-observation query scores}, and the comb-smoothing's benefit
differs correspondingly: query scores share one fixed query length, so they land
on one clean, consistent quantum grid (\code{0.6} on the real motivating run)
that the smoothing closes cleanly; H1 training pairs come from
\code{build_sequence_matrix()}'s reference-vs-reference alignment, whose
length varies pair to pair, so the "grid" is really many different,
overlaid quanta -- the quantum this function's own diagnostic estimates
for real H1 training data is correspondingly much finer (measured
\code{~0.01} on that same real run, versus the query side's \code{0.6}), and the
quanta-separation guard built for a single consistent grid does little
to rescue this specific case: \code{Stats$h1_bimodality$flag} can still read
\code{TRUE} for real H1 training data with this exact "spike at 100" shape,
verified directly against the real run above. Recording rather than
warning is exactly why this residual case is safe to leave as-is: the
evidence (including the estimated \code{quantum} field, small here as a
visible signal that the flagged split may be exactly this artifact) is
preserved for an analyst to inspect, and no automatic action is ever
taken on it here. This function deliberately does NOT \code{warning()} when
the check flags bimodal training data -- \code{calibrate_query_noise()} is
the one place a single offset/line is actually about to be applied to
inference-time scores, so that is where the warning belongs (per the
user's explicit direction); warning
in both places for the same underlying fact would be redundant, not more
informative. \strong{This is a diagnostic only}: it never changes H1's fitted
mean/sigma, and training is never refused when
\code{Stats$h1_bimodality$flag} is \code{TRUE}. The remedy for genuinely mixed
sequencing platforms or markers is to train/calibrate them separately --
this package does not do that for you.
}

\examples{
\dontrun{
ref_matrix <- build_sequence_matrix(reference_df,
  rank_system = c("family", "genus", "species")
)
model <- train_likelihood_model(ref_matrix,
  rank_system = c("family", "genus", "species")
)
interpret_model(model)
}

}
\references{
Efron, B. and Morris, C. (1973). Stein's estimation rule and its
competitors -- an empirical Bayes approach. \emph{Journal of the American
Statistical Association}, 68(341), 117--130.
\doi{10.1080/01621459.1973.10481350}

Somervuo, P., Koskela, S., Pennanen, J., Nilsson, R.H. and Ovaskainen, O.
(2016). Unbiased probabilistic taxonomic classification for DNA barcoding.
\emph{Bioinformatics}, 32(19), 2920--2927.
\doi{10.1093/bioinformatics/btw346}

Genz, A. and Bretz, F. (2009). \emph{Computation of Multivariate Normal
and t Probabilities}. Lecture Notes in Statistics. Springer-Verlag,
Heidelberg. ISBN 978-3-642-01688-2.

Hebert, P.D.N., Cywinska, A., Ball, S.L. and deWaard, J.R. (2003).
Biological identifications through DNA barcodes. \emph{Proceedings of
the Royal Society of London B}, 270(1512), 313--321.
\doi{10.1098/rspb.2002.2218}
}
\seealso{
\code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}, \code{\link[=interpret_model]{interpret_model()}}
}
