% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_collapse.R
\name{detect_score_collapse}
\alias{detect_score_collapse}
\title{Detect score-collapse pipeline rules in a match object}
\usage{
detect_score_collapse(
  match_obj,
  score_col = "score_original",
  observation_id_col = "observation_id",
  perfect_threshold = NULL,
  min_fraction = 0.05
)
}
\arguments{
\item{match_obj}{Data frame. Standardised match object containing at least
\code{observation_id_col} and \code{score_col}.}

\item{score_col}{Character. Name of the score column (default
\code{"score_original"}).}

\item{observation_id_col}{Character. Name of the observation identifier
column (default \code{"observation_id"}).}

\item{perfect_threshold}{Numeric or \code{NULL}. Score value that defines a
perfect match.  Auto-detected when \code{NULL}: 100 for 0--100 scale,
1.0 for 0--1 scale.}

\item{min_fraction}{Numeric. Minimum fraction of observations that must show
a pattern for it to be flagged as \code{rule_detected = TRUE} (default
\code{0.05}).  Prevents single-observation artefacts from triggering a
detection.}
}
\value{
A named list:
\describe{
\item{\code{rule_detected}}{Logical.}
\item{\code{type}}{Character: \code{"perfect_only"}, \code{"max_score_ties"},
\code{"both"}, or \code{"none"}.}
\item{\code{n_total}}{Integer. Unique observations in \code{match_obj}.}
\item{\code{n_perfect_only}}{Integer. Singleton observations at perfect score.}
\item{\code{n_ties_only}}{Integer. Multi-candidate observations where all
candidates share the same score.}
\item{\code{fraction_perfect_only}}{Numeric.}
\item{\code{fraction_ties_only}}{Numeric.}
\item{\code{example_observations}}{Character. Up to 5 affected observation IDs.}
}
}
\description{
Inspects a match object for evidence that an upstream tool (e.g. BLAST with
a 100\\% rule, or a top-hit-only classifier) suppressed lower-scoring
candidates.  Two patterns are recognised:
}
\details{
\describe{
\item{\code{"perfect_only"}}{Observations with exactly one candidate whose
score equals the maximum possible value (100 for percent-identity; 1.0
for similarity). The pipeline appears to have dropped all sub-perfect
matches.}
\item{\code{"max_score_ties"}}{Observations with multiple candidates that all
share the same (maximum) score -- i.e. only tied top-hits were returned.}
}

Both patterns may coexist (\code{type = "both"}).
}
\examples{
m <- data.frame(
  observation_id = c("obs1", "obs2", "obs3", "obs3"),
  score_original = c(100, 100, 98, 97),
  taxon_name     = c("Sp_A", "Sp_B", "Sp_C", "Sp_D")
)
detect_score_collapse(m)

}
\seealso{
\code{\link[=restore_suppressed_candidates]{restore_suppressed_candidates()}}
}
