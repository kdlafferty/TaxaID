% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{filter_top_hypotheses}
\alias{filter_top_hypotheses}
\title{Keep only the finest-rank specific candidates per query}
\usage{
filter_top_hypotheses(likelihood_df, rank_system = NULL)
}
\arguments{
\item{likelihood_df}{Data frame -- the \verb{$likelihoods} component of the list
returned by \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}.}

\item{rank_system}{Character vector of rank names \strong{coarse to fine}.
Used to assign numeric rank scores for comparison. Default \code{NULL}
auto-detects from the \code{taxon_name_rank} values in \code{likelihood_df}.}
}
\value{
Filtered version of \code{likelihood_df}.
}
\description{
After \code{evaluate_likelihoods()}, each query may have specific candidates at
multiple ranks (e.g., both species- and genus-level hits).  This function
retains only the finest-rank specific candidates — coarser candidates are
redundant when a finer-rank hit exists — while keeping all
\code{"unreferenced_species"} and \code{"unreferenced_genus"} rows.
}
\examples{
\dontrun{
result <- evaluate_likelihoods(match_df, model)
filtered <- filter_top_hypotheses(result$likelihoods)
}

}
\seealso{
\code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}
}
