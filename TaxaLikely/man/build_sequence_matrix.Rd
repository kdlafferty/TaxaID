% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_sequence.R
\name{build_sequence_matrix}
\alias{build_sequence_matrix}
\title{Build a pairwise match-score matrix from reference sequences}
\usage{
build_sequence_matrix(
  reference_df,
  rank_system = NULL,
  max_dist = 0.25,
  min_seq_len = 100L,
  max_seq_len = 2000L,
  filter_unnamed = TRUE,
  max_seqs_per_taxon = NULL
)
}
\arguments{
\item{reference_df}{Data frame with one row per reference sequence.  Must
contain:
\describe{
\item{\code{composite_id}}{Unique sequence identifier (character).}
\item{\code{sequence}}{DNA string (character, IUPAC alphabet accepted).}
\item{rank columns}{One column per rank in \code{rank_system}
(e.g., \code{genus}, \code{species}).}
}}

\item{rank_system}{Character vector of rank names \strong{coarse to fine}
(e.g., \code{c("family", "genus", "species")}). Default \code{NULL}
auto-detects from columns in \code{reference_df}.}

\item{max_dist}{Numeric (default \code{0.25}).  Pairs with distance above this
threshold are dropped to save memory.  Roughly, 0.25 ≈ 75\% identity.
The 75\% identity threshold is a standard floor for retaining distantly
related taxa in barcode reference databases.}

\item{min_seq_len}{Integer (default \code{100}).  Sequences shorter than this
are discarded before alignment.}

\item{max_seq_len}{Integer (default \code{2000}).  Sequences longer than this
are discarded before alignment.}

\item{filter_unnamed}{Logical (default \code{TRUE}).  If \code{TRUE}, sequences whose
finest-rank taxonomy column (the last element of \code{rank_system}, typically
\code{species}) is blank (\code{""}) or \code{NA} are removed before alignment.  Blank
names produce spurious within-species pairs — two unidentified sequences
both labelled \code{""} are classified as conspecific even though they may
represent entirely different taxa.  In a broad 18S reference database this
can account for the majority of apparent within-species pairs.  Set to
\code{FALSE} only if blank finest-rank values are intentional.}

\item{max_seqs_per_taxon}{Integer or \code{NULL} (default \code{NULL}).  If supplied,
at most this many sequences are retained per finest-rank taxon before
alignment, chosen by random sampling using the current RNG state (set
\code{set.seed()} before calling for reproducibility).  This prevents
heavily-sequenced model organisms or domestic species from dominating
the within-species distribution and thereby distorting model training.
For typical vertebrate barcode databases a value of \code{10L}–\code{20L} is
sufficient; the resulting within-species pair counts per taxon are at most
\code{max_seqs_per_taxon * (max_seqs_per_taxon - 1) / 2}.  \code{NULL} disables
the cap (current behaviour).}
}
\value{
A data frame with one row per sequence pair within \code{max_dist}:
\describe{
\item{\code{id_x}, \code{id_y}}{\code{composite_id} values for each pair member.}
\item{\code{p_match}}{Match score (1 − distance), range (0, 1].}
\item{\code{coverage}}{Alignment coverage: number of positions where both
sequences contribute a non-gap character, divided by the shorter
unaligned sequence length.  Range (0, 1].  Values near 1.0 indicate
nearly complete overlap; values near 0.0 indicate highly gappy or
partial alignments that produce unreliable match scores.  Use
\code{\link[=calibrate_coverage_filter]{calibrate_coverage_filter()}} or \code{\link[=coverage_threshold]{coverage_threshold()}} to select a
minimum coverage threshold before calling \code{\link[=train_likelihood_model]{train_likelihood_model()}}.}
\item{\verb{\{rank\}.x}, \verb{\{rank\}.y}}{Taxonomy columns for each pair member.}
}
}
\description{
Aligns DNA sequences using \code{DECIPHER::AlignSeqs()}, computes pairwise
distances with \code{DECIPHER::DistanceMatrix()}, converts distances to match
scores (\code{p_match = 1 - distance}), and joins taxonomy metadata.  The
resulting data frame is the input to \code{\link[=flag_reference_errors]{flag_reference_errors()}} and
\code{\link[=train_likelihood_model]{train_likelihood_model()}}.
}
\section{Requirements}{

Packages \code{DECIPHER} and \code{Biostrings} must be installed
(\code{BiocManager::install("DECIPHER")}).  They are listed in \code{Suggests} because
they are only needed for this one function.
}

\examples{
\dontrun{
# Requires DECIPHER + Biostrings (Bioconductor)
ref_matrix <- build_sequence_matrix(
  reference_df,
  rank_system       = c("family", "genus", "species"),
  filter_unnamed    = TRUE,   # drop blank/NA species (default)
  max_seqs_per_taxon = 20L    # cap per-species sequences before alignment
)
head(ref_matrix)
}

}
\seealso{
\code{\link[=flag_reference_errors]{flag_reference_errors()}}, \code{\link[=train_likelihood_model]{train_likelihood_model()}}
}
