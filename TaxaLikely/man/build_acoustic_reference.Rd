% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_acoustic.R
\name{build_acoustic_reference}
\alias{build_acoustic_reference}
\title{Build an Acoustic Reference Dataset for Likelihood Model Training}
\usage{
build_acoustic_reference(
  birdnet_df,
  recordings_meta,
  rank_system = c("genus", "species"),
  min_confidence = 0,
  exclude_background = TRUE
)
}
\arguments{
\item{birdnet_df}{Data frame. Output of \code{\link[TaxaMatch:read_birdnet_output]{TaxaMatch::read_birdnet_output()}}
with \code{top_n >= 2} recommended.  Required columns: \code{observation_id},
\code{score}, \code{species}, \code{genus}, \code{source_file}.}

\item{recordings_meta}{Data frame. Output of \code{\link[=fetch_reference_recordings]{fetch_reference_recordings()}}
with \code{download = TRUE} so that \code{local_path} is populated.  Required
columns: \code{recording_id}, \code{species}, \code{genus}, \code{local_path}.  The \code{type}
column (when present) is used as \code{testid}.}

\item{rank_system}{Character vector of rank names coarse-to-fine.  Default
\code{c("genus", "species")}.  Must be present in both \code{birdnet_df} and
\code{recordings_meta}.}

\item{min_confidence}{Numeric. Drop BirdNET detections below this threshold
before building pairs.  Default \code{0} (keep all).}

\item{exclude_background}{Logical. If \code{TRUE} (default), drop detections
where the identified species appears in \code{recordings_meta$also_species}
(background species audible in the recording).  Background detections are
not false positives in the conventional sense and should not train H2/H3.}
}
\value{
A data frame with one row per (time window x BirdNET detection)
pair, containing:
\describe{
\item{\code{id_x}}{\code{observation_id} of the time window (the query).}
\item{\code{id_y}}{Synthetic unique identifier for the detection
(\code{"{observation_id}_det{rank}"}).}
\item{\code{p_match}}{BirdNET confidence (0--1).}
\item{\verb{\{rank\}.x}}{Ground-truth taxonomy from \code{recordings_meta}.}
\item{\verb{\{rank\}.y}}{BirdNET-detected taxonomy from \code{birdnet_df}.}
\item{\code{coverage}}{Numeric recording quality on a 0 to 1 scale, derived
from the Xeno-canto quality grade of the source recording:
A → 1.0, B → 0.8, C → 0.5, D → 0.3, E → 0.1.  \code{NA} when the
\code{quality} column is absent from \code{recordings_meta}.  Every pair from
the same recording shares the same value; use \code{\link[=coverage_threshold]{coverage_threshold()}}
or \code{\link[=calibrate_coverage_filter]{calibrate_coverage_filter()}} to select which quality grades to
include before calling \code{\link[=train_likelihood_model]{train_likelihood_model()}}.}
\item{\code{testid}}{Xeno-canto recording type (e.g. \code{"song"}, \code{"call"});
\code{NA} if absent.  Filter on this column to train type-specific models.}
\item{\code{recording_id}}{Xeno-canto recording ID for traceability.}
\item{\code{start_s}, \code{end_s}}{Time window boundaries (seconds).}
}
}
\description{
Joins BirdNET-Analyzer detections to Xeno-canto ground-truth labels and
produces a pairwise training dataset in the same format as
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}}, ready for \code{\link[=train_likelihood_model]{train_likelihood_model()}}.
}
\details{
Each time window in a reference recording becomes a query (\code{id_x}).  Each
BirdNET detection in that window becomes a candidate match (\code{id_y}).
Windows where BirdNET detects the correct species produce H1
(within-species) rows; detections of wrong species produce H2/H3
(cross-species) rows.  The match score is BirdNET confidence (0--1).

\strong{Controlling for recording type:} The \code{testid} column stores the
Xeno-canto \code{type} tag (e.g. \code{"song"}, \code{"call"}, \code{"flight call"}).
Train a separate model per type -- exactly as you would train a separate
model per barcode marker in eDNA.  Filter before training:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{songs  <- subset(ref_pairs, testid == "song")
model_song <- train_likelihood_model(songs,
                                     rank_system = c("genus", "species"))
calls  <- subset(ref_pairs, testid == "call")
model_call <- train_likelihood_model(calls,
                                     rank_system = c("genus", "species"))
}\if{html}{\out{</div>}}

\strong{Gap computation:} Set \code{top_n >= 2} in
\code{\link[TaxaMatch:read_birdnet_output]{TaxaMatch::read_birdnet_output()}} so each time window has at least two
candidates. The gap (best score minus runner-up score) is the key
discriminator in \code{\link[=train_likelihood_model]{train_likelihood_model()}} and is computed from the pair
rows produced here.

\strong{Background species:} Recordings often contain species other than the
target.  Use the \code{exclude_background} parameter to drop detections of
species listed in \code{recordings_meta$also_species}, preventing background
detections from being mis-labelled as H2/H3 errors.

\strong{Join key:} \code{source_file} from \code{birdnet_df} is matched to \code{local_path}
from \code{recordings_meta} by comparing file stems (stripping
\code{.BirdNET.results.csv} and the audio extension respectively).  Audio
files must have been downloaded with
\code{fetch_reference_recordings(download = TRUE)}.

\strong{Output format compatibility:} The \code{.x}/\code{.y} suffix convention and
\code{p_match} column name match the output of \code{\link[=build_sequence_matrix]{build_sequence_matrix()}}, so
\code{\link[=train_likelihood_model]{train_likelihood_model()}} and \code{\link[=flag_reference_errors]{flag_reference_errors()}} accept this
data frame directly.
}
\examples{
\dontrun{
# 1. Fetch reference recordings and download audio
recs <- fetch_reference_recordings(
  species      = c("Turdus migratorius", "Setophaga petechia"),
  quality      = c("A", "B"),
  type         = "song",
  download     = TRUE,
  download_dir = "reference_audio/"
)

# 2. Run BirdNET-Analyzer on reference_audio/ (Python, outside R)
#    pip3 install birdnetlib
#    See TaxaMatch README for the analysis script.

# 3. Read BirdNET detections (top_n = 3 to enable gap computation)
birdnet_df <- TaxaMatch::read_birdnet_output(
  "birdnet_results/",
  top_n = 3L
)

# 4. Build pairwise training dataset
ref_pairs <- build_acoustic_reference(
  birdnet_df      = birdnet_df,
  recordings_meta = recs,
  rank_system     = c("genus", "species")
)

# 5. Train one model per recording type
model_song <- train_likelihood_model(
  subset(ref_pairs, testid == "song"),
  rank_system = c("genus", "species")
)
}
}
\seealso{
\code{\link[=fetch_reference_recordings]{fetch_reference_recordings()}},
\link[TaxaMatch:read_birdnet_output]{TaxaMatch::read_birdnet_output()},
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}}, \code{\link[=train_likelihood_model]{train_likelihood_model()}}
}
