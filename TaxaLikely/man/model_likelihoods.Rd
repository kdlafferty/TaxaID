% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_likelihoods.R
\name{model_likelihoods}
\alias{model_likelihoods}
\title{Apply the bivariate-normal model to produce score_likelihood values}
\usage{
model_likelihoods(
  scored_df,
  model_params,
  rank_system = NULL,
  ratio_threshold = 0.01,
  min_match_threshold = 0.5,
  n_sims = 0L,
  min_coverage = NULL,
  verbose = FALSE
)
}
\arguments{
\item{scored_df}{Data frame from \code{\link[=assign_scores]{assign_scores()}} with
\code{score_type = "similarity"}.  Must contain \code{observation_id},
\code{taxon_name}, \code{taxon_name_rank}, \code{hypothesis_type}, and
\code{score_original} (or \code{score} / \code{p_match}).}

\item{model_params}{Object of class \code{"taxa_model_params"} from
\code{\link[=train_likelihood_model]{train_likelihood_model()}}.}

\item{rank_system}{Character vector of rank names coarse to fine.
\code{NULL} (default) auto-detects from \code{scored_df}.}

\item{ratio_threshold}{Numeric (default \code{0.01}). Minimum likelihood
ratio; hypotheses below this threshold are dropped.}

\item{min_match_threshold}{Numeric (default \code{0.50}). Raw score below
which a candidate receives likelihood 0.}

\item{n_sims}{Integer (default \code{0L}). Monte Carlo simulations.
\code{0} = deterministic only.}

\item{min_coverage}{Numeric or \code{NULL}. Coverage pre-filter. See
\code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}} for details.}

\item{verbose}{Logical (default \code{FALSE}). Print species-specific
parameter fallback messages.}
}
\value{
A named list identical in structure to \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}:
\describe{
\item{\code{$likelihoods}}{Data frame with one row per
\code{observation_id} x hypothesis, including \code{score_likelihood},
\code{score_likelihood_mean}, \code{score_likelihood_sd}, and
\code{score_method = "bivariate_normal"}.}
\item{\code{$unresolved}}{Rows from \code{scored_df} for queries that
produced no usable likelihoods. Empty data frame if none.}
}
}
\description{
The bivariate-normal modeling step of the unified likelihood pipeline.
Accepts a data frame produced by \code{\link[=assign_scores]{assign_scores()}} with
\code{score_type = "similarity"} (which adds \code{score_norm} and
\code{score_method = "similarity"} but does \strong{not} write
\code{score_likelihood}).
}
\details{
Internally calls \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}} on the
\code{"specific_candidate"} rows and adds
\code{score_method = "bivariate_normal"} to the result.
}
\examples{
\dontrun{
hyp_df  <- unreferenced_candidates(match_df)
sc_df   <- assign_scores(hyp_df, score_type = "similarity")
result  <- model_likelihoods(sc_df, model_params = model,
                             rank_system = c("family", "genus", "species"))
head(result$likelihoods)
}

}
\seealso{
\code{\link[=assign_scores]{assign_scores()}}, \code{\link[=unreferenced_candidates]{unreferenced_candidates()}},
\code{\link[=compute_likelihoods]{compute_likelihoods()}}, \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}
}
