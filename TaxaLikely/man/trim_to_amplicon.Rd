% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim_to_amplicon.R
\name{trim_to_amplicon}
\alias{trim_to_amplicon}
\title{Extract the amplicon region from over-length reference sequences (in-silico PCR)}
\usage{
trim_to_amplicon(
  reference_df,
  primer_fwd = NULL,
  primer_rev = NULL,
  barcode_term = NULL,
  min_len = NULL,
  max_len = NULL,
  max_mismatch_rate = 0.15,
  verbose = TRUE
)
}
\arguments{
\item{reference_df}{Data frame with at least \code{composite_id} and \code{sequence}
columns (the same object produced by \code{\link[=fetch_ncbi_reference_sequences]{fetch_ncbi_reference_sequences()}}
/ \code{\link[=read_reference_fasta]{read_reference_fasta()}} and consumed by \code{\link[=build_sequence_matrix]{build_sequence_matrix()}}).}

\item{primer_fwd, primer_rev}{Character scalars: forward and reverse primer
sequences, 5' to 3', IUPAC-degenerate bases allowed. Supply both, or
neither (to look up \code{barcode_term} in
\link[TaxaTools:barcode_primer_defaults]{TaxaTools::barcode_primer_defaults} instead).}

\item{barcode_term}{Character scalar naming a registered primer set (e.g.
\code{"MiFishU"}), resolved via \code{\link[TaxaTools:resolve_barcode_primers]{TaxaTools::resolve_barcode_primers()}}. Also
used to auto-resolve \code{min_len}/\code{max_len} via
\code{\link[TaxaTools:resolve_barcode_lengths]{TaxaTools::resolve_barcode_lengths()}} when those are not supplied
directly.}

\item{min_len, max_len}{Integer. The plausible amplicon length range for
this marker. Sequences at or below \code{max_len} are left untouched
(assumed already barcode-length); an extracted amplicon outside
\verb{[min_len, max_len]} is treated as an implausible match and rejected.
Default \code{NULL} auto-resolves from \code{barcode_term} when supplied.}

\item{max_mismatch_rate}{Numeric in \verb{[0, 1)} (default \code{0.15}). Maximum
fraction of primer positions allowed to mismatch the sequence at the
binding site (rounded down to an integer count of bases per primer).
Accounts for real SNP variation at primer-binding sites; degenerate
IUPAC bases in the primer itself are handled separately and do not
count as mismatches when compatible with the sequence base.}

\item{verbose}{Logical (default \code{TRUE}). Print progress/summary messages.}
}
\value{
\code{reference_df} with \code{sequence} updated in place for successfully
trimmed rows, plus two new columns:
\describe{
\item{\code{amplicon_trimmed}}{Logical. \code{TRUE} if this row's sequence was
replaced with an extracted amplicon.}
\item{\code{amplicon_trim_note}}{Character. One of
\code{"within_length_range_no_trim_needed"},
\code{"extracted_via_primer_match_sense_strand"},
\code{"extracted_via_primer_match_antisense_strand"},
\code{"primers_not_found_or_implausible_span"},
\code{"non_iupac_dna_skipped"}, \code{"invalid_dna_string"}, or
\code{"missing_sequence"}.}
}
}
\description{
GenBank mixes short, purpose-cut barcode submissions with much longer
sequences (full mitochondrial genomes, whole-genome scaffolds, full
ribosomal operons) that happen to contain the target locus somewhere
inside them. \code{\link[=build_sequence_matrix]{build_sequence_matrix()}}'s \code{min_seq_len}/\code{max_seq_len}
filter excludes these outright -- correct for well-sampled species, but
for a poorly-sampled species whose only GenBank record is over-length,
exclusion throws away the only genuine reference data available for it.
}
\details{
\code{trim_to_amplicon()} locates the forward and reverse primer-binding sites
within each over-length sequence and extracts just the amplicon between
them -- the same result a real PCR reaction targeting that marker would
have produced -- so the sequence can still contribute real same-species
training data instead of being dropped.

Sequences already within \verb{[min_len, max_len]} are left completely
unchanged (most purpose-cut barcode submissions have already had their
primers trimmed off at deposition, so attempting primer matching on them
would often fail even though the sequence is perfectly usable). Only
sequences longer than \code{max_len} are searched for primer sites.
}
\section{Failure mode}{

When a primer site cannot be found within tolerance on either strand, or
the two matched sites imply an implausible amplicon length (outside
\verb{[min_len, max_len]} -- e.g. a spurious match pairing far from the real
target), the sequence is left unchanged (still over-length) and flagged in
\code{amplicon_trim_note}. It will still be excluded by
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}}'s own length filter, exactly as it would be
without ever calling this function -- this function only ever adds
sequences that would otherwise be lost, never removes ones that would
otherwise be kept.
}

\section{Pipeline placement}{

Standalone stage, run by the caller between
\code{\link[=fetch_ncbi_reference_sequences]{fetch_ncbi_reference_sequences()}}/\code{\link[=read_reference_fasta]{read_reference_fasta()}} and
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}}:
\preformatted{
reference_df <- fetch_ncbi_reference_sequences(...)
reference_df <- trim_to_amplicon(reference_df, barcode_term = "MiFishU")
ref_matrix   <- build_sequence_matrix(reference_df, ...)
}
}

\section{Primer source and scope}{

Primer sequences come from \link[TaxaTools:barcode_primer_defaults]{TaxaTools::barcode_primer_defaults}, populated
only for primer sets directly verified against their primary publication
-- currently MiFish-U and MiFish-E (12S; Miya et al. 2015). For any other
marker, either supply \code{primer_fwd}/\code{primer_rev} directly (from your own
wet-lab protocol or the relevant primer paper), or pre-trim sequences to
the amplicon region with the external CRABS tool
(\url{https://github.com/gjeunen/reference_database_creator}) before
calling \code{\link[=build_sequence_matrix]{build_sequence_matrix()}} -- see \code{read_crabs_output()}. This
function deliberately does not attempt to replicate CRABS's full
database-curation feature set (multi-database sourcing, dereplication
policy, taxonomic reconciliation); it does one narrow thing -- given a
sequence and a primer pair, extract the amplicon region or report failure.
}

\examples{
\dontrun{
# Requires Biostrings (Bioconductor)
ref_df <- fetch_ncbi_reference_sequences(
  taxa = "Rhacochilus", barcode_term = "MiFishU"
)
ref_df <- trim_to_amplicon(ref_df, barcode_term = "MiFishU")
table(ref_df$amplicon_trim_note)
}

}
\seealso{
\code{\link[=build_sequence_matrix]{build_sequence_matrix()}}, \code{\link[=fetch_ncbi_reference_sequences]{fetch_ncbi_reference_sequences()}},
\link[TaxaTools:barcode_primer_defaults]{TaxaTools::barcode_primer_defaults}
}
