% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_unreferenced.R
\name{expand_unreferenced_hypotheses}
\alias{expand_unreferenced_hypotheses}
\title{Expand unreferenced hypotheses from genus/family level to named species}
\usage{
expand_unreferenced_hypotheses(likelihood_df, unreferenced_df)
}
\arguments{
\item{likelihood_df}{Data frame -- the \verb{$likelihoods} component returned by
\code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}.  Must contain \code{observation_id},
\code{taxon_name}, \code{taxon_name_rank}, \code{hypothesis_type},
\code{score_likelihood}, \code{score_likelihood_mean}, \code{score_likelihood_sd}.}

\item{unreferenced_df}{Data frame of unreferenced but plausible species.
Must contain columns \code{species} (binomial name), \code{genus}, and \code{family}.
Built from TaxaExpect rows confirmed as unreferenced by
\code{\link[=audit_barcode_coverage]{audit_barcode_coverage()}}. May optionally contain an \code{observation_id}
column (Session 159): a row with \code{NA} (or when the column is absent
entirely) applies to every observation sharing its genus/family, the
original global-list behavior; a row with a real \code{observation_id}
applies only to that one observation. This lets a caller inject a
species that IS globally referenced (so it would never appear via
\code{\link[=audit_barcode_coverage]{audit_barcode_coverage()}}) but lacks reference evidence covering one
specific query's region -- see
\code{\link[=restore_suppressed_candidates]{restore_suppressed_candidates()}}'s \code{check_regional_overlap} mechanism,
whose rejected congeners are returned in exactly this shape via
\code{attr(result, "regional_unreferenced")}.}
}
\value{
\code{likelihood_df} with generic \code{"unreferenced_species"} and
\code{"unreferenced_genus"} rows replaced by named species rows where matches
are found.  Column set is unchanged; new rows carry \code{NA} for most extra
columns in the input (e.g. \code{constraint_applied}, which is genuinely
row-specific), except \code{score_likelihood_cov}, \code{score_likelihood_evidence},
and \code{h2_delta_source} when present -- these, like \code{score_likelihood}/
\code{score_likelihood_mean}/\code{score_likelihood_sd}, describe the shared H2/H3
value itself (identical across every expanded species under one generic
row) rather than anything row-specific, so they are copied through too.
}
\description{
Moved here from TaxaAssign (Session 150) -- this is the value-modeling half
of unreferenced-taxon handling ("borrow a likelihood from referenced
relatives"), which belongs next to \code{\link[=unreferenced_candidates]{unreferenced_candidates()}} (the
candidate-generation half) rather than in the posterior-computation
package. It still requires both a TaxaLikely likelihood object and a
TaxaExpect-derived unreferenced-species list, so it must still run after
both are available and before \code{\link[TaxaAssign:compute_posterior]{TaxaAssign::compute_posterior()}} -- that is
a workflow-ordering requirement, not a package-dependency one, since this
function only ever consumes plain data frames and never calls into
TaxaExpect or TaxaAssign itself.

After \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}}, the \code{"unreferenced_species"}
hypothesis is labelled at genus level (e.g. \code{taxon_name = "Fundulus"},
\code{taxon_name_rank = "genus"}) and the \code{"unreferenced_genus"} hypothesis at
family level.  This function replaces those generic rows with one named
row per plausible unreferenced species, enabling
\code{\link[TaxaAssign:compute_posterior]{TaxaAssign::compute_posterior()}} to join them directly to species-level
priors from TaxaExpect.
\subsection{Expansion rules (applied per \code{observation_id})}{

\enumerate{
\item The \code{"unreferenced_species"} (H2) row carries a genus label.
Species-level suppression: an unreferenced species in the H2 genus is
suppressed only if that exact species is already a \code{specific_candidate}
for this observation.  Other unreferenced congeners (e.g. F. parvipinnis
when F. lima is H1) are expanded normally and receive the H2
\code{score_likelihood} / \code{score_likelihood_mean} / \code{score_likelihood_sd}.
Exception: when an H1 \code{specific_candidate} row is genus-rank
(\code{taxon_name_rank == "genus"}), the entire genus is suppressed -- the
genus is already represented with its own calibrated score.
\item The \code{"unreferenced_genus"} (H3) row carries a family label.
Unreferenced species whose family matches that label, whose genus
differs from the H2 genus, \emph{and} that are not already a
species-level H1 \code{specific_candidate} (or whose genus is covered by a
genus-rank H1), receive the H3 likelihood values.
\item If no named species are found for the H2 genus (none are locally
plausible), the generic H2 row is dropped.  Likewise for H3.
}
}

\subsection{H2 and H3 likelihoods}{

The H2 likelihood is independently modelled by TaxaLikely from sister-species
matches (cross-species matches within the same genus in the reference
database).  The H3 likelihood is modelled from sister-genus matches.
These are not borrowed from H1 rows -- they reflect the expected score
distribution for an unreferenced congener or confamilial, respectively.
All named species produced by expansion share the same H2 (or H3) likelihood;
TaxaExpect priors differentiate among them geographically.
}

\subsection{Building \code{unreferenced_df}}{

\enumerate{
\item Start with TaxaExpect's plausible species list for the site
(includes \code{genus} and \code{family} columns from TaxaFetch).
\item Subtract species already in TaxaMatch -- these have reference
sequences by definition.
\item Call \code{\link[=audit_barcode_coverage]{audit_barcode_coverage()}} with
\code{species_list} = the TaxaExpect species and \code{match_df} = the TaxaMatch
reference species. Species with NCBI barcode count = 0 are truly
unreferenced.
\item Filter TaxaExpect rows to those confirmed as unreferenced and
select \code{species}, \code{genus}, \code{family}.
}
}

\subsection{Pipeline order}{

Run \emph{before} \code{\link[=apply_coverage_constraints]{apply_coverage_constraints()}}: coverage
constraints must see the named rows, not the generic genus/family rows.
}
}
\note{
For a fully runnable, non-\verb{\dontrun{}} demonstration (including how
\code{result}/\code{unreferenced_species_result} are derived), see
\code{inst/review_function_inputs.R} Section 7 in the package source.
}
\examples{
\dontrun{
expanded <- expand_unreferenced_hypotheses(
  result$likelihoods,
  unreferenced_df = unreferenced_species_result
)
}

}
\seealso{
\code{\link[TaxaAssign:compute_posterior]{TaxaAssign::compute_posterior()}}, \code{\link[=evaluate_likelihoods]{evaluate_likelihoods()}},
\code{\link[=audit_barcode_coverage]{audit_barcode_coverage()}}, \code{\link[=apply_coverage_constraints]{apply_coverage_constraints()}},
\code{\link[=restore_suppressed_candidates]{restore_suppressed_candidates()}} for the \code{check_regional_overlap}
mechanism that produces an observation-scoped \code{unreferenced_df} addition
}
