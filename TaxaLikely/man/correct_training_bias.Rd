% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_training_bias.R
\name{correct_training_bias}
\alias{correct_training_bias}
\title{Correct classifier scores for training-database representation bias}
\usage{
correct_training_bias(
  scored_df,
  count_col,
  score_col = "score_original",
  tau = 0
)
}
\arguments{
\item{scored_df}{Data frame of raw classifier output, one row per
candidate species per observation. Must contain \code{score_col}.}

\item{count_col}{Character scalar. Name of the column holding each
candidate's training-database representation count (e.g.
\code{"n_observations"} for iNaturalist CV output, \code{"n_recordings"} for
BirdNET/Xeno-canto output after joining
\code{audit_acoustic_coverage(xc_recordings = TRUE)}'s census onto
\code{scored_df} by taxon). If absent from \code{scored_df}, a warning is
issued and every row falls through unchanged (equivalent to all
counts being \code{NA}).}

\item{score_col}{Character scalar (default \code{"score_original"}, matching
\code{\link[=assign_scores]{assign_scores()}}'s default \code{score_col}). Name of the raw score
column to correct.}

\item{tau}{Non-negative numeric scalar (default \code{0}). Global exponent
applied to every candidate's count (see Details) -- \code{tau = 0} disables
correction entirely (returns scores unchanged); \code{tau = 1} is the
theoretically Fisher-consistent full correction; values above 1 apply
stronger-than-theoretical correction (Menon et al.'s own tuned optimum
on one benchmark was 2.6). Every real calibration run against this
function's own real classifier output (image, and acoustic on a
properly powered test) found \verb{tau ~= 0} optimal -- raise it only
after calibrating against real labeled data for your own data type
(see Details).}
}
\value{
\code{scored_df} with \code{score_col} overwritten by the corrected
score, plus three added columns: \code{score_uncorrected} (pre-correction
value), \code{n_used} (count applied per row, \code{NA} where unavailable),
and \code{tau_used} (exponent actually applied per row).
}
\description{
Discriminative classifiers (iNaturalist CV, BirdNET) trained by standard
cross-entropy estimate a Bayes posterior: their raw output for species
\eqn{i} is proportional to \eqn{L(obs \mid species_i) \times n_i}, where
\eqn{L(obs \mid species_i)} is the true visual/acoustic likelihood and
\eqn{n_i} is the number of training examples for species \eqn{i}. Raw
classifier scores therefore favor well-represented (common, well-
photographed/recorded) taxa over rare ones with equal true evidential
support. This function divides out an estimate of that training-count
bias so that scores across candidates are on a more comparable scale
before \code{\link[=assign_scores]{assign_scores()}} normalizes them.
}
\details{
\subsection{Logit adjustment (Menon et al. 2020), not adaptive per-candidate shrinkage}{

The correction is \eqn{score_i / n_i^{\tau}}, where \eqn{\tau} is a single
\strong{global, user-set scalar} applied identically to every candidate,
following the "logit adjustment" correction for long-tailed recognition
(Menon, Jayasumana, Rawat, Jain, Veit & Kumar, "Long-Tail Learning via
Logit Adjustment", ICLR 2021, arXiv:2007.07314). Their adjustment
subtracts \eqn{\tau \log(\pi_i)} from class \eqn{i}'s logit, where
\eqn{\pi_i} is class \eqn{i}'s training-set frequency; exponentiating,
this is equivalent to dividing the raw (pre-softmax) score by
\eqn{\pi_i^{\tau}}. Because \eqn{\pi_i = n_i / N} for a constant \eqn{N}
(total training count) shared by every candidate being compared for the
same observation, dividing by \eqn{n_i^{\tau}} instead of
\eqn{\pi_i^{\tau}} gives identical relative rankings and ratios among
candidates -- so \eqn{N} does not need to be known or estimated here.

At \eqn{\tau = 1}, this fully removes the \eqn{n_i} factor,
recovering an estimate of the true likelihood \eqn{L_i} exactly under the
\eqn{score_i \propto L_i \times n_i} model -- the value Menon et al. derive
as Fisher-consistent for the class-balanced error from Bayes' rule
directly. The default is \code{tau = 0} (no correction), not \code{1} -- see
"Why the default is \code{tau = 0}" below for why. \eqn{\tau} remains a
tunable argument (not hardcoded to any one value) because Menon et al.'s
own empirical tuning did not always land on 1 --
on CIFAR-10-LT they found a validation-tuned optimum of 2.6, i.e.
\emph{stronger} correction than the naive theoretical default improved
balanced accuracy further in that setting.
}

\subsection{Why a single global exponent, not an adaptive per-candidate one}{

An \strong{adaptive, per-candidate} exponent
\eqn{\tau_i = n_i / (n_i + prior\_weight)}, intended to shrink correction
strength toward zero for candidates with small \eqn{n_i} (treating a
small public-database count as a less trustworthy proxy), was considered
and rejected. Literature research found no support for a per-candidate
adaptive exponent in the long-tail/class-imbalance literature -- the
standard, theoretically justified form (logit adjustment) uses one fixed
scalar applied uniformly regardless of a given candidate's own \eqn{n_i}.
The adaptive version also behaves almost like a step function centered on
the candidate set's median count: candidates below the median are barely
corrected while candidates above it are divided by nearly their full raw
count, meaning any two candidates with comparable raw scores but very
different \eqn{n} would have the correction decide the outcome almost
entirely in favor of the lower-n candidate regardless of which one is
actually correct.
}

\subsection{Open caveat: noisy proxy counts}{

Menon et al.'s Fisher-consistency guarantee formally assumes \eqn{\pi_i}
(here, \eqn{n_i}) is a consistent estimate of the classifier's \emph{actual}
internal training-set frequency. In this package's use case, \eqn{n_i} is
instead a noisy \strong{external proxy} -- a public database's observation or
recording count (iNaturalist, Xeno-canto) -- not the classifier's true
internal training count, which is unknown. The literature research
conducted for this revision found no paper that directly studies how much
this guarantee degrades under a noisy external proxy; this is a genuine
open gap, not a settled question. \eqn{\tau} is therefore left tunable
(not fixed at the theoretical default) so it can be validated empirically
-- e.g. by bucketing real labeled observations by the log-ratio of the
true species' count to its best wrong competitor's count, and checking
whether correction helps or hurts accuracy in each bucket -- once enough
labeled data exists, rather than trusted uncritically at \eqn{\tau = 1}.

\strong{Why the default is \eqn{\tau = 0}:} every real calibration run against
this function's own real classifier output has found \eqn{\tau \approx 0}
optimal, not the theoretical \eqn{\tau = 1}. On 51 real,
taxonomic-scope-filtered camera-trap photos (8 species), with
\code{assign_scores()}'s \code{score_sharpness} calibrated jointly with \eqn{\tau}
(log-loss-minimizing, \code{TaxaLikely/inst/workflows/ calibrate_training_bias_tau.R}), log-loss and accuracy agree and both are
monotonic in \eqn{\tau} -- log-loss rises and accuracy falls steadily from
\eqn{\tau = 0} (82\% top-1 accuracy) to \eqn{\tau = 1} (63\%) and beyond:
for this image pathway, the correction should not be applied. On a
larger real BirdNET acoustic dataset -- 8 confusable clusters, 24
species, 2487 real detection windows -- the pooled result matches image:
\eqn{\tau \approx 0} is optimal, with log-loss and accuracy both getting
monotonically worse from \eqn{\tau = 0} through \eqn{\tau = 6}. Per-cluster
results are genuinely heterogeneous, though (2 of 8 clusters still prefer
high \eqn{\tau} even at ~150-170 windows each, unbracketed at the swept
grid's edge) -- a single global \eqn{\tau} does not fit this data well
even though the pooled default favors 0.

A user who does nothing gets no correction (matching every real result
obtained so far) rather than a correction contradicted by every real
result obtained so far. This is \strong{not} a claim that \eqn{\tau = 0} is
correct in general or for every data type -- only that it is the
evidence-backed starting point given what has actually been measured.
\eqn{\tau} (and, for the \code{similarity_softmax} pathway, \code{score_sharpness})
should still be calibrated per data type -- and, per the acoustic
per-cluster result above, potentially per taxon cluster -- before being
raised. Run \code{calibrate_training_bias_tau.R} against real labeled data for
any new data type before deciding whether to apply this correction at all.
}

\subsection{Missing or zero counts: deliberate deviation from strict logit adjustment}{

Strict logit adjustment assumes every class's \eqn{\pi_i} is known.
Candidates with \code{NA} or non-positive counts (failed lookups, or species
genuinely absent from the public database) instead fall through to the
\strong{uncorrected} score (\eqn{\tau} effectively 0 for that row only) --
a deliberate, practical deviation from the pure
literature form: applying full correction based on a missing or zero
count would either be undefined (division by zero at \eqn{\tau > 0}) or
arbitrary, and whether a zero/NA reflects genuine rarity or a lookup
failure cannot be distinguished from the count alone.
}

\subsection{Column contract}{

\code{score_col} is overwritten in place with the corrected value, so no
downstream call (e.g. \code{\link[=unreferenced_candidates]{unreferenced_candidates()}}, \code{\link[=assign_scores]{assign_scores()}})
needs to change -- they already consume \code{score_col} by default. The
pre-correction value is preserved under \code{score_uncorrected} for
debugging. Diagnostic columns \code{n_used} (the count actually applied,
\code{NA} preserved as-is) and \code{tau_used} (the exponent actually applied per
row -- either the global \code{tau} or 0 for a fallen-through row) are also
added.
}

\subsection{Pipeline placement}{

Run this on the raw multi-candidate classifier output, before
\code{\link[=unreferenced_candidates]{unreferenced_candidates()}} adds H2/H3 placeholder rows -- those rows
have no real score to correct and are anchored off the corrected H1
rows downstream:
\preformatted{
raw scored_df -> correct_training_bias() -> unreferenced_candidates() -> assign_scores()
}
}
}
\examples{
scored <- data.frame(
  observation_id = c("obs1", "obs1", "obs2"),
  taxon_name     = c("Turdus migratorius", "Turdus merula", "Limosa fedoa"),
  score_original = c(0.9, 0.85, 0.6),
  n_observations = c(500000, 20, 300)
)
corrected <- correct_training_bias(scored, count_col = "n_observations")
corrected[, c(
  "taxon_name", "score_uncorrected", "score_original",
  "n_used", "tau_used"
)]

}
\references{
Menon, A. K., Jayasumana, S., Rawat, A. S., Jain, H., Veit,
A., & Kumar, S. (2021). Long-Tail Learning via Logit Adjustment. ICLR
2021. \url{https://arxiv.org/abs/2007.07314}
}
\seealso{
\code{\link[=assign_scores]{assign_scores()}}, \code{\link[=unreferenced_candidates]{unreferenced_candidates()}}
}
