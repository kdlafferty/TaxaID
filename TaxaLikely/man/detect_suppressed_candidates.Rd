% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_collapse.R
\name{detect_suppressed_candidates}
\alias{detect_suppressed_candidates}
\title{Detect pipeline rules that suppress candidate rows in a match object}
\usage{
detect_suppressed_candidates(
  match_obj,
  score_col = "score_original",
  observation_id_col = "observation_id",
  perfect_threshold = 100,
  purity_threshold = 0.99,
  singleton_threshold = 0.98
)
}
\arguments{
\item{match_obj}{Data frame. Standardised match object containing at least
\code{observation_id_col}.  \code{score_col} is optional; Rules 1 and 2
are skipped when it is absent.}

\item{score_col}{Character. Name of the score column (default
\code{"score_original"}).}

\item{observation_id_col}{Character. Name of the observation ID column
(default \code{"observation_id"}).}

\item{perfect_threshold}{Numeric. Score at or above which a candidate is
considered a "perfect" match for Rule 1 detection (default \code{100}).
Set to 95 for pipelines that enforce a 95-percent identity floor, for example.}

\item{purity_threshold}{Numeric in (0, 1]. Fraction of qualifying
observations that must exhibit the pattern for the rule to be flagged
(default \code{0.99}).  A high value reduces false positives from
rounding artefacts.}

\item{singleton_threshold}{Numeric in (0, 1]. Fraction of all observations
that must be singletons (1 row) for Rule 3 to be flagged (default
\code{0.98}).}
}
\value{
A named list:
\describe{
\item{\code{rule_detected}}{Logical. \code{TRUE} if any rule is flagged.}
\item{\code{rules}}{Character vector of flagged rule names (may be empty).}
\item{\code{perfect_only}}{Logical.}
\item{\code{max_score_ties}}{Logical.}
\item{\code{best_only}}{Logical.}
\item{\code{has_score_col}}{Logical. Whether \code{score_col} was found.}
\item{\code{n_total}}{Integer. Unique observations.}
\item{\code{n_perfect_obs}}{Integer. Observations with >=1 score >=
\code{perfect_threshold}.}
\item{\code{purity_perfect}}{Numeric. Fraction of perfect-obs that are pure
(no sub-threshold rows).}
\item{\code{n_multi_obs}}{Integer. Observations with >1 row.}
\item{\code{purity_ties}}{Numeric. Fraction of multi-row obs with uniform
scores.}
\item{\code{frac_singleton}}{Numeric. Fraction of obs that are singletons.}
\item{\code{example_observations}}{Character. Up to 5 affected observation IDs.}
}
}
\description{
Inspects a match object for evidence that an upstream tool suppressed
lower-scoring candidates.  Three patterns are recognised:
}
\details{
\describe{
\item{\code{"perfect_only"}}{Among observations where at least one candidate
scores at or above \code{perfect_threshold}, none also contains a row
below that threshold.  Typical cause: a 100-percent rule that drops all
sub-perfect hits when a perfect match exists.}
\item{\code{"max_score_ties"}}{Multi-row observations show zero within-
observation score variance -only tied-top candidates were returned.}
\item{\code{"best_only"}}{Essentially all observations have exactly one
candidate row, indicating a top-1 pipeline, pre-computed consensus, or
absence of a score column.}
}

Detection is conservative: a rule is flagged only when the observed pattern
is present in at least \code{purity_threshold} of qualifying observations.
}
\examples{
m <- data.frame(
  observation_id = c("obs1", "obs1", "obs2"),
  score_original = c(100, 100, 99),
  taxon_name     = c("Sp_A", "Sp_B", "Sp_C")
)
detect_suppressed_candidates(m)

}
\seealso{
\code{\link[=restore_suppressed_candidates]{restore_suppressed_candidates()}}
}
