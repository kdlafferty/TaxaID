.Rproj.user
.Rhistory
.Rdata
.httr-oauth
.DS_Store
.quarto

# The four retired archive_* directories formerly kept locally, uncommitted
# (DECIPHER-based reference audit, the unused likelihood entry point, the
# unused reference wrappers, and the coverage-filter calibration pair) moved
# to the sibling TaxaID_dev repository (2026-09-20, A4 hygiene pass) and no
# longer live under this package directory at all. See TaxaLikely/CLAUDE.md's
# session notes and ecosystem_docs/NAME_CHANGE_HISTORY.md (also moved to
# TaxaID_dev) for the original design record.
