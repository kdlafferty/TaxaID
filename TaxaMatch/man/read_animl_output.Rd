% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_image.R
\name{read_animl_output}
\alias{read_animl_output}
\title{Read Animl Camera Trap Results into a Match Object}
\usage{
read_animl_output(
  files,
  file_col = "FileName",
  species_col = "prediction",
  score_col = "confidence",
  common_name_col = NULL,
  n_candidates = NULL,
  min_confidence = 0,
  top_n = NULL
)
}
\arguments{
\item{files}{Character vector. Paths to Animl result CSV files. Alternatively,
a path to a directory: all \verb{*.csv} files in that directory (non-recursive)
are read.}

\item{file_col}{Character. Name of the column containing the image file path
or filename. Default \code{"FileName"} (Animl R package manifest format).}

\item{species_col}{Character. Name of the column containing the species
prediction (scientific name). Default \code{"prediction"}. For wide-format
outputs (multiple candidates per row), set \code{n_candidates} and this
argument becomes the prefix (e.g., \code{"pred"} for \code{pred1}, \code{pred2}, ...).}

\item{score_col}{Character. Name of the confidence column. Default
\code{"confidence"}. For wide-format outputs, this is used as the prefix
(e.g., \code{"score"} for \code{score1}, \code{score2}, ...).}

\item{common_name_col}{Character or \code{NULL}. Name of a common name column,
if present. Default \code{NULL} (common name column set to \code{NA}).}

\item{n_candidates}{Integer or \code{NULL}. If \code{NULL} (default), expects
\strong{long format} — one row per image × candidate species, with \code{species_col}
and \code{score_col} holding the prediction and confidence directly. If a
positive integer, expects \strong{wide format} — one row per image crop with
candidate columns named \code{paste0(species_col, 1:n_candidates)} and
\code{paste0(score_col, 1:n_candidates)} (e.g., \code{pred1}/\code{score1} through
\code{pred3}/\code{score3}). The wide format is pivoted to long before filtering.}

\item{min_confidence}{Numeric. Detections below this confidence are dropped.
Default \code{0} (keep all).}

\item{top_n}{Integer or \code{NULL}. If supplied, only the top \code{n} candidates
(by confidence) within each image are retained. Default \code{NULL} (keep all).}
}
\value{
A data frame with one row per image × candidate species, containing:
\describe{
\item{\code{observation_id}}{Unique identifier derived from the image filename
stem (path stripped, extension(s) stripped). Multiple rows with the
same \code{observation_id} represent alternative species candidates for the
same image/crop — analogous to multiple BLAST hits per eDNA query or
multiple BirdNET candidates per time window.}
\item{\code{score}}{Animl classifier confidence (0–1). Pass as \code{score_col}
to \code{\link[=standardize_match_data]{standardize_match_data()}}.}
\item{\code{species}}{Species prediction as reported by Animl. May be
\code{"empty"} (no animal detected), \code{"human"}, or \code{"vehicle"} for
non-wildlife detections — filter these out before proceeding.}
\item{\code{genus}}{Genus name (first word of \code{species}). \code{NA} for
non-binomial labels such as \code{"empty"}.}
\item{\code{common_name}}{Common name from \code{common_name_col}, or \code{NA} if
that column is absent.}
\item{\code{source_file}}{Basename of the source CSV file.}
}
}
\description{
Reads one or more Animl (MegaDetector + SpeciesNet) result CSV files and
returns a tidy data frame in match object format, ready for
\code{\link[=standardize_match_data]{standardize_match_data()}} and downstream TaxaLikely processing.
}
\details{
\strong{Animl output formats:} The Animl R package
(\code{animl} on CRAN; wraps MegaDetector + SpeciesNet) supports multiple export
formats. Two are handled here:

\emph{Long format} (default, \code{n_candidates = NULL}): one row per image crop ×
candidate species. Columns: \code{FileName}, \code{prediction}, \code{confidence}. This
is the format produced when you export classification results row-by-row.

\emph{Wide format} (\code{n_candidates = 3}): one row per image crop with top-N
candidates in columns \code{pred1}/\code{score1}, \code{pred2}/\code{score2}, \code{pred3}/\code{score3}.
Pass \code{species_col = "pred"}, \code{score_col = "score"}, \code{n_candidates = 3}.

\strong{Filtering non-wildlife detections:} Animl assigns non-animal labels
(\code{"empty"}, \code{"human"}, \code{"vehicle"}) to images with no wildlife. Filter
these before standardizing:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{match_df <- read_animl_output("animl_results/") |>
  dplyr::filter(!species \%in\% c("empty", "human", "vehicle"))
}\if{html}{\out{</div>}}

\strong{observation_id encoding:} Each image (or bounding box crop, if cropped
images are used) is one \code{observation_id}. Multiple species candidates for
the same image share the same \code{observation_id}, enabling TaxaLikely's gap
metric (top-1 minus top-2 confidence) to operate correctly.

\strong{Taxonomy:} \code{genus} is derived as the first word of the species column.
For the full taxonomic hierarchy (family, order), run
\code{\link[TaxaTools:verify_taxon_names]{TaxaTools::verify_taxon_names()}} on the \code{species} column after filtering,
then re-run \code{\link[=standardize_match_data]{standardize_match_data()}}.

\strong{Downstream workflow:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{match_df <- read_animl_output("animl_results/", min_confidence = 0.5) |>
  dplyr::filter(!species \%in\% c("empty", "human", "vehicle")) |>
  standardize_match_data(
    observation_id_col = "observation_id",
    score_col          = "score",
    rank_system        = c("genus", "species")
  )
}\if{html}{\out{</div>}}
}
\examples{
# Long-format Animl output (one row per image x candidate)
tmp <- tempfile(fileext = ".csv")
write.csv(data.frame(
  FileName   = c("img001.jpg", "img001.jpg", "img002.jpg"),
  prediction = c("Odocoileus virginianus", "Cervus canadensis", "empty"),
  confidence = c(0.93, 0.05, 0.99),
  stringsAsFactors = FALSE
), tmp, row.names = FALSE)

result <- read_animl_output(tmp)
head(result)
unlink(tmp)
}
\seealso{
\code{\link[=standardize_match_data]{standardize_match_data()}},
\code{\link[TaxaLikely:build_acoustic_reference]{TaxaLikely::build_acoustic_reference()}}
}
