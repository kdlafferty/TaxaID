% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_marker_mismatch.R
\name{check_marker_mismatch}
\alias{check_marker_mismatch}
\title{Cross-Check a Reference Accession's Own Annotated Gene/Product Against an Expected Marker}
\usage{
check_marker_mismatch(
  accessions,
  expected_marker,
  ncbi_api_key = Sys.getenv("NCBI_API_KEY", unset = ""),
  verbose = TRUE
)
}
\arguments{
\item{accessions}{Character vector of accessions to check.}

\item{expected_marker}{Character scalar (e.g. \code{"12S"}, \code{"16S"}, \code{"COI"})
-- the marker/barcode the evaluation this accession was flagged under
was scoped to. Matched case-insensitively against a small internal
lookup of common marker-name synonyms as they actually appear in real
GenBank \verb{/gene}/\verb{/product} qualifiers (e.g. \code{"12S"} also matches
\code{"s-rRNA"}/\code{"small subunit ribosomal RNA"}); an unlisted marker falls
back to a literal, case-insensitive substring match on
\code{expected_marker} itself.}

\item{ncbi_api_key, verbose}{As in \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}.}
}
\value{
A data frame, one row per unique input accession:
\describe{
\item{\code{accession}}{As supplied.}
\item{\code{expected_marker}}{As supplied.}
\item{\code{annotated_genes}}{Semicolon-joined, deduplicated \verb{/gene}
qualifier values found anywhere in the record's feature table.
\code{NA} if none were found (may still have \verb{/product} annotation).}
\item{\code{annotated_products}}{Same, for \verb{/product}.}
\item{\code{marker_match}}{\code{TRUE} if ANY annotated \verb{/gene} or \verb{/product}
text matches \code{expected_marker}'s resolved pattern; \code{FALSE} if the
record has real gene/CDS/rRNA/misc_feature annotation but none of
it matches; \code{NA} if the record has NO such annotation to judge at
all -- a distinct, honest outcome, not evidence of mismatch.}
}
}
\description{
Before investigating a flagged accession as a possible SPECIES mislabel
(see \code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}}), this does a single cheap GBSeq
XML fetch and checks the record's own \verb{/gene} or \verb{/product}
feature-table qualifier against the marker an evaluation was scoped to
(e.g. does a "12S"-scoped audit's flagged record actually say
\verb{/product="16S ribosomal RNA"}?). No BLAST, no alignment -- much cheaper
than \code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}}'s deep dive, and meant to route a
flagged accession to a completely different, simpler resolution path
(correct the marker label, or exclude the accession from this marker's
own reference set) before ever reaching it.
}
\section{Why this exists (2026-08-08)}{

Directly grounded in a real, already-confirmed case from this ecosystem:
\code{AY850362} (a real GreatLakes 12S reference-database audit accession,
confirmed 2026-08-06/07) is a genuine 16S-vs-12S MARKER mislabel, not a
species mislabel. An earlier hypothesis -- that a disagreement resolved
only at a coarse taxonomic rank (family/order/phylum) signals a
marker/gene mislabel, while a disagreement at genus/species signals a
real species mislabel -- was tested directly against \code{MZ605481} (a real
confirmed SPECIES mislabel) and refuted: \code{MZ605481}'s own disagreement is
\emph{also} recorded at a coarse rank (order), yet is a real species mislabel,
not a marker mislabel. What actually distinguishes the two is each
record's own annotated gene/product metadata, not its taxonomic rank of
disagreement -- this function checks that directly instead.
}

\seealso{
\code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}}, \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}
}
