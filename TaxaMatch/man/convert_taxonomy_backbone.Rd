% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_taxonomy_backbone.R
\name{convert_taxonomy_backbone}
\alias{convert_taxonomy_backbone}
\title{Convert Match Object Taxonomy to a Target Backbone}
\usage{
convert_taxonomy_backbone(
  match_df,
  target_backbone_id,
  source_backbone_id = NULL,
  rank_system = c("order", "family", "genus", "species"),
  taxon_col = "taxon_name",
  update_taxon_name = TRUE,
  original_col = "taxon_name_original",
  backbone_col = "taxonomy_backbone",
  collision_col = "taxonomy_collision",
  verify_fn = TaxaTools::verify_taxon_names,
  verbose = TRUE
)
}
\arguments{
\item{match_df}{A data frame containing a taxon name column and rank columns.}

\item{target_backbone_id}{Integer. The target backbone identifier.
Standard IDs: 1 = Catalogue of Life, 3 = ITIS, 4 = NCBI, 9 = WoRMS,
11 = GBIF. See \url{https://verifier.globalnames.org/} for the full list.}

\item{source_backbone_id}{Integer or \code{NULL}. The backbone that produced
\code{match_df}'s current hierarchy. Used only to label the \code{taxonomy_collision}
column for rows not found in the target backbone. When \code{NULL} (default),
not-found rows are labelled \code{"original"}.}

\item{rank_system}{Character vector of rank column names to compare and
potentially update, listed broadest to finest (e.g.
\code{c("order", "family", "genus", "species")}). Columns absent from \code{match_df}
are silently skipped.}

\item{taxon_col}{Character. Name of the column containing the taxon name
used as the lookup key (default \code{"taxon_name"}).}

\item{update_taxon_name}{Logical. When \code{TRUE} (default), \code{match_df[[taxon_col]]}
is updated to the target backbone's accepted name (authority strings
stripped via \code{\link[TaxaTools:clean_taxon_names]{TaxaTools::clean_taxon_names()}}). The original value is
preserved in \code{match_df[[original_col]]}.}

\item{original_col}{Character. Name of the column to receive the original
taxon name when \code{update_taxon_name = TRUE} (default \code{"taxon_name_original"}).
Created if absent; unchanged if already present.}

\item{backbone_col}{Character. Name of the column recording which backbone
each row's hierarchy came from (default \code{"taxonomy_backbone"}). Created if absent.}

\item{collision_col}{Character. Name of the column recording the per-row
conversion outcome (default \code{"taxonomy_collision"}). Created if absent.}

\item{verify_fn}{Function. The name verification function to call. Must
accept a character vector as its first argument and a \code{backbone_id}
argument; must return a data frame with columns \code{user_supplied_name},
\code{matched_name}, \code{classification_path}, \code{classification_ranks}, and
\code{verified} (logical: \code{TRUE} when the name resolved in the target
backbone, \code{FALSE}/\code{NA} otherwise). An optional \code{matched_rank} column
(the rank \code{matched_name} actually resolved at) enables the rank
correction described below; a \code{verify_fn} without it is still fully
supported, just without that correction. Default:
\link[TaxaTools:verify_taxon_names]{TaxaTools::verify_taxon_names}.
Override for offline testing via dependency injection. The call is
wrapped in \code{tryCatch()}; a failure (e.g. network unavailable, API
rate-limited) raises a clear error naming the likely cause rather than
propagating whatever uninformative error the API layer produced.}

\item{verbose}{Logical. Print the backbone column mapping summary message
at the end. Default \code{TRUE}. \code{warning()}s for inconsistent taxonomy are
always issued regardless of this setting.}
}
\value{
\code{match_df} with rank columns potentially updated, plus \code{backbone_col},
\code{collision_col}, and (when \code{update_taxon_name = TRUE}) \code{original_col}
columns added -- \code{backbone_col}/\code{collision_col} are created if absent
and left unchanged (not overwritten) if already present, which matters
for iterative/multi-pass pipeline use. The attribute \code{backbone_cols} is
set: a named list mapping \code{"backbone_N_cols"} to the rank column names
that were subject to conversion, e.g.
\code{list(backbone_11_cols = c("order", "family"), backbone_4_cols = c("kingdom", "phylum"))}.
Because R uses copy-on-modify semantics, the caller's original
\code{match_df} object is untouched; the return value must be (re)assigned.
}
\description{
Looks up each unique taxon name in \code{match_df[[taxon_col]]} against a target
taxonomic backbone (e.g. GBIF, NCBI), then replaces rank columns with
the target backbone's hierarchy wherever the target provides a non-NA
value (\emph{per-column fallback}: ranks the target omits are left unchanged).
}
\details{
A \code{taxonomy_collision} column records what happened for each row:
\describe{
\item{\code{"consistent"}}{Taxon found in target backbone; all supplied rank columns agree with the original.}
\item{\code{"backbone_N[col1,col2]"}}{Taxon found; listed columns were changed to target backbone N values.}
\item{\code{"backbone_N"} or \code{"original"}}{Taxon not found in target backbone; hierarchy
unchanged. Label is \code{"backbone_N"} when \code{source_backbone_id} is supplied, otherwise
\code{"original"}.}
}

A \code{taxonomy_backbone} column records which backbone the row's hierarchy
was drawn from (\code{"backbone_N"} for found rows, source label for not-found rows).
}
\note{
This function issues one API call to \code{verify_fn} covering all unique
non-NA taxon names in \code{match_df}. Rank updates and collision detection are
performed with vectorised operations (no row-by-row loop).

Taxonomic backbones assign different family/order hierarchies to the same
species (e.g. NCBI places \emph{Girella nigricans} in Girellidae; GBIF places
it in Kyphosidae). Call \code{convert_taxonomy_backbone()} on your match object
before passing it to \code{\link[=filter_redundant_hypotheses]{filter_redundant_hypotheses()}} or to
\code{\link[TaxaAssign:join_priors]{TaxaAssign::join_priors()}} when the prior expansion taxonomy was built
from a different backbone. For eDNA metabarcoding specifically, WoRMS
(backbone 9) is the authoritative backbone for marine species; GBIF (11)
is the more general default for terrestrial/mixed studies.

Lookup is by taxon \emph{name}, not ID (the correct approach for cross-backbone
conversion, since IDs are backbone-specific), which means homonyms --
the same name used for unrelated taxa in different kingdoms, e.g. \emph{Morus}
(a plant genus, mulberry, and a bird genus, gannets) -- can resolve to the
more common usage rather than the intended one. Filter to a single kingdom
before conversion, or verify results for known homonymous genera. Each
unique \code{taxon_col} value is also resolved independently (no genus-up
cascade): if a species name is not found in the target backbone, that
row's hierarchy is left unchanged even when the genus or family \emph{is}
present in the target -- whether that is the desired behavior depends on
the use case.

Whitespace-only taxon names (e.g. \code{" "}) are treated as present, not
blank, by the \code{nzchar()} checks used throughout (\code{nzchar(" ")} is \code{TRUE});
if this is a realistic data-quality issue for your input, \code{trimws()} your
\code{taxon_col} before calling.
}
\section{Rank correction on fallback (2026-07-25)}{

When a row's own \code{taxon_name_rank} has no matching target-backbone value
at that same rank (e.g. a row claims \code{"species"} but the target only
resolved this name to genus), \code{match_df[[taxon_col]]} falls back to the
coarser resolved name -- and \code{taxon_name_rank} is now corrected to match
it, using \code{verify_fn}'s \code{matched_rank} column when present (silently
skipped for a \code{verify_fn} that predates it, e.g. injected for offline
testing). Real motivating case: an informally-named NCBI reference
("Inu sp. 1 sensu Shibukawa et al., 2020.") resolves against GBIF to the
genus \code{Luciogobius} (a synonym relationship -- GBIF's own backbone
considers \code{"Inu"} Snyder 1909 a synonym of \code{Luciogobius} Gill 1859; NCBI's
own taxonomy does not) with no species-level entry to fill. Before this
fix, \code{taxon_name} was correctly demoted to the genus but
\code{taxon_name_rank} silently stayed \code{"species"} -- a downstream slash-name
builder then treated the bare genus as if it were a complete binomial and
manufactured a fabricated pseudo-binomial (\code{"Inu Inu"}) from it. Confirmed
backbone-general, not GBIF-specific, before shipping -- see
\code{TaxaTools::verify_taxon_names()}'s own \verb{@section Synonym resolution and rank correctness}.

\strong{Finer rank columns are also cleared on the same rows} (e.g. \code{species}
set to \code{NA} when the row is demoted to genus), not just
\code{taxon_name}/\code{taxon_name_rank}. Found necessary by testing against a real
production workflow, not by inspection alone: the per-column fallback
(documented above) deliberately keeps each rank column's own cleaned
original value when the target backbone has no replacement for it --
correct for a column that genuinely didn't change, but it left a stale,
semantically-invalid value (\code{species = "Inu"}) in place even after
\code{taxon_name}/\code{taxon_name_rank} were correctly demoted. Any code that
re-derives a name from rank columns after calling this function (e.g. a
second \code{TaxaTools::create_taxon_names()} call, a real step some
production workflows already run "to re-derive taxon_name after backbone
conversion") would otherwise see \code{species} still populated, apply
"most specific non-NA rank wins", and silently undo the correction.
}

\section{Rank correction on a genus-collapsing name, any source (2026-08-21)}{

The \code{matched_rank}-driven correction above only fires when the target
backbone's own reported rank disagrees with the row's claimed rank. A
second, independent mechanism corrects \code{taxon_name_rank} whenever the
value that actually ended up in \code{taxon_name} was produced by
\code{\link[TaxaTools:clean_taxon_names]{TaxaTools::clean_taxon_names()}} COLLAPSING a real second token to
genus-only -- regardless of which of the three possible sources produced
it (a not-found row's fallback cleaning; a found row's \code{matched_name_clean}
fallback when its own rank had no target value; or a found row's own
\verb{target_<rank>} value at its claimed rank). This closes two real,
independently-discovered cases from GreatLakes2023 production data:
(1) an open-nomenclature, specimen-voucher-tagged reference label (e.g.
\code{"Ictalurus cf. pricei USON-01120-1"}) that never resolves in the target
backbone at all (\code{found_mask = FALSE}); and (2), found only after
verifying (1)'s original fix against a real re-run and still finding
stale rows: NCBI's own taxonomy DB genuinely contains leaf-level nodes
for informally-named specimens (e.g. a real node literally named
\code{"Ictalurus sp. UM 105-1789"}, ranked \code{"species"} by NCBI itself) -- here
\code{found_mask = TRUE} and \code{matched_rank} genuinely IS \code{"species"}
(backbone-consistent, so the \code{matched_rank}-driven correction correctly
does nothing), but the classification path's own species-rank VALUE is
that same informal label, which collapses to genus-only when cleaned.
No rank-MISMATCH-based correction can ever catch case (2) -- the
backbone's own rank claim is genuinely self-consistent; only the collapse
signal itself reveals the problem. Before this fix, \code{taxon_name_rank}
stayed stale at whatever rank the row claimed before conversion,
mislabeling a bare genus as if it were still species-level.

Deliberately tracks the actual collapse event through the pipeline (via
\code{collapsed_to_genus}) rather than re-deriving the signal from the final
value's shape (e.g. via \code{\link[TaxaTools:is_plausible_binomial]{TaxaTools::is_plausible_binomial()}}) -- that
function's binomial regex requires a literal space immediately after the
genus token, which a real hyphenated genus (e.g. \emph{Pseudo-nitzschia},
already a real fixture elsewhere in this ecosystem's own test suite)
fails, which would have wrongly demoted every hyphenated-genus species
row. Requires \code{"genus"} to be present in \code{rank_system}; a no-op
otherwise. Gracefully absent (no-op) for a \code{clean_taxon_names()}-alike
that predates the \code{collapsed_to_genus} attribute. Rank columns finer than
the demoted rank are cleared, same as the \code{matched_rank}-driven
correction above.

An R attribute \code{backbone_cols} is set on the returned data frame recording
which rank columns were subject to backbone conversion. A summary message
is also printed.
}

\section{Not-found rows are cleaned too}{

When a taxon is not found in the target backbone, its original rank
columns and \code{taxon_col} value are still run through
\code{\link[TaxaTools:clean_taxon_names]{TaxaTools::clean_taxon_names()}} before being used as the output value --
previously only target-backbone-matched values were cleaned, so an exotic
name that failed to resolve (e.g. a compound hybrid-formula name straight
from a raw reference-database accession label,
\code{"((Citrus unshiu x Citrus sinensis) x Citrus reticulata) x Citrus reticulata"})
passed through completely unmodified. This does NOT change which names are
sent to \code{verify_fn} or which rows count as "found" -- only the fallback
value's formatting.
}

\examples{
\dontrun{
# Convert a BLAST match object (NCBI backbone, backbone_id = 4) to GBIF (11)
match_obj_gbif <- convert_taxonomy_backbone(
  match_df           = match_obj,
  target_backbone_id = 11,
  source_backbone_id = 4,
  rank_system        = c("order", "family", "genus", "species")
)
attr(match_obj_gbif, "backbone_cols")
}

}
\seealso{
\code{\link[TaxaTools:verify_taxon_names]{TaxaTools::verify_taxon_names()}}, \code{\link[TaxaTools:clean_taxon_names]{TaxaTools::clean_taxon_names()}}
}
