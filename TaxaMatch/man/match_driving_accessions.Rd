% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_corroboration.R
\name{match_driving_accessions}
\alias{match_driving_accessions}
\title{Which Reference Accessions Ever Drive a Likelihood?}
\usage{
match_driving_accessions(
  match_df,
  score_col = "score_original",
  obs_col = "observation_id",
  species_col = "species",
  accession_col = "accession"
)
}
\arguments{
\item{match_df}{Data frame. A standardized match object with the four
columns named below.}

\item{score_col, obs_col, species_col, accession_col}{Character. Column
names; defaults \code{"score_original"}, \code{"observation_id"}, \code{"species"},
\code{"accession"}.}
}
\value{
Character vector of accessions, exactly as they appear in
\code{match_df} (version suffixes untouched, so they key the same cache rows
a plain \code{unique(match_df$accession)} would), unique, in first-appearance
order.
}
\description{
Returns the accessions that are the max-scoring accession of their species
for at least one observation (ties kept). \code{TaxaLikely::evaluate_likelihoods()}
reads the per-species best match, so every other accession never drives a
likelihood and no verdict on it can change an assignment. This is a COST
FILTER for the BLAST screen (\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}), not a
change to the match object: on the real PtConception 12S match object it
keeps 709 of 995 candidate accessions.
}
\details{
\verb{RESTORED_*} provenance accessions (from
\code{TaxaLikely::restore_suppressed_candidates()}) are dropped -- they are
not GenBank records and cannot be screened. The Mugu workflow did this by
hand at its call site; it is done here once.
}
\seealso{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}, \code{\link[=corroborate_references_locally]{corroborate_references_locally()}}
}
