% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_flagged_accessions.R
\name{review_flagged_accessions}
\alias{review_flagged_accessions}
\title{LLM Second-Look Review of Flagged Reference Accessions}
\usage{
review_flagged_accessions(
  evaluated_df,
  hierarchy_flags = c("incongruent", "insufficient_independent_evidence"),
  include_non_species_resolved = TRUE,
  llm_fn = getOption("TaxaID.llm_fn", TaxaTools::call_api),
  taxa_per_call = 10L,
  max_tokens = NULL,
  max_retries = 2L,
  pause_seconds = 1,
  verbose = TRUE
)
}
\arguments{
\item{evaluated_df}{Data frame -- \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}'s own
output. Must contain \code{accession}, \code{listed_taxon}, \code{hierarchy_flag}, one
row per accession. Every other column used in the prompt
(\code{finest_common_rank}, \code{best_agreeing_pident}, \code{best_disagreeing_pident},
\code{best_disagreeing_taxon}, \code{congruent_evidence_exists_anywhere},
\code{congruent_evidence_best_pident}, \code{taxonomy_resolution_source},
\code{listed_taxon_is_species}, \code{n_independent_top_matches},
\code{n_top_matches_available}, \code{frac_independent_below_min_congruent_rank})
is optional -- silently omitted from the prompt when absent, matching
\code{TaxaFlag::review_assignments()}'s established convention for optional
upstream columns. A duplicated \code{accession} value triggers a \code{warning()}
(see Details) rather than an error, since a caller may have joined this
onto a match object by mistake.}

\item{hierarchy_flags}{Character vector of \code{hierarchy_flag} values to
review. Default \code{c("incongruent", "insufficient_independent_evidence")}.}

\item{include_non_species_resolved}{Logical (default \code{TRUE}). Also review
any row with \code{listed_taxon_is_species == FALSE}, regardless of
\code{hierarchy_flag} -- a structurally different, orthogonal problem the
evaluation guide documents separately (see its own "listed_taxon_is_
species = FALSE" section). Silently ignored if \code{listed_taxon_is_species}
is absent from \code{evaluated_df}.}

\item{llm_fn}{Function. LLM provider function with signature
\verb{function(prompt_str, ...)}. Default \code{TaxaTools::call_api}.
\strong{Known footgun:} \code{call_api()}'s provider auto-detection is set up
by TaxaTools's own \code{.onAttach()}, which only runs via
\code{library(TaxaTools)} -- a fully-namespaced call (\verb{TaxaTools::}
everywhere, no \code{library()}) never triggers it, and \code{call_api()} silently
falls back to degraded/uniform output rather than erroring. Pass
\code{llm_fn} explicitly if every review comes back suspiciously uniform,
e.g. \code{function(p) TaxaTools::call_api(p, provider = "anthropic")}. See
\code{TaxaID/CLAUDE.md}'s "Known R Footguns" for the full record.}

\item{taxa_per_call}{Integer. Maximum accessions per LLM call. Default
\code{10L}.}

\item{max_tokens}{Integer or \code{NULL}. Forwarded as
\code{llm_fn(prompt, max_tokens = max_tokens)} whenever supplied. Default
\code{NULL} -- \code{llm_fn}'s own default applies.}

\item{max_retries}{Integer (default \code{2L}). A truncated, empty, or
unparseable batch response is automatically split in half and retried
(a smaller batch requests a proportionally shorter response) up to this
many times, matching \code{review_assignments()}'s own retry mechanism.}

\item{pause_seconds}{Numeric (default \code{1}). Seconds to pause between LLM
calls.}

\item{verbose}{Logical (default \code{TRUE}). Print progress messages.}
}
\value{
\code{evaluated_df} with 3 columns appended (\code{NA} for out-of-scope
rows):
\describe{
\item{\code{accession_likely_explanation}}{One of \code{"genuine_mislabel"},
\code{"poor_marker_resolution"}, \code{"sister_family_thin_coverage"},
\code{"hybrid_or_specimen_code_artifact"}, \code{"uncertain"} -- the LLM's own
categorization of WHY the accession was flagged, never a re-decided
\code{hierarchy_flag}.}
\item{\code{accession_review_confidence}}{\code{"high"} / \code{"moderate"} / \code{"low"}.}
\item{\code{accession_review_comment}}{Free-text note, or \code{NA}.}
}
Also carries \code{attr(result, "llm_prompts")} -- a named list of the exact
prompt string sent for each LLM call (named by batch number, with an
\code{"a"}/\code{"b"} suffix per retry sub-batch split), matching
\code{review_assignments()}'s own convention.
}
\description{
Sends \code{evaluate_reference_accessions()}'s flagged/borderline rows to an LLM
for a free-text second look -- the same "narrative-judgment layer added ON
TOP of statistical flags, never replacing them, never auto-acting" pattern
\code{TaxaFlag::review_assignments()} already established for posterior
taxonomic assignments (implements Question 2 of
\code{ecosystem_docs/REENTRY_PROMPT_flagged_accession_second_look.md}). The LLM
never re-decides \code{hierarchy_flag} -- it adds what the statistical check
structurally cannot: recognizing a known hybrid-cross name (e.g.
\code{"Ctenopharyngodon idella x Megalobrama amblycephala"}, a known Chinese
aquaculture hybrid), an informal specimen code (e.g. \code{"Serranidae sp. JL-2015"}), or a taxonomic-literature fact (a recent species split or
synonymy) that would explain an apparent disagreement.
}
\section{Prompt content, lifted from the evaluation guide, not re-derived}{

The prompt teaches the LLM \code{inst/reference_accession_evaluation_guide.md}'s
own 5-category decision framework (genuine mislabel / poor marker
resolving power / sister-family-thin-coverage artifact / hybrid-cross or
specimen-code artifact / non-species-resolved partner) using that guide's
own worked real examples (\verb{Stereolepis doederleini}, \verb{Abylopsis eschscholtzii}, \code{NC_028197}/\code{"Serranidae sp. JL-2015"}) -- the same
reasoning a careful human reviewer already applies by hand. Keep the guide
updated (and this function's prompt in sync with it) if real usage
surfaces a case class the guide doesn't yet cover.
}

\section{What the LLM outputs -- and does NOT output}{

A free-text \code{accession_review_comment} plus two small structured fields
(\code{accession_likely_explanation}, \code{accession_review_confidence}) -- never a
re-decided \code{hierarchy_flag}, and this function never calls
\code{\link[=remove_incongruent_references]{remove_incongruent_references()}} or otherwise mutates the evaluation
itself. This mirrors this ecosystem's own \code{trusted_rank} cautionary
history (\code{TaxaLikely::evaluate_likelihoods()}, built 2026-07-19, removed
2026-07-20 after a real ~30\% mismatch between the hypothesis it was
computed for and the one that actually won downstream -- see
\verb{[[project_rank_trust_mechanism_removed]]} in the project memory system):
a mechanism that recomputes/overrides an existing categorical verdict is
exactly the shape that broke there. A human reads
\code{accession_review_comment} before acting on it.
}

\section{Package placement}{

Lives in TaxaMatch, not TaxaFlag, deliberately. Every other reference-
accession-quality function (\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}},
\code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}}, \code{check_marker_mismatch()},
\code{\link[=flag_incongruent_references]{flag_incongruent_references()}}, \code{\link[=remove_incongruent_references]{remove_incongruent_references()}})
already lives here and operates on this same output shape.
TaxaMatch -> TaxaFlag is not currently a dependency edge in this
ecosystem -- \code{TaxaFlag::review_assignments()} reviews post-assignment
DETECTIONS (a structurally later pipeline stage, downstream of
TaxaAssign); this reviews pre-assignment REFERENCE DATABASE quality,
upstream of TaxaExpect/TaxaAssign entirely. No new dependency is needed
to place it here either way: this function calls \code{TaxaTools::call_api()}
the exact same way \code{review_assignments()} does, and TaxaMatch already
depends on TaxaTools for several other reasons (\code{is_plausible_binomial()},
\code{clean_taxon_names()}, \code{verify_taxon_names()}, \code{standard_ranks}).
}

\section{Scope (cost control)}{

Only reviews rows matching \code{hierarchy_flags}/\code{include_non_species_resolved}
-- not the whole evaluated population. On the real GreatLakes Goal-2
population this reentry prompt was written against, the default scope
selects 8 + 17 + 57 = 82 of 1,183 accessions (~7\%), a tractable LLM batch
size -- see the reentry prompt's own "Cost/scale" discussion.
}

\seealso{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} for the columns this function
reads, \code{TaxaFlag::review_assignments()} for the precedent this follows,
\code{inst/reference_accession_evaluation_guide.md} for the full guide this
function's prompt is built from.
}
