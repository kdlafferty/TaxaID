% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference_label_verdict.R
\name{verify_local_corroborations}
\alias{verify_local_corroborations}
\title{Audit thin locally-corroborated rows against their own corroborator's verdict}
\usage{
verify_local_corroborations(cache_dir, max_corroborators = 2L, verbose = TRUE)
}
\arguments{
\item{cache_dir}{The same persistent cache directory
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} was called with. Required -- a
\code{"locally_corroborated"} verdict only ever exists inside a persistent
cache (the skip mechanism has nothing to record it in otherwise).}

\item{max_corroborators}{Integer (default \code{2L}). A \code{"locally_corroborated"}
row resting on more than this many independent corroborators is left out
of the audit entirely -- many independent partners all being wrong the
same way is implausible, matching
\code{\link[=verify_removal_candidates]{verify_removal_candidates()}}'s identical threshold.}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
A data frame, one row per thin \code{"locally_corroborated"} accession:
\code{accession}, \code{listed_taxon}, \code{n_corroborators},
\code{local_corroborator_accession}, \code{corroborator_reference_action},
\code{corroborator_hierarchy_flag}, and \code{status} (\code{"flagged"} -- the
corroborator's own evidence reads something other than a clean \code{"keep"}
or \code{"untested"}; \code{"clean"} -- the corroborator has a real \code{"keep"}
verdict; \code{"unchecked"} -- the corroborator has never been independently
evaluated in this cache, so nothing is known either way). Never
\code{"flagged"} for \code{"untested"}: no usable evidence about a corroborator is
not evidence AGAINST it, the same distinction
\code{\link[=verify_removal_candidates]{verify_removal_candidates()}} draws for its own thin corroborators.
Zero rows, and zero NCBI calls always, when the cache has no thin
\code{"locally_corroborated"} rows.
}
\description{
\code{"locally_corroborated"} (2026-09-03) skips BLASTing an accession entirely
when the caller's own reference set already has an independent conspecific
deposit for it -- cached with TTL \code{Inf} and exempt from
\code{\link[=refine_reference_verdicts]{refine_reference_verdicts()}}'s trust-weighted refinement, since the MATCH
itself, once observed, is permanent. But the \emph{corroborator's own label} is
exactly as falsifiable as any other accession's (this is the same
\code{KJ135626}/\code{MZ605481} shape \code{\link[=verify_removal_candidates]{verify_removal_candidates()}} closed for the
removal veto, showing up in a different, larger population). Nothing
today ever asks whether a corroborator's own label later turned out to be
wrong.
}
\details{
This function audits that gap CHEAPLY -- it never BLASTs anything and
never touches NCBI. Both numbers it needs are already sitting in the
persistent cache for free: a \code{"locally_corroborated"} row already carries
its own corroborator count (\code{n_independent_top_matches}, reused from the
BLAST-diagnostic schema) and the corroborator's own accession
(\code{local_corroborator_accession}), and a corroborator that has EVER been
independently evaluated for any other reason already has its own verdict
sitting in the same cache file. So the audit is two free filters, not a
new evaluation: (1) scope to rows resting on \verb{<= max_corroborators}
corroborators (a spare resting on many independent partners agreeing is
not the same risk as one resting on one or two -- the same "thin"
threshold \code{\link[=verify_removal_candidates]{verify_removal_candidates()}} already uses); (2) self-join each
thin row's \code{local_corroborator_accession} against the SAME cache to read
its own verdict, if one exists.
}
\section{Deliberately does not screen the unresolved remainder}{

A corroborator with no cached verdict at all (never independently
evaluated) is reported \code{status = "unchecked"}, not sent to
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} -- unlike \code{\link[=verify_removal_candidates]{verify_removal_candidates()}}'s
own \code{screen_corroborators = TRUE}, which DOES screen its own (much
smaller, 1-4-per-site) unresolved corroborators. The
\code{locally_corroborated} population is the ordinary fast path for any
accession the caller's reference set happens to already double-cover --
plausibly hundreds of rows per project, not a handful -- so forcing every
unresolved corroborator through a fresh BLAST call here could mean a real,
surprise NCBI cost this function's whole point is to avoid. Screening the
unresolved remainder, if wanted, is a separate, explicit follow-up step
(e.g. passing \code{out$local_corroborator_accession[out$status == "unchecked"]}
to \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} directly), not something this
function does on your behalf.
}

\seealso{
\code{\link[=verify_removal_candidates]{verify_removal_candidates()}}, \code{\link[=score_reference_labels]{score_reference_labels()}},
\code{\link[=corroborate_references_locally]{corroborate_references_locally()}}
}
