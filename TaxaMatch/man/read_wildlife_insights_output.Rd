% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_image.R
\name{read_wildlife_insights_output}
\alias{read_wildlife_insights_output}
\title{Read Wildlife Insights / SpeciesNet JSON Results into a Match Object}
\usage{
read_wildlife_insights_output(
  files,
  min_confidence = 0,
  top_n = NULL,
  label_col = "label",
  score_col = "score"
)
}
\arguments{
\item{files}{Character vector. Paths to SpeciesNet/Wildlife Insights JSON
files.  Alternatively, a path to a directory: all \verb{*.json} files are
read.  A single JSON file may contain predictions for multiple images.}

\item{min_confidence}{Numeric. Detections below this score are dropped.
Default \code{0} (keep all).}

\item{top_n}{Integer or \code{NULL}. If supplied, only the top \code{n} candidates
(by confidence) per image are retained. Default \code{NULL} (keep all).}

\item{label_col}{Character. Name of the label/species field inside each
prediction object.  Default \code{"label"} (SpeciesNet v2 format).
Use \code{"species"} for older exports.}

\item{score_col}{Character. Name of the confidence field inside each
prediction object.  Default \code{"score"}.}
}
\value{
A data frame with one row per image x candidate species, containing:
\describe{
\item{\code{observation_id}}{Image filename stem (derived from the key in
the \code{"predictions"} object).}
\item{\code{score}}{SpeciesNet confidence (0--1).}
\item{\code{species}}{Species binomial as returned by SpeciesNet. May be
\code{"blank"}, \code{"human"}, or other non-wildlife labels — filter these
before proceeding.}
\item{\code{genus}}{Genus name (first word of \code{species}). \code{NA} for
non-binomial labels.}
\item{\code{category}}{Detection category from the SpeciesNet output (e.g.,
\code{"animal"}, \code{"blank"}, \code{"human"}, \code{"vehicle"}). \code{NA} if absent.}
\item{\code{source_file}}{Basename of the source JSON file.}
}
}
\description{
Reads one or more JSON output files from the SpeciesNet Python classifier
(used by Wildlife Insights and standalone) and returns a tidy data frame
in match object format, ready for \code{\link[=standardize_match_data]{standardize_match_data()}} and downstream
TaxaLikely processing.
}
\details{
\strong{Supported format:} SpeciesNet v2+ batch output JSON, which contains a
top-level \code{"predictions"} object keyed by image filename, with each value
being a list of prediction objects.  A single JSON file can describe many
images.

\strong{Running SpeciesNet:} SpeciesNet is a Python package from Google /
Wildlife Insights:

\if{html}{\out{<div class="sourceCode bash">}}\preformatted{pip install speciesnet
python -m speciesnet.scripts.run_model \\
  --folders /path/to/images \\
  --predictions_json speciesnet_output.json
}\if{html}{\out{</div>}}

Then read the output:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{match_df <- read_wildlife_insights_output("speciesnet_output.json") |>
  subset(!species \%in\% c("blank", "human", "vehicle")) |>
  standardize_match_data(
    observation_id_col = "observation_id",
    score_col          = "score",
    rank_system        = c("genus", "species")
  )
}\if{html}{\out{</div>}}

\strong{Multiple candidates:} SpeciesNet typically returns only the top
prediction per image (one row per image). If your output has multiple
candidates per image, use \code{top_n} to control how many are retained.
}
\examples{
# Minimal synthetic SpeciesNet JSON
tmp <- tempfile(fileext = ".json")
writeLines(
  '{"predictions":{
     "IMG_001.jpg":[
       {"label":"Odocoileus virginianus","score":0.94,"category":"animal"}],
     "IMG_002.jpg":[
       {"label":"blank","score":0.99,"category":"blank"}]
  }}',
  tmp
)
result <- read_wildlife_insights_output(tmp)
head(result)
unlink(tmp)
}
\seealso{
\code{\link[=read_animl_output]{read_animl_output()}}, \code{\link[=standardize_match_data]{standardize_match_data()}},
\code{\link[TaxaLikely:build_image_reference]{TaxaLikely::build_image_reference()}}
}
