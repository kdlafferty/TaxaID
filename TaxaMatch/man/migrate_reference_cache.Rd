% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migrate_reference_cache.R
\name{migrate_reference_cache}
\alias{migrate_reference_cache}
\title{Migrate a Reference-Accession Cache to a New params_key}
\usage{
migrate_reference_cache(
  cache_dir,
  to_key = NULL,
  from_key = NULL,
  verbose = TRUE
)
}
\arguments{
\item{cache_dir}{Character. The directory holding
\code{reference_accession_cache.rds}.}

\item{to_key}{Character or \code{NULL} (default). The \code{params_key} to migrate
to. \code{NULL} uses the key \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}'s own current
defaults produce (built by the same internal code the function uses).
If you screen with non-default verdict-affecting arguments (\code{top_n},
\code{max_hits}, ...), pass the key your call produces -- read it off
\code{params_key} in a row that call has written.}

\item{from_key}{Character vector or \code{NULL} (default). Which existing keys
to migrate from. \code{NULL} means every key other than \code{to_key}.}

\item{verbose}{Logical (default \code{TRUE}). Print the counts.}
}
\value{
Invisibly, a list: \code{to_key}, \code{n_carried_forward} (rows rewritten),
\code{n_left_to_reevaluate} (rows under \code{from_key} NOT rewritten, by flag in
\code{left_by_flag}), \code{n_already_current}, \code{backup_path}.
}
\description{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} stamps every cached row with one global
\code{params_key} (its verdict-affecting arguments plus an internal cache
version). Bumping that version -- as the 2026-09-03 switch to the
primer-stripped \code{query_span = "amplicon"} did (\code{"v5_amplicon_query"}) --
invalidates every existing row: ~3,000 real ones (PtConception 995;
GreatLakes 1060 + 280 + 690). This helper rewrites the key on the rows the
change cannot have altered, so only the rest re-BLAST.
}
\section{Why only \code{"congruent"} rows are carried forward}{

\code{"congruent"} asserts that corroborating evidence WAS found. Stripping the
primers from the query only ADDS short amplicon-only deposits to what the
hit list can contain; it cannot withdraw a match that was already
observed, so a \code{"congruent"} verdict under the old query is still
\code{"congruent"} under the new one. Every other flag (\code{"incongruent"},
\code{"insufficient_independent_evidence"}, \code{"not_evaluated_oversized"},
\code{"not_evaluated_wrong_marker"}) is a
statement about absence or about not knowing -- exactly what the new query
can overturn -- so those rows are left under the old key and re-evaluate on
the next call. That is the cheap part (PtConception: 76 rows; GreatLakes:
~123), and it is where every actionable verdict lives.

The sidecar \code{reference_pair_cache.rds} (per-partner votes) is migrated for
the same accessions, so \code{\link[=refine_reference_verdicts]{refine_reference_verdicts()}} keeps their votes
and a later re-evaluation replaces rather than duplicates them.
}

\section{Backup}{

The cache file is copied to
\verb{reference_accession_cache.rds.bak_pre_<to-version>} (and the pair cache to
\verb{reference_pair_cache.rds.bak_pre_<to-version>}) before anything is
rewritten, following the 2026-08-11/13 \verb{.bak_pre_*} convention. An
existing backup of that name is never overwritten -- it holds the earliest
pre-migration state, which is the one worth keeping.
}

\examples{
\dontrun{
migrate_reference_cache("ptcon_ref_eval_cache")
}

}
\seealso{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}
}
