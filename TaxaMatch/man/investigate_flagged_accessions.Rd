% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/investigate_flagged_accession.R
\name{investigate_flagged_accessions}
\alias{investigate_flagged_accessions}
\title{Deep-Dive Verification for a Batch of Flagged Reference Accessions}
\usage{
investigate_flagged_accessions(
  accessions,
  species = NULL,
  max_related = 30L,
  method = c("remote", "local"),
  database = "nt",
  score_range = 8,
  min_score = 70,
  max_hits = 20L,
  submission_window = 5L,
  min_coverage = 0.5,
  max_length_ratio = 3,
  cache_dir = tools::R_user_dir("TaxaMatch", "cache"),
  inconclusive_ttl_days = 30,
  ncbi_api_key = Sys.getenv("NCBI_API_KEY", unset = ""),
  verbose = TRUE
)
}
\arguments{
\item{accessions}{Character vector of flagged accessions to investigate.
Not deduplicated automatically (each is looked up in the persistent
cache independently, so a duplicate is cheap -- a cache hit after the
first occurrence -- rather than an error).}

\item{species}{Character vector the same length as \code{accessions}, or
\code{NULL} (default, every accession's species is fetched from its own
GenBank record). A single accession's own override may be \code{NA} to
fall back to fetching that one accession's species while other
elements stay overridden.}

\item{max_related, method, database, score_range, min_score, max_hits, submission_window, min_coverage, max_length_ratio, cache_dir, inconclusive_ttl_days, ncbi_api_key, verbose}{As in \code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}} -- applied uniformly across the
whole batch (not per-accession).}
}
\value{
A named list (names = \code{accessions}), one element per accession,
each in the same shape \code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}} returns.
}
\description{
The natural \code{investigate_flagged_accessions()} (plural) batch wrapper
\code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}}'s own docs point at (Question 3, item
2): runs the identical per-accession investigation over a whole list,
but shares a single in-memory NCBI-search lookup across the batch (so a
\code{listed_species} or \code{disagreeing_taxon} repeated across several
independently-flagged accessions is only fetched from NCBI once, not once
per accession) and shares the same persistent cache
\code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}} itself uses -- calling this after
already having called \code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}} on some of the
same accessions (or vice versa) correctly reuses those cached results.
}
\section{Not the same as cluster-level mislabel detection}{

This function shares WORK across a batch (search results, cache entries)
-- it does not look for cross-accession PATTERNS (e.g. several
independently-flagged accessions all disagreeing toward the same
species). That is a separate, higher-value, not-yet-designed idea (see
the reentry prompt's Question 3, item 3) -- deliberately out of scope
here.
}

\seealso{
\code{\link[=investigate_flagged_accession]{investigate_flagged_accession()}}, \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}
}
