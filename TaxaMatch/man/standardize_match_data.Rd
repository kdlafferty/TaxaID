% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize_match_data.R
\name{standardize_match_data}
\alias{standardize_match_data}
\title{Standardize Raw Match Data to Canonical Match Object}
\usage{
standardize_match_data(
  data = NULL,
  observation_id_col,
  score_col,
  rank_system = NULL,
  col_map = NULL,
  lowercase_names = TRUE
)
}
\arguments{
\item{data}{A data frame, a file path (character string), or \code{NULL}. When
\code{NULL} an interactive file chooser (\code{file.choose()}) opens. CSV (\code{.csv})
and tab-delimited (\code{.tsv}, \code{.txt}) files are supported when a path is
supplied.}

\item{observation_id_col}{Character. Name of the column that holds the unique
query identifier (e.g. \code{"ESVId"} for MiFish eDNA output).}

\item{score_col}{Character. Name of the column holding the raw match score
(e.g. \code{"PercMatch"}). Values may be on any numeric scale; normalisation
is performed later in TaxaLikely.}

\item{rank_system}{Character vector of taxonomic rank column names,
listed broadest to finest (e.g.
\code{c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species")}).
Column matching is case-insensitive. When \code{NULL} (default), rank columns
are auto-detected by matching column names against a built-in list of
standard rank names (\code{domain} through \code{form}).}

\item{col_map}{Optional named character vector of additional column renames
applied before the core standardisation step, via
\code{\link[TaxaTools:rename_cols]{TaxaTools::rename_cols()}}. Format: \code{c("OldName" = "new_name")}. Useful
when source files use non-standard column names that are not auto-detected.}

\item{lowercase_names}{Logical. When \code{TRUE} (default), all column names in
the output are converted to lowercase as the final step. This produces a
fully consistent canonical object (e.g. \code{kingdom}, \code{testid}, \code{accession})
and avoids case-sensitivity surprises in downstream joins. Set to \code{FALSE}
to preserve original column name casing.}
}
\value{
A data frame with at minimum:
\describe{
\item{\code{observation_id}}{Unique query identifier (renamed from \code{observation_id_col}).}
\item{\code{score}}{Raw match score (renamed from \code{score_col}).}
\item{\code{taxon_name}}{Most specific non-NA taxon name (derived).}
\item{\code{taxon_name_rank}}{Rank of \code{taxon_name}, lowercase (derived).}
}
All other input columns are retained unchanged.
}
\description{
Reads raw match data (from a data frame or file), renames the observation
identifier and score columns to canonical names (\code{observation_id} and \code{score}),
auto-detects or validates taxonomic rank columns, and derives \code{taxon_name}
and \code{taxon_name_rank} via \code{\link[TaxaTools:create_taxon_names]{TaxaTools::create_taxon_names()}}.
}
\details{
The result is a canonical match object ready for input to TaxaLikely.
One row per \code{observation_id} x reference match (e.g. one ESV x one accession
hit). Sample context (site, date, replicate) is stored in a separate table
and joined to the likelihood output downstream — it is not part of the match
object.
}
\examples{
\dontrun{
match_obj <- standardize_match_data(
  data = blast_results,
  observation_id_col = "qseqid",
  score_col = "pident"
)
}

}
\seealso{
\code{\link[TaxaTools:create_taxon_names]{TaxaTools::create_taxon_names()}}, \code{\link[TaxaTools:rename_cols]{TaxaTools::rename_cols()}}
}
