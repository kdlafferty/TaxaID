% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_image.R
\name{read_inaturalist_cv_output}
\alias{read_inaturalist_cv_output}
\title{Read iNaturalist Computer Vision API Results into a Match Object}
\usage{
read_inaturalist_cv_output(
  files,
  score_type = c("combined_score", "score"),
  min_confidence = 0,
  top_n = NULL
)
}
\arguments{
\item{files}{Character vector. Paths to iNaturalist CV JSON files.
Alternatively, a path to a directory: all \verb{*.json} files in that
directory (non-recursive) are read.}

\item{score_type}{Character. Which score to use from the API response.
\code{"combined_score"} (default) incorporates community identification
frequency and is generally more calibrated. \code{"score"} is the raw
computer-vision softmax score.}

\item{min_confidence}{Numeric. Detections below this score are dropped.
Default \code{0} (keep all).}

\item{top_n}{Integer or \code{NULL}. If supplied, only the top \code{n} candidates
(by score) within each image are retained. Default \code{NULL} (keep all).}
}
\value{
A data frame with one row per image x candidate taxon, containing:
\describe{
\item{\code{observation_id}}{Unique identifier derived from the JSON filename
stem (the name you gave the saved response file, ideally the image
filename stem).}
\item{\code{score}}{iNaturalist CV score (0--1) as selected by
\code{score_type}.}
\item{\code{species}}{Scientific name (binomial) if the API returned a
species-rank taxon; coarser name otherwise.}
\item{\code{genus}}{Genus name (first word of \code{species}). \code{NA} for
non-binomial labels.}
\item{\code{common_name}}{Common name from the API response, or \code{NA}.}
\item{\code{taxon_rank}}{Rank reported by the API (e.g., \code{"species"},
\code{"genus"}). Useful for filtering to species-only suggestions.}
\item{\code{source_file}}{Basename of the source JSON file.}
}
}
\description{
Reads one or more saved JSON files from the iNaturalist computer vision API
and returns a tidy data frame in match object format, ready for
\code{\link[=standardize_match_data]{standardize_match_data()}} and downstream TaxaLikely processing.
}
\details{
Each JSON file should be the raw API response for one image, saved to disk
after calling the iNaturalist API endpoint
\verb{POST https://api.inaturalist.org/v2/computervision/score_image}.
See the \verb{@details} section for a minimal API call example.

\strong{Saving API responses:} Each JSON file must be the API response for
exactly one image.  Name the file using the image stem so that
\code{observation_id} is interpretable downstream:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Requires httr2 and jsonlite
img_path <- "field_images/IMG_001.jpg"
resp <- httr2::request("https://api.inaturalist.org/v2") |>
  httr2::req_url_path("/computervision/score_image") |>
  httr2::req_body_multipart(
    image    = curl::form_file(img_path),
    jwt      = your_inat_api_token   # from inaturalist.org
  ) |>
  httr2::req_perform()
json_path <- sub("\\\\.jpg$", ".json", img_path)
writeLines(httr2::resp_body_string(resp), json_path)
}\if{html}{\out{</div>}}

Then: \code{read_inaturalist_cv_output("field_images/")}.

\strong{Species rank filtering:} iNaturalist CV returns suggestions at any rank
(species, genus, family, ...). Filter before standardizing:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{match_df <- read_inaturalist_cv_output("inat_results/") |>
  subset(taxon_rank == "species") |>
  standardize_match_data(
    observation_id_col = "observation_id",
    score_col          = "score",
    rank_system        = c("genus", "species")
  )
}\if{html}{\out{</div>}}
}
\examples{
# Minimal synthetic iNaturalist CV JSON
tmp <- tempfile(fileext = ".json")
writeLines(
  '{"results":[
     {"combined_score":0.87,"score":0.91,
      "taxon":{"name":"Danaus plexippus","rank":"species",
               "preferred_common_name":"Monarch"}},
     {"combined_score":0.07,"score":0.06,
      "taxon":{"name":"Limenitis archippus","rank":"species",
               "preferred_common_name":"Viceroy"}}
  ]}',
  tmp
)
result <- read_inaturalist_cv_output(tmp)
head(result)
unlink(tmp)
}
\seealso{
\code{\link[=read_animl_output]{read_animl_output()}}, \code{\link[=standardize_match_data]{standardize_match_data()}},
\code{\link[TaxaLikely:build_image_reference]{TaxaLikely::build_image_reference()}}
}
