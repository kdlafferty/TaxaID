% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/investigate_flagged_accession.R
\name{investigate_flagged_accession}
\alias{investigate_flagged_accession}
\title{Deep-Dive Verification for a Single Flagged Reference Accession}
\usage{
investigate_flagged_accession(
  accession,
  species = NULL,
  max_related = 30L,
  method = c("remote", "local"),
  database = "nt",
  score_range = 8,
  min_score = 70,
  max_hits = 20L,
  submission_window = 5L,
  min_coverage = 0.5,
  max_length_ratio = 3,
  cache_dir = tools::R_user_dir("TaxaMatch", "cache"),
  inconclusive_ttl_days = 30,
  ncbi_api_key = Sys.getenv("NCBI_API_KEY", unset = ""),
  verbose = TRUE
)
}
\arguments{
\item{accession}{Character scalar. The flagged accession to investigate.}

\item{species}{Character or \code{NULL} (default). The accession's own listed
species -- if \code{NULL}, fetched from its real GenBank record.}

\item{max_related}{Integer (default \code{30L}). Caps how many conspecific/
disagreeing-taxon accessions are fetched and compared against, for each
of the two comparisons -- a deliberate bound, not exhaustive
reference-database compilation (see \code{\link[TaxaLikely:fetch_ncbi_reference_sequences]{TaxaLikely::fetch_ncbi_reference_sequences()}}
for that).}

\item{method, database}{Passed to BOTH the disagreeing-taxon re-BLAST AND
the two \code{.blast_against_comparison_set()} comparisons (self-consistency,
cross-taxon) -- see \code{\link[=blast_sequences]{blast_sequences()}}.}

\item{score_range, min_score, max_hits}{Passed only to the disagreeing-taxon
re-BLAST -- see \code{\link[=blast_sequences]{blast_sequences()}}. The two comparison-set BLAST calls
use their own internal, deliberately permissive values (see
\code{.blast_against_comparison_set()}), since they exist to honestly report
every comparison-set accession's real identity/coverage, not to apply a
score-window cutoff.}

\item{submission_window}{Integer (default \code{5L}). Same-submission-batch
independence-filter window, matching
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}'s own default.}

\item{min_coverage}{Numeric (default \code{0.5}). Passed to
\code{.blast_against_comparison_set()} -- see that function's own
documentation. Comparisons below this floor are retained in the output
(never silently dropped) but excluded from the printed headline
mean/range.}

\item{max_length_ratio}{Numeric (default \code{3}). Passed to
\code{.search_species_accessions()} -- REQUIRED for correctness, not an
optional tuning knob (see that function's own \verb{@section Length-ratio pre-filter}, added 2026-08-08 after live testing against the real
\code{MZ605481} case found a plain NCBI organism-name search can be
dominated by whole-genome-assembly records for a species with a
published reference genome, leaving almost no length-comparable
candidates in the raw result at all).}

\item{cache_dir}{Character or \code{NULL}. Default
\code{tools::R_user_dir("TaxaMatch", "cache")}. Set \code{NULL} to disable
caching entirely (every call re-investigates from scratch). See
\verb{@section Caching} above.}

\item{inconclusive_ttl_days}{Numeric (default \code{30}). See \verb{@section Caching} above.}

\item{ncbi_api_key, verbose}{As in \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}.}
}
\value{
A list:
\describe{
\item{\code{accession}, \code{listed_species}}{As given/discovered.}
\item{\code{conspecific_comparison}}{data.frame(accession, pident,
coverage, meets_min_coverage, create_date) -- one row per real
conspecific accession BLAST actually returned a hit for. ALWAYS
check \code{coverage}/\code{meets_min_coverage} before trusting \code{pident} --
a high \code{pident} at low coverage is not meaningful evidence of
anything, see \code{min_coverage} above.}
\item{\code{disagreeing_taxon}}{The species name of the top independent
disagreeing BLAST hit, or \code{NA} if none found.}
\item{\code{disagreeing_taxon_comparison}}{Same shape as
\code{conspecific_comparison}, for \code{disagreeing_taxon}'s own other
accessions.}
}
Also prints a narrative summary comparing the two, unless \code{verbose = FALSE}.
}
\description{
For ONE accession \code{evaluate_reference_accessions()} flagged (typically
\code{"incongruent"}), runs two targeted, more expensive comparisons than that
broad-scan function ever attempts -- deliberately meant to run only on
the small subset a caller has already decided is worth a closer look,
not at production scale.
}
\section{Why this exists (2026-08-07)}{

Manually reviewing \code{evaluate_reference_accessions()}'s real flagged
accessions by hand found that the raw top BLAST hits alone can be
actively misleading -- \code{PV382872}'s top hits (an eel, a catfish, a
parasitic isopod, all ~99.5\% identity) looked like severe contamination
until direct verification showed every one was from the SAME real
submission batch as the query itself, correctly already excluded by
\code{evaluate_reference_accessions()}'s own independence filter, but
invisible without re-fetching the raw GenBank records by hand. This
function automates that verification step, applying the SAME
independence-filter logic (\code{.build_submission_batch_lookup()}/
\code{.same_submission_batch()}) so a caller doesn't have to re-derive it by
hand every time.
}

\section{The two comparisons}{

\describe{
\item{Self-consistency}{How well does the flagged accession match
OTHER real GenBank accessions of its own listed species (searched
directly by species name, not discovered via \code{blast_sequences()}'s
own \code{score_range}/\code{max_hits} window, which can miss real conspecifics
sitting just outside it)? High identity here is reassuring; low
identity is a much stronger warning sign than
\code{evaluate_reference_accessions()} alone can report, since it means
the accession doesn't even resemble other deposits of its own
claimed species.}
\item{Cross-taxon consistency}{How well does the flagged accession
match other real accessions of whichever taxon its top INDEPENDENT
disagreeing BLAST hit belongs to (freshly re-discovered via a live
BLAST call, independence-filtered the same way)? High identity here
paired with low self-consistency above is the strongest evidence
this function can produce for a genuine mislabel -- e.g. the real
\code{MZ605481} case (self-consistency not yet checked at the time of
writing; cross-taxon consistency to \verb{Cyprinus carpio} was confirmed
by hand at 100\% identity across 20 independent real accessions).}
}
}

\section{Both comparisons now run via BLAST, not pairwise alignment (2026-08-08)}{

\code{MZ605481}'s own real motivating case came back inconclusive in BOTH
directions the first time this function shipped: 0 of 30+ candidate
accessions found via NCBI species search cleared a 50\% coverage floor on
either side, because NCBI's \verb{[Organism]}-based search returns records of
ANY length (most real GenBank deposits for both \emph{Pseudorasbora parva}
and \emph{Cyprinus carpio} are full ~16kb mitogenomes, against which a 173bp
amplicon can only ever find a small, non-meaningful local-alignment
fragment). The ORIGINAL evidence that made \code{MZ605481} a real
\code{candidate_mislabel} in the first place was never a pairwise alignment
at all -- it was a direct \code{\link[=blast_sequences]{blast_sequences()}} call, which already
enforces \code{min_query_coverage} internally and found 20 independent,
coverage-safe \emph{Cyprinus carpio} hits at genuine 100\% identity. Both
comparisons here now reuse that proven mechanism (\verb{.blast_against_ comparison_set()}, internal) instead of a hand-rolled
\code{pwalign::pairwiseAlignment()} loop -- see
\code{ecosystem_docs/REENTRY_PROMPT_investigate_flagged_accession_prefilter_group_posthoc.md},
Question 1, Option A.
}

\section{Two more rounds of live testing found Option A alone still wasn't enough (2026-08-08, same day)}{

The FIRST shipped version of \code{.blast_against_comparison_set()} was a
post-hoc filter (unrestricted BLAST, then keep only hits that happen to
match the comparison set) -- live-tested against the real \code{MZ605481}
case and found to return ZERO matches on both sides, even though 30 real
conspecific accessions were independently confirmed to exist. Fixed by
switching to NCBI's \code{ENTREZ_QUERY} mechanism (\code{method = "remote"}),
which restricts the BLAST search SPACE itself to the comparison-set
accessions -- verified in isolation against one known-good accession
(\code{OP739039}, 100\% identity/coverage recovered correctly). Re-running the
full \code{MZ605481} case with THAT fix still returned zero matches -- direct
inspection of the real candidate accessions (\verb{.search_species_ accessions("Pseudorasbora parva")}'s own output) found the root cause
was one level further upstream than expected: 26 of 30 candidates were
whole-chromosome shotgun-assembly records 60-80+ million bp long
(\verb{Pseudorasbora parva} has a published reference genome), and the one
real short candidate that DID have usable sequence content was an
entirely different gene (\code{COI}, not \verb{12S}) -- neither BLAST restriction
mechanism can find a meaningful alignment against a candidate that
either isn't practically alignable at that scale or covers a
non-overlapping genomic region entirely. This is exactly the reentry
prompt's own originally-deferred "Option B" (length-ratio candidate
pre-filtering) -- deferred at design time as "cheaper but doesn't fully
solve the problem," now confirmed live to be a REQUIRED companion to
Option A, not an alternative to it. Implemented in
\code{.search_species_accessions()} (new \code{reference_length}/
\code{max_length_ratio} params, using NCBI's already-batched, free \code{slen}
ESummary field to filter BEFORE ever fetching full sequence content --
fetching a 70-million-bp record's full \code{GBSeq_sequence} is exactly what
was silently failing/returning \code{NA} for those chromosome accessions).
}

\section{Caching (2026-08-08)}{

Implements Question 3, item 1: a persistent, cross-call cache keyed on
\verb{(accession, species override)}, mirroring
\code{evaluate_reference_accessions()}'s own asymmetric-TTL philosophy. A
result where NEITHER comparison cleared \code{min_coverage} on either side
(the real \code{MZ605481}-class \code{"inconclusive_length_mismatch"} outcome)
expires after \code{inconclusive_ttl_days} and is retried on the next call --
new, shorter GenBank deposits for either species could genuinely change
that answer. Any other result is cached indefinitely (a confident
self/cross-taxon comparison doesn't change once computed). A cache HIT
still prints the identical narrative summary a fresh call would (unless
\code{verbose = FALSE}), so this is safe to call repeatedly without losing the
printed output. Set \code{cache_dir = NULL} to disable caching entirely.
}

\seealso{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}, \code{\link[=investigate_flagged_accessions]{investigate_flagged_accessions()}}
}
