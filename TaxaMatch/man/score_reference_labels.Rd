% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference_label_verdict.R
\name{score_reference_labels}
\alias{score_reference_labels}
\title{Score Reference Labels: a Numeric Label Confidence and a Categorical Action}
\usage{
score_reference_labels(
  evaluation,
  margin_scale = 1,
  margin_cap = 5,
  action_remove_below = 0.05,
  action_inspect_below = 0.25,
  action_caution_below = 0.75,
  overwrite = FALSE
)
}
\arguments{
\item{evaluation}{Data frame. Output of \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}
(or any data frame carrying its diagnostic columns -- a cache file read
straight off disk works).}

\item{margin_scale}{Numeric (default \code{1}). Percent-identity points per unit
of log-odds. Larger = the identity margin matters less relative to the
vote. See \verb{@section Where the numbers come from}.}

\item{margin_cap}{Numeric (default \code{5}). Caps \verb{|d|}, and supplies the value
used for the two one-sided cases (corroborated with nothing contradicting
it; contradicted with nothing corroborating it anywhere).}

\item{action_remove_below, action_inspect_below, action_caution_below}{Numeric
thresholds on \code{label_confidence} (defaults \code{0.05}, \code{0.25}, \code{0.75})
separating \code{"remove"}/\code{"inspect"}/\code{"caution"}/\code{"keep"}.}

\item{overwrite}{Logical (default \code{FALSE}). \code{TRUE} recomputes and replaces
\code{label_confidence}/\code{label_identity_margin}/\code{reference_action} if they are
already present; \code{FALSE} errors instead, so a second call with different
parameters can't silently produce a mixed-provenance table.}
}
\value{
\code{evaluation} with three columns added:
\describe{
\item{\code{label_confidence}}{Numeric in (0, 1). \code{NA} for a row with no
computed congruence at all (\code{"not_evaluated_oversized"}, or a fetch
failure).}
\item{\code{label_identity_margin}}{Numeric, the capped \code{d} in
percent-identity points. \code{NA} when the row carries no identity
information of any kind.}
\item{\code{label_quality}}{Numeric in (0, 1]. The MODEL-facing companion to
\code{label_confidence}: the same evidence divided by the highest
confidence a maximally-corroborated reference with this row's own
partner count could achieve, capped at 1. \code{NA} where there were zero
valid comparison partners. This -- never \code{label_confidence} -- is what
\code{TaxaLikely::evaluate_likelihoods(reference_quality_col=)} should be
pointed at. See \verb{@section Two scales, and which one a model reads}.}
\item{\code{reference_action}}{\code{"keep"}, \code{"caution"}, \code{"inspect"},
\code{"remove"}, or \code{"untested"}.}
}
Row count and order are unchanged.
}
\description{
Adds two derived columns to an \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} result:
\code{label_confidence} (numeric, high = the listed label is more likely
CORRECT) and \code{reference_action} (\code{"keep"}/\code{"caution"}/\code{"inspect"}/
\code{"remove"}/\code{"untested"}). Evidence and action are deliberately separate
columns -- the same split this ecosystem already uses for
\code{TaxaFlag::observation_validity} (numeric) vs \code{validity_flag}
(categorical), and for \code{TaxaHabitat::flag_institution_candidates()}'s
classify-then-review shape. The numeric is what a downstream model
consumes (see \code{TaxaLikely::evaluate_likelihoods(reference_quality_col=)});
the categorical is for humans and for \code{\link[=remove_incongruent_references]{remove_incongruent_references()}}.
}
\section{Two scales, and which one a model reads (2026-09-02)}{

\code{label_confidence} is a probability and cannot reach 1 -- Jeffreys smoothing
floors the disagreement fraction at \code{0.5/(n+1)} and the identity margin is
capped, so a PERFECT reference scores 0.99939 with 5 partners. That is the
honest number and it is kept as-is: a Bayesian does not reach certainty from
five neighbours, and rounding it to 1 to suit a consumer would make the
human-facing column dishonest.

But a consumer that reads the value as a RATIO, where 1 means "no
adjustment", cannot use that scale. \code{TaxaLikely::evaluate_likelihoods( reference_quality_col=)} is exactly such a consumer, and feeding it
\code{label_confidence} applied a ~0.03\% sigma widening to every candidate in the
dataset: measured on the real PtConception data, 279 of the 408 H1 rows that
moved had \code{label_confidence > 0.99} and moved because of the ceiling, not
because their reference was dubious -- which left the Thread-3 validation
run unable to measure the covariate at all.

Hence two columns. \code{label_quality} divides by the per-row achievable ceiling
so that "maximally corroborated for the evidence this row has" is exactly 1,
independent of partner count. The split is also forced rather than stylistic:
the ceiling depends on \code{n_independent_top_matches}, and by the time
\code{evaluate_likelihoods()} sees a quality value it has been aggregated per
candidate taxon and \code{n} is gone -- only this package can compute it.
}

\section{Polarity}{

HIGH \code{label_confidence} means MORE confidence that the label is correct,
i.e. LESS concern. This deliberately does NOT follow the ecosystem's
\verb{*_risk}/\verb{*_confusion_risk} convention (where high = more concern), which
is why the column is named \code{label_confidence} and not \code{label_risk} -- the
name states the direction. It is on the same scale and polarity as
\code{score_likelihood} and \code{TaxaFlag::observation_validity}, which is what a
consumer that multiplies it into a weight actually needs.
}

\section{Where the numbers come from}{

\code{label_confidence} is a pure function of columns
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} already caches, so it applies to any
existing cache with no \code{.EVAL_REF_ACC_VERSION} bump and no re-BLAST. Its
formula is a log-odds sum of the existing Jeffreys-smoothed vote and the
percent-identity margin the vote itself ignores:

\preformatted{
  logit(label_confidence) = logit(1 - frac_independent_below_min_congruent_rank)
                            + d / margin_scale
  d = (best_agreeing_pident, else congruent_evidence_best_pident)
      - best_disagreeing_pident,  capped to +/- margin_cap
}

\code{margin_scale} is the one free parameter: it says how many percent-identity
points are worth one unit of log-odds. The default \code{1} is not fitted -- it
is a stated convention, registered as such
(\code{ecosystem_docs/arbitrariness_audit.md}'s subject matter), chosen because
at MiFish-U amplicon lengths (~170 bp) one percent identity is roughly 1.7
nucleotide differences, the granularity at which this ecosystem already
treats identity differences as discriminating at species level. What the
default is checked against is behaviour, not fit: on the real 995-accession
PtConception evaluation it reproduces the measured, user-approved
evidence-gate split exactly (the 4 no-corroboration accessions score
<= 0.001 and are the only ones that reach \code{"remove"}; the 8 spared score
0.056 to 0.97 and reach at worst \code{"inspect"}), and leaves all 919
\code{"congruent"} accessions at \code{"keep"}.
}

\section{Why \code{"remove"} also requires no corroboration anywhere}{

\code{reference_action == "remove"} is gated on THREE conditions, not just a low
\code{label_confidence}: the accession must be flagged \code{"incongruent"}, have no
corroborating evidence anywhere in the untruncated independence-filtered
hit pool, AND score below \code{action_remove_below}. The corroboration test is
a hard veto rather than another additive term because one corroborating
record anywhere is qualitatively different from none -- it means some
independent submitter's sequence agrees with this label at family or finer.
The case that shows why a threshold alone is not enough is \verb{Oxylebius pictus} (\code{KM057978}), a real, locally-abundant painted greenling whose only
corroboration sits outside its top-5 window at 93.58, just below the 94.01
that contradicts it. It scores 0.056 -- a hair above the 0.05 default, so
the default threshold happens to spare it, but its fate would flip on any
small change to \code{margin_scale} or to \code{action_remove_below}. The veto is
what makes the outcome robust rather than lucky: raise
\code{action_remove_below} to 0.2 and it is still \code{"inspect"}, while the
genuinely uncorroborated accessions are still \code{"remove"}. That is the right
treatment: a borderline reference should widen uncertainty downstream (see
\code{TaxaLikely::evaluate_likelihoods(reference_quality_col=)}), not be
deleted.

\code{"remove"} is furthermore unreachable for any flag other than
\code{"incongruent"}. \code{"insufficient_independent_evidence"} is retryable, not
removable (it may simply mean a sparsely-referenced region of the
database), and \code{"not_evaluated_oversized"} was never submitted to BLAST at
all.
}

\examples{
\dontrun{
ev <- evaluate_reference_accessions(accs, cache_dir = "ref_eval_cache")
ev <- score_reference_labels(ev)
table(ev$reference_action)
}

}
\seealso{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}, \code{\link[=refine_reference_verdicts]{refine_reference_verdicts()}},
\code{\link[=remove_incongruent_references]{remove_incongruent_references()}}, \code{\link[=flag_incongruent_references]{flag_incongruent_references()}}
}
