% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference_label_verdict.R
\name{score_reference_labels}
\alias{score_reference_labels}
\title{Score Reference Labels: a Numeric Label Confidence and a Categorical Action}
\usage{
score_reference_labels(
  evaluation,
  margin_scale = 1,
  margin_cap = 5,
  action_remove_below = 0.05,
  action_inspect_below = 0.25,
  action_caution_below = 0.75,
  overwrite = FALSE,
  local_corroboration = NULL
)
}
\arguments{
\item{evaluation}{Data frame. Output of \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}
(or any data frame carrying its diagnostic columns -- a cache file read
straight off disk works).}

\item{margin_scale}{Numeric (default \code{1}). Percent-identity points per unit
of log-odds. Larger = the identity margin matters less relative to the
vote. See \verb{@section Where the numbers come from}.}

\item{margin_cap}{Numeric (default \code{5}). Caps \verb{|d|}, and supplies the value
used for the two one-sided cases (corroborated with nothing contradicting
it; contradicted with nothing corroborating it anywhere).}

\item{action_remove_below, action_inspect_below, action_caution_below}{Numeric
thresholds on \code{label_confidence} (defaults \code{0.05}, \code{0.25}, \code{0.75})
separating \code{"remove"}/\code{"inspect"}/\code{"caution"}/\code{"keep"}.}

\item{overwrite}{Logical (default \code{FALSE}). \code{TRUE} recomputes and replaces
\code{label_confidence}/\code{label_identity_margin}/\code{reference_action} (and the
local-corroboration columns) if they are already present; \code{FALSE} errors
instead, so a second call with different parameters can't silently
produce a mixed-provenance table.}

\item{local_corroboration}{Data frame or \code{NULL} (default). Output of
\code{\link[=corroborate_references_locally]{corroborate_references_locally()}}. Joined by version-stripped
accession. See \verb{@section Local corroboration}.}
}
\value{
\code{evaluation} with seven columns added:
\describe{
\item{\code{label_confidence}}{Numeric in (0, 1). \code{NA} for a row with no
computed congruence at all (\code{"not_evaluated_oversized"},
\code{"not_evaluated_wrong_marker"}, or a fetch failure). \strong{Named
contract (2026-09-05 critical-fix-review finding C): this value
CANNOT REACH 1}, by construction -- the Jeffreys smoothing floors
the disagreement fraction at \code{0.5/(n+1)}, so even a perfectly
corroborated multi-partner accession tops out just under 1 (e.g.
\code{0.99939} for a real 5-partner case). Any future consumer reading
this as a RATIO where 1 means "no adjustment needed" (e.g. a
likelihood-covariate-style rescale) must normalize by the per-row
achievable ceiling first, or it will silently adjust every
candidate in the dataset -- this is exactly the bug the one prior
attempt to use this column as a likelihood covariate hit before
being removed entirely (see \verb{[[project_reference_quality_verdicts_ threads123]]} in the project memory system). Previously this
constraint was recorded only in a reentry doc, not here.}
\item{\code{label_identity_margin}}{Numeric, the capped \code{d} in
percent-identity points. \code{NA} when the row carries no identity
information of any kind.}
\item{\code{reference_action}}{\code{"keep"}, \code{"caution"}, \code{"inspect"},
\code{"remove"}, or \code{"untested"}. \code{"untested"} means NO USABLE EVIDENCE
WAS OBTAINED, which covers two different routes there: the query was
never submitted to BLAST (\code{"not_evaluated_oversized"},
\code{"not_evaluated_wrong_marker"}), or it was submitted and came back
with zero valid comparison partners
(\code{n_independent_top_matches == 0}; widened to include this case
2026-09-04 -- see \verb{@section No partners is not a coin flip}).}
\item{\code{action_reason}}{\code{"vetoed_by_local_corroboration"} where a
\code{"remove"} was downgraded to \code{"inspect"} by the local set,
\code{"locally_corroborated_not_blasted"} for a skipped row, \code{NA}
otherwise.}
\item{\code{corroboration_source}}{\code{"blast"}, \code{"local"}, \code{"both"}, or
\code{"none"}. \code{NA} for a row with no verdict at all.}
\item{\code{local_best_independent_pident}}{Percent identity (0-100) of
the best independent local conspecific; \code{NA} without a table.}
\item{\code{local_n_independent_conspecific}}{Its count; \code{NA} without a
table.}
}
Row count and order are unchanged.
}
\description{
Adds two derived columns to an \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} result:
\code{label_confidence} (numeric, high = the listed label is more likely
CORRECT) and \code{reference_action} (\code{"keep"}/\code{"caution"}/\code{"inspect"}/
\code{"remove"}/\code{"untested"}). Evidence and action are deliberately separate
columns -- the same split this ecosystem already uses for
\code{TaxaFlag::observation_validity} (numeric) vs \code{validity_flag}
(categorical), and for \code{TaxaHabitat::flag_institution_candidates()}'s
classify-then-review shape. The numeric grades the evidence; the categorical
is for humans and for \code{\link[=remove_incongruent_references]{remove_incongruent_references()}}.
}
\section{Polarity}{

HIGH \code{label_confidence} means MORE confidence that the label is correct,
i.e. LESS concern. This deliberately does NOT follow the ecosystem's
\verb{*_risk}/\verb{*_confusion_risk} convention (where high = more concern), which
is why the column is named \code{label_confidence} and not \code{label_risk} -- the
name states the direction. It is on the same scale and polarity as
\code{score_likelihood} and \code{TaxaFlag::observation_validity}, which is what a
consumer that multiplies it into a weight actually needs.
}

\section{No partners is not a coin flip}{

An accession with ZERO valid comparison partners
(\code{n_independent_top_matches == 0}) has \code{label_confidence = NA} and
\code{reference_action = "untested"}, rather than the 0.500 the arithmetic
would otherwise produce.

Why it would otherwise be 0.500: with no partners,
\code{frac_independent_below_min_congruent_rank} falls back to its 0.5 default
and the identity margin is \code{NA}, so the formula reduces to
\code{plogis(qlogis(0.5) + 0)}. That is a prior with no data, not a
measurement, and it lands squarely in the \code{"caution"} band -- i.e. the
screen asserting concern earned by an absence, against a base rate of 931
congruent out of 989 evaluated PtConception accessions.

Measured across four independent real caches (PtConception plus three
GreatLakes), the split is total and has no exceptions: all 98 zero-partner
rows scored exactly 0.500 and read \code{"caution"}, while all 55 rows with at
least one partner had corroborating evidence
(\code{congruent_evidence_exists_anywhere == TRUE}) and read \code{"keep"}.

The rule keys on the partner COUNT, deliberately not on \code{hierarchy_flag}.
An accession with 1-2 partners also reads
\code{"insufficient_independent_evidence"}, but it does have real evidence and
(55 of 55, measured) that evidence corroborates it; keying on the verdict
would wrongly blank those too.

Some of these rows are a \code{max_hits} truncation artifact rather than a
property of the accession -- re-running PtConception's 34 at
\code{max_hits = 100} resolved 12, including 7 zero-partner rows that moved
\code{"caution"} to \code{"keep"} (\code{diagnostics/insufficient_evidence_probe.R}). But
21 of 34 gained no hits at all when the window quintupled, so most of the
population is genuinely thin. \code{"untested"} is the honest reading either
way: we do not know.
}

\section{Where the numbers come from}{

\code{label_confidence} is a pure function of columns
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} already caches, so it applies to any
existing cache with no \code{.EVAL_REF_ACC_VERSION} bump and no re-BLAST. Its
formula is a log-odds sum of the existing Jeffreys-smoothed vote and the
percent-identity margin the vote itself ignores:

\preformatted{
  logit(label_confidence) = logit(1 - frac_independent_below_min_congruent_rank)
                            + d / margin_scale
  d = (best_agreeing_pident, else congruent_evidence_best_pident)
      - best_disagreeing_pident,  capped to +/- margin_cap
}

\code{margin_scale} is the one free parameter: it says how many percent-identity
points are worth one unit of log-odds. The default \code{1} is not fitted -- it
is a stated convention, registered as such
(\code{ecosystem_docs/arbitrariness_audit.md}'s subject matter), chosen because
at MiFish-U amplicon lengths (~170 bp) one percent identity is roughly 1.7
nucleotide differences, the granularity at which this ecosystem already
treats identity differences as discriminating at species level. What the
default is checked against is behaviour, not fit: on the real 995-accession
PtConception evaluation it reproduces the measured, user-approved
evidence-gate split exactly (the 4 no-corroboration accessions score
<= 0.001 and are the only ones that reach \code{"remove"}; the 8 spared score
0.056 to 0.97 and reach at worst \code{"inspect"}), and leaves all 919
\code{"congruent"} accessions at \code{"keep"}.
}

\section{Why \code{"remove"} also requires no corroboration anywhere}{

\code{reference_action == "remove"} is gated on THREE conditions, not just a low
\code{label_confidence}: the accession must be flagged \code{"incongruent"}, have no
corroborating evidence anywhere in the untruncated independence-filtered
hit pool, AND score below \code{action_remove_below}. The corroboration test is
a hard veto rather than another additive term because one corroborating
record anywhere is qualitatively different from none -- it means some
independent submitter's sequence agrees with this label at family or finer.
The case that shows why a threshold alone is not enough is \verb{Oxylebius pictus} (\code{KM057978}), a real, locally-abundant painted greenling whose only
corroboration sits outside its top-5 window at 93.58, just below the 94.01
that contradicts it. It scores 0.056 -- a hair above the 0.05 default, so
the default threshold happens to spare it, but its fate would flip on any
small change to \code{margin_scale} or to \code{action_remove_below}. The veto is
what makes the outcome robust rather than lucky: raise
\code{action_remove_below} to 0.2 and it is still \code{"inspect"}, while the
genuinely uncorroborated accessions are still \code{"remove"}. That is the right
treatment: a borderline reference should be surfaced for review, not
deleted.

\code{"remove"} is furthermore unreachable for any flag other than
\code{"incongruent"}. \code{"insufficient_independent_evidence"} is retryable, not
removable (it may simply mean a sparsely-referenced region of the
database), \code{"not_evaluated_oversized"} and
\code{"not_evaluated_wrong_marker"} (2026-09-04) were never submitted to BLAST
at all, and \code{"locally_corroborated"} (2026-09-03) was deliberately not
submitted because the caller's own reference set already corroborates it
-- it reads \code{"keep"} with \code{label_confidence = NA} (there is no BLAST
evidence to grade).
}

\section{Local corroboration}{
 provenance and the veto (2026-09-03):
When \code{local_corroboration} (\code{\link[=corroborate_references_locally]{corroborate_references_locally()}} output) is
supplied, three things happen, and one deliberately does not.
\code{corroboration_source} records where the corroboration for each label
came from: \code{"blast"} (\code{congruent_evidence_exists_anywhere}), \code{"local"}
(\code{local_tier == "corroborated"}, or the row was skipped as
\code{"locally_corroborated"}), \code{"both"}, or \code{"none"}.
\code{local_best_independent_pident} and \code{local_n_independent_conspecific}
carry the local numbers beside the BLAST ones. And a row that resolves to
\code{"remove"} while the local set corroborates it is VETOED to \code{"inspect"},
with \code{action_reason = "vetoed_by_local_corroboration"}.

What does not happen: \code{label_confidence} stays the BLAST-only probability.
Local evidence is not folded into it, on purpose, so a reviewer can see
the two disagree -- that disagreement is exactly what found the
primer-inclusive-query blind spot (\code{KM057996}, \emph{Zaniolepis frenata},
actioned \code{"remove"} by BLAST while \code{OQ846041} in the local set matched it
at 100\% over 97\% of the amplicon; see \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}'s
\verb{@section Why the query is the primer-stripped amplicon}). All four
columns are present (\code{NA}/\code{"none"}-filled) even when no table is
supplied, so downstream code can rely on them.
}

\examples{
\dontrun{
ev <- evaluate_reference_accessions(accs, cache_dir = "ref_eval_cache")
ev <- score_reference_labels(ev)
table(ev$reference_action)
}

}
\seealso{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}, \code{\link[=refine_reference_verdicts]{refine_reference_verdicts()}},
\code{\link[=remove_incongruent_references]{remove_incongruent_references()}}, \code{\link[=flag_incongruent_references]{flag_incongruent_references()}}
}
