% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_flagged_accessions.R
\name{resolve_review_overrides}
\alias{resolve_review_overrides}
\title{Derive Removal Overrides from an LLM Second-Look Review}
\usage{
resolve_review_overrides(
  review_result,
  keep_explanations = c("poor_marker_resolution", "sister_family_thin_coverage",
    "hybrid_or_specimen_code_artifact"),
  min_confidence = c("high", "moderate")
)
}
\arguments{
\item{review_result}{Data frame. Output of \code{\link[=review_flagged_accessions]{review_flagged_accessions()}}
(must have \code{accession}, \code{accession_likely_explanation},
\code{accession_review_confidence} columns).}

\item{keep_explanations}{Character vector (default
\code{c("poor_marker_resolution", "sister_family_thin_coverage", "hybrid_or_specimen_code_artifact")}) -- the
\code{accession_likely_explanation} values that should override an automatic
removal. Deliberately excludes \code{"uncertain"} from the default: an LLM
verdict that is ITSELF uncertain should not automatically override a
hard BLAST-based \code{"incongruent"} flag -- pass \code{"uncertain"} explicitly
if you want to trust it too. \code{"genuine_mislabel"} is never included
(that verdict CONFIRMS removal, it never overrides it).}

\item{min_confidence}{Character vector (default \code{c("high", "moderate")}).
Only a \code{accession_review_confidence} value in this set is trusted as an
override -- a \code{"low"}-confidence review falls through to removal
(the same conservative-default behavior as an unreviewed accession),
even if its \code{accession_likely_explanation} is in \code{keep_explanations}.}
}
\value{
Character vector of accessions to pass directly as
\code{remove_incongruent_references(override_accessions = ...)}.
}
\description{
Bridges \code{\link[=review_flagged_accessions]{review_flagged_accessions()}}'s LLM-based second-look verdicts to
\code{\link[=remove_incongruent_references]{remove_incongruent_references()}}'s \code{override_accessions} argument,
automating a "remove everything flagged by default, but let a specific
reviewed explanation override that default for the one accession it
actually applies to" pipeline -- rather than choosing between removing
every flagged accession (including real, correctly-labeled records the
raw BLAST verdict can't distinguish from a genuine mislabel) or removing
nothing until every flag has been manually reviewed.
}
\section{Real motivating case (2026-08-19)}{

A real GreatLakes match-candidate screen flagged \verb{Stereolepis doederleini} (\code{LC649807}) \code{hierarchy_flag = "incongruent"}. A separate,
earlier investigation into this exact accession found it is very likely
a correctly-labeled record with genuinely poor marker resolution at this
rank, not a real mislabel -- exactly the ambiguity
\code{\link[=remove_incongruent_references]{remove_incongruent_references()}}'s own \verb{@section Use flag_incongruent_references() first} documents. This function is the
automated version of applying that finding: run
\code{\link[=review_flagged_accessions]{review_flagged_accessions()}} once, and any accession the LLM
categorizes as something other than a genuine mislabel is kept, without
needing a human to individually re-decide each one every time the
pipeline re-runs.
}

\examples{
\dontrun{
evaluation <- evaluate_reference_accessions(accessions_to_screen)
review <- review_flagged_accessions(evaluation, llm_fn = my_llm_fn)
overrides <- resolve_review_overrides(review)
match_df_clean <- remove_incongruent_references(match_df, evaluation,
  override_accessions = overrides)
}

}
\seealso{
\code{\link[=review_flagged_accessions]{review_flagged_accessions()}}, \code{\link[=remove_incongruent_references]{remove_incongruent_references()}}
}
