% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_reference_accessions.R
\name{verify_flagged_references}
\alias{verify_flagged_references}
\title{Verify a Small, Flagged Reference Subset via BLAST, Not the Whole Database}
\usage{
verify_flagged_references(
  flagged,
  error_types = "likely_mislabeled",
  trust_insufficient_evidence = FALSE,
  cache_dir = tools::R_user_dir("TaxaMatch", "cache"),
  ncbi_api_key = Sys.getenv("NCBI_API_KEY", unset = ""),
  barcode_term = NULL,
  ...
)
}
\arguments{
\item{flagged}{Either a data frame (output of
\code{TaxaLikely::flag_reference_errors()}, with \code{id_x}/\code{error_type}
columns) or a plain character vector of accession IDs to verify.}

\item{error_types}{Character vector (default \code{"likely_mislabeled"}).
When \code{flagged} is a data frame, only rows whose \code{error_type} is in this
set are verified. The default matches what
\code{train_likelihood_model()} actually removes by default --
\code{"unverified_singleton_high_match"} is computed by
\code{flag_reference_errors()} but never acted on automatically, so
verifying it too roughly doubles NCBI cost for a category that isn't
currently removing anything from training. Pass
\code{c("likely_mislabeled", "unverified_singleton_high_match")} to verify
both.}

\item{trust_insufficient_evidence}{Logical (default \code{FALSE}). Whether an
\code{"insufficient_independent_evidence"} verdict (broader evidence exists,
but too little of it to say either way) counts as verified-clean.
\code{FALSE} is the conservative choice -- an accession this ambiguous stays
removed from training rather than being restored on thin grounds.}

\item{cache_dir, ncbi_api_key, barcode_term, ...}{Forwarded to
\code{evaluate_reference_accessions()}. \code{cache_dir} deliberately shares that
function's own default (\code{tools::R_user_dir("TaxaMatch", "cache")}) --
pass an explicit, project-scoped path shared with a real match-candidate
screen so any accession appearing in both populations is served from
cache for free rather than BLASTed twice.}
}
\value{
A list: \code{verified_clean} (character vector of accessions safe to
pass to \code{flag_reference_errors(verified_clean=)}/
\code{train_likelihood_model(verified_clean=)} -- everything NOT confirmed
\code{"incongruent"}), and \code{evaluation} (the full
\code{evaluate_reference_accessions()} output, for review). \code{verified_clean}
is \code{character(0)} and \code{evaluation} is \code{NULL} when \code{flagged} contains no
matching accessions -- no NCBI call is made in that case.
}
\description{
Bridges \code{TaxaLikely::flag_reference_errors()}'s free, offline (but known
over-flagging) within-reference-set mislabel screen to this package's
stronger, BLAST-based \code{evaluate_reference_accessions()} -- purpose-built
so a caller never has to BLAST an entire training reference database to
get the benefit of the better screen.
}
\section{Why the flagged subset, not the whole reference set (2026-08-18)}{

\code{TaxaLikely::train_likelihood_model()} calls \code{flag_reference_errors()}
unconditionally on every training run and silently drops every
\code{"likely_mislabeled"} accession before fitting H1/H2/H3 -- this has
always been true, it just went unnoticed until a user asked directly
whether it was happening at all. That screen is known to over-flag (see
\code{flag_reference_errors()}'s own \verb{@param verified_clean}): a real pilot
check against a real GreatLakes 12S reference set found 0 of 40
randomly-sampled \code{"likely_mislabeled"} accessions confirmed as genuine
mislabels by this package's own \code{evaluate_reference_accessions()} (a
BLAST-based check against a broad, independent database) -- 85\% looked
like false positives.

\code{evaluate_reference_accessions()} is the right tool to adjudicate this,
but BLASTing an entire training reference database (which can be LARGER
than a typical match-candidate screening population -- confirmed on real
GreatLakes data, ~2,650 accessions vs. a 1,183-accession match-candidate
run that already tripped a real NCBI CPU-budget rejection) risks exactly
the shutout this package's rate-limit resilience
(\code{evaluate_reference_accessions(chunk_size=, max_consecutive_batch_failures=)}) exists to survive, not avoid entirely.
\code{flag_reference_errors()} is already running for free (no NCBI call,
reuses the \code{seq_matrix} already built for training) -- this function
verifies only the small subset it actually flagged, turning "BLAST
thousands of accessions to find a few real mislabels" into "BLAST only
the disputed ones."
}

\examples{
\dontrun{
seq_matrix <- TaxaLikely::build_sequence_matrix(reference_df,
  rank_system = c("family", "genus", "species"))
errors <- TaxaLikely::flag_reference_errors(seq_matrix)
result <- verify_flagged_references(errors,
  cache_dir = "~/my_project_ref_eval_cache")
lik_model <- TaxaLikely::train_likelihood_model(seq_matrix,
  rank_system = c("family", "genus", "species"),
  verified_clean = result$verified_clean)
}

}
\seealso{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}},
\code{\link[TaxaLikely:flag_reference_errors]{TaxaLikely::flag_reference_errors()}}
}
