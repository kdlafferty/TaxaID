% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_reference_accessions.R
\name{evaluate_reference_accessions}
\alias{evaluate_reference_accessions}
\title{Evaluate Reference-Accession Quality via Unrestricted BLAST Comparison}
\usage{
evaluate_reference_accessions(
  accessions,
  cache_dir = tools::R_user_dir("TaxaMatch", "cache"),
  insufficient_evidence_ttl_days = 180,
  top_n = 5L,
  min_congruent_rank = "family",
  hierarchy_incongruent_threshold = 0.5,
  min_independent_partners = 3L,
  submission_window = 5L,
  method = c("remote", "local"),
  database = "nt",
  score_range = 8,
  min_score = 70,
  max_hits = 20L,
  ncbi_api_key = Sys.getenv("NCBI_API_KEY", unset = ""),
  poll_max_wait = 1800,
  barcode_term = NULL,
  verbose = TRUE
)
}
\arguments{
\item{accessions}{Character vector of NCBI accessions to evaluate.
Deduplicated internally.}

\item{cache_dir}{Character or \code{NULL}. Default
\code{tools::R_user_dir("TaxaMatch", "cache")}. Set \code{NULL} to disable
caching entirely (every call re-evaluates every accession).}

\item{insufficient_evidence_ttl_days}{Numeric (default \code{180}). See
Caching above.}

\item{top_n}{Integer (default \code{5L}). Max independent BLAST hits ranked
per accession for the congruence verdict.}

\item{min_congruent_rank}{Character (default \code{"family"}). Passed to the
shared congruence math -- see
\code{TaxaLikely::audit_reference_database()}'s own roxygen for why
\code{"family"}, not a coarser rank, is this ecosystem's default.}

\item{hierarchy_incongruent_threshold}{Numeric (default \code{0.5}). An
accession's smoothed \code{frac_independent_below_min_congruent_rank} at or
above this value reads \code{"incongruent"}.}

\item{min_independent_partners}{Integer (default \code{3L}). Fewer independent
top matches than this reads \code{"insufficient_independent_evidence"}
regardless of the fraction -- mirrors
\code{TaxaLikely::repair_thin_evidence()}'s identical floor.}

\item{submission_window}{Integer (default \code{5L}). Days (or accession-number
proximity) within which two accessions are treated as the same
submission batch, and therefore non-independent evidence of each other.}

\item{method, database, score_range, min_score, max_hits}{Passed through to
\code{blast_sequences()} -- see that function's own documentation.}

\item{ncbi_api_key}{Character or \code{NULL}. Optional NCBI API key for higher
rate limits (also forwarded to \code{blast_sequences()}).}

\item{poll_max_wait}{Numeric (default \code{1800}, i.e. 30 minutes). Forwarded
to \code{blast_sequences(poll_max_wait =)} -- how long to keep polling NCBI
for one BLAST batch's results before giving up on it. An accession
whose batch times out (even after \code{blast_sequences()}'s own automatic
halved-batch-size retry) is treated the same as one NCBI has no record
for at all -- excluded from this call's cache write and reported via
\verb{$unresolved}-style warning, NOT scored as
\code{"insufficient_independent_evidence"} -- a real, previously-possible
silent-miscache risk found 2026-08-09 on a real, large (1,183-
accession), multi-hour remote-BLAST run: sustained NCBI queue
congestion caused most batches after the first to time out at the old
hardcoded 600s ceiling, and every one of those accessions would
otherwise have been cached as a false \code{"insufficient_independent_ evidence"} verdict for up to \code{insufficient_evidence_ttl_days} (180 days
by default) -- masking the real infrastructure failure as if it were a
genuine evidentiary finding.}

\item{barcode_term}{Character or \code{NULL} (default). When supplied, any
query sequence exceeding the marker's expected length (via
\code{TaxaTools::resolve_barcode_lengths(barcode_term)}) is trimmed to its
amplicon region (via \code{TaxaTools::resolve_barcode_primers(barcode_term)}
\itemize{
\item the same primer-matching algorithm as \code{TaxaLikely::trim_to_amplicon()},
duplicated here -- see \code{.extract_amplicon_one_tm()}'s own documentation)
before being BLASTed, instead of submitting the full sequence.
BLASTing a full-length over-length reference (e.g. a complete
mitogenome, ~16.5kb) against a broad database is dramatically more
CPU-expensive than BLASTing its short barcode region, and was found
2026-08-09 to be the real cause of a live NCBI server-side CPU-budget
rejection on a real, large run whose queries were often full
mitogenomes (see \code{poll_max_wait}'s own documentation for the real
captured case) -- the accession's own species-identity signal lives in
the short barcode region regardless, so trimming answers the identical
question at a fraction of the cost. A sequence whose primer sites
can't be found is left at full length (never dropped or errored) and
BLASTed as before. \code{NULL} (default) submits every sequence at full
length, unchanged from prior behavior.
}}

\item{verbose}{Logical (default \code{TRUE}). Print progress messages.}
}
\value{
A data frame, one row per unique input accession:
\describe{
\item{\code{accession}}{Exactly as supplied by the caller.}
\item{\code{listed_taxon}}{The accession's own labeled organism (from its
real GenBank record).}
\item{\code{n_independent_top_matches}}{Independent BLAST hits actually
used for the verdict (\verb{<= top_n}).}
\item{\code{n_top_matches_available}}{All BLAST hits before the
independence filter -- diagnostic only.}
\item{\code{frac_independent_below_min_congruent_rank}}{Jeffreys-smoothed
fraction of independent hits disagreeing at or above
\code{min_congruent_rank}.}
\item{\code{finest_common_rank}}{Finest rank at which the single best
independent hit agrees with the listed taxon (\code{NA} if none), walking
the full \code{kingdom}->\code{species} ladder -- see \verb{@section Coarse-rank diagnostic} above.}
\item{\code{best_hit_pident}}{Percent identity (0-100) of the single best
independent hit, regardless of whether it agrees or disagrees.}
\item{\code{best_agreeing_pident}}{Percent identity of the highest-identity
independent hit that agrees at or above \code{min_congruent_rank}, among
the \code{top_n} slice used for the verdict itself. \code{NA} if none agree.}
\item{\code{best_disagreeing_pident}}{Percent identity of the highest-
identity independent hit that disagrees (coarser than
\code{min_congruent_rank}), among the \code{top_n} slice. \code{NA} if none
disagree. See \verb{@section Identity diagnostics} below for why this
matters -- a high value here is a much stronger mislabel signal
than a low one, information the binary \code{hierarchy_flag} alone
cannot convey.}
\item{\code{congruent_evidence_exists_anywhere}}{Logical. Unlike
\code{hierarchy_flag} (computed only from the \code{top_n} closest
independent hits), this asks whether ANY independent hit anywhere
in the full BLAST result (up to \code{max_hits}) agrees at or above
\code{min_congruent_rank}. \code{FALSE} here is a stronger statement than an
\code{"incongruent"} \code{hierarchy_flag} alone -- it means the listed rank
has no representation at all among this accession's independent
matches, not just none close enough to make the \code{top_n} cut.}
\item{\code{congruent_evidence_best_pident}}{Percent identity of the best
such anywhere-agreeing hit. \code{NA} when
\code{congruent_evidence_exists_anywhere} is \code{FALSE}.}
\item{\code{hierarchy_flag}}{\code{"congruent"}, \code{"incongruent"}, or
\code{"insufficient_independent_evidence"}. \code{NA} if the accession's own
GenBank record could not be fetched (a \code{warning()} is issued
listing these; not cached, so a subsequent call retries them).}
\item{\code{evaluated_at}}{When this verdict was computed (\code{NA} for a
fetch failure).}
\item{\code{cache_hit}}{\code{TRUE} if this row was read from \code{cache_dir}
rather than recomputed this call.}
\item{\code{taxonomy_resolution_source}}{\code{"direct"} (the accession's own
NCBI taxonomy, the normal case), \code{"hybrid_maternal_proxy"} (a
hybrid-labeled accession whose coarser-rank lineage was resolved
from its maternal parent species instead -- see \verb{@section Hybrid-labeled accessions} below), \code{"hybrid_unresolved"} (detected
as hybrid-labeled but no usable parent species name could be
extracted from the label, so the accession's own -- structurally
incomplete -- NCBI lineage is used as-is), or \code{NA} for a fetch
failure.}
\item{\code{listed_taxon_is_species}}{Logical. \code{FALSE} when \code{listed_taxon}
does not structurally look like a species-level binomial (via
\code{\link[TaxaTools:is_plausible_binomial]{TaxaTools::is_plausible_binomial()}} -- e.g. a family name used in
place of a genus with an informal specimen code, such as
\code{"Serranidae sp. JL-2015"}, a real GreatLakes case). A
structurally different problem from both mislabeling
(\code{hierarchy_flag}) and hybrid-labeling
(\code{taxonomy_resolution_source}): such a reference can't
discriminate at species level regardless of whether it's
internally self-consistent, so this can be \code{FALSE} even when
\code{hierarchy_flag} reads \code{"congruent"}. \code{NA} for a fetch failure
(never \code{FALSE} -- a fetch failure means "not evaluated," not
"evaluated and found non-species").}
}
}
\description{
For each accession, BLASTs its own sequence against a broad,
\strong{unrestricted} database (not scope-limited to any caller-chosen
taxon list), applies the same-submission-batch independence filter, and
computes a Jeffreys-smoothed taxonomic-hierarchy congruence verdict --
the same congruence math
\code{TaxaLikely::audit_reference_database()}/\code{classify_reference_accessions()}
used to compute from a narrow, taxon-list-scoped DECIPHER alignment, now
fed from real, broad BLAST hits instead.
}
\section{Why this exists (and why the old approach was abandoned)}{

The superseded approach's "among"/foreign comparison population for ANY
accession was exactly and only whatever else got fetched under the
caller's own \code{taxa} argument -- no broader NCBI comparison existed
anywhere in that pipeline. This produces both false positives (real,
correctly-labeled accessions read "incongruent" purely because nothing
else from their own family happened to be in the caller's list) and false
negatives (a mislabeled accession's true contaminating identity can only
ever be caught if its genus happens to be on the caller's list too).
BLASTing each accession against an unrestricted database removes the
\code{taxa}-list dependency for both directions at once. See
\code{ecosystem_docs/REENTRY_PROMPT_blast_based_reference_quality.md} for the
full real-data evidence (a real 6-genus GreatLakes 12S test flagged 15
genuine, Smithsonian-vouchered \code{Menidia} accessions "incongruent" purely
because \code{Menidia}'s family had no other representative on the list).
}

\section{Coarse-rank diagnostic (2026-08-07)}{

\code{finest_common_rank} walks the FULL \code{kingdom}->\code{species} ladder
(\code{TaxaTools::standard_ranks}), not just \code{min_congruent_rank} and finer.
\code{hierarchy_flag}'s classification threshold is unaffected -- it still
fires at \code{min_congruent_rank} (default \code{"family"}) exactly as before --
but \code{finest_common_rank} can now report a real coarser match (e.g.
\code{"order"}) instead of collapsing straight to \code{NA} the moment \code{"family"}
fails. This distinction is real, not cosmetic: found live-testing against
a real PtConception 18S accession (\verb{Abylopsis eschscholtzii},
\code{KY594854}/\code{KX384617}) whose top independent BLAST hits are consistently
\code{Diphyidae} -- a SISTER family within the same order (Siphonophorae,
Calycophorae), not an unrelated organism. Both accessions flagged
\code{"incongruent"} under the family-only version with no way to tell that
apart from a genuine cross-phylum mismatch; \code{finest_common_rank = "order"}
now reports that real, more specific picture. The likely underlying
cause here is 18S's well-documented poor resolving power within
Calycophorae combined with thin \code{Abylidae} GenBank coverage at this
locus -- NOT necessarily a mislabel -- and this diagnostic is what makes
that distinguishable at all from the output alone, without re-BLASTing
by hand. Query-side taxonomy for every rank is resolved via the internal
\code{.resolve_taxonomy_by_acc()} (the same NCBI-taxonomy-DB mechanism already
used to classify BLAST hits), not \code{TaxaTools::fill_higher_ranks()}
(GNVerifier-backed, and only ever resolved genus/family) -- keeps both
sides of every comparison authority-consistent.
}

\section{Caching}{

Deduplication (\code{unique(accessions)}) happens regardless of caching -- an
accession is never re-evaluated twice within one call. A persistent,
cross-run cache underneath (keyed by accession alone, not by
taxon/genus/project) is what lets a project only pay evaluation cost for
accessions that are genuinely new or previously out of scope anywhere --
no \code{taxa} list to decide up front. Staleness is handled asymmetrically:
\code{"congruent"}/\code{"incongruent"} verdicts are cached indefinitely (an
accession's own sequence/label doesn't change once deposited); only
\code{"insufficient_independent_evidence"} verdicts expire after
\code{insufficient_evidence_ttl_days} and are retried, since new NCBI deposits
could genuinely change that specific answer. A cached row is also treated
as stale (recomputed) if any parameter that affects the verdict itself
(\code{top_n}, \code{min_congruent_rank}, \code{submission_window},
\code{hierarchy_incongruent_threshold}, \code{min_independent_partners},
\code{score_range}, \code{min_score}, \code{max_hits}, \code{method}, \code{database}) differs from
the call that produced it.
}

\section{Hybrid-labeled accessions (2026-08-10)}{

NCBI's own taxonomy entry for a hybrid-cross-labeled organism (e.g.
\code{"Ctenopharyngodon idella x Megalobrama amblycephala"}) is genuinely
incomplete -- confirmed live against several real GreatLakes candidates:
the lineage terminates at \code{"unclassified Cyprinoidei"}, with no family,
genus, or species populated at all. Left as-is, such an accession can
never agree with any independent hit at family rank or finer (there is
nothing on the query side to compare), which this function's rank-walk
mechanically reads as maximal disagreement -- a real, confirmed
false-positive mode (10 of 13 \code{"incongruent"} flags on a real
1,183-accession GreatLakes run were this artifact, not genuine
mislabels; the other 3, with complete normal lineages, were real
candidates worth reviewing).

Fixed by resolving the accession's maternal parent species' OWN real
lineage instead, for every rank coarser than species: since mtDNA is
maternally inherited in fish, a hybrid's barcode sequence genuinely IS
its maternal parent's lineage at kingdom through genus, even though it
is (correctly) not literally the same SPECIES as that parent. The
maternal parent's name is extracted from \code{listed_taxon} via
\code{TaxaTools::clean_taxon_names()}'s existing 3-token simplification (it
already keeps only the first genus + epithet, discarding everything
from \code{" x ..."} onward -- the same simplification this ecosystem
already applies to hybrid-formula names elsewhere), then resolved via
\code{TaxaTools::verify_taxon_names(backbone_id = 4L)} (NCBI, the same
authority every other taxonomy resolution in this function uses).
\code{species.x} (the finest rank) is deliberately left as the accession's
own real listed hybrid label, never replaced by the proxy -- a hybrid
genuinely is not the same species as its maternal parent, so a spurious
species-level agreement would be biologically wrong in the opposite
direction. Detection requires BOTH \code{listed_taxon} containing a
standalone \code{" x "} token (the standard nomenclatural hybrid marker) AND
the accession's own resolved \code{family} being unresolvable -- so an
ordinary, non-hybrid taxon with a genuinely incomplete NCBI lineage
(e.g. a real undescribed/unclassified species) is never routed through
this maternal-parent substitution, which would have no biological
justification for a non-hybrid. A leading lowercase breeding/ploidy-
manipulation modifier (e.g. \code{"androgenetic"}, \code{"autodiploid"},
\code{"autotetraploid"} -- all found on real GreatLakes records, 2026-08-11)
is stripped before \code{clean_taxon_names()} runs, since the maternal-
inheritance argument still holds (these manipulate the nuclear genome,
not which egg's cytoplasm/mitochondria the offspring develops in). A
label the hybrid-marker regex catches but still cannot be parsed into a
usable proxy even after that stripping falls back to the accession's
own unresolved lineage, \code{taxonomy_resolution_source = "hybrid_unresolved"} -- an honest admission, not a guess.
}

\section{Identity diagnostics (2026-08-07)}{

\code{hierarchy_flag}/\code{finest_common_rank} alone cannot distinguish a genuine
mislabel from "the listed rank has no well-covered independent relative
in GenBank at this marker" -- both produce identical rank-agreement
output. Percent identity is the cheapest available discriminator and was
already being computed (\code{p_match}, used only to ORDER hits) and then
discarded. A DISAGREEING hit at ~99\% identity is a real mislabel signal
(the deposited sequence is nearly identical to something in a different
family); a disagreeing hit at ~85-90\% is unremarkable for a conserved
marker (e.g. 18S) with poor resolving power at that rank. This does NOT
resolve the ambiguity on its own -- it is a design-consult finding
(2026-08-07, grounded in a real case: \verb{Abylopsis eschscholtzii},
\code{KY594854}/\code{KX384617}, both flagged \code{"incongruent"} with disagreeing
hits at family that are consistently a SISTER family within the same
order) that these columns give a caller genuinely new information to
judge that question with, not a verdict. No classification threshold in
this function reads these columns; they are informational only.
}

\seealso{
\code{\link[=remove_incongruent_references]{remove_incongruent_references()}}, \code{\link[=flag_incongruent_references]{flag_incongruent_references()}},
\code{\link[=blast_sequences]{blast_sequences()}}
}
