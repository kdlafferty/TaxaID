% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_reference_accessions.R
\name{flag_incongruent_references}
\alias{flag_incongruent_references}
\title{Annotate a Match Object with Reference-Accession Quality, Without Removing Anything}
\usage{
flag_incongruent_references(match_df, evaluation)
}
\arguments{
\item{match_df}{Data frame. A standardized match object (from
\code{\link[=standardize_match_data]{standardize_match_data()}}) containing an \code{accession} column.}

\item{evaluation}{Data frame. Output of \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}.}
}
\value{
\code{match_df} with \code{hierarchy_flag}, \code{finest_common_rank},
\code{frac_independent_below_min_congruent_rank}, \code{n_independent_top_matches},
\code{n_top_matches_available}, \code{best_hit_pident}, \code{best_agreeing_pident},
\code{best_disagreeing_pident}, \code{congruent_evidence_exists_anywhere}, and
\code{congruent_evidence_best_pident} joined on, plus \code{label_confidence},
\code{label_identity_margin}, \code{reference_action} and \code{listed_taxon_is_species}
whenever \code{evaluation} carries them (it always does when it came from
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} or \code{\link[=score_reference_labels]{score_reference_labels()}}; an
\code{evaluation} read straight off a cache file created before these
columns existed will not).
A row whose accession was not found in \code{evaluation} gets \code{NA} in all of
these (not evaluated yet, not evidence of anything). Row count and order
are unchanged.
}
\description{
The RECOMMENDED default consumer of \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} --
left-joins its evaluation columns onto \code{match_df} by accession
(version-suffix-stripped, same convention as
\code{\link[=remove_incongruent_references]{remove_incongruent_references()}}) so \code{hierarchy_flag} and the identity/
coverage diagnostics travel with the match object for review, without
ever discarding a candidate taxon.
}
\section{Why flag-by-default, not drop-by-default}{

A real case
(\verb{Abylopsis eschscholtzii}, \code{KY594854}/\code{KX384617}) shows why an
unreviewed hard drop is the wrong default: \code{hierarchy_flag = "incongruent"} cannot currently distinguish a genuine mislabel from
"correct label, but this marker has poor resolving power at this rank
for this clade and GenBank coverage is thin" -- see
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}'s own \verb{@section Identity diagnostics}.
Dropping the accession destroys the candidate taxon entirely and
irreversibly on the strength of a verdict that can be wrong in exactly
this ambiguous way. The same pattern recurs elsewhere in this ecosystem
with unrelated mechanisms, for the identical reason
-- \code{TaxaLikely::apply_coverage_constraints(constraint_behavior)}
defaults to \code{"relabel"} rather than \code{"zero"} (a genus-completeness
NCBI-query result treated as certain ground truth would permanently
discard a correct hypothesis with no way for downstream evidence to
recover it), and
\code{TaxaFetch::filter_gbif_quality()} defaults to
\code{flag_institution} rather than \code{exclude_institution} (real,
correctly-labeled observations near a
biodiversity institution would otherwise be auto-removed alongside real
errors,
since proximity to an institution -- like thin reference coverage for a
correctly-labeled clade -- is not itself proof of error). Use this
function by default; reach for \code{\link[=remove_incongruent_references]{remove_incongruent_references()}}
deliberately, after review, not as the default pipeline step.
}

\section{When \code{evaluation} carries more than one row per accession}{

\code{evaluate_reference_accessions()} keys its own cache on the literal
(version-suffixed) accession string, so two rows of \code{evaluation} can share
the same version-stripped accession -- e.g. \code{"AB123.1"} and \code{"AB123.2"},
evaluated independently. No other function in this package picks a single
row among same-accession duplicates (\code{remove_incongruent_references()}
removes a \code{match_df} row if ANY of its accession's evaluation rows was
flagged, never selects one; \code{.local_corroboration_columns()} keeps
whichever row happens to sort first). In the absence of an established
rule, this function keeps the row with the HIGHEST numeric version
suffix (an accession with no version suffix sorts below any versioned
one) before joining, so every \code{match_df} row is matched to at most one
\code{evaluation} row and row count/order are preserved exactly as documented
below.
}

\seealso{
\code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}, \code{\link[=remove_incongruent_references]{remove_incongruent_references()}}
}
