% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference_label_verdict.R
\name{verify_removal_candidates}
\alias{verify_removal_candidates}
\title{Verify Removal Candidates Against a Wider Evidence Window}
\usage{
verify_removal_candidates(
  evaluation,
  ...,
  audit_max_hits = 100L,
  cache_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{evaluation}{An \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}} result. Needs
\code{accession} plus either \code{reference_action} or the diagnostic columns
\code{\link[=score_reference_labels]{score_reference_labels()}} derives it from.}

\item{...}{Forwarded to \code{\link[=evaluate_reference_accessions]{evaluate_reference_accessions()}}. Must carry the
production run's own verdict-affecting arguments (see above).}

\item{audit_max_hits}{Integer (default \code{100L}). The wider window. Note
that on real data 13 of 34 accessions were still saturated at 100, so a
clean result here bounds the problem rather than closing it.}

\item{cache_dir}{Directory for the audit's own cache, or \code{NULL} (default)
for no caching. Because \code{max_hits} is in \code{params_key} an audit can never
overwrite a production row, but a separate directory keeps the
production cache free of rows no production call will ever read.}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
A data frame with one row per removal candidate: \code{accession},
\code{listed_taxon}, \code{action_production}/\code{action_audit},
\code{anywhere_production}/\code{anywhere_audit},
\code{n_partners_production}/\code{n_partners_audit},
\code{n_hits_audit}, \code{still_saturated} (the audit itself hit
\code{audit_max_hits}, so its own window is also truncated), \code{spared}
(the accession is no longer actioned \code{"remove"}), and -- when
\code{cache_dir} is supplied -- \code{n_corroborators}, \code{best_corroborator_rank}
and \code{corroborators}, a short list of the strongest partners that agreed.
Zero rows, and zero NCBI calls, when nothing would be removed.
}
\description{
Re-evaluates ONLY the accessions an evaluation would actually remove, at a
larger \code{max_hits}, and reports which of them stop being removable. Nothing
is removed, nothing is written back to the production cache, and no
verdict is overwritten -- this is the pre-removal audit step, not a
replacement for the screen.
}
\section{Why this exists}{

\code{congruent_evidence_exists_anywhere} is the hard veto that spares an
accession from \code{reference_action == "remove"} however low its
\code{label_confidence}. It is documented as walking the pool "not limited to
\code{top_n}" -- and it is, but the pool is itself capped by \code{max_hits}
(default 20). On the real PtConception 12S screen, 899 of 989 accessions
(91\%) came back AT that cap, so in practice "anywhere" has meant "anywhere
in the top 20".

That was measured, not suspected
(\code{diagnostics/veto_truncation_probe.R}, 2026-09-04): at \code{max_hits = 100},
\code{congruent_evidence_exists_anywhere} flipped \code{FALSE} to \code{TRUE} for 8 of 15
veto-critical accessions, and \code{OQ846263} (\emph{Rathbunella hypoplecta}) --
one of only two accessions that PtConception run would have removed --
turned out to be corroborated and dropped to \code{"inspect"}. \code{KM057967}
(\emph{Jordania zonope}) still removed at 100 hits, correctly: it is a genuine
singleton. Both congruent controls were unchanged, as they must be, since
widening a window cannot withdraw corroboration already observed.

Raising the \code{max_hits} DEFAULT was considered and deliberately not done:
\code{max_hits} is part of \code{params_key}, so a change would invalidate every
cached row across every project cache (~3,000 real rows) for a screen that
has been NCBI-throttled before. Auditing only the removal candidates is
the cheap targeted alternative -- a real population is 1-4 accessions, and
removal is the one decision this package makes destructively.
}

\section{Comparability, and why the params_key is checked}{

The audit is only meaningful if the re-evaluation differs from the
production run in \code{max_hits} ALONE. Pass through \code{...} exactly the same
verdict-affecting arguments the production screen used -- \code{barcode_term}
above all, since an untrimmed query is a different query. This function
compares the \code{params_key} its own call will produce against the one
carried on \code{evaluation} and warns, naming the fields, if anything other
than \code{max_hits} differs. Heed that warning: a mismatch does not make the
call fail, it makes the result mean nothing.
}

\section{Read the corroborators, not just \code{spared}}{

\code{congruent_evidence_exists_anywhere} counts a corroborating partner
without any notion of whether that partner's own label is trustworthy, so
a mislabeled reference can be rescued by another instance of the SAME
mislabel. This is not hypothetical -- it happened on this function's first
real use. GreatLakes \code{KJ135626} (\emph{Pseudorasbora parva}) came back
\code{spared = TRUE}, rescued by exactly one partner agreeing at species rank:
\code{MZ605481}, which this project's own
\code{diagnostics/reference_accession_ground_truth.csv} records as a
\code{candidate_mislabel} whose real identity is \emph{Cyprinus carpio}.
\code{KJ135626}'s own best disagreeing hit is \emph{Cyprinus carpio} at 100\%. Two
copies of one error agreeing with each other is not corroboration, and the
LLM reviewer's independent \code{"genuine_mislabel"} call on that accession was
the better answer.

\code{refine_reference_verdicts()} cannot close this: it discounts a partner by
that partner's OWN verdict, and a corroborator which is merely a BLAST hit
-- not itself in the screened population -- has no verdict to discount.
Widening the window makes the exposure larger, not smaller, since it
admits more potential bad corroborators. So treat \code{spared} as a prompt to
look, not a conclusion: a row rescued by one or two partners gets an
explicit warning naming them.
}

\seealso{
\code{\link[=remove_incongruent_references]{remove_incongruent_references()}}, \code{\link[=score_reference_labels]{score_reference_labels()}}
}
