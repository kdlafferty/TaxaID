---
title: "TaxaAssign code review"
output: html_document
---
### Code Review

Purpose: To produce high quality code that is well written and organized according to best coding practices.

* [x] **Functionality:** The code is readable, organized, and efficient. 
  * Is the code readable? 
  * Can you understand it?
    * Code that is extremely cryptic and hard to follow is not ready to be released publicly. If the reviewer has trouble understanding it, a wider audience will, too. The intended audience should also be considered. The code should be understandable by someone with knowledge of the coding language and the problem to be solved.
    
   The code is readable and organized. Some of the same difficulties noted in other packages persist, but those may be unavoidable.
   
* [x] **Optimizations:** The code does not have any areas that could be optimized in a more efficient way. 
  * Is additional documentation required?
  
  The documentation seems efficient. Code reproduction could be reduced and/or simplified in places, see individual comments.
  
* [x] **Coding standards:** The code does not contain any errors or bad coding practices. 
  * Are there docstrings for functions that describe what the function does and its inputs/outputs? 
  * Are in-line comments informative?
  
  I did not notice any major coding standard issues.

* [x] **Automated tests:** Confirm that all tests pass when run locally.

  Here is the output from running `test_local` on the test directory: `[ FAIL 0 | WARN 13 | SKIP 1 | PASS 615 ]`

* [x] **Vulnerabilities:** The code does not have any areas that are vulnerable in any way.

   I do not have the domain knowledge to assess any security issue or vulnerabilities. The package does query some APIs.
  
Any Additional comments, list below in comment section.

### Domain Review

Purpose: To ensure the code produces outputs, data, interfaces, or software that is biologically reasonable when compared to other similar information available. Review should be conducted by someone with applicable subject-matter knowledge.

* [x] **Scientific Rigor:** The code/software produces outputs that are biologically reasonable. 
  * Are the values and units for constants (e.g., density of granite) correct? 
  * Are any claims of what the code does substantiated? 
  * If tests are included, are they adequate to evaluate functionality?

  See previous comments on tests. The code appears to work as advertised. I could not run all functions as example data was not readily available.

* [x] **Outputs:** The code/software produces outputs that are comparable, or within reasonable limits, to outputs of similar code, models, interfaces. 
  * If equations are solved, are the equations input correctly into the code?
  
  I do not have the domain knowledge to fully evaluate all equations, but I did not see any obvious errors.

* [x] **Algorithms:** The code uses algorithms that are appropriate for its intended purpose. 
  * Are standard data processing techniques (e.g., filtering) used appropriately? 
  * Do you see any potential flaws or shortcomings in the methods, inputs, or algorithms used in the code?

  I did not see any obvious errors.
  
Any Additional comments, list below in comment section.

- When building the package, the warning below is issued. This has been observed in other `TaxaID` packages.

```
NB: this package now depends on R (>= 4.1.0)
  WARNING: Added dependency on R >= 4.1.0 because package code uses the
  pipe |> or function shorthand \(...) syntax added in R 4.1.0.
  File(s) using such syntax:
    'assign_taxa_llm.R' 'combine_multisite_priors.R'
    'compute_posterior.R' 'generate_report.R' 'join_priors.R'
    'run_bayesian_pipeline.R' 'update_prior_from_consensus.R'
```

- I have mostly ignore it, but almost every function in all packages has arguments on newlines but the first character is not aligned. Not a big deal but an easy fix.

- Suggest removing references to dates or session numbers.

- All the packages in the `TaxaID` ecosystem so far have sorted ranks by position, often with a user-defined vector. I suggest defining a function once, perhaps in `TaxaTools`, that does this for all possible ranks and that can find the minimum from a known sequence. Ideally, this would be immutable by the user. This is tricky in R though and might require a custom s4 class.

- If possible, all exported functions should have a minimal runnable example, written as-is in the examples section.

Comments on specific files:

**adjust_inat_range_priors.R**

* [x] reviewed

- No examples. Include reproducable minimal example.

- I think alpha / (alpha + beta) is calculating `beta_mean` from `TaxaExpect`, right? Could this be defined in TaxaTools and reused? The formula is used here on lines 122-123 and 147-150. 

**assign_taxa_llm.R**

* [x] reviewed

- Lines 196-235: this section does present some interesting caveats concerning functionality and assumptions, but consider whether it should be included in its entirety in the final product.

- Line 267: I'm not sure this file is actually in `TaxaMatch`.

- Line I looked through the prompt generated for a simulated dataset created with `make_match_df` from the test file, and I don't see how the LLM knows where the observation is without `context`. Maybe I missed something, but shouldn't context be required? 

- Line 712: could this be a requirement for inputs that `hypothesis_type` and `taxon_name_rank` can't be included?

**build_context.R**

* [x] reviewed

- Example not runnable as-is.

- Should this be in `TaxaHabitat`?

- Line 111: got this odd warning running with misspelled `taxon_names`, should it say something like `"Sequoia sempivirens not found"`?

```
Warning message:
parse_hierarchical_habitat_response: 1 taxon/taxa missing from response: Sequoia sempivirens 
```

- I've mentioned this in other reviews but I think prompt building could be done using one function, with prompt generation specified by argument (e.g. `prompt_type = "habitat context"`).

**combine_multisite_priors.R**

* [x] reviewed

- Example not runnable as-is.

- Line 160: suggest not referencing session numbers in error messages.

- It looks like `main_habitat` from inputs is not preserved in the outputs. Is this intentional?

**compute_posterior.R**

* [x] reviewed

- Example not runnable as-is.

- Line 33: why not require `prior_alpha` and `prior_beta` and always sample from `Beta(alpha, beta)`?

- Line 185: `sigma` is an existing R function.

- `rtruncnorm_at_zero` zero-truncation does seem to shift the mean of the distribution away from `mu` when it is small and `sigma` is large (i.e. mu = 0.001, sigma = 2), but I only tested it once so I might have not thought everything through.

- I don't recall exactly what the distribution of `score_likelihood` should be at this point, but it's not a truncated Gaussian, is it? Presumably it conforms to some known distribution, why not sample from that?

**consensus_refinement.R**

* [x] reviewed

- It's internal so maybe it needs less documentation, but documentation's pretty minimal.

- Indirectly tested in `run_bayesian_pipeline` and `run_llm_pipeline`.

- Didn't run directly as it needs many parts, ran idirectly via `run_bayesian_pipeline` and `run_llm_pipeline`.

**expand_unreferenced.R**

* [x] reviewed

- Since this code has not been released suggest removing from `TaxaAssign` entirely, since backward compatibility is less of a concern right now.

**generate_report.R**

* [x] reviewed

- Would this function make more sense in `TaxaTools`? How could this be combined with functions of similar functionality in this package ecosystem?

- Example not runnable as-is.

- Instead of trying to detect what the workflow is, suggest requiring that it be specified.

- Under what circumstances would `utils::citation("TaxaAssign")` return an error when this package is installed? I guess it's not a problem (you can never be to careful) but the `tryCatch` seems unnecessary here. Why can't the citation for this just be the string on line 222? Lines 211-228 could be collapsed to one or two lines.

- Line 547: double check if it always true that `score_sharpness=0.1` when it's not specified here. Is there any other way this number could be different? Suggest changing if there is any way this could happen, even if this is the default. Same forother values here (i.e. `absent_prob` and `n_sims`).

**group_priors.R**

* [x] reviewed

- Line 16: how is `TaxaFlag` relevant here?

- Should `rank_cols` have a default?

- Suggest curly brackets or spacing lines 102-104 to make the early return more distinct when reading.

**join_priors.R**

* [x] reviewed

- Example not runnable as-is.

- Line 52 and elsewhere: `rank_system` and other taxonomic rankings are filtered by position. Aren't rank hierarchies known? Would it be safer to compare to a known lookup?

- Is `template_keep_cols` ever used?

- Lines 151, 297: this is the same function as used in `TaxaExpect`, could it be defined once?

- Line 193: `dark floor`?

- Line 327: This formula is reproduced in some form multiple times, consider defining once.

- Line 460: Suggest including this explanation in the README.

- Line 506: Suggest requiring a single specific format for `site`. 

- Line 551: This file is not in this package, if it is important it should be documented within the package.

- Line 754: suggest requring coordinates since they are known.

- Lines 1088-1093 (and elsewhere): could a merge be used effectively here?

**posterior_consensus.R**

* [x] reviewed

- Example not runnable as-is.

- Line 137: consider whether this adds to the documentation for most users.

- Lines 578-581: if this used the format below, it would be easier to read.

```
if (test_condition) {
  result <- do_something(object)
} else {
  result <- NA_real_
}
```
- Inconsistent curly brace use lines 783-793. Suggest using them in all locations.

- Like all functions working with ranks in this package ecosystem, `.find_lca` assumes that taxon ranks are in a specific order. Suggest defining them once for all functions here and in other packages and referencing directly. Ideally this would be an immutable object but that might require a custom class. This applies to several functions in this file alone.

- Lines 906, 1004: are these always species or can this be accessing a attribute that only lists genus? Could it read, say "Pinus" instead of "Pinus contorta"?

**report_assign.R**

* [x] reviewed

- Lines 10-11: weird spaces.

- should this be in `TaxaTools`?

- Example not runnable as-is.

- Line 65: suggest requiring workflow be specified instead of guessing based on column names.

**run_bayesian_pipeline.R**

* [x] reviewed

- This seems bigger than this package somehow- dependent on all or many of the others so far.

- Example not runnable as-is.

- Examples in tests are all designed to fail at some point so couldn't test all functionality.

**run_llm_pipeline.R**

* [x] reviewed

- Example not runnable as-is.

- Line 32: doesn't all the LLM stuff require `TaxaTools`? I'd definitely say it's a dependency.

**score_consensus.R**

* [x] reviewed

- Example not runnable as-is.

- Line 243: suggest requiring a specific format (either 0-1 or 0-100) instead of guessing.

- Line 257: suggest testing for and requiring that these be on the same scale instead of fixing.

- Line 307: usual comment about ranks applies here too.

- Lines 328-338: Why reassign to `chunk`? I think you could just keep working with the same vector `scores` until step 2 is complete.

- Line 344: this object doesn't need to be named, could replace `unique_taxa` with `!duplicated(chunk$taxon_name)` in line 345.

- Line 423: Could this be a vectorized filter? A little tricky to test on the fly.

**site_utils.R**

* [x] reviewed

- Not sure any of these functions are directly tested.

- Example data not available so I dodn't run directly.

- Line 21: `TaxaTools::call_api` usually results in an error, often package must be loaded.

- Maybe I'm just not seeing it, but why does `.resolve_site` need to take multiple formats?

- Test for `NULL` in `main_habitat` in `.latlon_to_grid` should be done immediately, otherwise effort on grid stuff in earlier lines is wasted.

- `taxaexpect_priors` should have `lat` and `lon` preserved to avoid parsing ID.

- `.find_nearest_grid`: I don't think this distance formulation works correctly with spherical coordinates. 

- Line 162: is it possible to have length missmatches for `taxaexpect_priors` and `unique_locs` columns?

- Line 205: see previous comment about spherical coordinates, but doesn't this just reproduce the effort from `.find_nearest_grid`?

- Line 2018: what happens if `grid_rows` has no values?

**slash_taxon.R**

* [x] reviewed

- Example not runnable as-is.

- `.make_slash_name` uses spaces to split binomials. This is probably okay and follows a pattern that is used throughout these packages, but it could be a source of failure if the assumption isn't met. The `@note` on line 113 suggests that this is a possibility, consider if this could be improved to reduce this weakness.

- `add_slash_taxon` does some ranking by posterior, consider renaming to better fit it's actual function. Currently seems like a wrapper for `.make_slash_name`.

- I wonder if having list columns is the best way to do this. Is there any way the order of the list columns could get corrupted? Could they be combined somehow?

**suggest_unreferenced_species.R**

* [x] reviewed

- Lines 45-46: could this just iterate over `genera`, which would allow any length?

- Line 146: probably safer to iterate instead of selecting the first element, even if you're pretty sure it's always length 1.

- Line 342: example not runnable as-is.

- Line 491: I don't think this object is incuded in `TaxaMatch`. Without it example not runnable.

- I had to explicitly load `TaxaTools` to run.

- Running on the match_df created with`make_spg_match_df` in the test file, I get the following, which I think means no unreferenced species are suggested:

```
Unreferenced species detected: 0
Access full details: attr(x, "census") | attr(x, "plausible")
```

**TaxaAssign-package.R**

* [x] reviewed

**update_prior_from_consensus.R**

* [x] reviewed

- Line 116: this seems like a developer note to self instead of helpful documentation.

- Example not runnable as-is.

- Line 381: consider if date will be meaningful for future users.

- Using the test data, one row had `prior_updated = TRUE` when the prior values were unchanged from the input.
