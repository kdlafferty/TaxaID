% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_consensus.R
\name{posterior_consensus}
\alias{posterior_consensus}
\title{Derive Consensus Taxonomy from a Posterior Dataframe}
\usage{
posterior_consensus(
  posterior_df,
  rank_system = NULL,
  cumulative_threshold = 0.9,
  min_posterior = 0.05,
  posterior_col = "posterior_mean",
  lookup_missing_taxonomy = FALSE,
  backbone_id = NULL,
  species_reference = NULL,
  group_priors = NULL
)
}
\arguments{
\item{posterior_df}{Dataframe. Output of \code{\link[=compute_posterior]{compute_posterior()}} or
\code{\link[=assign_taxa_llm]{assign_taxa_llm()}}. Required columns: \code{observation_id}, \code{taxon_name},
\code{taxon_name_rank}, \code{hypothesis_type}, and the column named by
\code{posterior_col}.}

\item{rank_system}{Optional character vector of taxonomy column names,
coarse-to-fine (e.g. \code{c("family", "genus", "species")}). If \code{NULL}
(default), standard taxonomy columns present in \code{posterior_df} are
detected automatically from \verb{kingdom, phylum, class, order, family, genus, species}. Genus is always derivable from species binomials even
when the \code{genus} column is absent.}

\item{cumulative_threshold}{Numeric in (0, 1]. Cumulative posterior
probability threshold for the plausible hypothesis set. Hypotheses are
included in descending posterior order until this fraction of total
probability is reached. At 0.90, the plausible set contains the fewest
hypotheses accounting for at least 90\% of posterior mass, analogous to a
90\% credible interval. Default 0.9. For example, at 0.9 with posteriors
(0.6, 0.25, 0.10, 0.05), the plausible set includes the top 2
hypotheses (0.6 + 0.25 = 0.85 < 0.9, so the third is also included:
0.6 + 0.25 + 0.10 = 0.95 >= 0.9). The LCA of these three hypotheses
becomes the consensus.}

\item{min_posterior}{Numeric in [0, 1). Minimum individual posterior
probability to retain a hypothesis. Hypotheses below this threshold are
excluded before computing the LCA consensus. At 0.05, a hypothesis must
hold at least 5\% posterior probability to influence the consensus taxon.
Default 0.05. Set to 0 to disable.}

\item{posterior_col}{Character. Name of the posterior column to rank
hypotheses by. Default \code{"posterior_mean"}.}

\item{lookup_missing_taxonomy}{Logical. If \code{TRUE}, calls
\code{TaxaTools::verify_taxon_names()} to fill in taxonomy columns for
\code{"unreferenced_species"} rows that have \code{NA} in those columns. Requires
TaxaTools to be installed and may make network requests. Default \code{FALSE}.}

\item{backbone_id}{Integer. Taxonomic backbone to use when
\code{lookup_missing_taxonomy = TRUE}. Passed to
\code{TaxaTools::verify_taxon_names()}. Required (errors) when
\code{lookup_missing_taxonomy = TRUE} -- no default, since the correct
backbone depends on which backbone your input taxonomy was verified
against, which varies by project. Common values: 1 = Catalogue of Life,
4 = NCBI, 9 = WoRMS, 11 = GBIF. See ecosystem CLAUDE.md for full list.
Ignored (may be left \code{NULL}) when \code{lookup_missing_taxonomy = FALSE}.}

\item{species_reference}{Optional. A plausible-species reference used to
downrank unresolved coarse-rank consensus assignments. Accepts two forms:
\itemize{
\item \strong{\code{unreferenced_species_result}} (from
\code{\link[=suggest_unreferenced_species]{suggest_unreferenced_species()}}): the full LLM-generated plausible
species list is extracted automatically from
\code{attr(x, "plausible")}. This includes referenced species (e.g.
\emph{Leptocottus armatus}) that the LLM flagged as plausible but that
were filtered out of the unreferenced vector by the NCBI step.
Use in the LLM workflow by passing the same object supplied to
\code{assign_taxa_llm(unreferenced_taxa = ...)}.
\item \strong{data.frame}: must contain a \code{taxon_name} column
(species-level names) and columns named after each coarser rank in
\code{rank_system} (e.g. \code{genus}, \code{family}). Pass
\code{taxaexpect_species_df} in the Bayesian workflow.
}
For each unresolved row where \code{consensus_rank} is not the finest rank,
the function looks up how many taxa at the next finer rank belong to the
consensus taxon. If exactly one, it downranks (recursively — e.g. family
to unique genus to unique species in one pass). Stops at any rank with
more than one option. Default \code{NULL} (no downranking).}

\item{group_priors}{Optional data frame from \code{\link[=compute_group_priors]{compute_group_priors()}}
(\code{rank}/\code{taxon}/\code{theta_sum}/\code{n_members} columns), the SUM of
\code{theta_mean} over every locally modelled member of a genus or family --
not just the candidates one observation's own evidence happened to
surface. Drives \code{consensus_prior} when a matching (\code{rank}, \code{taxon}) row
exists for an observation's \code{consensus_rank}/\code{consensus_taxon} (see that
column's own docs below for the full reasoning). Default \code{NULL}:
\code{consensus_prior} is \code{NA} for every row -- there is no fallback
computation (removed 2026-07-30; the previous candidate-scoped MAX was a
real underestimate, not a safe approximation).}
}
\value{
A dataframe with one row per \code{observation_id}:
\describe{
\item{\code{observation_id}}{Sample identifier (same type as input).}
\item{\code{consensus_taxon}}{Name of the LCA taxon, or \code{NA} if unresolvable
(all hypotheses excluded or no rank agrees).}
\item{\code{consensus_rank}}{Rank of the LCA (e.g. \code{"genus"}, \code{"family"}),
or \code{NA}.}
\item{\code{consensus_reason}}{How the consensus was reached:
\code{"unanimous"} (all plausible hypotheses agree at the finest rank),
\code{"single"} (only one hypothesis in the plausible set),
\code{"lca"} (upranked because hypotheses disagree at finer ranks),
\code{"threshold"} (rank-capped by \code{rank_thresholds} in
\code{\link[=score_consensus]{score_consensus()}}), or \code{NA} (unresolvable).}
\item{\code{is_resolved}}{\code{TRUE} when the LCA is at the finest rank in
\code{rank_system} (i.e. a single unambiguous species-level assignment).}
\item{\code{consensus_posterior}}{Sum of named posterior probabilities within
the consensus taxon at its assigned rank.  Because posteriors are
probabilities (full space = 1.0), this raw sum equals the probability
of the consensus taxon without requiring a denominator.  Computed over
\emph{all} named hypotheses before \code{min_posterior} and
\code{cumulative_threshold} filtering, so it is independent of threshold
settings and suitable for post-hoc confidence filtering (e.g. keep
only assignments with \code{consensus_posterior >= 0.95}).
\code{NA} when \code{consensus_taxon} is \code{NA}.}
\item{\code{consensus_confidence_score}}{Sum of \code{confidence_score} values for
all named hypotheses within the consensus taxon (computed over all named
hypotheses, not just the plausible set).  \code{confidence_score} is the
fraction of Monte Carlo simulations in which a hypothesis produced the
highest posterior; summing over in-LCA hypotheses gives the fraction of
simulations in which \emph{any} member of the consensus taxon won.
Complementary to \code{consensus_posterior}: while \code{consensus_posterior}
reflects mean posterior mass, \code{consensus_confidence_score} reflects how
consistently that taxon dominated across simulations.  \code{NA} when
\code{confidence_score} is absent from \code{posterior_df} (e.g. input from
\code{\link[=assign_taxa_llm]{assign_taxa_llm()}}) or when \code{consensus_taxon} is \code{NA}.}
\item{\code{n_plausible}}{Number of hypotheses in the plausible set (0 if
all hypotheses were excluded).}
\item{\code{plausible_taxa}}{List column: character vector of plausible taxon
names, sorted by descending posterior.}
\item{\code{plausible_posteriors}}{List column: named numeric vector of
posterior values for plausible taxa (names = taxon_name).}
\item{\code{downranked}}{Logical. \code{TRUE} when the initial LCA rank was
coarser than the final \code{consensus_rank} due to downranking via
\code{species_reference}. Only present when \code{species_reference} is
non-\code{NULL}. \code{FALSE} for all rows that were not downranked.}
\item{\code{winner_prior}}{Prior probability (\code{prior_mean}) of the
consensus taxon, taken from the highest-posterior hypothesis in the
plausible set. Use with \code{winner_likelihood} / \code{winner_likelihood_cov}
to detect implausible winners (e.g. a low-prior taxon winning due to
a reference error). \code{NA} when \code{prior_mean} is absent from
\code{posterior_df} (e.g. input from \code{\link[=assign_taxa_llm]{assign_taxa_llm()}}) or when
\code{consensus_taxon} is \code{NA}. NOTE: when \code{prior_mean} was sourced from
TaxaExpect, this value can be substantially inflated by
\code{update_prior_from_consensus()}'s cross-observation confirmation
boost -- it is NOT interchangeable with \code{winner_theta_mean} below for
occurrence-plausibility purposes; see that function's own "Rescaling
onto the occurrence scale" section.}
\item{\code{winner_theta_mean}}{The consensus taxon's raw occurrence-model
share (\code{theta_mean}, from \code{TaxaExpect::prepare_model_dataframe()} --
the fraction of local records attributable to this taxon; a
compositional share, not a presence probability). Unlike
\code{winner_prior}, this is immune to \code{update_prior_from_consensus()}'s
confirmation boost by construction, since that boost only ever
modifies \code{prior_mean}. \code{NA} when \code{theta_mean} is absent from
\code{posterior_df} or when \code{consensus_taxon} is \code{NA}.}
\item{\code{winner_likelihood}}{Point-estimate sequence-match likelihood
(\code{score_likelihood}) of the consensus taxon. \code{NA} when absent from
\code{posterior_df} or when \code{consensus_taxon} is \code{NA}.}
\item{\code{winner_likelihood_cov}}{Coverage-adjusted sequence-match
likelihood (\code{score_likelihood_cov}) of the consensus taxon. \code{NA}
when absent from \code{posterior_df} or when \code{consensus_taxon} is \code{NA}.}
\item{\code{winner_hypothesis_type}}{Character. The \code{hypothesis_type} of
the winning (highest-posterior) hypothesis row, e.g.
\code{"specific_candidate"}, \code{"unreferenced_species"}, or
\code{"rank_expanded"} (see \code{winner_rank_expanded} below). \code{NA} when the
winning row's \code{hypothesis_type} value is itself \code{NA}, or when
\code{consensus_taxon} is \code{NA}.}
\item{\code{winner_rank_expanded}}{Logical. \code{TRUE} when the winning
hypothesis was manufactured by \code{join_priors()}'s coarse-rank
expansion (\code{.expand_coarse_rank_rows()}) rather than resolved from a
real species-level match. This means the reported species-level
\code{consensus_taxon} was decided entirely by occurrence-prior mass
among candidates that all inherited one identical, uninformative
likelihood from the original coarse-rank (e.g. family-level)
identification -- no sequence/image/acoustic evidence discriminated
between them. This is intentional, sound behavior (using local
occurrence priors to resolve an otherwise-coarse ID) -- this column
exists so a downstream consumer can distinguish it from a
genuinely evidence-resolved species call, not to flag it as an
error. \code{NA} when \code{winner_hypothesis_type} is \code{NA}; \code{FALSE}
otherwise.}
\item{\code{winner_species_confusion_risk}, \code{winner_genus_confusion_risk},
\code{winner_family_confusion_risk}, \code{winner_own_rank_confusion_risk}}{Numeric. A
SECOND, model-independent diagnostic alongside
\code{TaxaLikely::evaluate_likelihoods()}'s \code{species_confusion_risk}/
\code{genus_confusion_risk}/\code{family_confusion_risk}/\code{own_rank_confusion_risk}
columns on the winning row. Each is a one-sided tail probability: how
often would a REAL congener/confamilial/cross-family pair score this
high or higher, given the winning candidate's own raw match score --
HIGHER values mean MORE confusable, i.e. WEAKER evidence for that
rank (see \code{evaluate_likelihoods()}'s own \verb{@section Confusion risk}
for the full interpretation). All three of
\code{winner_species_confusion_risk}/\code{winner_genus_confusion_risk}/
\code{winner_family_confusion_risk} are populated
(where computable) regardless of \code{winner_hypothesis_type};
\code{winner_own_rank_confusion_risk} is a convenience pointer at whichever
matches the winner's own resolved rank. \code{NA} when the source
column is absent from \code{posterior_df} (e.g. any upstream call
predating these columns) or when \code{consensus_taxon} is \code{NA}. Purely
informational -- never changes \code{consensus_taxon}/\code{consensus_rank}.}
\item{\code{consensus_confusion_risk}}{Numeric. The same confusion-risk
quantity, but rank-matched to \code{consensus_rank} rather than to
\code{primary_taxon}'s own rank (which is what
\code{winner_own_rank_confusion_risk} reports). When the LCA has climbed
to genus or family, the species-level value answers the wrong
question -- this one asks "could a confamilial genus (or a different
family) have scored this well", matching the rank actually being
reported. \code{NA} when \code{consensus_rank} is \code{NA}, is coarser than family,
or the corresponding source column is absent.}
\item{\code{primary_n_plausible_competitors},
\code{consensus_n_plausible_competitors}}{Integer. How many
locally-plausible RIVAL candidates this observation actually competed
against -- the question a confusion-risk value structurally cannot
answer, since it describes the marker's discriminating power for a
taxon in the abstract and never sees this observation's candidate
set. Read the two together: a low confusion risk means a relative
\emph{could not} have looked this good, while a non-zero competitor count
means a plausible relative \emph{was actually given the chance} to. A
count of \code{0} with an otherwise clean-looking call is the signature of
a reference-database representation gap -- the locally plausible
relatives were never in the candidate pool at all, so no threshold
applied to the existing candidates could have caught it.
"Plausible" means the candidate carries a real occurrence record,
read off \code{model_tier} (supplied by \code{\link[=join_priors]{join_priors()}} from TaxaExpect
priors) rather than off \code{prior_mean}'s value: a taxon never reported
locally has \code{model_tier = NA}, while a genuine singleton has a real
tier, even though both can share the same numeric floor prior.
Counted over every named hypothesis for the observation, before
\code{min_posterior}/\code{cumulative_threshold} filtering (a candidate that
competed and lost still competed), and EXCLUDING the row's own taxon
-- whether the winner itself is plausible is a prior-side question
already answered by \code{winner_prior}. A count of \code{0} therefore means
"nothing plausible to lose to", never "the winner is implausible".
The \code{primary_} version counts rival taxa; the \code{consensus_} version
counts distinct plausible groups at \code{consensus_rank} (rival genera
when the LCA landed at genus, rival families at family), reducing to
the \code{primary_} count at species rank. Both are \code{NA} when
\code{posterior_df} carries no \code{model_tier} column.}
\item{\code{winner_has_occurrence_record}, \code{consensus_prior},
\code{consensus_has_occurrence_record}}{Support for the
prior/occurrence-plausibility axis, answering what \code{winner_prior}'s
VALUE cannot: has this taxon ever been reported here at all? A
never-reported taxon and a genuine singleton can carry the SAME
numeric prior (both land on the dark-diversity floor) while meaning
opposite things, so presence is read off \code{model_tier} rather than
inferred from a low prior.
\code{winner_has_occurrence_record} is \code{TRUE} when the winning
hypothesis carries a real occurrence record, \code{NA} when
\code{posterior_df} has no \code{model_tier} column.
\code{consensus_prior} is a \code{theta_mean}-based group share
(2026-07-30; previously \code{prior_mean} -- changed because
\code{prior_mean} can be inflated by the confirmation boost above,
which would let one confirmed-elsewhere candidate make its whole
group look occurrence-expected regardless of real occurrence
support). Requires \code{group_priors}: when supplied and it has a
matching (\code{rank}, \code{taxon}) row for \code{consensus_rank}/
\code{consensus_taxon}, this is the exact SUM of \code{theta_mean} across
EVERY locally modelled member of the consensus taxon (see
\code{\link[=compute_group_priors]{compute_group_priors()}}) -- records are mutually exclusive across
taxa, so this is the group's true share by finite additivity, no
independence assumption needed. \code{NA} when \code{group_priors} is not
supplied, or has no matching row. There is deliberately no fallback
to a candidate-scoped MAX (removed 2026-07-30) -- that value is a
real underestimate on real data (e.g. Gobiidae: max 0.0164 vs. the
true group sum 0.0313), not a safe approximation to degrade to
silently.
\code{consensus_has_occurrence_record} (2026-07-30, new) is the
consensus-scope presence signal \code{consensus_prior}'s \code{NA} can no
longer safely double as (fixed a real bug: \code{consensus_prior} is
\code{NA} for two DIFFERENT reasons -- \code{group_priors} genuinely found no
local member, OR \code{group_priors} was never supplied at all -- and a
downstream consumer inferring "never reported" from \code{NA} alone
could not tell them apart, misreading "not checked" as "confirmed
absent"). \code{TRUE}/\code{FALSE} mean a real lookup against \code{group_priors}
was performed and found/didn't find a matching group; \code{NA} means
\code{group_priors} was not supplied (not checked at all).}
}
}
\description{
For each \code{observation_id}, identifies the minimal set of top-ranked hypotheses
that together account for \code{cumulative_threshold} of the named-taxon posterior
mass (after excluding hypotheses below \code{min_posterior}), then returns their
lowest common ancestor (LCA) as the consensus taxonomic assignment.
}
\details{
\strong{Which hypotheses are included:} All named hypotheses contribute to the
LCA — \code{"specific_candidate"}, \code{"unreferenced_species"} (congener without reference
sequence), \code{"unreferenced_genus"} (family-level unreferenced taxon: named species
from a genus absent in the reference), and \code{"unresolved_species"} (a species from a
census-complete genus whose identity is ambiguous among the known reference members;
produced by \code{\link[TaxaLikely:apply_coverage_constraints]{TaxaLikely::apply_coverage_constraints()}} with
\code{constraint_behavior = "relabel"}). Only the \code{"unknown_species"} catch-all row is
excluded.

\strong{LCA resolution:} The LCA is determined by walking taxonomy ranks from
finest to coarsest and finding the finest rank at which all plausible
hypotheses agree. Explicit taxonomy columns (e.g. \code{family}, \code{genus},
\code{species}) are used when present in \code{posterior_df}; genus can always be
derived from a species binomial even when no \code{genus} column is present.
Family and above require an explicit column.

\strong{Taxonomy columns in posterior_df:} \code{assign_taxa_llm()} automatically
carries taxonomy columns from \code{match_df} into its output. When using the
full pipeline (\code{compute_posterior()}), taxonomy columns pass through from
\code{evaluate_likelihoods()}. Unreferenced species rows have \code{NA} in taxonomy columns;
set \code{lookup_missing_taxonomy = TRUE} to attempt lookup via
\code{TaxaTools::verify_taxon_names()}.

\strong{Threshold interaction:}
\code{min_posterior} and \code{cumulative_threshold} work together:
\code{min_posterior} removes obvious noise hypotheses first (those with
negligible posterior mass), then \code{cumulative_threshold} selects the
plausible set from the remainder. Setting \code{min_posterior = 0} disables
noise filtering; setting it too high (e.g. 0.3) may exclude genuine
competing hypotheses. \code{cumulative_threshold = 0.9} is analogous to a
90\\% credible interval; increase toward 0.95--0.99 for more conservative
assignments (more upranking to genus/family); decrease to 0.8 for more
aggressive species-level calls.

\strong{Empirical sensitivity (2026-07-09, real data):} a grid sweep against
a real 3,000-observation subsample of PtConception 12S posteriors (see
\code{diagnostics/posterior_threshold_sweep.R}) found the two defaults are
NOT equally load-bearing. Holding \code{cumulative_threshold} at the
default 0.90, sweeping \code{min_posterior} from 0 to 0.20 moved the
finest-rank resolution rate by +8.2 points (72.3\\% -> 80.5\\%) -- a real,
fairly linear effect. Holding \code{min_posterior} at the default 0.05,
sweeping \code{cumulative_threshold} from 0.70 to 0.99 moved resolution by
only -3.3 points (79.6\\% -> 76.3\\%) -- \code{cumulative_threshold} does
comparatively little independent work once a reasonable \code{min_posterior}
floor is already in place. The two parameters interact sharply only in the
unrealistic corner of \code{min_posterior = 0} combined with
\code{cumulative_threshold = 0.99} (resolution drops to 57.9\\%), confirming
the mechanism described above is real but not a practical risk at the
current defaults. \strong{Caveat:} this sweep measures resolution
\emph{rate} (how often the pipeline commits to a finest-rank call), not
\emph{accuracy} (whether that call is correct) -- no ground-truth-validated
observation set was available to check against, so a higher
\code{min_posterior} driving up the resolved-\\% is not itself evidence that
those additional resolved calls are correct.

\strong{LCA method:}
Lowest Common Ancestor is the standard conservative consensus method in
molecular systematics (Huson et al., 2007, MEGAN). The implementation
walks from finest to coarsest rank and stops at the first rank where all
plausible hypotheses agree.
}
\examples{
\dontrun{
consensus <- posterior_consensus(
  result_updated,
  cumulative_threshold = 0.9,
  min_posterior = 0.05
)
head(consensus)
}

}
\seealso{
\code{\link[=assign_taxa_llm]{assign_taxa_llm()}}, \code{\link[=compute_posterior]{compute_posterior()}},
\code{\link[=suggest_unreferenced_species]{suggest_unreferenced_species()}}
}
