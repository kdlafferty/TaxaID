% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_llm_pipeline.R
\name{run_llm_pipeline}
\alias{run_llm_pipeline}
\title{Run the Full LLM-Shortcut Assignment Pipeline}
\usage{
run_llm_pipeline(
  match_df,
  context = NULL,
  auto_context = TRUE,
  geographic_hint = NULL,
  date = NULL,
  habitat_scheme = NULL,
  llm_fn = NULL,
  detect_unreferenced = TRUE,
  barcode_term = "12S",
  expand_to_family = TRUE,
  max_date = NULL,
  unreferenced_taxa = NULL,
  score_threshold = 80,
  top_n = 10L,
  score_sharpness = 0.1,
  unknown_lik_weight = 0.05,
  known_present = NULL,
  known_absent = NULL,
  absent_detection_prob = 0.8,
  taxa_per_call = 30L,
  pause_seconds = 1,
  prior_phi = c(high = 50, moderate = 10, low = 3),
  n_sims = 1000L,
  context_group = NULL,
  rank_system = c("family", "genus", "species"),
  cumulative_threshold = 0.9,
  min_posterior = 0.05,
  posterior_col = "posterior_point_est",
  backbone_id = 4L,
  lookup_missing_taxonomy = TRUE,
  presence_multiplier = 5,
  generate_report = FALSE,
  report_params = list(),
  reference_errors = NULL,
  verbose = TRUE
)
}
\arguments{
\item{match_df}{Data frame. Standardized match object from
\code{\link[TaxaMatch]{standardize_match_data}}, with columns
\code{observation_id}, \code{score}, \code{taxon_name}, \code{taxon_name_rank},
and taxonomy columns.}

\item{context}{Optional data frame or list with location/habitat context.
When \code{NULL} (default) and \code{auto_context = TRUE}, context is
auto-populated via \code{\link{build_context}}. When supplied, passed
directly to \code{\link{assign_taxa_llm}}.}

\item{auto_context}{Logical. When \code{TRUE} (default) and \code{context}
is \code{NULL}, automatically build context via \code{\link{build_context}}.
Requires the TaxaHabitat package.}

\item{geographic_hint}{Optional character string for
\code{\link{build_context}} (e.g. \code{"Southern California"}).
Ignored when \code{context} is supplied.}

\item{date}{Optional character string for \code{\link{build_context}}
(e.g. \code{"2025"}). Ignored when \code{context} is supplied.}

\item{habitat_scheme}{Passed to \code{\link{build_context}} for habitat
assignment. Default \code{NULL} (3-category).}

\item{llm_fn}{Function or NULL. LLM provider following the TaxaTools
\code{llm_fn} pattern. Default NULL resolves to
\code{TaxaTools::call_anthropic_api} (requires TaxaTools).}

\item{detect_unreferenced}{Logical. When \code{TRUE} (default), run
\code{\link{suggest_unreferenced_species}} to detect taxa absent from the
reference database. Set to \code{FALSE} to skip.}

\item{barcode_term}{Character. Barcode marker for unreferenced species
detection. Default \code{"12S"}.}

\item{expand_to_family}{Logical. Expand unreferenced search to family level.
Default \code{TRUE}. Passed to \code{\link{suggest_unreferenced_species}}.}

\item{max_date}{Optional character. NCBI date filter for unreferenced species
detection (e.g. \code{"2024/12/31"}).}

\item{unreferenced_taxa}{Optional character vector of known unreferenced
species. When supplied, \code{detect_unreferenced} is ignored and these
are passed directly to \code{\link{assign_taxa_llm}}.}

\item{score_threshold}{Numeric. Minimum score to include a candidate (0-100).
Default 80.}

\item{top_n}{Integer. Maximum candidates per observation sent to LLM. Default 10.}

\item{score_sharpness}{Numeric. Exponential weight sharpness for likelihood
proxy. Default 0.1.}

\item{unknown_lik_weight}{Numeric. Baseline likelihood for the unknown
species hypothesis. Default 0.05.}

\item{known_present}{Optional character vector of confirmed present species.}

\item{known_absent}{Optional character vector or data frame of confirmed
absent species. See \code{\link{assign_taxa_llm}} for details.}

\item{absent_detection_prob}{Numeric. Detection probability for known-absent
species. Default 0.80.}

\item{taxa_per_call}{Integer. Maximum taxa per LLM call. Default 30.}

\item{pause_seconds}{Numeric. Pause between LLM calls. Default 1.}

\item{prior_phi}{Named numeric vector mapping \code{information_quality}
to Beta concentration. Default \code{c(high = 50, moderate = 10, low = 3)}.}

\item{n_sims}{Integer. Monte Carlo simulations. Default \code{1000L}.}

\item{context_group}{Optional character vector of column names in
\code{context} for grouping observations. Default \code{NULL}.}

\item{rank_system}{Character vector of taxonomy ranks, coarse to fine.
Default \code{c("family", "genus", "species")}.}

\item{cumulative_threshold}{Numeric. Posterior probability threshold for
consensus. Default 0.90.}

\item{min_posterior}{Numeric. Minimum posterior to be considered plausible.
Default 0.05.}

\item{posterior_col}{Character. Column name for posterior values. Default
\code{"posterior_point_est"}.}

\item{backbone_id}{Integer. Backbone for taxonomy lookup. Default \code{4L}
(NCBI).}

\item{lookup_missing_taxonomy}{Logical. Look up missing taxonomy in
consensus. Default \code{TRUE}.}

\item{presence_multiplier}{Numeric. Multiplier for empirical Bayes
refinement. Default 5.}

\item{generate_report}{Logical. Generate a Methods + Results report.
Default \code{FALSE}.}

\item{report_params}{Named list of additional arguments passed to
\code{\link{generate_report}} (e.g. \code{data_type}, \code{marker},
\code{study_description}).}

\item{reference_errors}{Optional data frame. Output of
\code{\link[TaxaLikely]{flag_reference_errors}} (or
\code{model_params$reference_errors} from a trained model). When
supplied, mislabeled accessions are removed from \code{match_df}
before assignment. Default \code{NULL} (no filtering).}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A named list with components:
\describe{
\item{\code{$consensus}}{Final consensus data frame (one row per
\code{observation_id}), after empirical Bayes refinement.}
\item{\code{$result}}{Full posterior data frame (after refinement),
with all hypotheses per observation.}
\item{\code{$context}}{The context data frame used (auto-built or
user-supplied).}
\item{\code{$unreferenced}}{The unreferenced species result, or
\code{NULL} if skipped.}
\item{\code{$report}}{Report text from \code{generate_report()}, or
\code{NULL} if \code{generate_report = FALSE}.}
}
}
\description{
High-level wrapper that chains the LLM-shortcut workflow into a single call:
optionally build context, optionally detect unreferenced species, run
\code{assign_taxa_llm()}, derive consensus, refine via empirical Bayes, and
optionally generate a report.
}
\details{
This encapsulates ~7 function calls (Sections 2-5 of
\code{inst/TaxaAssign_llm_workflow.R}) into a single step. For fine-grained
control, use the individual functions directly.
}
\examples{
\dontrun{
out <- run_llm_pipeline(
  match_df        = match_obj,
  geographic_hint = "Southern California",
  barcode_term    = "12S"
)
head(out$consensus)
}

}
\seealso{
\code{\link{run_bayesian_pipeline}} for the model-based alternative,
\code{\link{assign_taxa_llm}}, \code{\link{build_context}}
}
