% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_multisite_priors.R
\name{combine_multisite_priors}
\alias{combine_multisite_priors}
\title{Combine Per-Site Priors for Multi-Site Observations}
\usage{
combine_multisite_priors(joined)
}
\arguments{
\item{joined}{Data frame, typically \code{\link[=join_priors]{join_priors()}} output. Must contain
\code{observation_id}, \code{taxon_name}, \code{taxon_name_rank}, \code{grid_id},
\code{main_habitat}, \code{prior_alpha}, \code{prior_beta} (all present in
\code{join_priors()}'s output). Observations detected at only one site (the
common case) pass through unchanged.}
}
\value{
\code{joined} with one row per \code{observation_id} x \code{taxon_name} x
\code{taxon_name_rank}, plus two new columns:
\describe{
\item{\code{n_sites_combined}}{Integer. Number of per-site rows combined
into this row (\code{1} when the candidate was detected at only one
site).}
\item{\code{combined_sites}}{Character. Pipe-delimited sorted list of the
combined \code{grid_id} values, \code{NA} for single-site rows. \code{grid_id} and
\code{main_habitat} themselves are set to \code{NA} on combined rows, since the
row no longer corresponds to a single site.}
}
\code{prior_alpha}, \code{prior_beta}, and \code{prior_mean} are recomputed for combined
rows; all other columns (likelihood columns, taxonomy, \code{hypothesis_type},
etc.) are inherited from one of the per-site rows, since they describe
the same underlying detection and are identical across sites for a given
candidate.
}
\description{
Bridges \code{\link[=join_priors]{join_priors()}} (Session 138: now site-preserving, i.e. one row per
\code{observation_id} x \code{taxon_name} x \code{taxon_name_rank} x \code{grid_id} x
\code{main_habitat}) to \code{\link[=compute_posterior]{compute_posterior()}}, which expects exactly one row per
candidate hypothesis per observation. When the same \code{observation_id} was
detected at more than one site (e.g. the same eDNA ASV recovered from reads
at two different sample sites), each candidate taxon otherwise arrives with
one prior row per site. This function combines those rows into one.
}
\details{
\subsection{Why this function exists}{

Before Session 138, \code{join_priors()}'s final deduplication step
(\code{distinct(observation_id, taxon_name, taxon_name_rank, .keep_all = TRUE)})
silently kept only the highest-\code{prior_mean} site per candidate -- each
candidate ended up matched to \emph{its own} most favorable site rather than the
site it was actually detected at, which could turn a confident, correct
call into a nonsensical tie. \code{join_priors()} now preserves one row per site
(its \code{distinct()} call additionally keys on \code{grid_id}/\code{main_habitat}); this
function is the deliberate combination step that must run on that
site-preserving output before \code{\link[=compute_posterior]{compute_posterior()}}.
}

\subsection{Combination rule: precision-weighted, not a plain product}{

A simpler design (evaluated and rejected during Session 138 design review)
would multiply each candidate's per-site \code{prior_mean} values together and
renormalize. That rule treats every site's point estimate as equally
trustworthy regardless of how much occurrence data backs it -- a site with
almost no data (small \code{prior_alpha + prior_beta}) counts exactly as much as
a site with abundant, high-confidence data. A worked comparison (site A:
\code{phi = 100}, confidently favors candidate X at 0.8; site B: \code{phi = 5},
weakly favors candidate Y at 0.6 -- i.e. sparse data giving a possibly
spurious signal in the \emph{opposite} direction) showed the plain-product rule
gives X only 72.7\% of the combined mass, and that running a full Monte
Carlo simulation (drawing \code{theta_site1 ~ Beta}, \code{theta_site2 ~ Beta},
multiplying, renormalizing per draw) does not fix this -- it shifts weight
\emph{toward} the noisier site's minority pick (69.2\%), a known artifact of
averaging a renormalized ratio of random variables, unrelated to
confidence. The precision-weighted logit combination implemented here
gives X 78.5\% -- it is the only one of the three that actually discounts
the low-confidence site (about 16x less weight than site A here, from the
ratio of their logit variances) rather than counting it at face value or
distorting the result in an uncontrolled direction.
}

\subsection{Ecological independence assumption}{

Combining priors multiplicatively/in log-odds across sites assumes the
sites are ecologically independent -- knowing the species is present at
site 1 tells you nothing extra about site 2 beyond the species' own
biology. If two "sites" are actually near-duplicate subsamples of the same
real location, this combination double-counts the same evidence and
overstates confidence. This is a judgment call for whoever builds the site
metadata (see \code{\link[TaxaMatch:join_event_site_metadata]{TaxaMatch::join_event_site_metadata()}}'s \code{site_metadata}
table), not something this function can detect or guard against.
}
}
\examples{
# A candidate ("Gadus morhua") detected at two sites, plus a single-site
# candidate ("Gadus chalcogrammus") for the same observation.
joined <- data.frame(
  observation_id  = c("ASV_1", "ASV_1", "ASV_1"),
  taxon_name      = c("Gadus morhua", "Gadus morhua", "Gadus chalcogrammus"),
  taxon_name_rank = "species",
  grid_id         = c("Grid_A", "Grid_B", "Grid_A"),
  main_habitat    = c("Neritic", "Neritic", "Neritic"),
  prior_alpha     = c(80, 3, 10),
  prior_beta      = c(20, 2, 90),
  stringsAsFactors = FALSE
)
combined <- combine_multisite_priors(joined)
combined

\dontrun{
joined    <- join_priors(likelihoods, taxaexpect_priors, site = site_df)
combined  <- combine_multisite_priors(joined)
posterior <- compute_posterior(combined)
}

}
\seealso{
\code{\link[=join_priors]{join_priors()}}, \code{\link[=compute_posterior]{compute_posterior()}}
}
