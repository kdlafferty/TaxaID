% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest_unreferenced_species.R
\name{suggest_unreferenced_species}
\alias{suggest_unreferenced_species}
\title{Suggest Unreferenced Species Using an LLM}
\usage{
suggest_unreferenced_species(
  match_df,
  context = NULL,
  barcode_term = "COI",
  llm_fn = NULL,
  expand_to_family = FALSE,
  max_date = NULL,
  min_len = NULL,
  max_len = NULL,
  taxa_per_call = 30L,
  pause_seconds = 1,
  ncbi_api_key = NULL,
  verbose = FALSE
)
}
\arguments{
\item{match_df}{Data frame.  Canonical match object from TaxaMatch (or
equivalent).  Required column: \code{taxon_name}.  Optional but strongly
recommended: \code{genus} (if absent, derived from \code{taxon_name}).  Required
for \code{expand_to_family = TRUE}: \code{family}.}

\item{context}{Optional named list or single-row data frame with location /
habitat context for the LLM.  Recognised fields: \code{ecoregion}, \code{lat},
\code{lon}, \code{date}, \code{habitat}.  NULL (default) sends no context.}

\item{barcode_term}{Character scalar or vector.  One or more marker search
terms (e.g. \code{"12S"}, \code{c("12S", "MiFish")}).  Multiple terms are OR-ed.
Default \code{"COI"}.}

\item{llm_fn}{Function or NULL.  Provider function following the TaxaTools
\code{llm_fn} pattern: accepts a single character string prompt and returns a
single character string response.  Default NULL resolves to
\code{TaxaTools::call_anthropic_api} (requires TaxaTools).}

\item{expand_to_family}{Logical.  If \code{TRUE}, genera for which the LLM
returned zero plausible species trigger a second LLM call asking for
plausible species in OTHER genera of the same family.  These
\strong{family-level unreferenced species} fill the role of TaxaLikely's H3 (unreferenced genus)
hypothesis with named species.  Requires a \code{family} column in \code{match_df}.
Default \code{FALSE}.}

\item{max_date}{Optional character scalar.  Restricts unreferenced species detection to
sequences present in NCBI on or before this date.  Format: \code{"YYYY"},
\code{"YYYY/MM"}, or \code{"YYYY/MM/DD"}.  NULL uses the current state of GenBank.}

\item{min_len}{Integer or NULL.  Minimum sequence length (\code{SLEN} filter).
NULL uses a barcode-specific default.}

\item{max_len}{Integer or NULL.  Maximum sequence length.  NULL uses the
barcode-specific default.}

\item{taxa_per_call}{Integer >= 1.  Maximum genera per LLM call.  Default 30.}

\item{pause_seconds}{Numeric.  Seconds to pause between LLM calls.  Default 1.}

\item{ncbi_api_key}{Optional NCBI API key.  Raises rate limit from 3 to 10
requests per second.  Can also be set via the \code{ENTREZ_KEY} environment
variable.}

\item{verbose}{Logical.  If \code{TRUE}, prints each prompt and raw LLM response.
Default \code{FALSE}.}
}
\value{
A character vector of unreferenced species names with class
\code{c("unreferenced_species_result", "character")}.  \code{length()} returns the total number of
unreferenced species (congener + family-level combined).  Pass directly to
\code{assign_taxa_llm(unreferenced_taxa = ...)}.
}
\description{
A fast, LLM-first alternative to \code{\link[TaxaLikely:audit_barcode_coverage]{TaxaLikely::audit_barcode_coverage()}} for
unreferenced species detection.  An \strong{unreferenced species} is a described taxon
that shares a genus (or family, with \code{expand_to_family = TRUE}) with a scored
reference match candidate but has \strong{no barcode sequence} for the target marker in NCBI.
}
\details{
\subsection{Algorithm}{

\enumerate{
\item \strong{Genus-level LLM call:} For each genus in \code{match_df}, the LLM lists
all biogeographically plausible species.  Genera are batched by
\code{taxa_per_call}.
\item \strong{Skip-list removal:} Species already in \code{match_df} have reference
sequences by definition and are removed from the candidate list.
\item \strong{NCBI barcode count:} For each remaining plausible species,
\code{rentrez::entrez_search()} is called with \code{retmax = 0} (count only).
Count = 0 or persistent failure (NA after 3 retries) \eqn{\to}{->} unreferenced.
\item \strong{Family-level expansion (optional):} When \code{expand_to_family = TRUE},
genera for which the LLM found \strong{zero} plausible species trigger a
second LLM call asking for plausible species in \strong{other genera} of the
same family.  Those candidates go through the same NCBI count step.
Requires a \code{family} column in \code{match_df}.
}
}

\subsection{Return value}{

The visible return is a character vector of unreferenced species names of class
\code{c("unreferenced_species_result", "character")}, suitable for direct use as
\code{assign_taxa_llm(unreferenced_taxa = ...)}.  \code{assign_taxa_llm} automatically
detects the \code{unreferenced_family} attribute and activates family-level
unreferenced species insertion when it is present.

Additional details are attached as attributes:
\itemize{
\item \code{attr(result, "plausible")} -- all LLM-generated species (genus-level)
before the NCBI filter (character vector).
\item \code{attr(result, "census")} -- data frame with one row per genus:
\code{genus}, \code{plausible_count}, \code{ncbi_count}, \code{unreferenced_count}.
\item \code{attr(result, "unreferenced_family")} -- named character vector mapping each
family-level unreferenced species to its family name.  \code{NULL} when
\code{expand_to_family = FALSE} or when no family-level unreferenced species were found.
\item \code{attr(result, "family_census")} -- data frame with one row per
expanded family: \code{family}, \code{plausible_count}, \code{ncbi_count}, \code{unreferenced_count}.
\code{NULL} when \code{expand_to_family = FALSE}.
}
}
}
\examples{
\dontrun{
match_df <- readRDS(
  system.file("match_obj.rds", package = "TaxaMatch")
)

ctx <- data.frame(
  ecoregion = "Southern California Bight",
  habitat   = "estuarine / coastal lagoon"
)

# Genus-level unreferenced species only
unref_names <- suggest_unreferenced_species(
  match_df, context = ctx, barcode_term = "12S",
  llm_fn = TaxaTools::call_anthropic_api, max_date = "2024/12/31"
)
cat("Unreferenced taxa found:", length(unref_names), "\n")

# With family-level expansion for genera with no local species
unref_names <- suggest_unreferenced_species(
  match_df, context = ctx, barcode_term = "12S",
  expand_to_family = TRUE, max_date = "2024/12/31"
)
attr(unref_names, "family_census")

# Pass directly to assign_taxa_llm
result <- assign_taxa_llm(match_df, context = ctx, unreferenced_taxa = unref_names)
}
}
\seealso{
\code{\link[=assign_taxa_llm]{assign_taxa_llm()}} to use the returned unreferenced species names in the
Bayesian assignment pipeline.
\code{\link[TaxaLikely:audit_barcode_coverage]{TaxaLikely::audit_barcode_coverage()}} for exhaustive (slower)
unreferenced species detection that does not require an LLM.
}
