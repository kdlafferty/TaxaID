% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_consensus.R
\name{score_consensus}
\alias{score_consensus}
\title{Derive Consensus Taxonomy from Raw Match Scores}
\usage{
score_consensus(
  match_df,
  min_score = 0,
  max_gap = Inf,
  rank_thresholds = NULL,
  whitelist = NULL,
  score_col = "score",
  rank_system = NULL
)
}
\arguments{
\item{match_df}{Data frame.
One row per \code{observation_id} x reference hit.  Required columns: \code{observation_id},
\code{taxon_name}, \code{taxon_name_rank}, and the column named by \code{score_col}.
Taxonomy columns (e.g. \code{family}, \code{genus}, \code{species}) are used for LCA
resolution when present; genus is always derivable from species binomials.}

\item{min_score}{Numeric.  Minimum score to retain a hit.  Hits below this
value are discarded before any other filtering.  Scale must match the
\code{score_col} values (e.g. 97 for percent identity, 0.97 for proportion).
Default \code{0} (no filtering).}

\item{max_gap}{Numeric.  Maximum score difference from the top hit (per
sample).  All hits within \code{max_gap} of the best score contribute to the
LCA.  For example, \code{max_gap = 1} keeps all hits within 1 unit of the top
score.  Default \code{Inf} (all hits above \code{min_score} contribute).}

\item{rank_thresholds}{Named numeric vector or \code{NULL}.  Maps rank names to
minimum scores, e.g. \code{c(species = 97, genus = 95, family = 90)}.  After
the LCA is computed, the consensus is capped at the finest rank whose
threshold the top score meets.  If the top score fails all thresholds, the
sample is unresolvable.  Applied independently of the LCA — so even if all
hits agree on species, the consensus is demoted to genus if the top score
is below the species threshold.  Default \code{NULL} (no rank capping).
The default covers 4 ranks (species, genus, family, order); users can
extend for additional ranks by passing a longer named vector.}

\item{whitelist}{Character vector or \code{NULL}.  Plausible taxon names (any
rank).  When supplied, the consensus taxon must appear in this list;
otherwise the consensus is upranked to the coarsest rank where a
whitelist member agrees with the retained candidates.  Default \code{NULL}
(no whitelist filtering).}

\item{score_col}{Character.  Column name containing match scores.
Default \code{"score"}.}

\item{rank_system}{Character vector of taxonomy column names, coarse to
fine (e.g. \code{c("family", "genus", "species")}).  If \code{NULL} (default),
standard columns present in \code{match_df} are detected automatically.}
}
\value{
A data frame with one row per \code{observation_id}:
\describe{
\item{\code{observation_id}}{Sample identifier.}
\item{\code{consensus_taxon}}{LCA taxon name, or \code{NA} if unresolvable.}
\item{\code{consensus_rank}}{Rank of the LCA (e.g. \code{"genus"}), or \code{NA}.}
\item{\code{consensus_reason}}{How the consensus was reached:
\code{"unanimous"} (all retained taxa agree at the finest rank),
\code{"single"} (only one taxon retained after filtering),
\code{"lca"} (upranked because retained taxa disagree at finer ranks),
\code{"threshold"} (rank-capped by \code{rank_thresholds}), or \code{NA}
(unresolvable).}
\item{\code{is_resolved}}{\code{TRUE} when the consensus is at the finest rank in
\code{rank_system}.}
\item{\code{top_score}}{Highest score among retained hits for this sample.}
\item{\code{n_retained}}{Number of hits retained after score + gap filtering
(before rank threshold and whitelist steps).}
\item{\code{n_taxa}}{Number of distinct \code{taxon_name} values among retained
hits.}
\item{\code{retained_taxa}}{List column: character vector of distinct taxon
names among retained hits, sorted by descending score.}
\item{\code{rank_capped}}{Logical.  \code{TRUE} when the consensus rank was
demoted by \code{rank_thresholds}.  Only present when \code{rank_thresholds}
is non-\code{NULL}.}
\item{\code{whitelist_capped}}{Logical.  \code{TRUE} when the consensus was
upranked because the original taxon was absent from \code{whitelist}.
Only present when \code{whitelist} is non-\code{NULL}.}
}
}
\description{
For each \code{observation_id}, applies conventional score-based filtering to derive a
consensus taxonomic assignment.  The algorithm:
}
\details{
\enumerate{
\item \strong{Score filter:} discard hits below \code{min_score}.
\item \strong{Gap filter:} among remaining hits, keep only those within \code{max_gap} of
the top score (per sample).
\item \strong{LCA:} compute the least common ancestor (LCA) of the retained hits.
\item \strong{Rank threshold cap:} if \code{rank_thresholds} is supplied, cap the
consensus at the finest rank whose minimum score the top hit meets.
\item \strong{Whitelist upranking:} if \code{whitelist} is supplied, verify the consensus
taxon appears in the whitelist.  If not, uprank to the coarsest rank
where at least one whitelist member agrees with the retained candidates.
}

This function does not require a trained likelihood model or priors.  It is
the conventional approach used in most metabarcoding and BLAST-based
pipelines.
}
\examples{
\dontrun{
sc <- score_consensus(
  match_df,
  min_score = 97,
  rank_thresholds = c(species = 98, genus = 95, family = 90)
)
head(sc)
}

}
\seealso{
\code{\link[=posterior_consensus]{posterior_consensus()}} for the Bayesian posterior-based approach.
}
