% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_consensus.R
\name{score_consensus}
\alias{score_consensus}
\title{Derive Consensus Taxonomy from Raw Match Scores}
\usage{
score_consensus(
  match_df,
  min_score = 0,
  max_gap = Inf,
  whitelist = NULL,
  score_col = "score_original",
  rank_system = NULL,
  rank_thresholds
)
}
\arguments{
\item{match_df}{Data frame.
One row per \code{observation_id} x reference hit.  Required columns: \code{observation_id},
\code{taxon_name}, \code{taxon_name_rank}, and the column named by \code{score_col}.
Taxonomy columns (e.g. \code{family}, \code{genus}, \code{species}) are used for LCA
resolution when present; genus is always derivable from species binomials.}

\item{min_score}{Numeric.  Minimum score to retain a hit.  Hits below this
value are discarded before any other filtering.  Scale must match the
\code{score_col} values (e.g. 97 for percent identity, 0.97 for proportion).
Default \code{0} (no filtering). Note: an ROC-style sweep against real 12S
reference data (\code{diagnostics/score_floor_roc_sweep.R}, 2026-07-09) found
that \code{min_score}/raw percent-identity alone cannot discriminate a true
species from its closest congener at almost any real-world threshold
(true-positive and congeneric-false-positive rates track each other
almost exactly up to ~97). \code{min_score}'s real value is excluding
obviously-wrong cross-family hits, not fine species-level discrimination
-- that discrimination is \code{rank_thresholds}' job (see below).}

\item{max_gap}{Numeric.  Maximum score difference from the top hit (per
sample).  All hits within \code{max_gap} of the best score contribute to the
LCA.  For example, \code{max_gap = 1} keeps all hits within 1 unit of the top
score.  Default \code{Inf} (all hits above \code{min_score} contribute).}

\item{whitelist}{Character vector or \code{NULL}.  Plausible taxon names (any
rank).  When supplied, the consensus taxon must appear in this list;
otherwise the consensus is upranked to the coarsest rank where a
whitelist member agrees with the retained candidates.  Default \code{NULL}
(no whitelist filtering).}

\item{score_col}{Character.  Column name containing match scores.
Default \code{"score_original"}.}

\item{rank_system}{Character vector of taxonomy column names, coarse to
fine (e.g. \code{c("family", "genus", "species")}).  If \code{NULL} (default),
standard columns present in \code{match_df} are detected automatically.}

\item{rank_thresholds}{Named numeric vector, or \code{NULL}.  Maps rank names to
minimum scores, e.g. \code{c(species = 97, genus = 95, family = 90)}.  After
the LCA is computed, the consensus is capped at the finest rank whose
threshold the top score meets.  If the top score fails all thresholds, the
sample is unresolvable.  Applied independently of the LCA — so even if all
hits agree on species, the consensus is demoted to genus if the top score
is below the species threshold.
\strong{No default -- errors if omitted (2026-07-23).} This function has no
way to know the marker or even whether \code{score_col} holds a DNA/image/
acoustic score, so no single fixed threshold set is safe to assume for
every caller (see Details for why the earlier GITA/Jonah Ventures
default, \code{c(species = 98, genus = 95, family = 90, phylum = 85)}, was
removed rather than kept as a default). Supply one of:
(1) your own thresholds, on whichever scale \code{score_col} uses (percent
identity or 0-1 proportion -- see the auto-rescale note below), or
(2) marker-specific thresholds derived from your own reference data via
\code{TaxaLikely::compute_rank_thresholds()} (per-rank Youden's J on a
\code{build_sequence_matrix()}-style pairwise distance matrix). Pass
\code{rank_thresholds = NULL} explicitly to disable rank capping entirely.
If \code{score_col}'s values look like a 0-1 proportion scale (max <= 1)
rather than 0-100 percent-identity, a supplied 0-100-scale
\code{rank_thresholds} is automatically rescaled by /100 (with an
informational message) -- pass your own already-scaled
\code{rank_thresholds} to silence this.}
}
\value{
A data frame with one row per \code{observation_id}:
\describe{
\item{\code{observation_id}}{Sample identifier.}
\item{\code{consensus_taxon}}{LCA taxon name, or \code{NA} if unresolvable.}
\item{\code{consensus_rank}}{Rank of the LCA (e.g. \code{"genus"}), or \code{NA}.}
\item{\code{consensus_reason}}{How the consensus was reached:
\code{"unanimous"} (all retained taxa agree at the finest rank),
\code{"single"} (only one taxon retained after filtering),
\code{"lca"} (upranked because retained taxa disagree at finer ranks),
\code{"threshold"} (rank-capped by \code{rank_thresholds}), or \code{NA}
(unresolvable).}
\item{\code{is_resolved}}{\code{TRUE} when the consensus is at the finest rank in
\code{rank_system}.}
\item{\code{top_score}}{Highest score among retained hits for this sample.}
\item{\code{n_retained}}{Number of hits retained after score + gap filtering
(before rank threshold and whitelist steps).}
\item{\code{n_taxa}}{Number of distinct \code{taxon_name} values among retained
hits.}
\item{\code{retained_taxa}}{List column: character vector of distinct taxon
names among retained hits, sorted by descending score.}
\item{\code{rank_capped}}{Logical.  \code{TRUE} when the consensus rank was
demoted by \code{rank_thresholds}.  Only present when \code{rank_thresholds}
is non-\code{NULL}.}
\item{\code{whitelist_capped}}{Logical.  \code{TRUE} when the consensus was
upranked because the original taxon was absent from \code{whitelist}.
Only present when \code{whitelist} is non-\code{NULL}.}
}
}
\description{
For each \code{observation_id}, applies conventional score-based filtering to derive a
consensus taxonomic assignment.  The algorithm:
}
\details{
\enumerate{
\item \strong{Score filter:} discard hits below \code{min_score}.
\item \strong{Gap filter:} among remaining hits, keep only those within \code{max_gap} of
the top score (per sample).
\item \strong{LCA:} compute the lowest common ancestor (LCA) of the retained hits.
\item \strong{Rank threshold cap:} if \code{rank_thresholds} is supplied, cap the
consensus at the finest rank whose minimum score the top hit meets.
\item \strong{Whitelist upranking:} if \code{whitelist} is supplied, verify the consensus
taxon appears in the whitelist.  If not, uprank to the coarsest rank
where at least one whitelist member agrees with the retained candidates.
}

This function does not require a trained likelihood model or priors.  It is
the conventional approach used in most metabarcoding and BLAST-based
pipelines.

\strong{Why \code{rank_thresholds} has no default (2026-07-23, supersedes the
2026-07-09 GITA/Jonah Ventures default):}
\code{min_score} and \code{max_gap} alone provide essentially no protection against
confusing a species with its closest congener -- an ROC-style sweep against
real 12S reference data found true-positive (within-species) and
false-positive (congeneric) rates track almost identically up to a ~97
percent-identity threshold (see \code{diagnostics/score_floor_roc_sweep.R}).
This is a real limitation of percent-identity thresholds generally --
\code{rank_thresholds} is what does the real species-level discrimination this
function offers, which is exactly why a caller getting NO thresholds at
all (the pre-2026-07-09 \code{NULL} default) was a real gap.

The 2026-07-09 fix picked ONE fixed threshold set (the conventional
GITA/Jonah Ventures percent-identity convention) as the default. That
default was itself later found unsafe to assume universally: this
function has no way to know what marker \code{score_col} was scored against,
or even whether the data is DNA, image, or acoustic evidence at all --
the real, calibrated threshold for "98\% identity means species-level
confidence" is a property of the SPECIFIC marker and reference database,
not a universal constant (mirrors \code{TaxaAssign::join_priors()}'s
\code{backbone_id} precedent: no safe default exists when the correct value
depends on data the function itself cannot see). Rather than continue
shipping a plausible-looking but potentially-wrong default, the function
now requires the caller to make this choice explicitly -- either supplying
real thresholds directly, or deriving marker-specific ones from real
reference data via \code{TaxaLikely::compute_rank_thresholds()} (which uses
the identical genus-/family-equal-weighted, Empirical-Bayes-shrunk
per-rank Youden's J logic \code{diagnostics/score_floor_roc_sweep.R}
prototyped). Pass \code{rank_thresholds = NULL} explicitly only if the specific
pipeline you're mirroring genuinely has no rank-threshold step (rare).
}
\section{Purpose -- why this function exists alongside the Bayesian pathway}{

\code{score_consensus()} is deliberately a \strong{reproduction of a conventional,
non-Bayesian pipeline's decision rule} (fixed percent-identity thresholds
per rank, e.g. the GITA / Jonah Ventures convention), not TaxaID's own
species-level discriminator. Its purpose is to let a user run the exact
same match data through both approaches and compare: what would a
conventional fixed-threshold workflow have called here, versus what does
TaxaID's Bayesian pathway (\code{TaxaLikely}'s bivariate-normal likelihoods ->
\code{\link[=compute_posterior]{compute_posterior()}} -> \code{\link[=posterior_consensus]{posterior_consensus()}}) call? This is the
intended, load-bearing use case -- mirroring another pipeline's process so
its output can be directly compared against TaxaID's.

Because of this, \code{rank_thresholds}' known inability to reliably separate a
true species from its closest congener (percent-identity alone cannot do
this at almost any real-world threshold -- see Details) is \strong{not a defect
to fix in this function}: it is a faithful reproduction of the same
limitation those conventional pipelines actually have. "Fixing" it (e.g. by
gating on reference completeness, or routing through the bivariate-normal
model) would defeat the comparison this function exists to provide. If you
need real species-vs-congener discrimination for an actual assignment,
use \code{\link[=posterior_consensus]{posterior_consensus()}}, not this function.
}

\examples{
match_df <- data.frame(
  observation_id  = c("S1", "S1", "S1"),
  taxon_name      = c("Gadus morhua", "Gadus chalcogrammus", "Gadus"),
  taxon_name_rank = c("species", "species", "genus"),
  score_original  = c(99, 98.5, 90),
  genus           = "Gadus",
  family          = "Gadidae"
)
sc <- score_consensus(
  match_df,
  min_score       = 97,
  rank_thresholds = c(species = 98, genus = 95, family = 90)
)
sc[, c("observation_id", "consensus_taxon", "consensus_rank", "is_resolved")]

}
\seealso{
\code{\link[=posterior_consensus]{posterior_consensus()}} for the Bayesian posterior-based approach.
}
