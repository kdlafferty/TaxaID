% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_prior_from_consensus.R
\name{update_prior_from_consensus}
\alias{update_prior_from_consensus}
\title{Update Priors from Consensus Assignments and Recompute Posteriors}
\usage{
update_prior_from_consensus(
  result,
  consensus,
  confirmation_quantile = 0.9,
  min_confirmation_confidence = 0.8,
  n_sims = 0,
  spatial_group_map = NULL
)
}
\arguments{
\item{result}{Dataframe. Output of \code{\link[=assign_taxa_llm]{assign_taxa_llm()}} or \code{\link[=compute_posterior]{compute_posterior()}}.
Must contain: \code{observation_id}, \code{taxon_name}, \code{score_likelihood},
\code{score_likelihood_mean}, \code{score_likelihood_sd}, \code{prior_mean}. When
\code{prior_alpha}/\code{prior_beta} (Beta shape parameters) are present, they are
recomputed alongside \code{prior_mean} for boosted rows, preserving the
original concentration (\code{alpha + beta}) so \code{\link[=compute_posterior]{compute_posterior()}}'s Monte
Carlo path stays consistent with the boosted point estimate -- fixes a
latent inconsistency in the previous design, where only \code{prior_mean} was
rescaled and a stale Beta shape could be sampled from if \code{n_sims > 0}.}

\item{consensus}{Dataframe. Output of \code{\link[=posterior_consensus]{posterior_consensus()}} run on \code{result}.
Must contain: \code{observation_id}, \code{consensus_taxon}, \code{is_resolved},
\code{consensus_posterior}.}

\item{confirmation_quantile}{Numeric in (0, 1]. Quantile of confirming
observations' \code{consensus_posterior} used as the candidate new
\code{prior_mean} for a confirmed species. Default 0.9. \code{1} is equivalent to
taking the maximum.}

\item{min_confirmation_confidence}{Numeric in [0, 1]. Minimum value the
\code{confirmation_quantile} must clear for a species to be treated as
confirmed at all. Default 0.8. Set to \code{0} to disable this gate entirely
(every confirmed species is used, however weakly confirmed -- restores
the previous design's lack of a confidence floor).}

\item{n_sims}{Integer. Passed to \code{\link[=compute_posterior]{compute_posterior()}} for the re-run.
Default 0 (point estimates only, fast). Set to 1000 to propagate
uncertainty — match the value used in the original run.}

\item{spatial_group_map}{Dataframe with \code{observation_id} and
\code{spatial_group_id} columns (e.g. from
\code{TaxaMatch::group_observations_by_bbox()}), optional. When supplied, only
observations whose \code{spatial_group_id} is shared with at least one other
observation can contribute confirmed species or receive a prior update;
observations in a single-observation spatial group (a singleton
\code{spatial_group_id} -- there is no separate naming convention for these,
see \code{group_observations_by_bbox()}) are always returned unchanged.
Default \code{NULL} (no group-based restriction — all observations
participate, matching this function's original behaviour).}
}
\value{
The full posterior dataframe with the same structure as \code{result},
plus one new column, \code{confirmed_without_occurrence_record} (logical,
\code{FALSE} unless set \code{TRUE} -- see @section Confirmed without an
occurrence record). Resolved observations are returned unchanged.
Unresolved observations in a multi-member spatial group (see
\code{spatial_group_map}) have updated \code{prior_mean} and freshly computed
posterior columns (\code{posterior_point_est}, \code{posterior_mean},
\code{posterior_sd}, \code{confidence_score}). Unresolved observations in a
single-observation spatial group are returned unchanged, same as
resolved ones. Sorted by \code{observation_id} then descending
\code{posterior_point_est}.
}
\description{
A one-pass empirical Bayes refinement step. Takes the output of
\code{\link[=posterior_consensus]{posterior_consensus()}} and uses species-level consensus assignments
(where \code{is_resolved = TRUE}) as evidence of true presence at the site.
For each confirmed species, the \code{confirmation_quantile}-th quantile of
\code{consensus_posterior} across all confirming ("donor") observations is
computed; if that value clears \code{min_confirmation_confidence}, it
\emph{substitutes} for \code{prior_mean} (never lowering it) in all \emph{unresolved}
observations, and \code{\link[=compute_posterior]{compute_posterior()}} is re-run for those observations
only.
}
\details{
\strong{Why only unresolved observations?} Resolved observations already have a clear
winner; updating their priors and re-running would not change the conclusion
and risks overconfidence. Unresolved observations are the ones that may benefit
from the additional site-level presence information.

\strong{Avoiding circularity:} Priors for observation B are updated using confirmations
from \emph{other} observations. An observation's own posterior never feeds back into its own
prior.

\strong{Multi-member vs. single-observation spatial groups:} this refinement is
only valid when other observations genuinely share a local species pool
with the one being updated -- i.e. they share a spatial group (a drawn
bounding box; see \code{TaxaMatch::group_observations_by_bbox()}). For a
single-observation spatial group (whether because it was submitted alone,
or because it fell outside every user-drawn box and was placed in its own
group rather than dropped), no other observation shares its local species
pool, so another observation's confirmed presence says nothing about it and
must not be used. Supply \code{spatial_group_map} to enforce this: only
observations whose \code{spatial_group_id} is shared with at least one other
observation are used as evidence sources or receive a prior update;
observations in a single-observation spatial group are always returned
unchanged, exactly like already-resolved observations.
}
\section{Confirmation-quantile design (Session 149)}{

The previous design (a fixed \code{presence_multiplier}, e.g. x5) applied the
same boost regardless of how many observations confirmed a species or how
confident those confirmations were -- flagged as High priority in
\code{ecosystem_docs/STATISTICAL_COMPONENT_SOUNDNESS_REVIEW.md}. This design
instead ties the boost directly to the strength of the confirming
evidence:
\enumerate{
\item For each confirmed species, take the \code{confirmation_quantile}-th
quantile (default 0.9) of \code{consensus_posterior} across all resolved
"donor" observations naming it. A high quantile behaves like a
near-maximum for a small donor pool (rewarding one strong,
high-quality confirmation the way a single excellent frame in an
otherwise-poor camera-trap burst should), but -- unlike a plain
maximum -- it converges to a \emph{stable} population value as the donor
pool grows, rather than drifting toward the degenerate ceiling of 1.0
regardless of whether the evidence is real. This matters specifically
for a pair of species a classifier cannot reliably separate: many
weak, correlated (not independent) confirmations split between them
would otherwise let a plain maximum -- or a probabilistic-OR
combination across all of them -- manufacture unwarranted confidence
for one or both, an effect that does not simply cancel out under
renormalization once a third, unrelated candidate is also present.
\item That quantile value is only used at all if it clears
\code{min_confirmation_confidence} (default 0.8) -- a barely-resolved
confirmation (e.g. just above whatever \code{posterior_consensus()}
threshold marked it "resolved") should not be trusted to inject a
large jump into an unrelated observation's prior.
\item Even then, it only \emph{replaces} \code{prior_mean} where it exceeds the
existing value (never-demote) -- a species already well-supported by
occurrence data is left alone.
}
This is still a threshold-based design (the quantile level and the
confidence floor are both free parameters), and it does not by itself
distinguish "confidence earned by genuinely strong evidence" from
"confidence attained despite thin/low-quality input" (e.g. a short,
low-coverage sequence read producing a spurious high-identity match) --
that would need a quality covariate on the underlying scores, a separate,
larger design question flagged for later rather than solved here.
A formally correct alternative would still be a Beta-Binomial
hierarchical model over species x observation within a site; this
quantile-based design is a more evidence-sensitive interim step than the
old fixed multiplier, not a replacement for that.
}

\section{Rescaling onto the occurrence scale (2026-07-30)}{

\code{consensus_posterior} is P(hypothesis | evidence) \emph{within one
observation} -- a probability over competing hypotheses. \code{prior_mean},
when sourced from \code{TaxaExpect}, is a \strong{compositional share of the local
record pool} (\code{theta_mean}, confirmed by reading
\code{TaxaExpect::prepare_model_dataframe()}'s binomial response directly:
\code{n_species / n_total_at_site}, one record attributable to exactly one
taxon). These are different sample spaces, and substituting one for the
other only ever inflates (never-demote guarantees it). Measured on real
Mugu data: 220 of 2540 rows were boosted, and all 220 landed in
[0.951, 1.0] -- every one overshooting the real occurrence-scale
ceiling (\code{max(theta_mean)} across every locally modelled taxon,
0.0865 on that dataset) by 11x to 1000x.

When a \code{theta_mean} column is present on \code{result}, the substitution is
rescaled onto that ceiling instead of being used directly:
\code{prior_new <- max(prior_old, q * max(result$theta_mean, na.rm = TRUE))},
where \code{q} is the confirmation-quantile value described above. This keeps
the confirmation-quantile logic and the never-demote guard unchanged --
only the target scale moves -- so a stronger/more numerous confirmation
(higher \code{q}) still produces a stronger boost, bounded by the model's own
observed maximum share rather than substituting a foreign-scale
constant. When \code{result} has no \code{theta_mean} column (e.g. LLM-pathway
priors from \code{\link[=assign_taxa_llm]{assign_taxa_llm()}}, which are not occurrence-share-based to
begin with), the previous direct-substitution behavior is used
unchanged -- the units mismatch this section fixes is specific to
occurrence-model-sourced priors.
}

\section{Confirmed without an occurrence record (2026-07-30)}{

A taxon can be confidently identified elsewhere in the dataset while
having no occurrence record at all (\code{theta_mean} \code{NA} for every row
naming it) -- e.g. a real Mugu case, \emph{Oncorhynchus mykiss}, which has no
row in \code{taxaexpect_priors} at all (see
\verb{[[project_urolophus_synonym_join_bug]]} in the project memory system --
this is a known, separate upstream join gap, not a sign the species is
actually rare). Such a row still gets boosted (the confirmation is real
identification evidence, worth keeping) but is flagged via a new
\code{confirmed_without_occurrence_record} column rather than silently
presented as occurrence-grounded. Boosting is \emph{not} suppressed for these
rows: gating on occurrence-record presence would currently punish
exactly the taxa the join gap affects (whole families, in the Mugu
salmonid case), which are neither rare nor implausible -- only
undercounted by an unrelated bug. Revisit suppression once that join gap
is fixed.
}

\examples{
\dontrun{
result_updated <- update_prior_from_consensus(
  result, consensus,
  confirmation_quantile        = 0.9,
  min_confirmation_confidence  = 0.8
)
}

}
\seealso{
\code{\link[=posterior_consensus]{posterior_consensus()}}, \code{\link[=compute_posterior]{compute_posterior()}}, \code{\link[=assign_taxa_llm]{assign_taxa_llm()}}
}
