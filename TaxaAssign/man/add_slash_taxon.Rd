% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slash_taxon.R
\name{add_slash_taxon}
\alias{add_slash_taxon}
\title{Add Slash Taxon Name and Irreducibility Flag to a Consensus Dataframe}
\usage{
add_slash_taxon(
  consensus_df,
  taxa_col = "plausible_taxa",
  posteriors_col = "plausible_posteriors"
)
}
\arguments{
\item{consensus_df}{Dataframe. Output of \code{\link[=posterior_consensus]{posterior_consensus()}}. Must
contain a list column of character vectors giving the plausible candidate
taxa per observation (see \code{taxa_col}).}

\item{taxa_col}{Character. Name of the list column containing per-observation
plausible taxa vectors. Default \code{"plausible_taxa"}.}

\item{posteriors_col}{Character. Name of the list column containing
per-observation posterior probability vectors, positionally aligned with
\code{taxa_col} (e.g., \code{"plausible_posteriors"} from \code{\link[=posterior_consensus]{posterior_consensus()}}).
When present, candidates within each slash name are ordered by descending
posterior (most probable first). When \code{NULL} or absent from \code{consensus_df},
candidates are ordered alphabetically. Default \code{"plausible_posteriors"}.}
}
\value{
\code{consensus_df} with additional columns appended:
\code{slash_taxon_name} (character) and \code{irreducible_consensus} (logical).
When \code{consensus_df} has a \code{consensus_taxon} column, two more columns are
added:
\describe{
\item{\code{consensus_OTU}}{Single reporting label per observation —
\code{slash_taxon_name} when non-\code{NA}, otherwise \code{consensus_taxon}. Use
this as the one column with a non-\code{NA} label for every resolved
observation.}
\item{\code{primary_taxon}}{\code{consensus_OTU} reduced to a single taxon name
by dropping everything from the first \code{/} or \code{+} onward — the
single most-reportable name when a slash/plus label is not wanted
(e.g. simple species tallies). For a slash set, this is whichever
candidate \code{.make_slash_name()} placed first (highest posterior when
\code{posteriors_col} is available, else alphabetical) — a convenience
label, not a statistically preferred pick.}
}
}
\description{
Appends two columns to the output of \code{\link[=posterior_consensus]{posterior_consensus()}}:
}
\details{
\itemize{
\item \strong{\code{slash_taxon_name}} — a compact human-readable label for the plausible
candidate set, following ornithological slash-species convention.
Same-genus candidates are abbreviated (\verb{Homo sapiens/heidelbergensis});
mixed-genus candidates are grouped by genus and joined with \code{+}
(\verb{Bos javanicus/primigenius + Bison bonasus}). \code{NA} for singletons and
unresolved observations.
\item \strong{\code{irreducible_consensus}} — logical. \code{TRUE} when the candidate set for
this observation cannot be further decomposed by reference to any other
observation in the dataframe. A set is irreducible when no other
(distinct) set in the data is the same size or smaller \emph{and} shares at
least one taxon. Singletons are always \code{TRUE} (a single species is
trivially irreducible). Unresolved observations (empty candidate set) are
always \code{FALSE}. Multi-taxon sets are \code{TRUE} when the marker/model cannot
distinguish the candidates anywhere else in the dataset — i.e., the slash
taxon is a genuine reporting unit, not a locally ambiguous observation that
is resolved elsewhere.
}

The irreducibility check operates on the full set of unique candidate
combinations present in \code{consensus_df}. Candidate sets are sorted and
deduplicated before comparison, so order differences in \code{plausible_taxa}
across rows do not affect the result. This matters because candidate order
is POSTERIOR order (it is what \code{primary_taxon} reads), so one biological
unit legitimately arrives in different orders on different observations.
Irreducibility is a property of the SET, not of the ranking within it.
Enforced and regression-tested since 2026-09-04; before that the signature
was built from the unsorted vector, so two orderings of one set each marked
the other reducible and every row of the unit went \code{FALSE}.
}
\note{
\strong{Non-binomial names in \code{plausible_taxa}:} Entries that are not a
two-word binomial (e.g. \code{"Thunnus aff."}, a bare genus like \code{"Perca"})
can corrupt slash-name formatting. Two distinct, legitimate causes
produce this, not just one: (1) GBIF occurrence records returned when
querying at genus or family rank -- prevent by passing
\code{require_species = TRUE} to \code{\link[TaxaFetch:filter_gbif_quality]{TaxaFetch::filter_gbif_quality()}} before
occurrence data enters the pipeline; and (2) genuine genus-level
fallback hypotheses from the sequence/BLAST pathway (e.g.
\code{hypothesis_type == "unreferenced_genus"}, or a
\code{TaxaMatch::convert_taxonomy_backbone()} fallback to a bare genus when no
species-level match exists) -- this is expected, intended behavior for
real production data with a genuinely thin reference database, not a
data-quality problem to fix upstream (found 2026-09-05 on real
GreatLakes/PtConception 12S data: warnings fired on real genus-only
candidates like \code{"Perca"}/\code{"Ictalurus"}/\code{"Fundulus"} with no GBIF
involvement at all). This warning cannot distinguish the two causes --
use \code{TaxaTools::is_plausible_binomial()} directly, cross-referenced
against each flagged name's \code{hypothesis_type}, to tell them apart.
}
\examples{
posterior_df <- data.frame(
  observation_id = c("S1", "S1", "S2"),
  taxon_name = c("Homo sapiens", "Homo heidelbergensis", "Bos taurus"),
  taxon_name_rank = "species",
  hypothesis_type = "specific_candidate",
  genus = c("Homo", "Homo", "Bos"),
  family = c("Hominidae", "Hominidae", "Bovidae"),
  posterior_mean = c(0.55, 0.45, 1.0),
  posterior_point_est = c(0.55, 0.45, 1.0)
)
consensus <- posterior_consensus(posterior_df, min_posterior = 0)
consensus <- add_slash_taxon(consensus)
consensus[, c("observation_id", "slash_taxon_name", "irreducible_consensus")]

# All reportable observations: singletons + irreducible slash taxa
reportable <- consensus[consensus$irreducible_consensus \%in\% TRUE, ]

}
