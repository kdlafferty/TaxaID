% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slash_taxon.R
\name{add_slash_taxon}
\alias{add_slash_taxon}
\title{Add Slash Taxon Name and Irreducibility Flag to a Consensus Dataframe}
\usage{
add_slash_taxon(consensus_df, taxa_col = "plausible_taxa")
}
\arguments{
\item{consensus_df}{Dataframe. Output of \code{\link[=posterior_consensus]{posterior_consensus()}}. Must
contain a list column of character vectors giving the plausible candidate
taxa per observation (see \code{taxa_col}).}

\item{taxa_col}{Character. Name of the list column containing per-observation
plausible taxa vectors. Default \code{"plausible_taxa"}.}
}
\value{
\code{consensus_df} with two additional columns appended:
\code{slash_taxon_name} (character) and \code{irreducible_consensus} (logical).
}
\description{
Appends two columns to the output of \code{\link[=posterior_consensus]{posterior_consensus()}}:
}
\details{
\itemize{
\item \strong{\code{slash_taxon_name}} — a compact human-readable label for the plausible
candidate set, following ornithological slash-species convention.
Same-genus candidates are abbreviated (\verb{Homo sapiens/heidelbergensis});
mixed-genus candidates are grouped by genus and joined with \code{+}
(\verb{Bos javanicus/primigenius + Bison bonasus}). \code{NA} for singletons and
unresolved observations.
\item \strong{\code{irreducible_consensus}} — logical. \code{TRUE} when the candidate set for
this observation cannot be further decomposed by reference to any other
observation in the dataframe. A set is irreducible when no other
(distinct) set in the data is the same size or smaller \emph{and} shares at
least one taxon. Singletons are always \code{TRUE} (a single species is
trivially irreducible). Unresolved observations (empty candidate set) are
always \code{FALSE}. Multi-taxon sets are \code{TRUE} when the marker/model cannot
distinguish the candidates anywhere else in the dataset — i.e., the slash
taxon is a genuine reporting unit, not a locally ambiguous observation that
is resolved elsewhere.
}

The irreducibility check operates on the full set of unique candidate
combinations present in \code{consensus_df}. Candidate sets are sorted and
deduplicated before comparison, so order differences in \code{plausible_taxa}
across rows do not affect the result.
}
\note{
\strong{Invalid species names upstream:} Non-binomial entries such as
\code{"Thunnus aff."}, \code{"Thunnus cf."}, or \code{"Canis sp. Russia/33500"} can
corrupt slash names. These arise from GBIF records returned when querying
at genus or family rank. Prevent them by passing
\code{require_species = TRUE} to \code{\link[TaxaFetch:filter_gbif_quality]{TaxaFetch::filter_gbif_quality()}} before
occurrence data enters the pipeline. Use
\code{TaxaTools::is_plausible_binomial()} to audit candidate sets if unexpected
slash names appear.
}
\examples{
\dontrun{
consensus <- posterior_consensus(posterior_df)
consensus <- add_slash_taxon(consensus)

# All reportable observations: singletons + irreducible slash taxa
reportable <- consensus[consensus$irreducible_consensus \%in\% TRUE, ]
}

}
