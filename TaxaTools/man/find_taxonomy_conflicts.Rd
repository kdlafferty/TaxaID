% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_taxonomy_conflicts.R
\name{find_taxonomy_conflicts}
\alias{find_taxonomy_conflicts}
\title{Find Taxonomic Conflicts in a Taxonomy Data Frame}
\usage{
find_taxonomy_conflicts(df, rank_system = NULL)
}
\arguments{
\item{df}{Data frame with taxonomy columns (e.g. \code{family}, \code{genus},
\code{species}).}

\item{rank_system}{Character vector of rank column names, coarse to fine.
If \code{NULL} (default), auto-detected via \code{\link[=detect_ranks]{detect_ranks()}}.}
}
\value{
A data frame of conflicts with columns:
\describe{
\item{\code{taxon_name}}{The taxon name that has conflicting higher-rank
assignments.}
\item{\code{taxon_rank}}{The rank of the conflicting taxon (e.g.
\code{"genus"}).}
\item{\code{parent_rank}}{The coarser rank where disagreement was found
(e.g. \code{"family"}).}
\item{\code{parent_values}}{Semicolon-separated string of the distinct
parent values found (e.g. \code{"Cottidae; Scorpaenidae"}).}
\item{\code{n_values}}{Number of distinct parent values.}
}
Returns an empty data frame (0 rows, same columns) when no conflicts
are found.
}
\description{
Detects higher-rank inconsistencies: cases where the same taxon name at
one rank is assigned to different parent taxa at a coarser rank across
rows.
}
\details{
These conflicts arise when merging taxonomy from multiple sources (GBIF +
NCBI + WoRMS) or when supplementing verified taxonomy with manual
corrections.  Adapted from the GITA pipeline's
\code{f_find_taxonomic_inconsistencies()}, vectorised for performance.
}
\examples{
df <- data.frame(
  family  = c("Cottidae", "Scorpaenidae", "Cottidae"),
  genus   = c("Cottus",   "Cottus",        "Enophrys"),
  species = c("Cottus asper", "Cottus rhotheus", "Enophrys bison"),
  stringsAsFactors = FALSE
)
find_taxonomy_conflicts(df)
# Returns: Cottus has conflicting family values (Cottidae; Scorpaenidae)

}
\seealso{
\code{\link[=detect_ranks]{detect_ranks()}}, \code{\link[=change_backbone]{change_backbone()}}
}
