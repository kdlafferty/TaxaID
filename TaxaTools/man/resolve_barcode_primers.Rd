% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barcode_utils.R
\name{resolve_barcode_primers}
\alias{resolve_barcode_primers}
\title{Resolve Primer Sequences from a Barcode/Primer-Set Term}
\usage{
resolve_barcode_primers(barcode_term)
}
\arguments{
\item{barcode_term}{Character scalar naming a specific primer set (e.g.
\code{"MiFishU"}, \code{"mifish-e"}). Matching is case-insensitive and ignores
\code{-}/\verb{_}/space separators.}
}
\value{
A list with \code{fwd}, \code{rev} (character, 5'-3') and \code{amplicon_range}
(integer \code{c(min_bp, max_bp)}).
}
\description{
Looks up \code{barcode_term} in \link{barcode_primer_defaults}. Unlike
\code{\link[=resolve_barcode_lengths]{resolve_barcode_lengths()}}, matching requires the \emph{specific} primer
variant (e.g. \code{"MiFishU"}, not bare \code{"MiFish"}) because different variants
of what is loosely called "the same marker" can have genuinely different
primer sequences -- silently picking one would be a real correctness risk,
not a convenience shortcut. An ambiguous or unlisted term errors with
guidance rather than guessing.
}
\examples{
resolve_barcode_primers("MiFishU")
resolve_barcode_primers("mifish-e")

}
