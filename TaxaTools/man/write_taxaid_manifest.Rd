% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_manifest.R
\name{write_taxaid_manifest}
\alias{write_taxaid_manifest}
\title{Write a TaxaID Build Manifest}
\usage{
write_taxaid_manifest(path, packages = NULL)
}
\arguments{
\item{path}{File path to write (\code{.rds}).}

\item{packages}{Passed to \code{\link[=taxaid_build_manifest]{taxaid_build_manifest()}}.}
}
\value{
The manifest, invisibly.
}
\description{
Write a TaxaID Build Manifest
}
\examples{
\dontrun{
# In a workflow preamble: record the current library once, then check it on
# every later run.
manifest <- file.path(OUT_DIR, paste0(OUT_PREFIX, "_build_manifest.rds"))
if (!file.exists(manifest)) {
  write_taxaid_manifest(manifest)
} else {
  check_taxaid_manifest(manifest)
}
}
}
\seealso{
\code{\link[=check_taxaid_manifest]{check_taxaid_manifest()}}
}
