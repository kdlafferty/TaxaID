% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill_higher_ranks.R
\name{parse_classification_path}
\alias{parse_classification_path}
\title{Parse a Rank Value from a Pipe-Delimited Classification Path}
\usage{
parse_classification_path(path, ranks, target_rank)
}
\arguments{
\item{path}{Character scalar.  Pipe-delimited taxon names, e.g.
\code{"Animalia|Chordata|Cottidae|Cottus"}.}

\item{ranks}{Character scalar.  Pipe-delimited rank labels aligned
with \code{path}, e.g. \code{"kingdom|phylum|family|genus"}.}

\item{target_rank}{Character scalar.  The rank to extract, e.g.
\code{"family"} or \code{"order"}.}
}
\value{
The matched taxon name string, or \code{NA_character_} if
\code{target_rank} is not present in \code{ranks} or inputs are \code{NA}.
}
\description{
Extracts a single rank value from the \code{classification_path} and
\code{classification_ranks} columns returned by \code{\link[=verify_taxon_names]{verify_taxon_names()}}.
Both columns use \code{|} as a delimiter and are positionally aligned.
}
\details{
This function is vectorised over \code{path} and \code{ranks} via \code{\link[=mapply]{mapply()}}.
To parse an entire column from \code{\link[=verify_taxon_names]{verify_taxon_names()}} output use:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{verified <- verify_taxon_names(stringr::word(species_names, 1))
families <- mapply(
  parse_classification_path,
  verified$classification_path,
  verified$classification_ranks,
  MoreArgs = list(target_rank = "family")
)
}\if{html}{\out{</div>}}
}
\examples{
parse_classification_path(
  path        = "Animalia|Chordata|Cottidae|Cottus",
  ranks       = "kingdom|phylum|family|genus",
  target_rank = "family"
)
# [1] "Cottidae"

parse_classification_path(
  path        = "Animalia|Chordata|Cottidae|Cottus",
  ranks       = "kingdom|phylum|family|genus",
  target_rank = "order"
)
# [1] NA

}
\seealso{
\code{\link[=verify_taxon_names]{verify_taxon_names()}}, \code{\link[=fill_higher_ranks]{fill_higher_ranks()}}
}
