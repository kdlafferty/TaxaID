% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/escalate_taxonomic_rank.R
\name{escalate_taxonomic_rank}
\alias{escalate_taxonomic_rank}
\title{Escalate a Taxon to the Next Coarser Rank}
\usage{
escalate_taxonomic_rank(
  taxon_name,
  current_rank,
  rank_system = standard_ranks,
  max_levels = 2L,
  backbone_id = 4L,
  fallback_backbone_id = 11L,
  verbose = TRUE
)
}
\arguments{
\item{taxon_name}{Character scalar. The taxon name currently being queried.}

\item{current_rank}{Character scalar. The rank \code{taxon_name} is at; must be
present in \code{rank_system}.}

\item{rank_system}{Character vector of rank names, coarse to fine (see
\link{standard_ranks}). Default \code{standard_ranks}.}

\item{max_levels}{Integer. Maximum number of rank levels to walk up from
\code{current_rank} in a single call. Default \code{2L} (e.g. genus -> family ->
order).}

\item{backbone_id}{Integer. Primary backbone for the classification
lookup. Default \code{4L} (NCBI). Set \code{NULL} to skip straight to
\code{fallback_backbone_id}.}

\item{fallback_backbone_id}{Integer. Secondary backbone tried if the
primary backbone doesn't resolve \code{taxon_name}. Default \code{11L} (GBIF). Set
\code{NULL} or equal to \code{backbone_id} to skip.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A list with \code{taxon_name} and \code{rank}. Both are \code{NA_character_} if
\code{current_rank} is already the coarsest rank in \code{rank_system}, if the
classification cannot be resolved at all, or if no coarser rank resolves
within \code{max_levels} steps.
}
\description{
Given a taxon name currently being queried at \code{current_rank}, looks up its
full classification via \code{\link[=verify_taxon_names]{verify_taxon_names()}} and returns the name at the
next coarser rank in \code{rank_system} -- e.g. broadening a genus with no
reference sequences or occurrence records to its family. If the immediate
next-coarser rank is itself absent from the classification path (a gap in
the backbone's own data, not a fetch-availability decision), continues
walking coarser ranks, up to \code{max_levels} steps from \code{current_rank},
before giving up.

This function only walks the taxonomic hierarchy -- it has no notion of
whether a fetch at any rank actually returned usable data. A consuming
retry loop (e.g. broadening a reference-sequence or occurrence fetch when
a singleton's own genus comes back empty) calls this once per escalation
step, refetches at the returned rank/name, and calls again with that pair
as the new \code{current_rank}/\code{taxon_name} if the refetch is still empty.
}
\examples{
\dontrun{
escalate_taxonomic_rank("Rhacochilus", current_rank = "genus")
# list(taxon_name = "Embiotocidae", rank = "family")
}

}
\seealso{
\code{\link[=verify_taxon_names]{verify_taxon_names()}}, \code{\link[=parse_classification_path]{parse_classification_path()}},
\link{standard_ranks}
}
