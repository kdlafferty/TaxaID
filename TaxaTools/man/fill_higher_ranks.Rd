% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill_higher_ranks.R
\name{fill_higher_ranks}
\alias{fill_higher_ranks}
\title{Fill Higher Taxonomic Ranks from Local Data and Backbone APIs}
\usage{
fill_higher_ranks(
  taxon_names,
  local_sources = list(),
  backbone_id = 4L,
  fallback_backbone_id = 11L,
  verbose = TRUE
)
}
\arguments{
\item{taxon_names}{Character vector of taxon names.  Binomial species names
are expected; genus is extracted as the first word.  Single-word names
(genus only) are accepted and returned as-is in the \code{genus} column.}

\item{local_sources}{List of data frames to consult before any API call.
Each element must contain at least \code{genus} and \code{family} columns; elements
lacking those columns are silently skipped.  Sources are consulted in
order; the first non-NA family for each genus wins.}

\item{backbone_id}{Integer.  Primary backbone for API fallback.
Default \code{4L} (NCBI).  Set \code{NULL} to skip the API entirely.}

\item{fallback_backbone_id}{Integer.  Secondary backbone used when
\code{backbone_id} returns no match.  Default \code{11L} (GBIF).  Set \code{NULL} or
equal to \code{backbone_id} to skip.}

\item{verbose}{Logical.  Print progress messages for API lookups.
Default \code{TRUE}.}
}
\value{
A tibble with one row per element of \code{taxon_names} (preserving
duplicates and order), with columns:
\describe{
\item{\code{taxon_name}}{The original input name.}
\item{\code{genus}}{First word of \code{taxon_name}, EXCEPT when an API lookup
resolved that genus to a backbone-flagged synonym at genus rank --
in that case the backbone's own currently-accepted genus name is
returned instead (2026-07-25; see \verb{@section Genus correction}
below). Local-source-only lookups (no API call needed) always keep
the locally-extracted genus unchanged.}
\item{\code{family}}{Looked-up family, or \code{NA} if unresolved.}
}
A warning is issued for any names whose family could not be resolved after
all fallbacks.
}
\description{
Given a character vector of taxon names (typically species binomials), derives
\code{genus} (first word of the binomial) and looks up \code{family} using a
priority-ordered fallback chain:
\enumerate{
\item \strong{Local sources} — data frames already in scope (e.g. \code{reference_df},
\code{match_obj}, \code{gbif_std}).  Fast, offline, covers most cases.
\item \strong{Primary backbone} — \code{verify_taxon_names()} queried at genus level
(\code{backbone_id}; default NCBI = 4).
\item \strong{Fallback backbone} — repeated with \code{fallback_backbone_id} (default
GBIF = 11) for any genera still unresolved.
}

Queries are made at the \strong{genus level}, so species absent from a backbone
(e.g. newly described species, cross-backbone synonyms) are still resolved
provided their genus is present.
}
\details{
\strong{Backbone queries are at genus level.}  \code{verify_taxon_names()} is called
with the unique unresolved genera (not the full species names).  This means
a species absent from NCBI is still resolved provided its genus is present
— which covers the vast majority of cross-backbone synonym situations.

\strong{Column name conflicts in local sources} (e.g. a source has both \code{genus}
and \code{Genus}) are handled by \code{tolower(names(...))} before joining.
}
\section{Genus correction (2026-07-25)}{

Before this fix, \code{genus} was always the locally-extracted first word of
\code{taxon_name}, even when the API lookup showed that genus was a taxonomic
synonym under the queried backbone -- e.g. an informally-named reference
("Inu sp. 1 sensu Shibukawa et al., 2020.") extracted \code{genus = "Inu"},
and stayed \code{"Inu"} even though GBIF's own backbone resolves it to the
currently-accepted genus \code{"Luciogobius"}. This mattered beyond cosmetics:
\code{TaxaAssign::join_priors()} joins this function's \code{genus}/\code{family} output
against a likelihood-side coarse-rank \code{taxon_name} (produced by
\code{TaxaMatch::convert_taxonomy_backbone()}, which -- also as of 2026-07-25
-- prefers the current name over a synonym) via an EXACT STRING match. If
the two functions disagreed on which form to report for the same genus,
that join silently failed and the observation lost its coarse-rank
species expansion entirely, falling back to the dark-diversity floor
instead of real occurrence-based priors. \code{genus} is now corrected to the
backbone's resolved name whenever an API lookup found one cleanly at
genus rank, keeping this function consistent with
\code{convert_taxonomy_backbone()}. Verified backbone-general (both NCBI and
GBIF checked live for the real "Inu" case) before shipping.
}

\examples{
\dontrun{
unreferenced_df <- fill_higher_ranks(
  coverage$unreferenced,
  local_sources = list(reference_df, match_obj, gbif_std)
)
# rename for expand_unreferenced_hypotheses():
unreferenced_df <- dplyr::rename(unreferenced_df, species = taxon_name)
}

}
\seealso{
\code{\link[=verify_taxon_names]{verify_taxon_names()}}, \code{\link[=parse_classification_path]{parse_classification_path()}}
}
