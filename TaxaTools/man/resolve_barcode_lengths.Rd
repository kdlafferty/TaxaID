% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barcode_utils.R
\name{resolve_barcode_lengths}
\alias{resolve_barcode_lengths}
\title{Resolve Barcode Length Bounds from a Barcode Term}
\usage{
resolve_barcode_lengths(barcode_term, min_len = NULL, max_len = NULL)
}
\arguments{
\item{barcode_term}{Character vector of barcode marker names (e.g.
\code{"MiFishU"}, \code{"COI"}, \code{c("12S", "16S")}).}

\item{min_len, max_len}{Optional integer overrides.  When non-\code{NULL},
replace the auto-detected bound.}
}
\value{
Integer vector of length 2: \code{c(min_bp, max_bp)}.
}
\description{
Looks up \code{barcode_term} in \link{barcode_length_defaults} and returns
\code{c(min_bp, max_bp)}.
}
\details{
For vector input (multiple marker names), collects all matched ranges and
returns \code{c(min(mins), max(maxes))} so the query covers all supplied terms.
User-supplied \code{min_len} / \code{max_len} override the resolved bounds.
}
\examples{
resolve_barcode_lengths("MiFishU")
# 100 600
resolve_barcode_lengths(c("12S", "16S"))
# 100 700  (union of both ranges)
resolve_barcode_lengths("COI", min_len = 500)
# 500 900  (min_len overrides auto-detected)

}
