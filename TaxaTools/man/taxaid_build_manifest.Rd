% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_manifest.R
\name{taxaid_build_manifest}
\alias{taxaid_build_manifest}
\title{Record Which TaxaID Code a Run Is Using}
\usage{
taxaid_build_manifest(packages = NULL)
}
\arguments{
\item{packages}{Character vector of package names. Defaults to the nine
TaxaID packages.}
}
\value{
A data frame with columns \code{package}, \code{version}, \code{built},
\code{code_hash}, \code{installed}. Packages that are NOT installed appear with
\code{installed = FALSE} rather than being dropped -- absence has to be
representable for \code{\link[=check_taxaid_manifest]{check_taxaid_manifest()}} to report it.
}
\description{
Captures, for each TaxaID package, its version, \code{Built} timestamp and a hash
of its installed code. Save it beside a run's outputs and every result
becomes traceable to the exact build that produced it.
}
\examples{
\dontrun{
m <- taxaid_build_manifest()
saveRDS(m, file.path(OUT_DIR, paste0(OUT_PREFIX, "_build_manifest.rds")))
}
}
\seealso{
\code{\link[=write_taxaid_manifest]{write_taxaid_manifest()}}, \code{\link[=check_taxaid_manifest]{check_taxaid_manifest()}}
}
