% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_section.R
\name{new_report_section}
\alias{new_report_section}
\title{Create a Report Section Object}
\usage{
new_report_section(
  package,
  section,
  methods,
  results = NULL,
  citations = NULL,
  params = NULL,
  statistics = NULL
)
}
\arguments{
\item{package}{Character. Package name (e.g. \code{"TaxaFetch"}).}

\item{section}{Character. Short section identifier (e.g. \code{"fetch"},
\code{"match"}, \code{"likelihood"}).}

\item{methods}{Character. Methods text (template-based, deterministic).}

\item{results}{Character. Results text (template or LLM-generated).}

\item{citations}{Character vector or \code{NULL}. Bibliographic citations
associated with this pipeline step.}

\item{params}{Named list or \code{NULL}. Key parameters used in this step
(for reproducibility and downstream assembly).}

\item{statistics}{Named list or \code{NULL}. Summary statistics computed
in this step.}
}
\value{
A \code{report_section} object (S3 list).
}
\description{
Constructor for the \code{report_section} S3 class used by per-package
report functions throughout the TaxaID ecosystem. Each package's
\code{report_*()} function returns a \code{report_section} object that
can be printed standalone or assembled into a unified report via
\code{\link{assemble_report}}.
}
\examples{
sec <- new_report_section(
  package    = "TaxaFetch",
  section    = "fetch",
  methods    = "Occurrence records were obtained from GBIF.",
  results    = "A total of 1,234 records were compiled.",
  citations  = c("GBIF.org. GBIF Occurrence Download via rgbif"),
  params     = list(year_range = "2000,2024"),
  statistics = list(n_records = 1234L)
)
print(sec)

}
