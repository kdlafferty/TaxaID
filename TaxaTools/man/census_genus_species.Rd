% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/census_genus_species.R
\name{census_genus_species}
\alias{census_genus_species}
\title{Census described species within genera (or higher ranks) via GBIF backbone}
\usage{
census_genus_species(
  genus_keys,
  match_species = NULL,
  rank = "genus",
  status_filter = "ACCEPTED",
  verbose = TRUE
)
}
\arguments{
\item{genus_keys}{Named integer vector: names are taxon names (e.g., genus
names), values are GBIF usageKeys. Obtain from GBIF occurrence data
(\code{genusKey} column) or from \code{rgbif::name_backbone()}.}

\item{match_species}{Character vector of species already in the reference
database (e.g., \code{unique(match_df$species)}). If provided, the function
computes per-genus completeness: how many described species are missing
from the reference. If NULL (default), only the GBIF census is returned
without reference comparison.}

\item{rank}{Character string indicating the rank of the input keys. Default
\code{"genus"}. For higher ranks (e.g., \code{"family"}, \code{"order"}), the function
recursively enumerates child genera, then species within each genus.}

\item{status_filter}{Character vector of GBIF \code{taxonomicStatus} values to
include. Default \code{"ACCEPTED"}. Set to \code{c("ACCEPTED", "DOUBTFUL")} to
include taxonomically uncertain species.}

\item{verbose}{Logical. If TRUE (default), prints progress messages.}
}
\value{
A data frame with one row per queried taxon (genus or higher rank):

\describe{
\item{group}{Character. Taxon name (genus/family/order).}
\item{gbif_key}{Integer. GBIF usageKey used for the query.}
\item{total_described}{Integer. Number of accepted species in GBIF backbone.}
\item{in_reference}{Integer. Species found in \code{match_species} (NA if
\code{match_species} not provided).}
\item{n_missing}{Integer. \code{total_described - in_reference} (NA if
\code{match_species} not provided).}
\item{missing_species}{List-column. Character vector of missing species
names per genus (empty list if \code{match_species} not provided or genus
is complete).}
\item{described_species}{List-column. Character vector of all described
species names per genus.}
\item{status}{Character. \code{"complete"}, \code{"singleton_missing"}, or
\code{"incomplete"} (NA if \code{match_species} not provided).}
}

The complete species list across all queried taxa is available as
\code{attr(result, "all_species")} (a flat character vector).
}
\description{
Queries the GBIF backbone taxonomy to enumerate all described species within
each queried taxon. Optionally compares against a reference species list
(e.g., from a match object) to classify genera by completeness.
}
\details{
For \code{rank = "genus"}, the function makes one GBIF API call per genus
(\code{rgbif::name_usage(key, data = "children")}). For higher ranks (family,
order), it first retrieves child genera, then recurses into each genus —
this can be slow for large families (e.g., Gobiidae with 300+ genera).

The \code{status} column classifies each genus by reference completeness:
\itemize{
\item \code{"complete"}: all described species are in the reference (n_missing == 0).
H2 "unreferenced species" hypotheses should be suppressed.
\item \code{"singleton_missing"}: exactly one described species is missing
(n_missing == 1). The H2 hypothesis can be named directly.
\item \code{"incomplete"}: multiple described species are missing (n_missing > 1).
A generic H2 phantom is appropriate.
}
}
\examples{
\dontrun{
# Census species in genus Clevelandia (GBIF key 2394389)
census <- census_genus_species(c(Clevelandia = 2394389))

# With reference comparison
census <- census_genus_species(
  c(Clevelandia = 2394389, Fundulus = 2347676),
  match_species = c("Clevelandia ios", "Fundulus parvipinnis")
)
}

}
