% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_faire.R
\name{to_faire}
\alias{to_faire}
\title{Export a TaxaID Data Frame in FAIRe Checklist Format}
\usage{
to_faire(
  input_df,
  table_type = c("taxaFinal", "taxaRaw"),
  checkls_ver = "1.02",
  assay_name = NULL
)
}
\arguments{
\item{input_df}{A TaxaID data frame. Any output from \code{TaxaMatch}, \code{TaxaLikely},
or \code{TaxaAssign} is accepted. Columns not covered by the FAIRe field
mapping are retained with their original names.}

\item{table_type}{Character. \code{"taxaFinal"} (post-curation assignments,
e.g. posterior consensus output; default) or \code{"taxaRaw"} (pre-curation
assignments, e.g. direct match output). Affects only the \code{faire_table}
attribute attached to the returned data frame.}

\item{checkls_ver}{Character. FAIRe checklist version to record in the
\code{checkls_ver} column. Default \code{"1.02"} (current stable version).}

\item{assay_name}{Character or \code{NULL}. Value to use for the FAIRe
\code{assay_name} field when \code{df} does not contain a \code{testid} column. If
\code{NULL} and \code{testid} is absent, \code{assay_name} is omitted from the output
with a message.}
}
\value{
A data frame with FAIRe-compatible column names and a
\code{faire_table} attribute recording \code{table_type} and \code{checkls_ver}.
}
\description{
Converts a TaxaID output data frame (match object, likelihood object, or
posterior object) to the column naming conventions of the
\href{https://github.com/FAIR-eDNA/FAIRe_checklist}{FAIRe eDNA metadata checklist}
(Takahashi et al. 2025), specifically the \code{taxaRaw} and \code{taxaFinal} classes.
}
\details{
\strong{Column mapping applied (TaxaID \eqn{\to} FAIRe):}
\itemize{
\item \code{observation_id} \eqn{\to} \code{seq_id}
\item \code{taxon_name} \eqn{\to} \code{scientificName}
\item \code{taxon_name_rank} \eqn{\to} \code{taxonRank}
\item \code{score} \eqn{\to} \code{percent_match}
\item \code{coverage} \eqn{\to} \code{percent_query_cover}
\item \code{testid} \eqn{\to} \code{assay_name}
\item \code{accession} \eqn{\to} \code{accession_id}
\item \code{kingdom}, \code{phylum}, \code{class}, \code{order}, \code{family}, \code{genus},
\code{species} -- unchanged (already match FAIRe / Darwin Core names)
}

\strong{Columns constructed:}
\itemize{
\item \code{verbatimIdentification} -- semicolon-delimited taxonomy string
(FAIRe convention) built from whichever of kingdom through species are
present and non-NA in each row.
\item \code{specificEpithet} -- species epithet extracted from the \code{species}
column (second word of the binomial), when \code{species} is present.
\item \code{checkls_ver} -- FAIRe checklist version (Mandatory field).
}

\strong{Scope:} The FAIRe checklist covers the full eDNA workflow including
sample collection, extraction, PCR, and sequencing. TaxaID outputs map
only to the \code{taxaRaw} and \code{taxaFinal} classes (bioinformatics taxonomic
assignment tables). Sample-level metadata (\code{samp_name}, \code{eventDate},
\code{decimalLatitude}, etc.) must be supplied by the user from field records
and joined to this output before submission.
}
\examples{
match_df <- data.frame(
  observation_id  = c("ASV1", "ASV2"),
  taxon_name      = c("Fundulus parvipinnis", "Atherinops affinis"),
  taxon_name_rank = c("species", "species"),
  score           = c(98.7, 95.1),
  coverage        = c(0.98, 0.94),
  accession       = c("MG002616.1", "KT215432.1"),
  family          = c("Fundulidae", "Atherinopsidae"),
  genus           = c("Fundulus", "Atherinops"),
  species         = c("Fundulus parvipinnis", "Atherinops affinis"),
  testid          = c("MiFishU", "MiFishU"),
  stringsAsFactors = FALSE
)

faire_df <- to_faire(match_df, table_type = "taxaRaw")
names(faire_df)
}
\references{
Takahashi et al. (2025). FAIRe: A metadata checklist for FAIR
environmental DNA data. \emph{Environmental DNA}.
\doi{10.1002/edn3.70100}
}
\seealso{
\code{\link[=rename_cols]{rename_cols()}} for general-purpose column renaming.
}
