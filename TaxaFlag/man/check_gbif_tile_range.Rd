% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_gbif_tile_range.R
\name{check_gbif_tile_range}
\alias{check_gbif_tile_range}
\title{Spatial-isolation signal for a taxon from GBIF's occurrence-density map tiles}
\usage{
check_gbif_tile_range(
  taxon_key,
  query_lat,
  query_lon,
  zoom = 6L,
  buffer_px = 512L,
  escalate = TRUE,
  min_zoom = 0L,
  base_url = "https://api.gbif.org/v2/map/occurrence/density"
)
}
\arguments{
\item{taxon_key}{Integer. GBIF backbone \code{usageKey} for the taxon. Resolve
via \code{TaxaFetch::get_keys_from_context()} or \code{rgbif::name_backbone()}.}

\item{query_lat, query_lon}{Numeric scalars. The point to check.
\code{query_lat} must be within Web Mercator's valid range
(approximately -85.05 to 85.05 -- the projection is undefined at the
poles).}

\item{zoom}{Integer in \verb{[0, 12]}. Starting tile zoom level. Controls both
the real-world size of each density cell and how much ground a fixed
\code{buffer_px} covers -- a coarser (smaller) zoom gives a wider, coarser
view for the same download cost; a finer (larger) zoom gives tighter
precision over a smaller area. Default \code{6L} is roughly continental
scale (tens to low hundreds of km per pixel, depending on latitude).}

\item{buffer_px}{Integer. How far around the query point to scan for the
nearest occupied cell, in tile pixels. Tiles are fetched in whole
512x512 blocks, so this is rounded up to the nearest multiple of 512.}

\item{escalate}{Logical. Widen the search to coarser zoom levels when
nothing is found at \code{zoom} -- see "Zoom escalation" above. Default
\code{TRUE}.}

\item{min_zoom}{Integer in \verb{[0, zoom]}. Coarsest zoom level escalation is
allowed to reach. Default \code{0L} (the whole world, one tile). Ignored
when \code{escalate = FALSE}.}

\item{base_url}{Character. GBIF map tile base URL. Exposed for testing.}
}
\value{
A one-row data frame:
\describe{
\item{taxon_key, query_lat, query_lon}{As supplied.}
\item{zoom_requested}{The \code{zoom} argument, as supplied.}
\item{zoom_used}{The zoom level the reported values were actually
computed at -- \code{zoom_requested} unless escalation stepped down to
find something. \code{NA} when \code{escalate = TRUE} (the default) and
nothing was found anywhere from \code{zoom} down to \code{min_zoom} (nothing
was ever "used"). When \code{escalate = FALSE}, this is always
\code{zoom_requested}, even if nothing was found there -- the single
attempted zoom is still reported, for reference.}
\item{escalated}{Logical. \code{TRUE} when \code{zoom_used != zoom_requested}
-- the answer came from a coarser search than requested.}
\item{tile_size}{Pixel width/height of one fetched tile (512, GBIF's
\verb{@1x.png} size).}
\item{n_tiles_fetched}{Total tiles downloaded across every zoom level
attempted -- transparency on the real cost of the call, including
any escalation.}
\item{resolution_km_per_px}{Real-world ground distance per pixel at
\code{query_lat}/\code{zoom_used} (Web Mercator distorts by latitude and
zoom) -- the unit \code{patch_size_px}/\code{patch_area_km2} are derived
from.}
\item{point_occupied}{Logical. Does the query point's own cell (at
\code{zoom_used}) have at least one occurrence?}
\item{dist_nearest_occupied_km}{Distance from the query point to the
nearest occupied cell found. \code{0} when \code{point_occupied}. \code{NA} when
\code{beyond_buffer}.}
\item{patch_size_px}{Size (in cells, at \code{zoom_used}) of the occupied
blob nearest the point (8-connected), grown from that nearest
cell. Growth is capped at a small fixed number of iterations for
bounded latency (see \code{patch_size_capped}), so this is exact for a
small/isolated patch (the case this function is for) but only a
lower bound once a patch is large enough to hit the cap. \code{NA} when
\code{beyond_buffer}.}
\item{patch_size_capped}{Logical. \code{TRUE} when patch growth hit the
iteration cap before converging -- \code{patch_size_px}/\code{patch_area_km2}
are then a lower bound, not exact. In practice this itself is
informative: a capped patch is, by construction, one that already
covers a wide area at \code{zoom_used}, i.e. clearly not an isolated
report. \code{NA} when \code{beyond_buffer}.}
\item{patch_area_km2}{\code{patch_size_px * resolution_km_per_px^2} -- the
pixel count converted to real-world area, since a raw pixel count
is only interpretable alongside the zoom it was measured at. A
lower bound when \code{patch_size_capped}. \code{NA} when \code{beyond_buffer}.}
\item{patch_diameter_km}{\code{sqrt(patch_size_px) * resolution_km_per_px}
-- a rough linear length scale for the same patch, in the same
units as \code{dist_nearest_occupied_km}, meant for eyeballing the two
against each other (e.g. "the isolated point is 340 km from a
patch about 15 km across"). A lower bound when \code{patch_size_capped}.
\code{NA} when \code{beyond_buffer}.}
\item{beyond_buffer}{Logical. With \code{escalate = TRUE} (default):
\code{TRUE} only when no occupied cell was found anywhere from \code{zoom}
down to \code{min_zoom} -- with the default \code{min_zoom = 0}, this means
the species has no occurrence anywhere in GBIF's density map at
all (a real, decisive finding, not just "outside this window").
With \code{escalate = FALSE}: \code{TRUE} when nothing was found within the
single requested \code{zoom}/\code{buffer_px} window.}
}
}
\description{
Downloads the GBIF occurrence-density PNG tile(s) covering a query point
and a surrounding buffer, and reports how isolated that point is from the
taxon's known range: whether the point itself falls in an occupied cell,
the real-world distance to the nearest occupied cell, and the size of the
occupied blob nearest the point (a weak proxy for "one lone report" vs.
"a small cluster of independent reports").
}
\details{
This is the cheap, global, approximate complement to
\code{\link[=compute_local_occurrence_distance]{compute_local_occurrence_distance()}}'s free, local, exact view: the
local function can only speak to whatever bounding box a study's own GBIF
fetch covered, while this one gives the missing global context (is a
species that is novel to this study also far from its known range
everywhere, or is it novel here simply because no one sampled here
before) for the cost of a handful of small tile downloads, regardless of
how many occurrence records exist worldwide.
}
\section{What the PNG can and can't tell you}{

GBIF's density tiles encode density as a colour ramp, which is not a
reliable source for exact counts. This function only ever reads the
\strong{alpha channel} (transparent = zero occurrences in that cell, any
non-zero alpha = at least one) -- a presence/absence signal, not a
decoded density value. \code{patch_size_px}/\code{patch_area_km2} are weak,
resolution-limited proxies for how much local support an isolated
detection has (a bigger blob suggests more than one independent nearby
report), not an exact record count; for that, follow up with a targeted
GBIF occurrence query at just the flagged point.
}

\section{What isolation does and doesn't mean}{

A point far from the nearest occupied cell is equally consistent with a
genuine rarity/vagrancy report and with a misidentified or mis-georeferenced
record -- this function cannot distinguish the two on geometry alone. Error
screening against implausible occurrences belongs to
\code{TaxaFetch::check_geographic_outliers()}; this function is for a
downstream, different question -- for a detection already judged
plausible, how far outside the species' main range does it sit.
}

\section{Zoom escalation}{

A fixed \code{zoom}/\code{buffer_px} only ever covers a bounded window (e.g. the
default is roughly continental scale) -- a species genuinely absent from
that window could be just outside it, or on another continent, and a flat
"not found" (\code{NA} distance) can't tell those apart. When \code{escalate = TRUE}
(the default) and nothing is found at \code{zoom}, the same \code{buffer_px} window
is re-tried one zoom level coarser (each step downward roughly doubles the
real-world ground the same pixel buffer covers), continuing down to
\code{min_zoom} (default \code{0L} -- the whole world in a single tile) until an
occupied cell is found or \code{min_zoom} is exhausted. This is cheap in the
common case: escalation only fires for calls that would otherwise return
\code{NA}, each step costs the same handful of tiles as the original call, and
it terminates as soon as anything is found -- confirmed on a real case
(a European species queried from the Great Lakes) resolving after 3 extra
steps at a real, plausible transatlantic distance, instead of \code{NA}. Set
\code{escalate = FALSE} to restore the original single-zoom, bounded-latency
behaviour, where \code{beyond_buffer} means only "not within this one window."
\code{resolution_km_per_px}/\code{dist_nearest_occupied_km}/\code{patch_size_px} etc. are
always reported at \code{zoom_used} (whichever level actually found something,
or the originally requested \code{zoom} if nothing was found at any level) --
compare against \code{zoom_used}, not \code{zoom}, when judging precision.
}

\examples{
\dontrun{
check_gbif_tile_range(
  taxon_key = 2379089, # Neogobius melanostomus
  query_lat = 41.67, query_lon = -87.15
)
}

}
\seealso{
\code{\link[=compute_local_occurrence_distance]{compute_local_occurrence_distance()}}
}
