% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_local_occurrence_distance.R
\name{compute_local_occurrence_distance}
\alias{compute_local_occurrence_distance}
\title{Distance from a query point to the nearest already-fetched GBIF occurrence}
\usage{
compute_local_occurrence_distance(
  taxon_names,
  query_lat,
  query_lon,
  occurrence_data,
  taxon_col = "taxon_name",
  lat_col = "decimalLatitude",
  lon_col = "decimalLongitude",
  date_col = NULL
)
}
\arguments{
\item{taxon_names}{Character vector. One or more taxon names to check
(e.g. every \code{consensus_taxon} flagged \code{"unexpected"}/\code{"unprecedented"}
in one review session). Matched exactly against
\code{occurrence_data[[taxon_col]]}.}

\item{query_lat, query_lon}{Numeric scalars. The point to measure distance
from -- typically the study site's own centroid.}

\item{occurrence_data}{Data frame of already-fetched occurrence records
(e.g. \code{all_occurrences}, \code{occurrences_clean}). Must contain \code{taxon_col},
\code{lat_col}, \code{lon_col}.}

\item{taxon_col, lat_col, lon_col}{Character scalars. Column names in
\code{occurrence_data}. Defaults match \code{TaxaFetch}'s DarwinCore-aligned
convention.}

\item{date_col}{Character or \code{NULL}. Optional column in \code{occurrence_data}
giving the nearest record's collection date/year (e.g. GBIF's own
\code{"year"} standard column). When supplied and present, the matched
nearest record's raw value is surfaced as \code{nearest_date} -- lets a
caller weigh a fresh vs. a decades-old nearest record differently
without a second lookup. Default \code{NULL} (no age column requested,
fully backward compatible -- \code{nearest_date} is simply absent from the
output). No parsing/normalization is done on the raw value; a \code{"year"}
column comes back as whatever numeric/character type it already was.}
}
\value{
A data frame, one row per unique entry in \code{taxon_names}:
\describe{
\item{taxon_name}{As supplied.}
\item{n_local_records}{Count of \code{occurrence_data} rows for this taxon
with non-missing coordinates. \code{0} for a taxon with no local record
at all -- for a genuinely unprecedented taxon this IS the answer,
not a failure to find one.}
\item{dist_nearest_km}{Geodesic (great-circle) distance in km from the
query point to the nearest such record. \code{NA} when
\code{n_local_records == 0}.}
\item{nearest_lat, nearest_lon}{Coordinates of that nearest record.
\code{NA} when \code{n_local_records == 0}.}
\item{nearest_date}{The nearest record's raw \code{date_col} value. Only
present when \code{date_col} is supplied and found in \code{occurrence_data}.
\code{NA} when \code{n_local_records == 0} or the nearest record's own
\code{date_col} value is missing.}
}
}
\description{
For each taxon in \code{taxon_names}, finds the nearest record of that taxon
already present in \code{occurrence_data} and reports the geodesic distance
from \code{query_lat}/\code{query_lon} to it. \code{occurrence_data} is meant to be data
a workflow already fetched for its own purposes (e.g. the
\code{all_occurrences}/\code{occurrences_clean} objects \code{TaxaFetch} produces during
the normal pipeline) -- this function costs nothing beyond arithmetic, no
new GBIF call, since that regional data is already in hand.
}
\details{
Designed as the free, local, exact complement to
\code{\link[=check_gbif_tile_range]{check_gbif_tile_range()}}'s cheap, global, approximate view: a taxon with
\code{n_local_records = 0} here is, by construction, exactly the situation that
produces \code{TaxaAssign::compute_group_priors()}'s
\code{consensus_has_occurrence_record = FALSE} (an "unprecedented" call from
\code{\link[=add_posthoc_assessment]{add_posthoc_assessment()}}), so this answers \emph{why} that flag fired using
data the pipeline already paid for, rather than re-querying GBIF. Because
\code{occurrence_data} is whatever bounding box a study's own GBIF fetch used,
this function can only speak to that regional extent -- a taxon absent
here may still occur well outside it; \code{\link[=check_gbif_tile_range]{check_gbif_tile_range()}} is the
tool for that wider question.
}
\examples{
occ <- data.frame(
  taxon_name        = c("Neogobius melanostomus", "Neogobius melanostomus"),
  decimalLatitude   = c(41.60, 42.10),
  decimalLongitude  = c(-87.10, -87.80),
  year              = c(2019, 2003)
)
compute_local_occurrence_distance(
  taxon_names     = c("Neogobius melanostomus", "Salmo salar"),
  query_lat       = 41.67, query_lon = -87.15,
  occurrence_data = occ,
  date_col        = "year"
)

}
\seealso{
\code{\link[=check_gbif_tile_range]{check_gbif_tile_range()}}
}
