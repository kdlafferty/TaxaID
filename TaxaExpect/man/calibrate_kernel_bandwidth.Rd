% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_kernel_bandwidth.R
\name{calibrate_kernel_bandwidth}
\alias{calibrate_kernel_bandwidth}
\title{Calibrate kernel bandwidths by leave-one-block-out composition prediction}
\usage{
calibrate_kernel_bandwidth(
  occurrence_data,
  site_habitat,
  lambda_grid = c(10, 25, 50, 100, 200),
  m_grid = 1,
  covariate_col = NULL,
  lambda_covariate_grid = NULL,
  block_size_deg = 0.5,
  min_block_records = 20L,
  smoothing = 0.5,
  taxon_col = "taxon_name",
  lat_col = "decimalLatitude",
  lon_col = "decimalLongitude",
  habitat_col = "main_habitat"
)
}
\arguments{
\item{occurrence_data, site_habitat, taxon_col, lat_col, lon_col, habitat_col}{As in \code{\link[=estimate_kernel_priors]{estimate_kernel_priors()}}.}

\item{lambda_grid}{Numeric vector of candidate geographic bandwidths (km).
Default \code{c(10, 25, 50, 100, 200)}.}

\item{m_grid}{Numeric vector of candidate back-off masses. Default \code{1}
(calibrate lambda only). Supply several values to tune \code{m} jointly.}

\item{covariate_col, lambda_covariate_grid}{Optional: a numeric record
column (e.g. depth) and candidate bandwidths for its kernel factor. When
supplied, every (lambda, lambda_covariate, m) combination is scored; the
block's mean covariate value stands in for the "site" value. \code{NULL}
(default) disables the covariate factor.}

\item{block_size_deg}{Numeric scalar: CV block size in degrees (default
\code{0.5}). A block enters scoring only if it holds at least
\code{min_block_records} records.}

\item{min_block_records}{Integer, default \code{20L}.}

\item{smoothing}{Laplace pseudo-count added to every species when forming
a predicted composition, so held-out species never score \code{-Inf}
(default \code{0.5}).}
}
\value{
A list with \describe{
\item{results}{Data frame: one row per parameter combination plus the
\code{regional} and \code{nearest_block} references; columns \code{lambda_km},
\code{lambda_covariate}, \code{m}, \code{mean_logloss} (simple mean over blocks),
\code{weighted_logloss} (record-weighted), \code{blocks_beating_nearest}.}
\item{best}{The row minimizing \code{weighted_logloss} among kernel rows.}
\item{n_blocks}{Number of scored blocks.}
}
}
\description{
Chooses \code{lambda_km} (and optionally the covariate bandwidth and the
back-off mass \code{m}) for \code{\link[=estimate_kernel_priors]{estimate_kernel_priors()}} empirically: records are
partitioned into spatial blocks; each block's species composition is
predicted from all records \emph{outside} it, using the same kernel
machinery evaluated at the block's own data centroid; predictions are
scored by per-record multinomial log-loss against the block's actual
records. Two reference predictors are always scored alongside for
context: \code{regional} (all held-out records, unweighted -- "is locality
worth anything?") and \code{nearest_block} (the single nearest other block --
the retired one-cell architecture).
}
\details{
The spatial blocks are a cross-validation device only -- they impose no
structure on the estimator being calibrated (this is the one place a grid
survives the kernel redesign, and the only job it keeps).
}
\seealso{
\code{\link[=estimate_kernel_priors]{estimate_kernel_priors()}}
}
