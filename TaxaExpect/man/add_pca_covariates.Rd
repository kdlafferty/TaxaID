% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_pca_covariates.R
\name{add_pca_covariates}
\alias{add_pca_covariates}
\title{Reduce Correlated Covariates via PCA Before Biodiversity Modeling}
\usage{
add_pca_covariates(model_df, cor_threshold = 0.7, prefix = "PC")
}
\arguments{
\item{model_df}{A data frame, typically the output of
\code{\link[=prepare_model_dataframe]{prepare_model_dataframe()}}.  Must contain at least two columns ending in
\code{"_s"} (scaled covariates produced by \code{prepare_model_dataframe()}).}

\item{cor_threshold}{Numeric scalar.  Pairs of \verb{_s} columns with
\verb{|r| > cor_threshold} are replaced by PCA scores.  Default \code{0.7} matches
the threshold used in \code{\link[=prepare_model_dataframe]{prepare_model_dataframe()}}.}

\item{prefix}{Character scalar.  Prefix for the new PC column names.
Default \code{"PC"}.  PC columns are named \verb{<prefix>1_s}, \verb{<prefix>2_s}, etc.}
}
\value{
The input data frame with correlated \verb{_s} columns replaced by PC
score columns (\verb{<prefix>N_s}).  Two attributes are set:
\describe{
\item{\code{scale_params}}{Preserved unchanged from \code{model_df} (original
covariate scaling parameters; retained for new-site prediction via
\code{\link[=apply_pca_transform]{apply_pca_transform()}}).}
\item{\code{pca_rotation}}{Named list:
\code{source_cols} (replaced \verb{_s} column names),
\code{pc_cols} (new PC column names),
\code{rotation} (PCA loading matrix, n_source × n_pc),
\code{prefix} (the prefix used).}
}
If no column pairs exceed \code{cor_threshold} the input is returned unchanged.
}
\description{
Replaces scaled covariate columns (\verb{<cov>_s}) that exceed a pairwise
correlation threshold with orthogonal PCA scores.  Use this when
\code{\link[=prepare_model_dataframe]{prepare_model_dataframe()}} warns about multicollinearity.

The output has the same structure as \code{\link[=prepare_model_dataframe]{prepare_model_dataframe()}} output and
can be passed directly to \code{\link[=train_biodiversity_model]{train_biodiversity_model()}}.
}
\details{
PCA is applied to all \verb{_s} columns involved in any correlated pair;
uncorrelated \verb{_s} columns are left in place.  All principal components are
retained — the output columns are orthogonal by construction, which
eliminates collinearity without discarding variance.

\code{prcomp()} is called with \code{center = TRUE} so PC scores are zero-mean and
exactly orthogonal.  The \verb{_s} columns are already ≈ 0-mean from
\code{prepare_model_dataframe()}; re-centering has negligible effect on values.

\strong{Prediction-time note:} \code{\link[=generate_full_priors]{generate_full_priors()}} scales new sites using the
original \code{scale_params} entries.  When a model was trained with PCA
covariates, call \code{\link[=apply_pca_transform]{apply_pca_transform()}} on the scaled new-site data before
passing it to \code{\link[=generate_full_priors]{generate_full_priors()}}.
}
\examples{
\dontrun{
model_df     <- prepare_model_dataframe(gridded_data,
                  covariates = c("lat_r", "lon_r", "depth"))
model_df_pca <- add_pca_covariates(model_df)
model_obj    <- train_biodiversity_model(
                  model_df_pca,
                  formula = cbind(n_species, n_other) ~
                    main_habitat + (1 | taxon_name) + (0 + PC1_s | taxon_name))
}

}
\seealso{
\code{\link[=prepare_model_dataframe]{prepare_model_dataframe()}}, \code{\link[=apply_pca_transform]{apply_pca_transform()}},
\code{\link[=train_biodiversity_model]{train_biodiversity_model()}}
}
