% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_theta_surface.R
\name{as.data.frame.taxaexpect_theta_surface}
\alias{as.data.frame.taxaexpect_theta_surface}
\title{Long-format data frame of a theta surface, for analysis}
\usage{
\method{as.data.frame}{taxaexpect_theta_surface}(
  x,
  row.names = NULL,
  optional = FALSE,
  taxon = NULL,
  drop_na = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{taxaexpect_theta_surface} object from \code{\link[=plot_theta_surface]{plot_theta_surface()}}.}

\item{row.names, optional}{Ignored; present for S3 consistency with
\code{\link[=as.data.frame]{as.data.frame()}}.}

\item{taxon}{Optional character vector restricting the output to specific
taxa (must be a subset of the taxa \code{x} was built with -- this does not
recompute the surface for a new taxon). Default \code{NULL}: every taxon in
\code{x}.}

\item{drop_na}{Logical (default \code{TRUE}). Drop rows where \code{theta} is \code{NA}
(outside a \code{mask}, or beyond where the fetch could support an estimate
at all). \code{FALSE} keeps every lattice cell, \code{NA}s included.}

\item{...}{Ignored.}
}
\value{
A data frame with columns \code{lon}, \code{lat}, \code{taxon}, \code{theta}, \code{n_eff},
\code{W} -- one row per requested lattice cell x taxon combination.
}
\description{
The rendered map is not the only thing a \code{taxaexpect_theta_surface} is
useful for -- \verb{$surface} already carries every value, but as one matrix per
taxon keyed by two separate lattice-axis vectors, which is awkward to
analyse directly (a caller has to know the row/column orientation, and
whether \verb{$surface$theta} is a bare matrix or a named list depends on
whether \code{plot_theta_surface()} was called with one taxon or several). This
unpacks it into one row per lattice cell x taxon: \code{lon}, \code{lat}, \code{taxon},
\code{theta}, \code{n_eff}, \code{W}.
}
\details{
A \code{mask} (or a lattice-edge cell too far from every record to be
evaluated at all) already leaves \code{theta} as \code{NA} there -- so
\code{drop_na = TRUE} (the default) is what makes this "theta within the
search polygon," with no separate spatial filter needed: the masking
already happened when the surface was built.
}
\examples{
\dontrun{
r <- plot_theta_surface(kp, occ, taxon = c("Species_A", "Species_B"))
df <- as.data.frame(r, taxon = "Species_A")
# e.g. the highest-theta cells inside the search polygon:
head(df[order(-df$theta), ], 10)
}
}
