% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_pca_covariates.R
\name{apply_pca_transform}
\alias{apply_pca_transform}
\title{Apply a Stored PCA Transformation to New Sites}
\usage{
apply_pca_transform(new_sites, pca_rotation)
}
\arguments{
\item{new_sites}{A data frame containing the \verb{_s} columns listed in
\code{pca_rotation$source_cols} (e.g., \code{lat_r_s}, \code{lon_r_s}).}

\item{pca_rotation}{The \code{pca_rotation} attribute from the output of
\code{\link[=add_pca_covariates]{add_pca_covariates()}}, or the \verb{$pca_rotation} slot of the fitted model
object returned by \code{\link[=train_biodiversity_model]{train_biodiversity_model()}}.}
}
\value{
\code{new_sites} with the source \verb{_s} columns replaced by the PC score
columns.
}
\description{
Applies the PCA rotation produced by \code{\link[=add_pca_covariates]{add_pca_covariates()}} to a new-sites
data frame that already has the source \verb{_s} columns (e.g., produced by
\code{\link[=generate_full_priors]{generate_full_priors()}}'s internal scaling step).

Use this as a pre-processing step before \code{\link[=generate_full_priors]{generate_full_priors()}} when the
training model used PCA-transformed covariates.
}
\examples{
\dontrun{
# Typical workflow when model was trained with PCA covariates:
pca_rot       <- attr(model_df_pca, "pca_rotation")
new_sites_pca <- apply_pca_transform(new_sites_scaled, pca_rot)
priors        <- generate_full_priors(model_obj, new_sites_pca)
}

}
\seealso{
\code{\link[=add_pca_covariates]{add_pca_covariates()}}, \code{\link[=generate_full_priors]{generate_full_priors()}}
}
