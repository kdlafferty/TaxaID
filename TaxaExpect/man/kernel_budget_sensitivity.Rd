% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_budget_sensitivity.R
\name{kernel_budget_sensitivity}
\alias{kernel_budget_sensitivity}
\title{Sensitivity of a kernel Good-Turing budget to its counting radius}
\usage{
kernel_budget_sensitivity(
  fit,
  occurrence_data,
  support_weight_grid = exp(-(1:5)),
  lambda_grid = NULL,
  verbose = FALSE
)
}
\arguments{
\item{fit}{A \code{"taxaexpect_kernel_priors"} object from
\code{\link[=estimate_kernel_priors]{estimate_kernel_priors()}}.}

\item{occurrence_data}{The same occurrence data \code{fit} was computed from. The
fit does not retain the records, so they must be supplied again; passing
different data silently answers a different question, and a mismatch at the
fit's own settings is reported by \verb{$reproduces_fit}.}

\item{support_weight_grid}{Numeric vector in (0, 1] of counting radii to
sweep. Default \code{exp(-(1:5))}, i.e. 1 to 5 bandwidths, which brackets the
\code{exp(-3)} default on both sides.}

\item{lambda_grid}{Optional numeric vector of geographic bandwidths (km).
\code{NULL} (default) sweeps only the counting radius, holding \code{lambda_km} at
the fit's own value -- the cleaner diagnostic, since it moves one boundary
and nothing else. Supply a grid to see the joint sensitivity.}

\item{verbose}{Logical. Message each setting as it is computed.}
}
\value{
An object of class \code{"taxaexpect_kernel_budget_sensitivity"}: a list
with
\describe{
\item{budget}{Long data frame, one row per (setting, sampling group):
the \verb{$budget} columns plus \code{lambda_km}, \code{support_weight},
\code{radius_lambdas} (\code{-log(support_weight)}) and \code{radius_km}.}
\item{summary}{One row per sampling group: the range of \code{f1}, \code{f2},
\code{chao_missing} and \code{theta_present} over the sweep,
\code{theta_present_spread} (max/min over settings where it is defined),
\code{theta_present_at_fit}, and \code{n_unpriced} (settings where the group had
no defined price).}
\item{at_fit}{The budget at the fit's own settings.}
\item{reproduces_fit}{Logical: whether the fit's own settings reproduced
the fit's \verb{$budget} exactly. \code{FALSE} means \code{occurrence_data} is not the
data \code{fit} was computed from.}
\item{params}{The sweep's own provenance.}
}
}
\description{
Re-computes the \verb{$budget} of a fitted \code{\link[=estimate_kernel_priors]{estimate_kernel_priors()}} object
across a grid of counting radii (\code{support_weight}) and, optionally,
geographic bandwidths (\code{lambda_km}), and summarizes how far each group's
budget quantities move. Report this next to any \code{chao_missing} or
\code{theta_present} figure you intend to act on.
}
\details{
\code{support_weight} is the kernel weight at which a record starts counting
toward the discrete support statistics \code{f1} (singletons) and \code{f2}
(doubletons). It is a pure counting boundary: moving it changes neither
\code{theta} nor \code{n_eff} nor \code{missing_mass}'s definition, only which records are
inside the neighborhood being counted. Sweeping it therefore isolates the
budget's dependence on that boundary from every other modelling choice.

\code{chao_missing} is \verb{f1^2 / (2 f2)}, so it is quadratic in \code{f1} and inverse in
\code{f2}. With \code{f2} in single digits -- the usual case for anything but the
largest group -- one doubleton entering or leaving the neighborhood moves
the estimate by tens of percent. The \code{f2_min} column of \verb{$summary} is the
number to look at first for that figure. \code{theta_present} is priced from
\code{missing_mass / f1} (2026-09-05, open decision #1 of the kernel budget/
pricing re-entry doc, resolved), so it no longer inherits \code{f2}'s
instability -- but it still moves with the counting radius through \code{f1} and
\code{missing_mass} themselves, which is what \code{theta_present_spread} reports.

The sweep calls \code{\link[=estimate_kernel_priors]{estimate_kernel_priors()}} itself rather than recomputing
the statistics from the fit, so the numbers reported here cannot drift from
what the estimator would say. That costs one O(records) pass per setting
(~2 s per pass at 2 million records).
}
\examples{
occ <- data.frame(
  taxon_name = c(rep(c("A", "B", "C"), each = 8), "D", "E", "F", "G"),
  decimalLatitude = 34 + rnorm(28, 0, 0.1),
  decimalLongitude = -119 + rnorm(28, 0, 0.1),
  main_habitat = "Marine"
)
fit <- estimate_kernel_priors(occ, 34, -119, "Marine", lambda_km = 25)
kernel_budget_sensitivity(fit, occ)
}
\seealso{
\code{\link[=estimate_kernel_priors]{estimate_kernel_priors()}}, \code{\link[=calibrate_kernel_bandwidth]{calibrate_kernel_bandwidth()}}
}
