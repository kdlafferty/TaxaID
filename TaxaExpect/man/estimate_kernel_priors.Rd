% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_kernel_priors.R
\name{estimate_kernel_priors}
\alias{estimate_kernel_priors}
\title{Estimate site priors by distance-kernel weighting of occurrence records}
\usage{
estimate_kernel_priors(
  occurrence_data,
  site_lat,
  site_lon,
  site_habitat,
  lambda_km,
  m = 1,
  covariate_col = NULL,
  site_covariate = NULL,
  lambda_covariate = NULL,
  lambda_latitude = NULL,
  site_id = NULL,
  taxon_col = "taxon_name",
  lat_col = "decimalLatitude",
  lon_col = "decimalLongitude",
  habitat_col = "main_habitat",
  support_weight = exp(-3)
)
}
\arguments{
\item{occurrence_data}{Data frame of cleaned, habitat-labelled occurrence
records (one row per record).}

\item{site_lat, site_lon}{Numeric scalars. The sampling site coordinates.}

\item{site_habitat}{Character scalar. Focal habitat; records are
stratified to \code{habitat_col == site_habitat} before any weighting.}

\item{lambda_km}{Numeric scalar. Geographic kernel bandwidth in km.
Required, no default: the defensible value is dataset-specific -- derive
it with \code{\link[=calibrate_kernel_bandwidth]{calibrate_kernel_bandwidth()}} (leave-one-block-out composition
prediction) rather than guessing.}

\item{m}{Numeric scalar >= 0. Regional pseudo-records for the Dirichlet
back-off (default \code{1}, a weakly-informative single pseudo-record;
tunable via \code{\link[=calibrate_kernel_bandwidth]{calibrate_kernel_bandwidth()}}). \code{m = 0} disables
shrinkage.}

\item{covariate_col}{Optional character. Name of a numeric record column
(e.g. \code{"depth_m"}) for the product kernel. \code{NULL} (default) disables it.}

\item{site_covariate}{Numeric scalar. The site's own value of
\code{covariate_col}. Required when \code{covariate_col} is supplied.}

\item{lambda_covariate}{Numeric scalar. Covariate kernel bandwidth, in the
covariate's own units. Required when \code{covariate_col} is supplied.
Records with \code{NA} covariate values receive the neutral weight 1 for the
covariate factor (distance factor still applies) rather than being
dropped.}

\item{lambda_latitude}{Optional numeric scalar. Bandwidth (km) for a
climate-similarity factor \verb{exp(-111 * ||lat| - |site_lat|| / lambda_latitude)} on the ABSOLUTE latitude difference -- records from
the site's own climate band (same |latitude|, either hemisphere) keep
full weight while records poleward/equatorward of it decay, making
north-south kilometers cost more than east-west kilometers. \code{NULL}
(default) disables the factor. Tunable via
\code{\link[=calibrate_kernel_bandwidth]{calibrate_kernel_bandwidth()}}'s \code{lambda_latitude_grid}.}

\item{site_id}{Character scalar. Identifier stamped on the output rows'
\code{grid_id} column (kept under that name for downstream join
compatibility; the value is treated as opaque everywhere downstream).
Default: \code{"Site_<lat>_<lon>"}.}

\item{taxon_col, lat_col, lon_col, habitat_col}{Column names in
\code{occurrence_data} (defaults \code{"taxon_name"}, \code{"decimalLatitude"},
\code{"decimalLongitude"}, \code{"main_habitat"}).}

\item{support_weight}{Numeric in (0, 1]. A record counts toward the
discrete neighborhood-support statistics (singleton detection, record
counts) when its total kernel weight is at least
\code{support_weight} (default \code{exp(-3)}, i.e. within ~3 bandwidths).
Continuous quantities (\code{theta}, \code{n_eff}) always use all records.}
}
\value{
An object of class \code{"taxaexpect_kernel_priors"}: a list with
\describe{
\item{priors}{Data frame: \code{taxon_name}, \code{grid_id}, \code{main_habitat},
\code{alpha}, \code{beta}, \code{theta_mean}, \code{theta_sd}, \code{prior_branch},
\code{effective_records}.}
\item{n_eff}{Kish effective sample size at the site.}
\item{W}{Total kernel weight.}
\item{singletons}{Data frame of neighborhood singletons (species with
exactly one supporting record): \code{taxon_name}, record \code{lat}/\code{lon},
\code{weight}, \code{effective_records}.}
\item{missing_mass}{Weighted Good-Turing missing mass: the summed
effective share of singleton species -- the budget available to the
resident-undetected branch (consumed by later machinery, not here).}
\item{f1, f2}{Neighborhood-support singleton and doubleton species
counts (records with kernel weight >= \code{support_weight}).}
\item{chao_missing}{Chao (1984) estimated number of locally-present
but unrecorded species: \code{f1^2/(2*f2)}, with the standard
\code{f1*(f1-1)/2} fallback when \code{f2 = 0}; \code{0} when
\code{f1 = 0}.}
\item{theta_present}{\code{missing_mass / chao_missing}: the typical
unseen resident's share-if-present -- the per-spot value of the
Good-Turing budget, used by
\code{\link{apply_undetected_evidence}}'s curve pricing.
\code{NA} when no singleton anchor exists.}
\item{regional_composition}{Named numeric: the unweighted
habitat-stratified composition used for back-off.}
\item{params}{The call's tuning values, for provenance.}
}
}
\description{
Computes each species' expected share of legitimate detections at a
sampling site (\code{theta}) directly from habitat-stratified occurrence
records, weighting every record by its distance to the site:
\deqn{w_r = \exp(-d_r/\lambda) \times \exp(-|c_r - c_{site}|/\lambda_c)}
where \eqn{d_r} is the great-circle-approximate distance (km, cosine-
corrected longitude) and the optional second factor is a covariate kernel
(e.g. depth -- distinguishes pelagic from nearshore species; see
\code{covariate_col}). Species shares are shrunk toward the regional
composition by \code{m} pseudo-records (Dirichlet back-off):
\deqn{\theta_i = (c_i s + m p_i) / (n_{eff} + m)}
with \eqn{c_i} the species' summed record weights, \eqn{s = n_{eff}/W} the
effective-scale factor, \eqn{W} the total weight, \eqn{n_{eff} = W^2 /
\sum w^2} (Kish effective sample size), and \eqn{p_i} the regional
(unweighted, habitat-stratified) composition. \code{alpha}/\code{beta} carry a
Beta parameterization with total concentration \eqn{n_{eff} + m}, so the
prior's spread honestly reflects how many effective records back it.
}
\details{
A hard grid cell is the special case of a top-hat kernel: with all
weights equal, \code{theta} reduces to ordinary record shares, \code{n_eff} to the
record count, and the singleton set to species seen exactly once.
}
\section{Why an estimator, not a spatial model}{

Leave-one-cell-out validation on real Great Lakes data (2026-08-30 Phase 1
diagnostic) found distance-kernel composition prediction beats both a
single nearest cell (the previous architecture; worst of all options
tested) and unweighted regional pooling, with an interior bandwidth
optimum. The kernel runs in O(records) with no optimization step (~0.1 s
at 1.8 million records), so cost is independent of species count. Do NOT
replace this with per-species latent spatial fields (GP/SPDE/GAMM): that
family was evaluated during the original model design and is
computationally infeasible at many-species scale.
}

\section{Output schema (kernel-priors redesign)}{

The returned \verb{$priors} table uses the post-redesign schema: \code{prior_branch}
(here always \code{"resident_observed"}: species with real local evidence in
the focal habitat) and \code{effective_records} (the species' kernel-effective
record count \code{c_i * n_eff / W}, in units of records) replace the retired
\code{model_tier} tier1/tier2 vocabulary. Undetected/evidence/domestic rows
belong to other branches and are appended by their own generators, not
this function.
}

\seealso{
\code{\link[=calibrate_kernel_bandwidth]{calibrate_kernel_bandwidth()}} to choose \code{lambda_km},
\code{lambda_covariate}, and \code{m} by held-out prediction.
}
