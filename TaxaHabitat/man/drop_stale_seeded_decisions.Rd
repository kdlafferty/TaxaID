% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_review_decisions.R
\name{drop_stale_seeded_decisions}
\alias{drop_stale_seeded_decisions}
\title{Drop Seeded Review Decisions That the Automatic Classifier Has Since Overtaken}
\usage{
drop_stale_seeded_decisions(
  occurrence_data,
  path,
  seeded_pattern = "^seeded from",
  dry_run = TRUE,
  backup = TRUE
)
}
\arguments{
\item{occurrence_data}{A dataframe freshly returned by
\code{\link[=flag_habitat_inconsistencies]{flag_habitat_inconsistencies()}} -- i.e. carrying the CURRENT automatic
\code{spatial_flag}, before any decisions are applied.}

\item{path}{Character. Path to the \verb{*_spatial_review_decisions.rds} file.}

\item{seeded_pattern}{Character regex identifying seeded rows by their
\code{decided_at} value. Default \code{"^seeded from"}, which is what
\code{\link[=save_spatial_review_decisions]{save_spatial_review_decisions()}} writes.}

\item{dry_run}{Logical. When \code{TRUE} (default) nothing is written; the
function reports what it would drop. Set \code{FALSE} to rewrite the file.}

\item{backup}{Logical. When writing, first copy the existing file to
\verb{<path>.bak_<timestamp>}. Default \code{TRUE}.}
}
\value{
Invisibly, a list with \code{n_decisions}, \code{n_seeded}, \code{n_real},
\code{n_stale}, \code{stale_point_ids} and \code{path}. Called for its message output and,
when \code{dry_run = FALSE}, its side effect.
}
\description{
A decision file seeded with \code{before = NULL} records "accept the automatic
classification" for every point, with \code{decided_at} set to a
\code{"seeded from ..."} string rather than a timestamp. Those are not reviewer
judgements. When the automatic classifier later changes its mind about a
point -- because a bug was fixed, a habitat vocabulary was extended, or a
threshold moved -- the seeded verdict silently overrides the new one, and
\code{\link[=apply_spatial_review_decisions]{apply_spatial_review_decisions()}} reports \code{n_pending_review = 0}. The site
then looks fully reviewed while carrying the old classifier's answer.
}
\details{
This function removes exactly those entries: \strong{seeded} decisions whose
automatic verdict has changed. They return to the pending pool and the review
gadget asks about them.
}
\section{Why real decisions are never dropped}{

A reviewer who marked a point \code{likely} when the classifier said \code{unlikely}
did so deliberately, and a later change of the classifier's mind must not
erase that. Only rows whose \code{decided_at} matches \code{seeded_pattern} are
eligible, so a genuine review is preserved even when the automatic verdict
moves underneath it. Measured on the three production files,
\strong{all 244,860 decisions were seeded and none were real} -- so on those files
every changed point is eligible, which is the situation this exists for.
}

\examples{
\dontrun{
flagged <- flag_habitat_inconsistencies(occurrences_with_habitat)
# look first
drop_stale_seeded_decisions(flagged, "Site_spatial_review_decisions.rds")
# then act
drop_stale_seeded_decisions(flagged, "Site_spatial_review_decisions.rds",
                            dry_run = FALSE)
}
}
\seealso{
\code{\link[=save_spatial_review_decisions]{save_spatial_review_decisions()}}, \code{\link[=apply_spatial_review_decisions]{apply_spatial_review_decisions()}}
}
