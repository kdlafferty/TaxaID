% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_habitat_lookup.R
\name{build_habitat_lookup}
\alias{build_habitat_lookup}
\title{Build a per-taxon habitat lookup with an on-disk cache}
\usage{
build_habitat_lookup(
  taxon_list,
  habitat_scheme = NULL,
  llm_fn = getOption("TaxaID.llm_fn", TaxaTools::call_api),
  cache_dir = NULL,
  extra_covariates = character(0),
  chunk_size = 60L,
  geographic_context = NULL,
  cache_tag = "",
  pause_seconds = 1,
  verbose = TRUE
)
}
\arguments{
\item{taxon_list}{Character vector of taxon names.}

\item{habitat_scheme}{Passed to \code{\link[=build_habitat_prompt]{build_habitat_prompt()}}: \code{NULL} (the
three-realm default), \code{"IUCN_L1"}, or a scheme data frame.}

\item{llm_fn}{Passed to \code{TaxaTools::prompt_api()}. Defaults to the
\code{TaxaID.llm_fn} option, else \code{TaxaTools::call_api}.}

\item{cache_dir}{Character or \code{NULL} (default). Directory for the
per-taxon cache. \code{NULL} disables caching entirely (identical to the
uncached three-step pattern). Workflows should pass a project-local
directory, e.g. \code{file.path(OUT_DIR, paste0(OUT_PREFIX, "_habitat_cache"))}.}

\item{extra_covariates, chunk_size, geographic_context}{Passed to
\code{\link[=build_habitat_prompt]{build_habitat_prompt()}}.}

\item{cache_tag}{Character (default \code{""}). Free-text component of the
cache key; change it to force fresh verdicts (e.g. after a model change).}

\item{pause_seconds, verbose}{Passed to \code{TaxaTools::prompt_api()}.}
}
\value{
A data frame with one row per unique taxon in \code{taxon_list}
(the same shape \code{\link[=parse_hierarchical_habitat_response]{parse_hierarchical_habitat_response()}} returns), in
\code{taxon_list} order, with a \code{cache_summary} attribute:
\code{n_total}, \code{n_from_cache}, \code{n_called}, \code{n_cached_new}.
}
\description{
One call that does what the three-step pattern
\code{\link[=build_habitat_prompt]{build_habitat_prompt()}} -> \code{TaxaTools::prompt_api()} ->
\code{\link[=parse_hierarchical_habitat_response]{parse_hierarchical_habitat_response()}} does, but only asks the LLM about
taxa it has not already classified under the same scheme. The result is
the \code{habitats_df} that \code{\link[=assign_habitat_biological]{assign_habitat_biological()}} consumes.
}
\section{Why cache this at all}{

A habitat verdict decides which occurrence records count toward a
habitat-stratified site prior in TaxaExpect. If the LLM answers
differently on two runs, a species can move from "locally observed" to
"locally undetected" (or back) with no change in the data, and every
downstream call that species competes in moves with it. Caching makes an
identical re-run reproduce the same priors, and makes the habitat step
free after the first run. Nothing here changes what the LLM is asked or
how its answer is parsed.
}

\section{What the cache key covers}{

The taxon name, the habitat columns the scheme asks for (derived exactly
as \code{\link[=build_habitat_prompt]{build_habitat_prompt()}} derives them, so a changed scheme is a miss),
\code{extra_covariates}, \code{geographic_context}, and \code{cache_tag}. The LLM
function itself cannot be hashed -- if you change providers or models and
want fresh verdicts, pass a new \code{cache_tag} (e.g. the model name) or
clear the directory with \code{\link[=taxahabitat_clear_cache]{taxahabitat_clear_cache()}}.
}

\section{What is and is not cached}{

Only a taxon whose parsed row carries a resolved \code{Habitat} (at least one
non-zero weight) is written. A taxon the LLM skipped, or that sat in a
failed chunk, comes back with \code{Habitat = NA} and is NOT cached, so it is
re-asked on the next run instead of being frozen as unknown.
}

\examples{
\dontrun{
scheme <- data.frame(l1_name = c("Lentic", "Lotic"), stringsAsFactors = FALSE)
habitat_lookup <- build_habitat_lookup(
  unique(occurrences$taxon_name),
  habitat_scheme = scheme,
  llm_fn = function(p, ...) TaxaTools::call_anthropic_api(p),
  cache_dir = file.path(OUT_DIR, paste0(OUT_PREFIX, "_habitat_cache"))
)
attr(habitat_lookup, "cache_summary")
}
}
\seealso{
\code{\link[=build_habitat_prompt]{build_habitat_prompt()}}, \code{\link[=parse_hierarchical_habitat_response]{parse_hierarchical_habitat_response()}},
\code{\link[=assign_habitat_biological]{assign_habitat_biological()}}, \code{\link[=taxahabitat_clear_cache]{taxahabitat_clear_cache()}}
}
