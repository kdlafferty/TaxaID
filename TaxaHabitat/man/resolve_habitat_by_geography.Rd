% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_habitat_geography.R
\name{resolve_habitat_by_geography}
\alias{resolve_habitat_by_geography}
\title{Resolve Unassigned Habitats From Point Geography}
\usage{
resolve_habitat_by_geography(
  occurrence_data,
  habitat_col = "main_habitat",
  lat_col = "decimalLatitude",
  lon_col = "decimalLongitude",
  candidate_mass = 0.8,
  coast_buffer_m = 1000,
  verbose = TRUE
)
}
\arguments{
\item{occurrence_data}{Output of \code{\link[=assign_habitat_biological]{assign_habitat_biological()}}, carrying a
\code{"habitat_proportions"} attribute. Without it nothing can be resolved and
the input is returned unchanged with a message.}

\item{habitat_col, lat_col, lon_col}{Column names.}

\item{candidate_mass}{Passed to the candidate rule; see
\code{\link[=review_spatial_flags]{review_spatial_flags()}}.}

\item{coast_buffer_m}{Half-width of the coastal band, in metres. Points
within it are treated as shoreline and left unresolved. Default \code{1000},
matching \code{\link[=flag_habitat_inconsistencies]{flag_habitat_inconsistencies()}}.}

\item{verbose}{Logical. Report what was resolved.}
}
\value{
\code{occurrence_data} with \code{main_habitat} filled in where geography was
decisive, plus a \code{habitat_source} column. The \code{"habitat_proportions"}
attribute is preserved.
}
\description{
Fills in \code{main_habitat} for points the assemblage consensus could not
resolve, by asking where the point actually is. A taxon whose weights span
freshwater, estuarine and marine is not uncertain about its habitat -- it
uses all three -- so over open ocean it is marine, and inland it is not.
The consensus threshold cannot express that, because it never sees the
location.
}
\details{
Only points with \strong{exactly one} admissible candidate are resolved. A point
at the shoreline, where the map cannot separate marine from estuarine from
terrestrial, is left unassigned deliberately: that is a real ambiguity and
belongs in \code{\link[=review_spatial_flags]{review_spatial_flags()}}, not in an automatic rule.
}
\section{What resolves, and what does not}{

\describe{
\item{ocean}{Only a marine, non-estuarine candidate.}
\item{inland}{Any non-marine candidate. Resolves only when one such
candidate exists -- a \code{Freshwater | Terrestrial} point inland stays
ambiguous, because without a lake or river layer the map cannot tell a
pond from a hillside.}
\item{coastal}{Nothing. Every habitat is admissible within
\code{coast_buffer_m} of the shore, so geography discriminates nothing.}
}
\strong{Measured on the full production PtConception 12S pool} (220,311 points,
31,383 unassigned): \strong{6,222 points resolved, 20\%} -- 2,940 ocean, 3,282
inland -- leaving 25,161 where geography discriminates nothing. 5.9 seconds.

A 6,523-point coastal extract of the same site gave 34\% (221 of 646), and
that figure was previously quoted here. It is \strong{not representative}: the
extract is coastal-heavy, and its unassigned points are Cottidae, whereas
production's are 97\%-by-record birds (Laridae, Anatidae, Gaviidae). Quote
the production number. The general lesson is that a fixture chosen for
convenience validated the mechanism without exercising the case that
matters.

What it resolves in production is itself worth knowing: \strong{27,400 records, of
which 97\% are birds} -- Laridae 17,737, Anatidae 3,095, Gaviidae 2,399,
Phalacrocoracidae 2,004. Those records are currently excluded from the
resident prior only because their habitat is unassigned, which is an
accident rather than a scope decision. If birds are out of scope for the
marker, filter families before the fetch; do not rely on this function
declining to resolve them. Note also that the remaining 467,614 unassigned
records stay unassigned -- most bird records sit at coastal points, where
nothing resolves -- so the volume entering the prior is far smaller than the
unassigned pool's size suggests.
}

\section{Provenance}{

A resolved point gets \code{habitat_source = "geography"}; a point the consensus
had already settled keeps \code{"consensus"}. A geography-resolved habitat is a
weaker claim than an assemblage consensus and it feeds habitat-stratified
priors, so the distinction is recorded in the data rather than left to be
inferred. The point's \code{habitat_proportions} vector is \strong{not} rewritten --
the taxon really does use those habitats; only \code{main_habitat} is decided.
}

\examples{
\dontrun{
occ <- assign_habitat_biological(occurrences, habitat_lookup, threshold = 0.5)
occ <- resolve_habitat_by_geography(occ)
flagged <- flag_habitat_inconsistencies(occ)
}
}
\seealso{
\code{\link[=assign_habitat_biological]{assign_habitat_biological()}}, \code{\link[=flag_habitat_inconsistencies]{flag_habitat_inconsistencies()}},
\code{\link[=review_spatial_flags]{review_spatial_flags()}}
}
