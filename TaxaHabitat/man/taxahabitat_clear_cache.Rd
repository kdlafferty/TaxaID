% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_habitat_lookup.R
\name{taxahabitat_clear_cache}
\alias{taxahabitat_clear_cache}
\title{Report and clear TaxaHabitat's on-disk habitat-verdict cache}
\usage{
taxahabitat_clear_cache(
  cache_dir = tools::R_user_dir("TaxaHabitat", "cache"),
  older_than_days = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{cache_dir}{Character. The directory passed to
\code{\link[=build_habitat_lookup]{build_habitat_lookup()}}'s \code{cache_dir}. Defaults to
\code{tools::R_user_dir("TaxaHabitat", "cache")}, matching the sibling
packages; workflows that pass a project-local directory should pass the
same one here.}

\item{older_than_days}{Optional numeric. Delete only entries older than
this many days. \code{NULL} (default) considers every entry.}

\item{dry_run}{Logical. \code{TRUE} reports what would be deleted without
deleting it.}
}
\value{
Invisibly, the inventory data frame
(\code{TaxaTools::list_cache_files()} output) of the files considered.
}
\description{
Lists, and optionally deletes, the per-taxon files written by
\code{\link[=build_habitat_lookup]{build_habitat_lookup()}} when it is given a \code{cache_dir}. Entries have no
built-in expiry -- a verdict stays valid until the scheme, covariates,
context, or \code{cache_tag} change, which changes its key and makes it a miss
anyway -- so pruning is about disk usage and about deliberately forcing a
fresh classification, not about correctness.
}
\examples{
\dontrun{
taxahabitat_clear_cache(dry_run = TRUE)
}
}
\seealso{
\code{\link[=build_habitat_lookup]{build_habitat_lookup()}}, \code{TaxaTools::report_and_clear_cache()},
\code{TaxaFlag::taxaflag_clear_cache()}
}
