# TaxaID Name Change History
# Full log of function, column, parameter, and file renames across the ecosystem.
# Archived from CLAUDE.md (Sessions 19–123, most recently Session 124). See CLAUDE.md for anything newer.

| Date | Old name | New name | Package | Type | Downstream impact |
|---|---|---|---|---|---|
| 2026-02-18 | `f_spellcheck_sci_names` | `verify_sci_names` | TaxaTools | function | None yet |
| 2026-02-18 | `spellchecked_names` | `matched_name` | TaxaTools | column | Check scripts |
| 2026-02-18 | `bestResult_classificationPath` | `classification_path` | TaxaTools | column | Check scripts |
| 2026-02-18 | `bestResult_classificationRanks` | `classification_ranks` | TaxaTools | column | Check scripts |
| 2026-02-27 | `integrate_local_sources` | `combine_occurrence_sources` | TaxaExpect | function | No callers yet |
| 2026-02-27 | `make_hierarchical_habitat_prompt` | `build_habitat_prompt` | TaxaExpect | function | Now returns S3 object |
| 2026-02-27 | `call_anthropic_api` | `prompt_anthropic_api` | TaxaExpect | function | Now takes `habitat_prompt` object |
| 2026-02-27 | `submit_manual` | `prompt_manual` | TaxaExpect | function | Now takes `habitat_prompt` object |
| 2026-02-27 | `make_habitat_prompt` | *(deleted)* | TaxaExpect | function | Flat pipeline removed |
| 2026-02-27 | `assign_habitat_llm` | *(deleted)* | TaxaExpect | function | Flat pipeline removed |
| 2026-02-27 | `parse_habitat_response` | *(deleted)* | TaxaExpect | function | Flat pipeline removed |
| 2026-02-27 | `plot_raw_habitat_points` | *(deleted)* | TaxaExpect | function | Superseded |
| 2026-02-28 | `removeShape` | `removeMarker` | TaxaExpect | internal | Fixed deselect bug |
| 2026-02-28 | `filter_gbif_quality` | `filter_gbif_quality` | TaxaExpect | param added | New `max_coord_uncertainty` arg |
| 2026-03-01 | `build_neighbor_graph` | *(deleted)* | TaxaExpect | function | Superseded |
| 2026-03-01 | `compute_species_amplitude` | *(deleted)* | TaxaExpect | function | Superseded |
| 2026-03-01 | `update_theta_local` | *(deleted)* | TaxaExpect | function | Superseded |
| 2026-03-01 | `calibrate_prior_cap` | *(deleted)* | TaxaExpect | function | Superseded |
| 2026-03-04 | `"ok"` flag value | `"likely"` | TaxaExpect | `spatial_flag` value | Update filter scripts |
| 2026-03-04 | `"suspect"` flag value | `"questionable"` | TaxaExpect | `spatial_flag` value | Update filter scripts |
| 2026-03-04 | `"likely_error"` flag value | `"unlikely"` | TaxaExpect | `spatial_flag` value | Update filter scripts |
| 2026-03-10 | `CLAUDE_CONTEXT.md` | `AI_CONTEXT.md` | Ecosystem | context file | — |
| 2026-03-26 | `AI_CONTEXT.md` | `CLAUDE.md` | Ecosystem | context file | Renamed for Claude Code auto-detection |
| 2026-03-26 | `ecosystem_docs/CLAUDE.md` | `TaxaID/CLAUDE.md` | Ecosystem | context file | Moved to parent dir for true auto-detection |
| 2026-03-13 | *(Session 19)* | TaxaExpect split → TaxaFetch | Ecosystem | package | TaxaFetch created |
| 2026-03-13 | `combine_occurrence_sources` | *(retired)* | TaxaExpect/TaxaFetch | function | Replaced by `rename_cols()` + `stack_occurrences()` |
| 2026-03-13 | *(new)* | `rename_cols()` | TaxaTools | function | General DwC column rename utility |
| 2026-03-13 | *(new)* | `stack_occurrences()` | TaxaFetch | function | Row-bind occurrence frames |
| 2026-03-14 | *(Session 20)* | Weighted multi-habitat pipeline | TaxaFetch | breaking change | Wide weighted output replaces long format |
| 2026-03-15 | *(Session 21)* | Habitat scheme pipeline redesign | TaxaFetch | breaking change | NULL default → 3-category |
| 2026-03-24 | `prompt_anthropic_api` | `prompt_api` | TaxaFetch | function | All workflow scripts updated Session 26 |
| 2026-03-24 | *(Session 26 — new)* | `call_gemini_api()` | TaxaFetch | function | Google Gemini provider |
| 2026-03-24 | *(Session 26 — new)* | `call_openai_api()` | TaxaFetch | function | OpenAI provider |
| 2026-03-24 | *(Session 26 — new)* | `call_ollama_api()` | TaxaFetch | function | Ollama local provider |
| 2026-03-26 | *(Session 27 — fix)* | bracket strip order | TaxaTools | bug fix | `clean_taxon_names()`: brackets now stripped before capital-letter filter |
| 2026-03-26 | `verify_sci_names` | `verify_taxon_names` | TaxaTools | function | Renamed for consistency |
| 2026-03-26 | `create_taxon_name` | `create_taxon_names` | TaxaTools | function | Renamed (plural) |
| 2026-03-27 | `screen_spatial_formula` | `screen_spatial_formula` | TaxaHabitat → TaxaExpect | function | Moved; belongs with biodiversity modelling pipeline |
| 2026-03-27 | *(Session 30 — scope change)* | TaxaMatch revised to thin shell | TaxaMatch | package | Modeling functions moved to new TaxaLikely package |
| 2026-03-27 | *(Session 30 — new)* | TaxaLikely created | Ecosystem | package | Score→likelihood conversion; source: Universal_Biological_Classifier_Working_2.R |
| 2026-03-27 | *(Session 32 — breaking)* | `evaluate_likelihoods()` return type | TaxaLikely | interface | Now returns list `$likelihoods` + `$unresolved`; callers use `result$likelihoods` |
| 2026-03-27 | *(Session 32)* | `audit_reference_coverage()` `database` param removed | TaxaLikely | function | NCBI-only now via rentrez; taxize dependency removed |
| 2026-03-28 | *(Session 33 — new)* | `audit_barcode_coverage()` | TaxaLikely | function | Barcode-aware unreferenced species detection via NCBI nucleotide; replaces `audit_reference_coverage()` for eDNA. |
| 2026-03-28 | *(Session 33 — planned → Session 40)* | `expand_unreferenced_hypotheses()` | TaxaAssign | function | Moved to TaxaAssign (convergence point). |
| 2026-03-28 | *(Session 33 — fix)* | `assign_taxa_llm()` JSON parse regex | TaxaAssign | bug fix | Added `(?s)` PCRE flag; fixed range_status all-NA |
| 2026-03-29 | *(Session 34 — redesign)* | `audit_barcode_coverage()` | TaxaLikely | function | Corrected unreferenced definition; census redesigned: `in_reference`/`has_seqs_not_in_ref`/`unreferenced`/`is_complete`. `species_list` param added. |
| 2026-03-29 | *(Session 35)* | `suggest_unreferenced_species()` | TaxaAssign | function | Implemented in TaxaAssign. Returns `unreferenced_species_result` S3 object. |
| 2026-03-30 | *(Session 36)* | `expand_to_family` param | TaxaAssign | `suggest_unreferenced_species()` | Family-level unreferenced taxon expansion. |
| 2026-03-30 | *(Session 36)* | `habitat_fit` field | TaxaAssign | `assign_taxa_llm()` output | LLM returns `habitat_fit` ("expected"/"occasional"/"unlikely"). |
| 2026-03-30 | *(Session 36)* | `known_present`, `known_absent`, `absent_detection_prob` | TaxaAssign | `assign_taxa_llm()` params | Known presence/absence context. |
| 2026-03-31 | *(Session 39)* | `filter_redundant_hypotheses()` | TaxaMatch | function | Drop higher-rank rows superseded by finer-rank within same lineage + observation_id. |
| 2026-03-30 | *(Session 37)* | `ghost` (bool) | `hypothesis_type` (character) | TaxaAssign | Values: "specific_candidate" / "unreferenced_species" / "unreferenced_genus". |
| 2026-03-30 | *(Session 37)* | `habitat_affinity` | `habitat_fit` | TaxaAssign | LLM categorical column in `assign_taxa_llm()` output. |
| 2026-03-30 | *(Session 37)* | `missing_species` | `unreferenced_species` | TaxaAssign + TaxaLikely | `hypothesis_type` value. |
| 2026-03-30 | *(Session 37)* | `missing_genus` | `unreferenced_genus` | TaxaAssign + TaxaLikely | `hypothesis_type` value. |
| 2026-03-30 | *(Session 37)* | `Main_Habitat` | `main_habitat` | TaxaHabitat, TaxaExpect, TaxaFetch | snake_case consistency (145 occurrences). |
| 2026-03-30 | *(Session 38)* | `consensus_taxonomy()` | TaxaAssign | function | LCA among plausible posterior hypotheses. |
| 2026-03-30 | *(Session 38)* | `update_prior_from_consensus()` | TaxaAssign | function | Empirical Bayes refinement. |
| 2026-03-31 | *(Session 39)* | `suggest_plausible_ghosts()` | `suggest_unreferenced_species()` | TaxaAssign | function rename; S3 class `spg_result` → `unreferenced_species_result`. |
| 2026-03-31 | *(Session 39)* | `$ghosts` | `$unreferenced` | TaxaLikely | `audit_barcode_coverage()` return value; census column also renamed. |
| 2026-03-31 | *(Session 40)* | `expand_unreferenced_hypotheses()` | TaxaAssign | function | Moved from TaxaLikely; lives in `TaxaAssign/R/expand_unreferenced.R`. |
| 2026-04-06 | *(Session 49)* | `consensus_taxonomy()` | `posterior_consensus()` | TaxaAssign | **Breaking.** File: `R/consensus_taxonomy.R` → `R/posterior_consensus.R`. All callers updated. |
| 2026-04-06 | *(Session 49)* | `score_consensus()` | TaxaAssign | function | Conventional score-based consensus (min_score, max_gap, rank_thresholds, whitelist). |
| 2026-04-07 | *(Session 50)* | 5 workflow scripts replace monolithic workflow | TaxaLikely | workflow | `inst/TaxaLikely_workflow.R` superseded by `inst/workflows/1_*` through `5_*`. |
| 2026-04-09 | *(Session 52)* | `clean_taxon_names()` length-preserving | TaxaTools | function | **Breaking.** Invalid names now → NA instead of dropped. Callers needing old behavior: add `|> na.omit() |> unique()`. |
| 2026-04-13 | *(Session 56)* | `taxonomy_ranks` param | `rank_system` | TaxaTools | `create_taxon_names()` param — harmonize with TaxaLikely/TaxaAssign. |
| 2026-04-13 | *(Session 56)* | `rank_order` param | `rank_system` | TaxaMatch + TaxaAssign | `filter_redundant_hypotheses()` + `join_priors()` params. |
| 2026-04-13 | *(Session 56)* | `traitor_threshold` param | `mislabel_threshold` | TaxaLikely | `flag_reference_errors()` + `train_likelihood_model()`. |
| 2026-04-15 | *(Session 57)* | `%||%` exported from TaxaTools | TaxaTools | operator | 5 duplicate definitions removed from downstream packages. |
| 2026-04-15 | *(Session 57)* | `standard_ranks`, `extended_ranks`, `detect_ranks()` | TaxaTools | exported | Centralized rank definitions; downstream packages use `TaxaTools::standard_ranks`. |
| 2026-04-15 | *(Session 57)* | `combine_occurrence_sources()` | *(deleted)* | TaxaFetch | Dead code; superseded by `rename_cols()` + `stack_occurrences()`. |
| 2026-04-16 | *(Session 58)* | `build_priors()` | TaxaExpect | function | High-level wrapper: GBIF fetch → habitat → grid → model → priors → backbone translation. |
| 2026-04-16 | *(Session 58)* | `run_bayesian_pipeline()` | TaxaAssign | function | High-level wrapper for full Bayesian assignment. |
| 2026-04-16 | *(Session 58)* | `run_llm_pipeline()` | TaxaAssign | function | High-level wrapper for LLM-shortcut workflow. |
| 2026-04-28 | *(Session 60)* | TaxaFlag created | Ecosystem | package | Post-assignment anomalous detection flagging. |
| 2026-04-29 | *(Session 61)* | `remove_flagged_references()` | TaxaLikely | function | Remove mislabeled accessions; `model_params$reference_errors` slot added. |
| 2026-04-29 | *(Session 61)* | `rank_system` auto-detection | TaxaLikely | `train_likelihood_model()` + `evaluate_likelihoods()` | Default NULL auto-detects from column names. |
| 2026-05-01 | *(Session 64)* | `pdf_path` added to `pdf_structure` return | TaxaFetch | bug fix | Was missing; `call_api_pdf()` received NULL. |
| 2026-05-05 | *(Session 68)* | TaxaWizard created | Ecosystem | package | Conversational workflow designer. |
| 2026-05-06 | *(Session 69)* | Graph-based workflow engine | TaxaWizard | architecture | `inst/graph/workflow_graph.json` + 22 code snippets. |
| 2026-05-07 | *(Session 71)* | TaxaWorkflow | TaxaWizard | package | Renamed for clarity. |
| 2026-05-11 | *(Session 72)* | `verify_taxon_names()` NCBI direct bypass | TaxaTools | function | **Breaking for backbone_id=4.** Bypasses GlobalNames; uses batched `entrez_search()` + XML. |
| 2026-05-14 | *(Session 73)* | `min_phi` param | TaxaExpect | `generate_full_priors()` + `build_priors()` | Phi floor (default 2) prevents unstable MC posteriors. |
| 2026-05-19 | *(Session 77)* | `main_habitat` now required | TaxaAssign | `.latlon_to_grid()`, `join_priors()`, `run_bayesian_pipeline()` | **Breaking.** Habitat auto-selection removed. Must specify `main_habitat` in `site` param. |
| 2026-05-19 | *(Session 77)* | `census_genus_species()` | TaxaTools | function | GBIF backbone census: enumerate described species per genus/family. |
| 2026-05-20 | *(Session 79)* | `sample_id` | `observation_id` | TaxaMatch, TaxaLikely, TaxaAssign, TaxaFlag, TaxaWizard | **Breaking ecosystem rename** (~865 occurrences, ~141 files). L2 identifier renamed to eliminate ambiguity with L1 "sample" (collection event). `sample_id_col` → `observation_id_col` (TaxaMatch); `sample_col` → `event_col` (TaxaFlag); `sample_meta` → `event_meta` (TaxaAssign). |
| 2026-05-21 | *(Session 81)* | `habitat_observed_elsewhere` | `observed_in_habitat` | TaxaExpect | column + flag | TRUE = species recorded in this habitat type during training. |
| 2026-05-21 | *(Session 81)* | `least common ancestor` | `lowest common ancestor` | TaxaAssign + ecosystem | terminology | Standardized to conventional bioinformatics term (MEGAN, Kraken). |
| 2026-05-21 | *(Session 82)* | `options(TaxaID.llm_fn)` auto-detection | TaxaTools `.onAttach()` | global option | Priority: Anthropic > Gemini > OpenAI. All `llm_fn` defaults use `getOption("TaxaID.llm_fn", ...)`. |
| 2026-05-21 | *(Session 83)* | `R/model_registry.R` + `inst/model_tiers.json` | TaxaTools | module | Tier-based model discovery: `list_models()`, `refresh_models()`, `set_model()`. |
| 2026-05-21 | *(Session 83)* | `call_azure_api()` | TaxaTools | function | Azure OpenAI provider; `AZURE_OPENAI_API_KEY` env var. |
| 2026-05-22 | *(Session 84)* | `register_provider()` | TaxaTools | function | Session-only custom OpenAI-compatible provider registration. |
| 2026-05-23 | *(Session 85)* | `call_api()` | TaxaTools | function | **Generic LLM dispatcher.** All 5 provider functions now thin wrappers. `options(TaxaID.provider)` for active provider. `.onAttach()` sets `TaxaID.llm_fn = call_api`. |
| 2026-05-23 | *(Session 86)* | `LICENSE.md` + `DISCLAIMER.md` | *(centralised)* | All packages | Removed from package roots; centralised at `TaxaID/` root only. |
| 2026-05-26 | *(Session 87)* | Xeno-canto API v2 → v3 | TaxaLikely | `fetch_reference_recordings()` | **Breaking.** v3 requires API key (`XC_API_KEY` env var); endpoint changed. |
| 2026-05-26 | *(Session 87)* | `call_anthropic_api_pdf()` | `call_api_pdf()` | TaxaFetch | function rename + generalize | Now supports all vision-capable providers via `call_api(images=)`. |
| 2026-05-26 | *(Session 88)* | `build_reference_matrix()` | `build_sequence_matrix()` | TaxaLikely | **Breaking.** File: `R/build.R` → `R/build_sequence.R`. 36 files updated. |
| 2026-05-27 | *(Session 91)* | `read_reference_fasta()` `taxonomy` param | now NULL-default | TaxaLikely | **Breaking.** Must supply exactly one of `taxonomy` (data frame) or `taxonomy_file` (TSV path). |
| 2026-05-27 | *(Session 92)* | `show_tokens` + `max_input_tokens` params | `call_api()` | TaxaTools | Token logging + pre-flight guard. `attr(result, "tokens")` always attached. |
| 2026-05-28 | *(Session 95)* | TaxaWizard acoustic/image/local-reference | TaxaWizard | graph + metadata | 4 new input nodes, 2 new intermediate nodes, 8 new edges, 6 new snippets. |
| 2026-06-02 | *(Session 99)* | `score` → `score_original` in match object | TaxaMatch | column | `standardize_match_data()` output; `.evaluate_one_query()` updated; `assign_taxa_llm()` updated. |
| 2026-06-02 | *(Session 99)* | `likelihood_point_est` → `score_likelihood` | TaxaLikely, TaxaAssign | column | All functions, tests, workflows, docs updated. |
| 2026-06-02 | *(Session 99)* | `likelihood_mean` → `score_likelihood_mean` | TaxaLikely, TaxaAssign | column | All functions, tests, workflows, docs updated. |
| 2026-06-02 | *(Session 99)* | `likelihood_sd` → `score_likelihood_sd` | TaxaLikely, TaxaAssign | column | All functions, tests, workflows, docs updated. |
| 2026-06-02 | *(Session 99)* | `"unknown_species"` → `NA` / `"unreferenced_family"` | TaxaAssign | `taxon_name` / `hypothesis_type` | Catch-all row in `assign_taxa_llm()` output; `posterior_consensus.R` filter updated. |
| 2026-06-02 | *(Session 99)* | `unreferenced_candidates()`, `assign_scores()`, `model_likelihoods()`, `compute_likelihoods()` added | TaxaLikely | functions | Unified modular pipeline; `expand_consensus_candidates()` deprecated. |
| 2026-06-09 | *(Session 103)* | `detect_score_collapse()` | `detect_suppressed_candidates()` | TaxaLikely | function rename + rewrite | Full rewrite; old function removed. Returns named list (rule_detected, rules, perfect_only, max_score_ties, best_only, diagnostics). |
| 2026-06-06 | *(Session 101)* | `review_habitat` / `review_geography` / `review_scope` / `review_contaminant` | `habitat_plausibility` / `geographic_plausibility` / `scope_plausibility` / `contamination_risk` | TaxaFlag `review_assignments()` | column renames | Unified vocabulary: plausibility = likely/possible/unlikely; contamination_risk = low/moderate/high. |
| 2026-06-06 | *(Session 101)* | `flag_{type}` / `flag_{type}_score` / `flag_{type}_reason` | `{type}_risk` / `{type}_score` / `{type}_reason` | TaxaFlag `flag_contaminant()` | column renames | Direction also changed: old `"likely"` → new `"low"` risk. |
| 2026-06-08 | *(Session 104)* | `flag_prior_mismatch()` | `add_posthoc_assessment()` | TaxaFlag | function replacement | Redesign from risk/score/reason triplet to single categorical column (7 values: sensible, limited_evidence, unexpected, unprecedented, suspect, vague_rank, modeled). |
| 2026-06-23 | *(Session 117 — new param)* | `generate_undetected_diversity(taxonomy=)` | TaxaExpect | param added | Optional data frame (`taxon_name` + taxonomy columns). Joins taxonomy onto singleton-mirror rows for downstream `join_priors()` group descent. All 7 workflows updated. |
| 2026-06-23 | *(Session 117 — new column)* | `source_taxon_name` | TaxaExpect `generate_full_priors()` output | column added | Links each singleton-mirror row back to the observed species it was derived from. Preserved when `generate_full_priors()` appends `undetected` rows; used by `join_priors(singleton_taxonomy=)` to re-join taxonomy. |
| 2026-06-23 | *(Session 117 — new param)* | `join_priors(singleton_taxonomy=)` | TaxaAssign | param added | Optional data frame (`taxon_name` + taxonomy columns). Enables hierarchical mass-conserving group priors for unmodelled candidates via `.compute_dark_diversity_groups()` (phylum→class→order→family→genus). All 7 workflows updated. |
| 2026-06-23 | *(Session 117 — new columns)* | `dark_diversity_group`, `n_singletons_group`, `n_undetected_group` | TaxaAssign `join_priors()` output | columns added | Diagnostic columns added when `singleton_taxonomy` is supplied. `dark_diversity_group`: taxonomy label of the group a candidate was placed in (e.g. `"genus:Syngnathus"`, `"no_phylum"`, `"zero_orders_in_Malacostraca"`). `n_singletons_group`: singletons informing the group prior. `n_undetected_group`: unmodelled candidates in the group. |
| 2026-06-27 | *(Session 122)* | `is_valid_species_name()` | `is_plausible_binomial()` | TaxaTools | function rename | Better describes intent (plausibility heuristic, not formal validity). 13 files updated across TaxaTools, TaxaLikely, TaxaAssign. Test file renamed to test-is_plausible_binomial.R. |
| 2026-07-01 | *(Session 123)* | `add_slash_taxon()` gains `consensus_OTU`/`primary_taxon` | TaxaAssign | columns added | Non-breaking (fires only when `consensus_taxon` present). Replaces identical hand-rolled logic independently duplicated in 3 real workflows. |
| 2026-07-01 | *(Session 123)* | `audit_barcode_coverage()` "Common mistake" doc fix | TaxaLikely | doc only, no logic change | `match_df` must be the actual match object, not a length-curated training `reference_df` — confirmed via real Mugu data (*Mustelus mosis* case); 3 real workflows updated to pass the correct object. |
| 2026-07-04 | *(Session 134b)* | `TaxaFetch::define_search_polygon()` | `TaxaTools::define_search_polygon()` | TaxaFetch → TaxaTools | function moved | Shared gadget: TaxaFetch's search-area use and TaxaMatch's spatial-group use both call it now. Adds `group_col` (color points overlay by group) and `init_polygon` (reopen an existing polygon for reshaping) params. `shiny`/`miniUI`/`leaflet` moved TaxaFetch Suggests → TaxaTools Suggests. |
| 2026-07-04 | *(Session 134b)* | `TaxaFetch::group_observations_by_bbox()` | `TaxaMatch::group_observations_by_bbox()` | TaxaFetch → TaxaMatch | function moved + reworked | Operates on `TaxaMatch::build_site_table()`'s output. `spatial_group_id` now defaults to the observation's own `observation_id` (set by `build_site_table()`) rather than a `"spatial_group_<n>"` string for ungrouped observations; last-drawn-wins overlap rule (was first-drawn-wins) with a warning; new end-of-loop review/edit/delete step. `sf` added to TaxaMatch Imports. |
| 2026-07-04 | *(Session 134b)* | `assign_spatial_group()` added | TaxaMatch | function | Validating manual `spatial_group_id` setter; collision guard stops if the target id is already used by an observation not named in the call. |
| 2026-07-04 | *(Session 134b)* | `build_site_table()` gains `spatial_group_id`/`spatial_group_N` output columns | TaxaMatch | columns added | Default `spatial_group_id` = the row's own `observation_id`, `spatial_group_N` = `1L`, on every row from the moment the table is built. Non-breaking (new columns only). |
| 2026-07-10 | *(Session 150)* | `TaxaAssign::expand_unreferenced_hypotheses()` | `TaxaLikely::expand_unreferenced_hypotheses()` | TaxaAssign → TaxaLikely | function moved | Package-placement fix (prompted by a manuscript-driven review of the ecosystem's 4 conceptual pipeline steps): this function models likelihoods for named unreferenced species by borrowing from referenced relatives, so it belongs next to `TaxaLikely::unreferenced_candidates()`, not in the posterior-computation package. Same algorithm, same output — no math change. `TaxaAssign::expand_unreferenced_hypotheses()` kept as a `.Deprecated()` forwarding wrapper (matches the Session 136 `fetch_reference_sequences()` pattern). Still runs after both TaxaLikely and a TaxaExpect-derived unreferenced-species list are available and before `TaxaAssign::compute_posterior()` — a workflow-ordering requirement only, not a package dependency (the function only ever consumes plain data frames). |
| 2026-08-31 | *(kernel-priors B7)* | GLMM prior-fitting path deprecated (gentle): `build_priors()`, `optimize_grid_size()`, `prepare_model_dataframe()`, `add_pca_covariates()`, `compute_moran_basis()`, `screen_spatial_formula()`, `train_biodiversity_model()`, `train_biodiversity_model_by_group()`, `generate_full_priors()` | TaxaExpect | deprecation, no behavior change | Each emits a once-per-session `rlang::inform()` notice (shared `.frequency_id`, so a full pipeline run prints it once) + roxygen `@section Deprecated` pointing at `estimate_kernel_priors()`/`calibrate_kernel_bandwidth()`. Functions remain fully functional until the remaining GLMM workflows (PtConception, Mugu) migrate to the kernel estimator, then the chain is archived (DECIPHER-module precedent). `model_tier` vocabulary (`tier1`/`tier2`/`tier3_undetected`/`tier_undetected_evidence`/`tier_domestic_food`) doc-deprecated on its emitters: kernel-path output replaces it with `prior_branch` + `effective_records`; full retirement lands with the archival. |
| 2026-08-31 | *(curve pricing)* | `apply_undetected_evidence(pricing = c("blend","curve"))` added; `estimate_kernel_priors()` emits `f1`/`f2`/`chao_missing`/`theta_present`; `generate_presence_curve_evidence()` + `generate_user_specified_evidence()` added | TaxaExpect | additive params/functions | Curve pricing: `theta = w * theta_present` (mass/Chao), `theta_absent = 0`, replacing the floor-additive blend on the kernel path. One distance curve (`w_scale*exp(-min(d,d_cap)/(k*d_half))`) prices named regionals, watch-lifted invaders (k = bandwidth stretch = log-space interpolation), and the non-regional distance clamp; `user_specified` evidence rows carry provenance + a dilution-threshold disclosure. Blend mode byte-identical (regression-tested). GL-validated (Lamar 594 co-detections, precision 0.853, +4 species strictly additive). |
| 2026-09-01 | *(ncbi-screen-robustness)* | `evaluate_reference_accessions()` gains `max_query_len`/`max_batch_bp`/`prioritize_uncached`/`retry_insufficient`; new `hierarchy_flag` value `"not_evaluated_oversized"`; `blast_sequences()`/`.blast_remote()` gain `max_batch_bp` | TaxaMatch | additive params + new verdict value, no signature break | Implements `ecosystem_docs/REENTRY_PROMPT_eval_ref_accessions_long_sequence_robustness.md`. New `.extract_feature_table_fallback()` (`R/trim_query_to_amplicon.R`) reuses `check_marker_mismatch()`'s own GBSeq feature-table fetch/matching internals (`.fetch_marker_annotation()` gains `feature_from`/`feature_to` columns) rather than duplicating them, to rescue a query too long to primer-trim before it's ever submitted to BLAST at full length. A query still over-length after both rescues gets `"not_evaluated_oversized"` (TTL-retryable like `"insufficient_independent_evidence"`; confirmed `remove_incongruent_references()`/`flag_incongruent_references()` never treat it as a flag). `blast_sequences()`'s batching is now length-aware (`max_batch_bp`, a batch closes on cumulative bp too, one very long query rides alone). All 4 new `evaluate_reference_accessions()` params deliberately excluded from `params_key` -- no cache invalidation, `.EVAL_REF_ACC_VERSION` unchanged, ~1,163 existing cached rows untouched. `devtools::test()` 950/0 (up from 886), `check()` 0/0/0. |
| 2026-09-01 | *(theta-surface)* | NEW `TaxaExpect::plot_theta_surface()` | TaxaExpect | function added; supersedes `plot_theta_map_interactive()` on the kernel-priors path only | KDE prior-field map for `estimate_kernel_priors()` output: evaluates the identical estimator on an `n_grid` lattice via binned `stats::fft` convolution (no new dependency), so the map is the estimator, not a rendering of it. `plot_theta_map_interactive()` itself is UNCHANGED and remains correct for the deprecated-but-live GLMM/grid path (it parses `Grid_<lat>_<lon>` ids, which kernel `site_id`s are not). Site-identity invariant (surface theta at the site coordinates == `estimate_kernel_priors()` theta, same `lambda_km`/`m`/`lambda_latitude`) verified to ~1e-2 grid-quantization tolerance; FFT convolution verified against brute force (~1e-9). `grDevices`/`graphics` added to TaxaExpect Imports. Five production workflows' `if (!USE_KERNEL_PRIORS) plot_theta_map_interactive(...)` gates are the wiring candidates for this function's kernel branch (see `TaxaExpect/CLAUDE.md`'s top note and `ecosystem_docs/SPEC_plot_theta_surface.md`'s Status section for the by-file list) -- not wired in this change; those workflow files live outside this repo. |
| 2026-09-03 | *(local-corroboration)* | `evaluate_reference_accessions()` internal cache version `"v4_hybrid_maternal_proxy"` -> `"v5_amplicon_query"`; gains `query_span` (in `params_key`), `local_corroboration`, `skip_locally_corroborated`; new `hierarchy_flag` value `"locally_corroborated"`; NEW `corroborate_references_locally()`, `match_driving_accessions()`, `migrate_reference_cache()`; `score_reference_labels()`/`refine_reference_verdicts()` gain `local_corroboration` + columns `action_reason`, `corroboration_source`, `local_best_independent_pident`, `local_n_independent_conspecific`; `review_flagged_accessions()` gains `local_min_overlap` | TaxaMatch | cache-version bump (invalidates every existing cached row -- migrate with `migrate_reference_cache()`); everything else additive, no signature break | Implements `ecosystem_docs/REENTRY_PROMPT_local_corroboration_and_primer_stripped_screen.md`. (1) The screen's BLAST query is now the primer-STRIPPED amplicon (`.trim_queries_to_amplicon(strip_primers = TRUE)`, `.resolve_trimmed_span_max(strip_primers=)` is the one length bound for both spans): the primer-inclusive 217 bp query could never see a 169 bp amplicon-only conspecific deposit (blastn raw score 338 vs >= 359 for every >= 93% full-length relative; live-confirmed `KM057996`/`OQ846041`). (2) `corroborate_references_locally()` tiers references by an independent conspecific in the workflow's own `seq_matrix` (`coverage >= 0.8`, identity >= 0.99, independence via the screen's own `.same_submission_batch()`); PtCon 995 candidates: never-drives 286 / singleton 554 / same-batch-only 23 / disagree 26 / corroborated 106, reproduced through the package by `diagnostics/local_corroboration_tiers_ptcon.R`. (3) `match_driving_accessions()` = the per-(observation, species) max-scoring accessions (709 of 995 on PtCon), a cost filter for the screen. (4) Corroborated accessions are skipped by the screen (`"locally_corroborated"`, TTL Inf). (5) `score_reference_labels()` vetoes a BLAST `"remove"` to `"inspect"` when the local set corroborates it (`action_reason = "vetoed_by_local_corroboration"`); `label_confidence` stays BLAST-only. On the real PtCon cache exactly one row flips: KM057996. `devtools::test()` 1228/1228 (0 failures, 0 warnings), `check()` 0 errors / 0 warnings / 0 notes. |
| 2026-09-08 | *(flag_reference_errors retirement)* | `TaxaLikely::flag_reference_errors()` and `.compute_reference_qc_stats()` removed entirely; `TaxaLikely::remove_flagged_references()` removed entirely; `TaxaMatch::verify_flagged_references()` removed entirely; `TaxaLikely::train_likelihood_model()` loses `mislabel_threshold`/`singleton_match_threshold`/`verified_clean`/`mislabel_behavior` params and the `reference_errors` output slot; `TaxaLikely::build_site_reference()` loses `flag_errors`/`$errors`; `TaxaAssign::run_llm_pipeline()`/`run_bayesian_pipeline()` lose the `reference_errors`-driven auto-filter | TaxaLikely, TaxaMatch, TaxaAssign | **breaking, deleted not deprecated** (matches this project's own zero-external-users precedent: `fetch_reference_sequences()`, `audit_barcode_coverage_ncbi()`, `expand_consensus_candidates()`, `read_wildlife_insights_output()`) | `flag_reference_errors()` was TaxaLikely's own free, within-reference-set mislabel heuristic, run unconditionally inside `train_likelihood_model()`. It became redundant once `TaxaMatch::corroborate_references_locally()` (2026-09-03) shipped a free, zero-NCBI-cost check doing the same triage job more precisely (independence-filtering + coverage-checking) for the same cost, feeding `TaxaMatch::evaluate_reference_accessions(local_corroboration=, skip_locally_corroborated=TRUE)` (the BLAST-based screen, which already computes `reference_action` internally via `score_reference_labels()` -- no separate scoring call needed). Reference-quality screening now lives entirely upstream, in a caller-orchestrated pre-filtering step against `reference_df`/`raw_df` *before* `train_likelihood_model()` is ever called -- TaxaLikely gains no replacement parameter, preserving its architectural no-TaxaMatch-dependency boundary. All 4 real external production workflows (GreatLakes2023_ConsensusWorkflow.R, PtConceptionWorkflow_12S_single_site.R, PtConceptionWorkflow_12S_multi_site.R, PtConceptionWorkflow_18S_2_single_site.R; both Mugu scripts never used this pattern) rewired to `TaxaMatch::corroborate_references_locally()` -> `evaluate_reference_accessions(local_corroboration=, skip_locally_corroborated=TRUE)`, filtering `seq_matrix` directly by `id_x`/`id_y` membership against `reference_action == "remove"` accessions. TaxaWizard's `matrix_to_clean` code-gen snippet rewritten to match. See `TaxaID/CLAUDE.md`'s 2026-09-08 top session note for the full record. |
| 2026-09-08 | *(suggest_unreferenced_species package placement)* | `TaxaAssign::suggest_unreferenced_species()` -> `TaxaLikely::suggest_unreferenced_species()` | TaxaAssign -> TaxaLikely | function moved (`.Deprecated()` forwarding wrapper kept in TaxaAssign); `rentrez` dropped from TaxaAssign Imports (no longer used there); `jsonlite` added to TaxaLikely Imports | Prompted by the user asking whether the same duplicate-mechanism pattern found in the `flag_reference_errors()` retirement (above) also existed for "unreferenced species" naming. Investigation found a DIFFERENT shape of problem, not a true duplicate: `suggest_unreferenced_species()`'s own roxygen already self-described as "a fast, LLM-first alternative to `TaxaLikely::audit_barcode_coverage()`" -- it does the identical reference-coverage-auditing job (species missing from the reference database) that `audit_barcode_coverage()`/`audit_acoustic_coverage()`/`audit_inat_coverage()` already do in TaxaLikely, whose own stated Package Purpose is "auditing taxonomic completeness (identifying unreferenced taxa missing from the reference)" -- the same package-placement logic already applied to `expand_unreferenced_hypotheses()`'s TaxaAssign -> TaxaLikely move (Session 150, 2026-07-10). A real-usage search (all 9 packages, `eDNA/`, `Stats and Data/`, and the broader `Rscripts/` tree) found the whole LLM-shortcut pathway it serves (`run_llm_pipeline()`/`assign_taxa_llm()`) has real but narrow use: `assign_taxa_llm()` is called live in `GreatLakes2023_ConsensusWorkflow.R` Section 8k, but only as an internal LLM-vs-Bayesian comparison diagnostic (never feeding TaxaFlag review/export) and without `unreferenced_taxa=`, so `suggest_unreferenced_species()` itself has zero calls anywhere outside 2 in-package `inst/` demo scripts (`demo_for_JV.R`, `TaxaAssign_llm_workflow.R`, both updated to the new namespace) -- confirming the move is low-risk. `assign_taxa_llm()`/`run_llm_pipeline()` themselves correctly stay in TaxaAssign (genuine posterior computation, just with an LLM-generated prior/likelihood proxy) -- TaxaAssign's `run_llm_pipeline()` internal call site updated to `TaxaLikely::suggest_unreferenced_species()` directly (with a `requireNamespace("TaxaLikely")` guard, since TaxaLikely is only in TaxaAssign's Suggests) to avoid triggering its own package's deprecation warning on every real call. Two pre-existing, unrelated roxygen bugs found and fixed while relocating the file's cross-references (`TaxaLikely::build_site_reference()`'s and `audit_acoustic_coverage()`'s own docs pointed `$unreferenced`'s flat character-vector output at `suggest_unreferenced_species()`, which has no parameter to accept a pre-built list -- corrected to `TaxaAssign::assign_taxa_llm(unreferenced_taxa = ...)`, the function whose parameter actually matches that shape). TaxaAssign's README/roxygen (`assign_taxa_llm()`, `run_llm_pipeline()`) rewritten per the user's explicit request to frame the LLM-shortcut pathway clearly as a fast approximation used in place of TaxaLikely's/TaxaExpect's modeled effort, not parallel first-class infrastructure. `devtools::test()`/`check()` clean both packages (TaxaLikely 1118/0, TaxaAssign 675/0), reinstalled. See `TaxaID/CLAUDE.md`'s and both packages' own 2026-09-08 top session notes for the full record. |
| 2026-09-09 | *(train_biodiversity_model_by_group retirement)* | `TaxaExpect::train_biodiversity_model_by_group()` removed from the live package; source + tests moved intact to `TaxaExpect/archive_glmm_by_group/` | TaxaExpect | archived, not deleted (matches this project's own DECIPHER-module retirement precedent: `TaxaLikely/archive_decipher_reference_audit/`) | Part of the GLMM prior-fitting path deprecated 2026-08-31 (see the row above and this file's own `@section Deprecated` block) in favor of `estimate_kernel_priors()`/`calibrate_kernel_bandwidth()`. Verified zero real callers ecosystem-wide two independent ways this session: (1) a bulk usage-audit grep across every real production workflow and every cross-package/same-package R call found no invocation; (2) a targeted follow-up specifically checked whether `PtConceptionWorkflow_18S_2_single_site.R` -- the exact "broad-marker data spanning multiple detection processes" scenario this function's own roxygen called its "recommended entry point" for -- actually calls it. It doesn't: that workflow hand-rolls its own per-group loop (`prepare_model_dataframe(sampling_group_col=)` then a manual loop calling `screen_spatial_formula()`/`train_biodiversity_model()` per group with its own `tryCatch()`), a strict superset of what this function did (adds AIC-based formula screening this function had no equivalent for) -- the packaged function was built but never adopted, and its own target use case evolved past it. Unlike the rest of the GLMM chain (whose other 8 members still have real callers via production workflows still mid-migration to the kernel path), this one function was safe to retire in isolation now. Every live roxygen `\link{}`/`[...]` cross-reference to the removed symbol fixed (not left dangling) in `generate_undetected_diversity.R`, `prepare_model_dataframe.R` (multiple), `compute_adaptive_sampling_groups.R` (including its file-header comment), `train_biodiversity_model.R` (both its `@section Multi-group data is refused` and its runtime `stop()` message), and `TaxaExpect-package.R`'s `@section` listing -- redirected to `estimate_kernel_priors(sampling_group_col=)` (added 2026-09-03 specifically to restore per-group/multi-detection-process stratification on the kernel path) as the current recommended path, or to "call `train_biodiversity_model()` once per group yourself" where the GLMM path itself is still what's being documented. `TaxaWizard/inst/graph/snippets/dist_to_priors_by_group.R` (the one real, non-production caller -- a TaxaWizard code-generation template snippet) rewritten to call `TaxaExpect::estimate_kernel_priors(sampling_group_col=)` instead. `TaxaExpect/.gitignore` gained an `archive_glmm_by_group/` entry (deliberately not committed, matching the DECIPHER precedent's own gitignore convention). See `TaxaExpect/CLAUDE.md`'s 2026-09-09 top session note for the full record and final `devtools::test()`/`check()` numbers. |
| 2026-09-09 | *(unused likelihood-entrypoint retirement)* | `TaxaLikely::compute_likelihoods()` and `TaxaLikely::model_likelihoods()` removed from the live package; source + tests moved intact to `TaxaLikely/archive_unused_likelihood_entrypoint/` | TaxaLikely | archived, not deleted (matches this project's own DECIPHER-module retirement precedent: `TaxaLikely/archive_decipher_reference_audit/`) | Re-verified a candidate finding from an earlier usage audit that flagged `compute_likelihoods()` (documented in this package's own CLAUDE.md as "the recommended high-level entry point" for the likelihood-scoring pipeline `unreferenced_candidates()` -> `assign_scores()` -> `model_likelihoods()`) as having zero real callers, and further claimed `assign_scores()`/`model_likelihoods()`/`unreferenced_candidates()` each had zero DIRECT real callers of their own outside `compute_likelihoods()` -- apparently orphaning all four together. That claim conflicted with the ROOT `CLAUDE.md`'s own "Ecosystem logic (no-score pathway)" section, which describes `unreferenced_candidates()`/`assign_scores()` as real, designed infrastructure for a different scenario (no-score/morphology/single-score classifiers) than the 8 real eDNA/BLAST production workflows the original audit checked -- exactly the shape of a false negative (checking the wrong workflow population, not that the function is unused). Re-investigated with real evidence rather than trusting either framing: `assign_scores()` genuinely has a real `score_type` parameter with `"none"`/`"similarity_softmax"` values matching the root doc's description exactly, and grepping `inst/workflows/*.R` across the WHOLE monorepo (not just the 8 eDNA production scripts) found real, DIRECT callers of `unreferenced_candidates()`/`assign_scores()` bypassing `compute_likelihoods()` entirely: `TaxaLikely/inst/workflows/image_acoustic_likelihood_workflow.R` (both its IMAGE and ACOUSTIC sections), `TaxaLikely/inst/workflows/6_no_score_pathway_workflow.R`, and cross-package in `TaxaAssign/inst/workflows/camera_trap_posterior_workflow.R` (called twice, once calibrated and once at the old default, for an explicit before/after comparison). **Verdict: split, not all-or-nothing.** `unreferenced_candidates()`/`assign_scores()` are real, adopted no-score/non-sequence-pathway infrastructure and were NOT touched. `model_likelihoods()` was checked separately per the re-verification's own instruction ("does it have any real caller outside `compute_likelihoods()`? If not, it's still orphaned even if its siblings are safe") -- its only caller outside `compute_likelihoods()` anywhere in the monorepo is its own required demo section in `inst/review_function_inputs.R` (a "one runnable section per exported function" reference gallery, not a simulated real-usage pipeline the way `inst/workflows/*.R` scripts are -- every exported function is structurally guaranteed a section there regardless of real adoption). No `inst/workflows/*.R` file anywhere calls it; the real sequence/BLAST pathway workflow (`sequence_likelihood_workflow.R`) calls `train_likelihood_model()` + `evaluate_likelihoods()` directly, confirming `model_likelihoods()` was never actually how any real bivariate-normal pathway got reached. `compute_likelihoods()` itself confirmed to have zero real callers anywhere (only tests and its own demo section). Both archived together (they share one file, `R/compute_likelihoods.R`). Cross-references fixed in `R/assign_scores.R`'s and `R/unreferenced_candidates.R`'s roxygen (`@details`/`@return`/`@seealso`), `inst/review_function_inputs.R` (demo subsections removed, Section 3 header rewritten), `TaxaLikely/README.md`, root `README.md`'s mermaid architecture diagram (DNA likelihoods entry point corrected to `evaluate_likelihoods`), and `inst/TaxaID_Workflow_Template_TEST.R`'s adaptation-notes comment. Root `CLAUDE.md`'s "Ecosystem logic (no-score pathway)" section left UNCHANGED -- it was found to be accurate (describes `unreferenced_candidates()`/`assign_scores()` only, never mentions the two archived functions). `TaxaLikely/.gitignore`/`.Rbuildignore` gained an `archive_unused_likelihood_entrypoint/` entry (deliberately not committed, matching the DECIPHER precedent's own convention). A dated addendum appended to `TaxaLikely/inst/taxalikely_review_response.md` (its own prior "compute_likelihoods.R" section, from the human code review, is left as a frozen historical record). See `TaxaID/CLAUDE.md`'s and `TaxaLikely/CLAUDE.md`'s own 2026-09-09 top session notes for the full record and final `devtools::test()`/`check()` numbers. |
| 2026-09-09 | *(build_site_reference retirement)* | `TaxaLikely::build_site_reference()` removed from the live package; source + tests moved intact to `TaxaLikely/archive_unused_reference_wrappers/` | TaxaLikely | archived, not deleted (matches this project's own DECIPHER-module retirement precedent: `TaxaLikely/archive_decipher_reference_audit/`) | A usage audit found `build_site_reference()` (the high-level `fetch_ncbi_reference_sequences()` -> `audit_barcode_coverage()` -> `write_reference_fasta()` wrapper) has zero real callers anywhere in the monorepo -- every real production workflow builds its reference database by calling the three component functions directly, and its own docs had already drifted (lost its `flag_errors`/`$errors` step after `flag_reference_errors()`'s retirement the day before, unnoticed) -- itself a signal of disuse. `write_reference_fasta()`, its only internal caller, was investigated separately and deliberately KEPT live/exported rather than archived alongside it: despite also having zero real callers of its own, it's a genuinely generic, standalone, round-trippable (with `read_reference_fasta()`) FASTA-export utility with its own dedicated test coverage, documented in the README as a first-class capability independent of the wrapper, and not tightly coupled to the wrapper's own output shape -- it's this package's ONLY reference-export capability, so archiving it too would have been a real capability loss, not dead-code cleanup. Cross-references fixed: `R/write_fasta.R`'s `@seealso`, `tests/testthat/test-write-fasta.R` (dropped 3 misplaced `build_site_reference()` tests), the stale `man/build_site_reference.Rd`, `TaxaLikely/README.md` (rewrote the site-reference-library section to chain the three functions directly), `inst/workflows/1_fetch_references_workflow.R` and `inst/workflows/local_reference_demo.R` (both trimmed of the wrapper), `inst/review_function_inputs.R` Section 10, and `inst/taxalikely_review_response.md` (dated addenda on both its `build_site_reference.R` and `write_fasta.R` sections). Cross-package: root `README.md`'s mermaid diagram, and `TaxaWizard`'s `taxa_to_site_refs` edge (snippet rewritten to chain the component functions; `workflow_graph.json`/`metadata/TaxaLikely.json`/`prompts/phase_classify.md` updated to match). See `TaxaLikely/CLAUDE.md`'s 2026-09-09 top session note for the full record and final `devtools::test()`/`check()` numbers. |
| 2026-09-09 | *(GLMM prior-fitting chain archival)* | `TaxaExpect::build_priors()`, `optimize_grid_size()`, `prepare_model_dataframe()`, `add_pca_covariates()`/`apply_pca_transform()`, `compute_moran_basis()`, `screen_spatial_formula()`, `train_biodiversity_model()`, `generate_full_priors()`, and `plot_theta_map_interactive()` removed from the live package; source + tests moved intact to `TaxaExpect/archive_glmm_prior_pipeline/`, consolidated with the already-archived `train_biodiversity_model_by_group()` (moved there from `archive_glmm_by_group/`, which no longer exists as a separate directory) | TaxaExpect | archived, not deleted (DECIPHER-module retirement precedent); `plot_theta_map_interactive()` is a 9th item, not on the original 2026-08-31 deprecation list, archived alongside the rest because it was found fully orphaned once the chain that fed it is gone | Closes out the "gentle" 2026-08-31 deprecation (see the row above): that deprecation deliberately stayed notice-only, zero-behavior-change, specifically because production workflows (PtConception x3, Mugu x2, GreatLakes) still ran the GLMM path, with the explicit statement that "archival lands with that migration." That migration is now complete -- all 6 real production workflows run the kernel path (`USE_KERNEL_PRIORS <- TRUE`) with the GLMM code retained only in the flag's `else` branch as dead fallback code. Verified via Step 0 before archiving anything: grepped every live kernel-path source file (`estimate_kernel_priors.R`, `calibrate_kernel_bandwidth.R`, `generate_undetected_diversity.R`, `apply_undetected_evidence.R`, `generate_domestic_food_priors.R`, `plot_theta_surface.R`, `generate_inat_range_evidence.R`, `generate_regional_proximity_evidence.R`, `fit_regional_presence_curve.R`, `kernel_budget_sensitivity.R`) for real (non-roxygen, non-comment) calls into any of the 8 candidates -- none found; every hit was a roxygen cross-reference, a comment, or a legacy-input-shape description in an error message. `create_sites_from_grid()` was investigated per explicit instruction and found to have a genuine, independent, live purpose on the kernel path -- confirmed via `diagnostics/kernel_budget_18S_sampling_groups.R`, which calls it purely as a spatial-binning utility feeding `compute_adaptive_sampling_groups()`/`estimate_kernel_priors(sampling_group_col=)`, with zero calls to any of the 8 archived functions -- so it was deliberately NOT archived, and its own roxygen/DESCRIPTION were updated to describe this dual history instead. `plot_theta_map_interactive()` was investigated separately: every real caller within this repo (the root generic template, a TaxaWizard code-gen snippet) traces back through the GLMM chain itself, and all 6 production workflows only ever called it from the same dead `else` branch as the other 8 -- confirmed fully orphaned, archived as a 9th item. `glmmTMB` dropped from TaxaExpect's DESCRIPTION Imports (no longer used anywhere in the live package); `shiny`/`miniUI` dropped from Suggests (their only live consumer was the now-archived gadget). Two internal helpers in `R/utils_internal.R` (`.parse_grid_id_coords()`, `.glmm_deprecation_notice()`) removed as fully orphaned once every one of their callers was archived. Every live roxygen `\link{}`/`[...]` cross-reference to an archived symbol fixed across all 9 packages (not left dangling) -- `TaxaExpect-package.R`, `compute_adaptive_sampling_groups.R`, `create_sites_from_grid.R`, `generate_undetected_diversity.R`, `report_priors.R` (in-package); `TaxaAssign::run_bayesian_pipeline()`'s `\link[TaxaExpect]{generate_full_priors}` and `TaxaFetch::dedupe_occurrences()`'s `\link[TaxaExpect]{prepare_model_dataframe}` (the only two real cross-package dangling links found ecosystem-wide) -- both redirected to `estimate_kernel_priors()`. `TaxaAssign::join_priors()`/`run_bayesian_pipeline()` (both read the `list-with-$priors` shape either `build_priors()` or `estimate_kernel_priors()` can produce, and the `search_center` attribute only `build_priors()` ever set) updated to describe both paths without touching the `model_tier`-based legacy-fallback handling itself, per explicit scope boundary. TaxaWizard: the `taxa_to_priors_wrapper`/`std_to_dist`/`dist_to_priors` edges and their snippets deleted outright (no kernel-path wrapper replaces `build_priors()`'s single-call convenience; a caller now chains `taxa_to_occ` -> `occ_to_std` -> `std_to_priors_kernel`/`dist_to_priors_by_group` instead); the now-unreachable `distributions` intermediate node removed; `dist_to_priors_by_group`'s own `from` corrected from `["distributions"]` to `["std_occurrences"]` (a stale field from the 2026-09-09-earlier `train_biodiversity_model_by_group()` rewrite -- that snippet never actually consumed a gridded `distributions` object); `priors_to_map` simplified to save-only (its old `plot_theta_map_interactive()` step is gone; the kernel path's own `plot_theta_surface()` needs the full `kernel_fit` object this generic edge doesn't have, so it stays wired inline inside `std_to_priors_kernel.R`/`dist_to_priors_by_group.R` instead); `metadata/TaxaExpect.json`'s `build_priors`/`optimize_grid_size`/`prepare_model_dataframe` entries removed, remaining entries' descriptions updated; `metadata/TaxaAssign.json`/`TaxaFetch.json` descriptions and `prompts/phase_parameterize.md`'s parameter-type guidance updated to stop citing the archived wrapper. `tests/testthat/test-graph.R` updated for the removed wrapper/edges (fewer real edges -> fewer enumerable combinatorial paths, matching this project's own documented precedent for this exact effect, not a regression). The 6 real external production workflows' dead `else { }` fallback branches (under `USE_KERNEL_PRIORS`) left functionally untouched, each gaining only a comment noting the archival and pointing at the archive location if the GLMM path is ever needed again; each file backed up first as `*.bak_pre_glmm_archive_comment` (not under git) and confirmed to still `parse()` cleanly. Root `inst/TaxaID_Workflow_Template_TEST.R`, `TaxaExpect/inst/TaxaExpect_workflow.R`, `TaxaExpect/inst/workflows/generate_priors_workflow.R`, and `TaxaAssign/inst/workflows/camera_trap_posterior_workflow.R` (in-repo GLMM-only teaching/demo scripts, not production workflows) each gained an equivalent archival notice rather than a rewrite. `TaxaExpect/vignettes/building-priors.Rmd` (the package's only vignette, entirely GLMM-focused, already `eval = FALSE`/`purl = FALSE` throughout so it poses no `R CMD check` risk) gained a top-of-file archived-pathway notice; `TaxaExpect/README.md`'s "Deprecated / Legacy" section renamed "Archived" and rewritten to state the stronger fact plainly (no longer present in the installed package at all, not just discouraged), pointing at `archive_glmm_prior_pipeline/`. A follow-up ecosystem-wide sweep for remaining plain-`\code{}` mentions (not caught by the original caller-direction grep, since these are internal `@noRd` helper files with no roxygen name for such a grep to match) found two more fully orphaned files whose SOLE caller was one of the 9 archived functions -- `TaxaExpect/R/recover_demoted_species.R` (`.recover_demoted_species()`, called only by `build_priors()`) and `TaxaExpect/R/utils_plot.R` (`.habitat_palette()`/`.he()`, called only by `plot_theta_map_interactive()`) -- both moved into `archive_glmm_prior_pipeline/R/` alongside the rest of the chain. `devtools::test()`/`check()` clean on TaxaExpect and TaxaWizard, both reinstalled. See `TaxaExpect/CLAUDE.md`'s and `TaxaWizard/CLAUDE.md`'s own 2026-09-09 top session notes for the full record and final numbers. |
| 2026-09-09 | *(create_sites_from_grid / compute_adaptive_sampling_groups retirement)* | `TaxaExpect::create_sites_from_grid()` and `TaxaExpect::compute_adaptive_sampling_groups()` removed from the live package; source + tests moved intact to `TaxaExpect/archive_glmm_prior_pipeline/`, joining the rest of the GLMM/grid chain (the row directly above) | TaxaExpect | archived, not deleted (DECIPHER-module retirement precedent); the dormant final pair from the same chain, archived a few hours after the rest for an independent reason | These two functions were deliberately kept live during the main same-day GLMM-chain archival (row above), on the strength of `create_sites_from_grid()`'s own roxygen claiming one remaining live purpose independent of the archived GLMM math: the spatial-binning step `compute_adaptive_sampling_groups()` uses to measure per-site effort ahead of `estimate_kernel_priors(sampling_group_col=)`. The user directly questioned that justification, prompting a fresh, careful re-check rather than taking the earlier verdict at face value: a repeat grep of the whole monorepo confirmed zero calls to either function in any of the 6 real production workflows, and `compute_adaptive_sampling_groups()`'s own package changelog already recorded "zero real callers affected" from a past parameter-default change -- i.e. the function was built but never actually adopted by any real study. The only remaining callers are 4 already-archived-pathway-notice `inst/` demo/template scripts (using `create_sites_from_grid()` for the OLD, now-fully-nonfunctional GLMM purpose, not the newer kernel-path purpose) and one diagnostics script, `diagnostics/kernel_budget_18S_sampling_groups.R`, which genuinely uses both functions for their current, real purpose -- left untouched, per this project's own established convention that a frozen, dated, one-time analysis script is a historical record, not live code to keep working. The user's own reasoning for archiving now: the scenario that would justify keeping this pair (a future marker/dataset with taxonomic breadth too large to hand-classify into detection-process sampling groups) is real in principle but has never actually materialized -- every real dataset this ecosystem has handled, including the taxonomically broadest one (the 18S workflow, ~10-11 groups), was successfully hand-classified by a domain expert instead. Cross-references fixed: `TaxaExpect-package.R`'s `@section Spatial modelling`/`@section Archived`, `estimate_kernel_priors.R`'s two `sampling_group_col` mentions of `compute_adaptive_sampling_groups()` (roxygen `@param` + an internal code comment), `TaxaWizard/inst/metadata/TaxaExpect.json`'s `sampling_group_col` entry, `TaxaExpect/README.md` (Key Functions + the "Archived: Grid + GLMM Pathway" section's function list), `ecosystem_docs/ECOSYSTEM_WORKFLOW.md` (a `create_sites_from_grid()`-keyed `sample_meta` code comment outside the section's existing archived-pathway banner). Two of the 4 archived-pathway-notice scripts (`TaxaAssign/inst/workflows/camera_trap_posterior_workflow.R`, root `inst/TaxaID_Workflow_Template_TEST.R`) had explicitly claimed "`create_sites_from_grid()` itself is unaffected (still live)" -- both notices corrected; the other two (`TaxaExpect/inst/TaxaExpect_workflow.R`, `TaxaExpect/inst/workflows/generate_priors_workflow.R`) never made that specific claim but gained a short addendum for consistency. `TaxaExpect/inst/taxaexpect_review_response.md` gained dated addenda on both functions' existing review-response sections (original answers left unrewritten). `stringr` dropped from TaxaExpect's DESCRIPTION Imports (its only caller, `create_sites_from_grid()`, is now archived). `devtools::test()`/`check()` clean on TaxaExpect, reinstalled. TaxaWizard's touched metadata JSON re-verified/reinstalled too (no snippet/graph-topology change -- only a description string). See `TaxaExpect/CLAUDE.md`'s later 2026-09-09 top session note for the full record and final numbers. |
| 2026-09-09 | *(create_sites_from_grid / compute_adaptive_sampling_groups REVERSAL of the row directly above)* | `TaxaExpect::create_sites_from_grid()` and `TaxaExpect::compute_adaptive_sampling_groups()` restored to the live package; source + tests moved back from `TaxaExpect/archive_glmm_prior_pipeline/` to `R/`/`tests/testthat/` | TaxaExpect | reversal of an archival, same day -- restored, not re-deleted; the rest of the GLMM/grid chain (the two rows above this one) stays archived, this reversal is scoped to only this pair | Reverses the row directly above, hours later the same day, for a real reason distinct from the "zero real callers" finding that row recorded (that finding is not disputed -- this is a different consideration, not a re-litigation of the same facts). A real safety gap was found while reviewing `TaxaExpect/README.md`: `estimate_kernel_priors(sampling_group_col = NULL)` has no guard at all against silently pooling data from genuinely incompatible detection processes (e.g. phytoplankton cell counts + bird point counts) -- confirmed directly in `R/estimate_kernel_priors.R`, `sampling_group_col` is purely optional and pools everything with zero warning or error when omitted. This is a real regression from the archived GLMM path, where `train_biodiversity_model()` had a hard `stop()` on a `sampling_group` column spanning more than one value; the kernel path has no equivalent. Auto-detecting the mixing was explicitly rejected -- this codebase has an established precedent of refusing to guess a domain classification a wrong guess could silently mis-price (see `generate_invasive_watch_evidence()`'s own roxygen) -- so the fix is making `sampling_group_col` unmissable in the docs, plus keeping a real tool available for a caller who can't hand-classify: `compute_adaptive_sampling_groups()`. The original archival's own reasoning (every real dataset this project's own analysts have handled was successfully hand-classified, so the automatic classifier has never been load-bearing) was correct as far as it went, but implicitly assumed the audience is this project's own internal team -- TaxaID is headed for a USGS software release and an MEE manuscript, and a future external user won't have the same domain depth the person who hand-built the real 18S workflow's 11-way classification did. Restored: `R/create_sites_from_grid.R`, `R/compute_adaptive_sampling_groups.R`, `tests/testthat/test-create_sites_from_grid.R`, `tests/testthat/test-compute_adaptive_sampling_groups.R` (moved back intact, unedited -- neither file's own content ever claimed archival, since the prior move didn't rewrite them). Cross-references fixed in the other direction: `TaxaExpect-package.R`'s `@section Spatial modelling`/`@section Archived` (both functions moved back to the live list, with the full two-stage-then-reversed history spelled out); `estimate_kernel_priors.R`'s `sampling_group_col` `@param` + internal code comment (restores the recommendation of `compute_adaptive_sampling_groups()` as the automatic alternative to hand-building the column); `TaxaWizard/inst/metadata/TaxaExpect.json`'s `sampling_group_col` description (restores the mention; confirmed via this file's own row above and the archival session note that neither function ever had its OWN top-level metadata entry in this file to restore); `TaxaAssign/inst/workflows/camera_trap_posterior_workflow.R` and root `inst/TaxaID_Workflow_Template_TEST.R` (both had their "create_sites_from_grid() itself is unaffected" claim corrected TO archived by the row above -- re-corrected back to live); `TaxaExpect/inst/TaxaExpect_workflow.R` and `inst/workflows/generate_priors_workflow.R` (both gained a further addendum noting create_sites_from_grid() is live again, though the rest of each script's GLMM chain stays archived and non-functional); `TaxaExpect/README.md` (the "Shared detection effort" mixing-safety paragraph made explicit that grouping is NOT automatic and `estimate_kernel_priors()` silently pools with no warning/error if `sampling_group_col` is omitted; both functions added back to Key Functions under "Kernel pathway (current, recommended)"); `ecosystem_docs/ECOSYSTEM_WORKFLOW.md`'s `sample_meta`/`create_sites_from_grid()` code-comment banner (the pattern itself stays superseded, for a reason unrelated to either function's archival status, but the specific "was archived" claim about `create_sites_from_grid()` is corrected); `TaxaExpect/inst/taxaexpect_review_response.md` gained a further dated addendum on both functions' sections (original archival addenda from the row above left unrewritten, per this project's own convention). `stringr`/`rlang` restored to TaxaExpect's DESCRIPTION Imports (both had exactly one real caller, `create_sites_from_grid()`'s `stringr::str_replace_all()`/`rlang::sym()`, dropped when that file was archived). `devtools::document()`/`test()`/`check()` re-verified clean on TaxaExpect, reinstalled; TaxaWizard's touched metadata JSON re-verified/reinstalled too. See `TaxaExpect/CLAUDE.md`'s later 2026-09-09 top session note for the full record and final numbers. |
| 2026-09-09 | *(create_sites_from_grid / compute_adaptive_sampling_groups FINAL re-archival, reversal of the row directly above)* | `TaxaExpect::create_sites_from_grid()` and `TaxaExpect::compute_adaptive_sampling_groups()` removed from the live package again; source + tests moved back from `R/`/`tests/testthat/` to `TaxaExpect/archive_glmm_prior_pipeline/{R,tests}/`, rejoining the rest of the GLMM/grid chain | TaxaExpect | archived, not deleted (DECIPHER-module retirement precedent); this is the THIRD flip for this pair in one day, and is the final decision -- not expected to reverse again | Reverses the restoration row directly above, for a reason distinct from either prior archival's "zero real callers" finding: the restoration's own stated justification (that `compute_adaptive_sampling_groups()` is a needed automatic fallback for external users who lack domain expertise to hand-classify `sampling_group`) was tested against real evidence and refuted. Real evidence gathered against the actual functions and a real, full-scale checkpoint (`PtCon18SSchulte_occurrences_clean.rds`, 1,375,345 real occurrence rows with a real, hand-built, expert 9-group `sampling_group` classification as ground truth): (1) `compute_adaptive_sampling_groups(min_n=100)` at three grid sizes (0.1/0.5/1.0 deg) produced 37/47/58 automatic groups against the expert's 10 -- the dominant real group (`macroinvertebrates`, 1.27M records, all genuinely detected by the same eDNA marker) was split into 28-39 separate automatic groups, because the algorithm's criterion (per-site record adequacy) answers a different question than what `sampling_group` means (shared detection process); (2) the algorithm collapsed two taxa the expert kept deliberately separate -- `parasites` (n=9, likely a genuine marine survey target) and `terrestrial_arthropods` (n=3, likely airborne contamination) -- into one `phylum:Arthropoda` group, purely because neither cleared `min_n` even at the phylum ceiling, exactly the "combining incommensurable detection methods" failure the mechanism exists to prevent; (3) grid-size sensitivity was confirmed non-trivial and counterintuitive -- a COARSER grid (1.0 deg) produced MORE automatic groups (58) than a finer one (0.1 deg, 37), since a coarser grid inflates apparent per-site density; (4) `estimate_kernel_priors(sampling_group_col="sampling_group")` was run directly on the real, UNMERGED 9-group expert classification (no merge step involved) and succeeded with zero errors for every group, including the tiniest (`terrestrial_arthropods` n_taxa=1, `parasites` n_taxa=3) -- degrading gracefully to appropriately wide `theta_sd` for sparse groups, directly refuting the premise that a sample-size-adequate merge step was ever necessary before calling the estimator; (5) a narrower, real, separate finding -- a single-TAXON group (not single-record) produces `theta_mean=1.0, theta_sd=0` for that taxon, mathematically correct but potentially misleading if read as "certain to be present" rather than "the only thing we have to compare it to" -- prompted a small, separate fix: `estimate_kernel_priors()` now emits a `cli::cli_warn()` for a single-taxon group, naming the group, without blocking the computation or changing its output. Cross-references reversed a third time: `TaxaExpect-package.R`'s `@section Spatial modelling`/`@section Archived`, `estimate_kernel_priors.R`'s `sampling_group_col` `@param` + internal code comment (both now state grouping must be built by hand, with no automated shortcut recommended), `TaxaWizard/inst/metadata/TaxaExpect.json`'s `sampling_group_col` description, `TaxaAssign/inst/workflows/camera_trap_posterior_workflow.R` and root `inst/TaxaID_Workflow_Template_TEST.R` (both re-corrected from "still live" back to archived), `TaxaExpect/inst/TaxaExpect_workflow.R` and `inst/workflows/generate_priors_workflow.R` (addenda corrected), `TaxaExpect/README.md` (the "Sampling-group utilities" Key Functions bullet removed entirely; the "Problematic mixing" paragraph rewritten to state plainly that grouping must be hand-built from real methodological knowledge, no automated or LLM-based shortcut exists, and that `estimate_kernel_priors()` does NOT require pre-merged, sample-size-adequate groups -- it produces honestly wide uncertainty for small groups on its own), `TaxaExpect/vignettes/building-priors.Rmd` (its sampling-group-correctness step rewritten to show a caller building `sampling_group` by hand via `dplyr::case_when()`, no grid step, straight into `estimate_kernel_priors(sampling_group_col=)`), `ecosystem_docs/ECOSYSTEM_WORKFLOW.md`'s superseded-pattern banner corrected back to "archived", `TaxaExpect/inst/taxaexpect_review_response.md` gained a third dated addendum on both functions' sections (prior addenda left unrewritten, per this project's own convention). `TaxaExpect::estimate_kernel_priors()` also gained the new single-taxon-group warning described above (Finding 5), with a new test. `stringr`/`rlang` dropped from TaxaExpect's DESCRIPTION Imports again (their only caller, `create_sites_from_grid()`, is archived again). See `TaxaExpect/CLAUDE.md`'s final 2026-09-09 top session note for the full evidence record and final `devtools::test()`/`check()` numbers. |
| 2026-09-09 | *(calibrate_coverage_filter / coverage_threshold retirement)* | `TaxaLikely::calibrate_coverage_filter()` and `TaxaLikely::coverage_threshold()` removed from the live package; source (`R/calibrate.R`) + test file (`tests/testthat/test-calibrate.R`) + dedicated demo workflow (`inst/workflows/calibration_demo.R`) moved intact to `TaxaLikely/archive_unused_coverage_calibration/{R,tests,inst/workflows}/` | TaxaLikely | archived, not deleted (DECIPHER-module/likelihood-entrypoint/reference-wrapper retirement precedent) | Decision evolved from "stays opt-in" (this same day, earlier session note) to "archived" after further discussion of the completed A/B evidence. A real A/B test on full-scale PtConception 12S data (`diagnostics/coverage_filter_ab_comparison.R`/`_v2.R`) found a real, reproduced H1 win-rate improvement (54.9%->65.1%, then 55.1%->65.8%) on queries the calibrated filter is willing to answer -- but also a real, quantified cost: 134 of 691 species (19.4%) lose every training pair at the calibrated threshold, and 25.1% of real evaluation queries end up unresolved rather than answered. This matches a pattern this ecosystem has independently hit and fixed twice before (`apply_coverage_constraints()`'s zero->relabel fix, `TaxaFetch::filter_gbif_quality()`'s exclude_institution->flag_institution fix): hard exclusion of data based on an imperfect quality proxy has real costs. Final reasoning for archiving now rather than keeping opt-in: the one piece of genuinely separable value these functions provide (a Youden's-J diagnostic on whether coverage is even predictive of pair quality in a given dataset) is a fairly standard statistic that doesn't need a dedicated exported function to access, and the exclusion-oriented framing around it is the specific part this ecosystem has already learned not to trust. The functions had zero real production adoption -- their only real callers anywhere were this package's own tutorial workflow scripts (`inst/workflows/sequence_likelihood_workflow.R`, `3_train_model_workflow.R`, and the dedicated `calibration_demo.R`), not any external production workflow. All three fixed: `sequence_likelihood_workflow.R` and `3_train_model_workflow.R` had their coverage-calibration steps removed (training now proceeds on the full, unfiltered `ref_matrix`; `4_score_to_likelihood_workflow.R` already handled a missing `coverage_threshold.rds` gracefully and needed only a comment update); `calibration_demo.R` moved into the archive alongside its own functions. Every roxygen cross-reference (`R/build_sequence.R`), `inst/review_function_inputs.R` (its Section 8 demo + several forward-reference comments), `README.md` (a whole "Reference Coverage Quality Filtering" section + 3 other mentions), and `inst/taxalikely_review_response.md` (a dated addendum appended to its existing `### calibrate.R` section, original review answers left unrewritten) updated. `ecosystem_docs/STATISTICAL_COMPONENT_CATALOG.md` (41->39 components, TaxaLikely 10->8, 2 rows removed) and `ecosystem_docs/STATISTICAL_COMPONENT_SOUNDNESS_REVIEW.md` (both removed rows had been sound=YES/priority L; ecosystem YES 6->4, Total 41->39, L 5->3) updated to match. `TaxaWizard/inst/metadata/TaxaLikely.json`'s two now-stale entries removed (both functions were metadata'd but never wired into any snippet or `workflow_graph.json` edge -- "diagnostic helpers with no graph edge", per that package's own prior session note). `TaxaLikely/CLAUDE.md`'s Function Inventory ("Coverage quality calibration" section) and internal-helpers table updated to "Archived"; a new top session note prepended (the prior "stays opt-in" note, with its own detailed A/B pros/cons analysis, is kept immediately below as the record of that analysis, not deleted). `.gitignore`/`.Rbuildignore` gained an `archive_unused_coverage_calibration` entry matching the existing three archive directories' convention. Two commented-out template pointers ("Optional: calibrate a coverage threshold...") also removed, one from `inst/TaxaID_Workflow_Template_TEST.R` (tracked in this repo) and one from the real external `~/My Drive/Rscripts/eDNA/PtConception/TaxaID_eDNA_Workflow_Template.R` (backed up first as `*.bak_pre_coverage_calibration_archive`, per this project's established convention for real external files not under git). `devtools::document()`/`test()`/`check()` re-verified clean on TaxaLikely, reinstalled. See `TaxaLikely/CLAUDE.md`'s top session note for the full record. |
