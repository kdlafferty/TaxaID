# TaxaID Ecosystem — Statistical Component Catalog

Generated by 9 parallel per-package cataloging agents (one per package), Session 2026-07-09.
Scope: every exported function across the ecosystem performing a statistical calculation —
probability estimation, prior/posterior combination, distributional assumption, weighting
scheme, hypothesis test, or a distance/similarity score used probabilistically.

This is a cataloging pass only. **No soundness judgments are made here** — that is a
separate, later pass (see `prior_validation` column for what has/has not already been
empirically checked, per project memory and code comments).

**39 statistical components found** across 9 packages (originally 41; 2 removed
2026-09-09 when `TaxaLikely::calibrate_coverage_filter()`/`coverage_threshold()` were
archived -- see `ecosystem_docs/NAME_CHANGE_HISTORY.md`):

| Package | Count |
|---|---|
| TaxaAssign | 15 |
| TaxaExpect | 2 |
| TaxaLikely | 8 |
| TaxaHabitat | 5 |
| TaxaMatch | 4 |
| TaxaFlag | 3 |
| TaxaFetch | 2 |
| TaxaTools | 0 |
| TaxaWizard | 0 |

TaxaTools and TaxaWizard were confirmed (not assumed) to contain zero qualifying
statistical components — TaxaTools is a shared-utilities package (name cleaning, LLM
plumbing, rank lookups) with no probabilistic content; TaxaWizard is an LLM-orchestration
package outside the TaxaID dependency chain. Both agents did full-file reads plus keyword
sweeps to confirm this rather than skipping the package.

**Metadata drift note (from TaxaWizard's agent):** `TaxaWizard/inst/metadata/TaxaAssign.json`'s
`join_priors` entry lists params `likelihoods_df, priors_df, grid_id, main_habitat, rank_system,
backbone_id` — four of these six names do not exist in the real signature at
`TaxaAssign/R/join_priors.R:608` (`join_priors(likelihoods, taxaexpect_priors, site = NULL,
taxonomy_lookup = NULL, rank_system = NULL, expansion_taxonomy = NULL, expansion_min_prior =
0.05, expansion_cumulative_prior = 0.90, singleton_taxonomy = NULL, backbone_id)`). Confirms
the drift already flagged in project memory ([[project_taxawizard_metadata_drift]]).

---

## TaxaAssign (15 components)

| package | function | location | stat_id | statistic_or_model | purpose | inputs | assumptions_embedded | existing_citation | prior_validation |
|---|---|---|---|---|---|---|---|---|---|
| TaxaAssign | compute_posterior | R/compute_posterior.R:99 (point-estimate block: 177-181) | TaxaAssign::compute_posterior::bayes_point_update | Discrete Bayes' rule (point-estimate posterior update: normalized likelihood × normalized prior, renormalized) | Estimates the posterior probability that each candidate taxon is the true source of an observation, given a match likelihood and an occurrence-based prior | `score_likelihood` and `prior_mean` per hypothesis row, both supplied upstream by `join_priors()`/`assign_taxa_llm()`; renormalized within each `observation_id` group | Hypotheses within an `observation_id` are mutually exclusive and exhaustive (posterior renormalized to sum to 1); likelihood and prior are combined as independent multiplicative factors (no interaction term); an all-zero likelihood/prior sum falls back to a uniform distribution over hypotheses rather than being left undefined | none found (roxygen states the Bayes formula but attributes it to no external source) | unvalidated |
| TaxaAssign | compute_posterior | R/compute_posterior.R:99 (Monte Carlo block: 183-266) | TaxaAssign::compute_posterior::mc_uncertainty_propagation | Monte Carlo uncertainty propagation via Normal-distributed likelihood draws and Beta-distributed prior draws, with a fixed-at-mean override when the Beta prior is J-shaped (alpha < 1) | Propagates uncertainty in both the sequence-match likelihood and the occurrence-based prior into a distribution over each candidate's posterior probability, yielding `posterior_sd` and `confidence_score` | `score_likelihood_mean`/`score_likelihood_sd` (TaxaLikely or LLM likelihood proxy) and `prior_alpha`/`prior_beta` (from `join_priors()`/`assign_taxa_llm()`); `n_sims` | Likelihood uncertainty is Normal(mean, sd) floor-clamped at 0 (not a true truncated-normal density); prior uncertainty is Beta(alpha, beta) except when alpha < 1 (J-shaped, simulation disabled in favor of a fixed point value); all-zero likelihood/prior mass columns treated as uninformative (uniform fallback) | none found | unvalidated (J-shaped-Beta override is a reasoned fix for a diagnosed artifact, not empirically validated) |
| TaxaAssign | combine_multisite_priors | R/combine_multisite_priors.R:148 (core weighting in `.combine_one_multisite_group`: 31-59) | TaxaAssign::combine_multisite_priors::precision_weighted_logit_combination | Precision-weighted (inverse-variance) fixed-effect combination in logit space, using exact mean/variance of logit(Beta(a,b)) via its Gamma-ratio representation, with large-phi delta-method approximation to recover combined concentration | Combines multiple sites' independent Beta occurrence priors for the same candidate taxon into one pooled prior, discounting low-data/high-uncertainty sites | `prior_alpha`/`prior_beta` per (`observation_id`,`taxon_name`,`taxon_name_rank`,`grid_id`) row, from `join_priors()`'s site-preserving output | Sites are ecologically independent (no shared detection process); only first two moments (logit mean/variance) of each Beta are combined, not full density; combined phi recovered via large-phi delta-method approximation that degrades for small phi | none found (Gamma-ratio representation described but no external source cited) | **Validated** — Session 138 worked comparison quoted in roxygen: plain-product rule gave the true candidate only 72.7% of combined mass; full Monte Carlo simulation shifted weight toward the noisier site's minority pick (69.2%); precision-weighted logit combination gave 78.5%. See [[project_precision_weighted_multisite_combination]] |
| TaxaAssign | posterior_consensus | R/posterior_consensus.R:233 (`.consensus_one_observation`: 403-425) | TaxaAssign::posterior_consensus::credible_set_lca | Minimal cumulative-mass credible set (Bayesian-credible-interval analog) combined with lowest-common-ancestor (LCA) taxonomic reduction | Determines the smallest set of top-ranked candidates jointly accounting for a threshold fraction of posterior mass, reports their taxonomic LCA as consensus | `posterior_mean` per hypothesis row from `compute_posterior()`/`assign_taxa_llm()`; `cumulative_threshold`, `min_posterior` | `min_posterior` filtering and `cumulative_threshold` selection applied sequentially, not independently; posterior mass summed directly as a proper probability distribution; taxonomic ranks assumed nested/hierarchical for LCA walk | "Lowest Common Ancestor is the standard conservative consensus method in molecular systematics (Huson et al., 2007, MEGAN)." | Sensitivity characterized empirically (Session 144, real 3,000-obs PtConception 12S subsample): min_posterior does real, roughly linear work (+8.2 resolution points 0→0.20); cumulative_threshold does comparatively little independent work. Explicitly caveated as resolution-RATE, not accuracy — no ground-truth-validated set available |
| TaxaAssign | score_consensus | R/score_consensus.R:147 (`.cap_rank_by_threshold`: 348-394) | TaxaAssign::score_consensus::rank_threshold_capping | Fixed percent-identity score thresholds per taxonomic rank (empirically-derived decision rule), auto-rescaled between 0-100/0-1 | Caps a sequence-match consensus taxon at the finest rank whose conventional score threshold the top hit meets | `score_original`/`score_col` per hit; `rank_thresholds` named vector (default GITA/Jonah Ventures values); LCA taxon | Single global threshold per rank applies uniformly across taxa/markers (no per-clade calibration); scale inferred solely from whether observed max score exceeds 1; thresholds are fixed, not fit to the dataset | "Default c(species=98, genus=95, family=90, phylum=85) — the conventional GITA/Jonah Ventures thresholds (fourth tier corrected order->phylum 2026-07-20; see AQUARIUM_BENCHMARK_DESIGN.md)" | **Validated (2026-07-09, Session 147):** ROC-style sweep against real 12S reference data (`diagnostics/score_floor_roc_sweep.R`) found raw percent-identity alone "cannot discriminate a true species from its closest congener at almost any real-world threshold" — see [[project_seqmatrix_diagnostic_uses]], [[project_framing_B_verdict]] |
| TaxaAssign | join_priors | R/join_priors.R:608 (fallback construction: 885-972) | TaxaAssign::join_priors::dark_diversity_floor | Mean-based Beta-parameter fallback prior (arithmetic mean of alpha/beta across Tier-3-undetected reference rows, site-then-global fallback hierarchy) | Assigns a non-zero occurrence prior to referenced species with no site-specific TaxaExpect prediction ("dark diversity" / unobserved taxa) | `alpha`/`beta`/`undetected_type` from `taxaexpect_priors` (TaxaExpect::estimate_kernel_priors() output), grouped by `grid_id` × `main_habitat` | Arithmetic mean of Beta params across undetected/global-floor rows treated as representative (implicitly exchangeable unmodelled species within site/habitat); `global_floor` row assumed distinct in kind from `singleton_mirror` rows | none found | unvalidated |
| TaxaAssign | join_priors | R/join_priors.R:608 (floor promotion: 974-995) | TaxaAssign::join_priors::singleton_floor_promotion | Floor/censoring rule using mean of Beta(singleton_alpha, singleton_beta) across singleton-mirror rows as a lower bound on a modelled species' `prior_mean` | Prevents a species with a poorly-fit site model from scoring below what an entirely unobserved species would get | `alpha`/`beta` (primary join) and `singleton_alpha`/`singleton_beta` (mean Beta of singleton-mirror rows) per `grid_id` × `main_habitat` | Mean detection rate among once-observed species assumed the correct floor for a modelled species' true theta; below-floor cases assumed model artifact (habitat mismatch), not genuine rarity | none found | unvalidated |
| TaxaAssign | join_priors | R/join_priors.R:608 (`.compute_dark_diversity_groups`: 263-436, invoked 1003-1109) | TaxaAssign::join_priors::hierarchical_dark_diversity_groups | Mass-conserving hierarchical (phylum→class→order→family→genus) recursive Beta group-prior construction; phi = effective_singletons × singleton_ess | Distributes fixed total "dark diversity" mass among unmodelled taxa by shared ancestry with observed singletons, so total mass scales with singleton count rather than candidate count | `unref_df`, `singleton_df` (joined via `source_taxon_name`), `global_singleton_mean`, `singleton_ess` (default 2) | Zero-singleton sub-clades pooled into one shared-budget group; ≥1-singleton sub-clades subdivide further; genus is always terminal grouping rank; phi is a simple product, not data-estimated; unknown-phylum candidates never pooled together | none found | unvalidated |
| TaxaAssign | join_priors | R/join_priors.R:608 (`.expand_coarse_rank_rows`: 42-227, invoked 847-883) | TaxaAssign::join_priors::coarse_rank_expansion_credible_set | Normalized-prior cumulative-mass credible set selection (same threshold logic as `posterior_consensus()`, applied prospectively to prior mass) | Expands a coarse-rank-only likelihood row into the fewest top species-level hypotheses jointly accounting for `expansion_cumulative_prior` of the constrained prior mass | `alpha`/`beta` of species-level rows in `taxaexpect_priors` matching the coarse-rank constraint; `expansion_min_prior`, `expansion_cumulative_prior` | Prior mass among same-coarse-rank species renormalized and treated as full candidate space, ignoring species outside `taxaexpect_priors` coverage; `score_likelihood` inherited unchanged from template row (uninformative post-normalization) | none found | unvalidated |
| TaxaAssign | assign_taxa_llm | R/assign_taxa_llm.R:284 (`.score_to_likelihood`: 559-686) | TaxaAssign::assign_taxa_llm::score_to_likelihood_softmax | Exponential score-weighting (softmax-like transform), `exp(score_sharpness * score)`, rescaled so named candidates share `(1 - unknown_lik_weight)` of total mass | Substitutes a tunable proxy for a calibrated sequence-match likelihood in the LLM-shortcut pipeline | `score_original` per candidate row (from `match_df`); `score_sharpness`, `unknown_lik_weight` | Likelihood assumed to scale exponentially with raw score, an arbitrary functional form not derived from calibration; unreferenced congeners/family members assigned median exponentiated score of referenced congeners/family; fixed `unknown_lik_weight` regardless of named-candidate count | none found | Sensitivity-tested (Session 145, one real LLM response), not accuracy-validated: score_sharpness had almost no effect (0.9922→0.9936 across full range) |
| TaxaAssign | assign_taxa_llm | R/assign_taxa_llm.R:284 (`.merge_llm_priors`: 699-793, Beta construction 775-787) | TaxaAssign::assign_taxa_llm::beta_prior_construction | Beta(alpha,beta) construction from LLM-elicited point estimate (`prior_mean`) and categorical concentration mapping keyed by LLM `information_quality` (high/moderate/low) | Converts LLM point-estimate prior weight into a full Beta usable by `compute_posterior()`'s MC path, LLM's self-reported confidence controlling concentration | `prior_mean`, `information_quality` per taxon; `prior_phi` named vector (default c(high=50, moderate=10, low=3)) or scalar override | LLM's categorical self-assessment assumed to map linearly onto a fixed pseudo-observation count; taxa omitted from LLM response default to "low" rather than flagged missing; flat scalar phi treated as acceptable substitute for tiered mapping | none found | Sensitivity-tested (Session 145), not accuracy-validated: flat scalar phi (5-80) gave statistically same resolution/confidence as tiered default; only phi 2-3 noticeably hurt — described as "a real, open question... not yet resolved" |
| TaxaAssign | assign_taxa_llm | R/assign_taxa_llm.R:284 (`.merge_llm_priors`: 699-793, suppression 754-765) | TaxaAssign::assign_taxa_llm::absence_suppression_bayesian_update | Bayesian absence-suppression update: `prior_mean *= (1 - detection_prob)` for known-absent species, then renormalize | Downweights the prior of a species surveyed for but not detected, proportional to survey detection power | `known_absent` (vector or df with `taxon_name`/`detection_prob`), `absent_detection_prob` fallback | Non-detection treated as conditionally independent evidence multiplying the prior; single scalar detection probability assumed to summarize survey power uniformly; only already-present hypotheses affected | none found (roxygen derives the update rule but cites no external source) | Tested only via disclosed synthetic overlay (Session 145, no real workflow supplies `known_absent`): "no meaningful aggregate effect... weakest of the four... not a validated finding either way" |
| TaxaAssign | update_prior_from_consensus | R/update_prior_from_consensus.R:96 (boost application: 201-219) | TaxaAssign::update_prior_from_consensus::presence_multiplier_empirical_bayes | One-pass empirical Bayes refinement via fixed multiplicative prior boost (`presence_multiplier`, default 5) for species confirmed present elsewhere, then renormalize and rerun `compute_posterior()` | Uses confident species-level assignments from other observations sharing a spatial group as indirect evidence to sharpen unresolved observations' priors | `consensus_taxon`/`is_resolved` from `posterior_consensus()`; `prior_mean` per hypothesis row; `presence_multiplier`; optional `spatial_group_map` | Confirmed detection at one observation raises plausibility of the same species at any other observation sharing a spatial group (site-level exchangeability); fixed multiplicative boost applied regardless of confirmation count/confidence; only unresolved observations updated (avoids circularity) | none found (roxygen disclaims: "A formally correct alternative would be a Beta-Binomial hierarchical model... the multiplier approximation is adequate in practice") | unvalidated |
| TaxaAssign | run_bayesian_pipeline | R/run_bayesian_pipeline.R:123 (consistency checks: 220-269) | TaxaAssign::run_bayesian_pipeline::data_consistency_threshold_check | Proportion-based data-consistency heuristic (genus-overlap fraction, habitat-prior-coverage fraction), each flagged via fixed 0.5 threshold | Flags likely input mismatches (e.g. `match_df`/`taxaexpect_priors` from different datasets/regions) before running the full pipeline | `genus` columns of `match_df` and `taxaexpect_priors`; `theta_mean`/`prior_mean` at resolved `grid_id` × `main_habitat` | Fixed 50% overlap/coverage threshold assumed to separate "consistent" from "inconsistent" regardless of study system/richness/habitat granularity; case/whitespace-normalized string matching assumed sufficient | none found | unvalidated |
| TaxaAssign | adjust_inat_range_priors | R/adjust_inat_range_priors.R:55 (elevation logic: 119-151) | TaxaAssign::adjust_inat_range_priors::singleton_floor_elevation | Deterministic threshold-gated substitution: replaces `prior_alpha`/`prior_beta` with singleton-mirror floor for unmodelled taxa confirmed `in_range=TRUE` at/above an observation-count threshold | Elevates a BLAST-detected candidate's occurrence prior when iNaturalist range-model evidence corroborates presence, treating it as equivalent to an observed singleton | `prior_alpha`/`prior_beta`/`prior_mean`, `singleton_alpha`/`singleton_beta` (both from `join_priors()`); `in_range`, `n_observations` (from `TaxaFetch::check_inat_range()`); `n_obs_threshold` (default 500) | iNaturalist evidence treated asymmetrically: `in_range=TRUE` informative, `in_range=FALSE` assumed uninformative (low observer effort, not absence); fixed n_observations≥500 threshold assumed to make the polygon reliable regardless of taxon/region; range-supported-but-unmodelled species assumed statistically equivalent to an observed singleton | none found | unvalidated |

## TaxaExpect (2 components)

| package | function | location | stat_id | statistic_or_model | purpose | inputs | assumptions_embedded | existing_citation | prior_validation |
|---|---|---|---|---|---|---|---|---|---|
| TaxaExpect | generate_undetected_diversity | R/generate_undetected_diversity.R:191 | TaxaExpect::generate_undetected_diversity::singleton_mirror_beta | Fixed-ESS moment-matching: alpha = theta_obs*ESS, beta=(1-theta_obs)*ESS, from a single observed detection rate (1/N) | Estimates a diffuse "unobserved-taxa" detection prior for an anonymous undetected-species proxy inherited from each once-detected species | `model_obj$singletons` (from `estimate_kernel_priors()`, or a fitted biodiversity model object); `singleton_ess` (default 2) | Single detection treated as representative of true theta; small fixed ESS produces sufficient diffuseness; independence across singleton mirrors | none found | unvalidated |
| TaxaExpect | generate_undetected_diversity | R/generate_undetected_diversity.R:219 | TaxaExpect::generate_undetected_diversity::global_floor_beta | Conjugate Beta-Binomial posterior update: uniform Beta(1,1) prior + 0 successes in N_total-1 trials → Beta(1,N_total-1); Jeffreys Beta(0.5,0.5) fallback when N_total < jeffreys_threshold | Establishes a minimum-probability "undetected species" competitor for TaxaAssign | `model_obj$N_total` (pooled community count); `jeffreys_threshold` | Beta-Binomial conjugacy; exchangeability of all undetected species; effort pooled across all taxa regardless of taxonomic group (documented "known limitation" diluting the floor when group sizes differ; `estimate_kernel_priors(sampling_group_col=)` addresses this by stratifying `N_total`/`N` per detection-process group) | none found | unvalidated |

## TaxaLikely (8 components; 2 rows removed 2026-09-09, see below)

| package | function | location | stat_id | statistic_or_model | purpose | inputs | assumptions_embedded | existing_citation | prior_validation |
|---|---|---|---|---|---|---|---|---|---|
| TaxaLikely | train_likelihood_model | R/train.R:458 | TaxaLikely::train_likelihood_model::eb_bivariate_normal | Empirical Bayes shrinkage (James-Stein/Efron-Morris type, w=N/(N+prior_weight)) fitting a per-species 2D Gaussian over (score_logit, gap_logit), optional lme4 random-intercept hierarchy + synthetic-anchor pseudo-data | Estimates each species' expected within-species match-score distribution (H1 "known species present" generative model) | `raw_df` pairwise reference-vs-reference scores from `build_sequence_matrix()`; `prior_weight`, `min_observed_sigma`, `anchor_perfect`, `mislabel_threshold` | Linear shrinkage of Normal means/variances toward a global mean; logit-normality of transformed percent-identity scores; independence of within-species pairs; exchangeability of species-level means; shared global covariance (Sigma) across species | Efron & Morris (1973), JASA 68(341); Somervuo et al. (2017), Bioinformatics 33(19); Genz et al. (2023) mvtnorm; Hebert et al. (2003), Proc R Soc B 270(1512) | unvalidated (anchoring/gap-ceiling values are heuristics per code comments; no stated empirical fit check) |
| TaxaLikely | evaluate_likelihoods | R/evaluate.R:610 (`.evaluate_one_query()`) | TaxaLikely::evaluate_likelihoods::h1h2h3_bivariate_likelihood | Bivariate/univariate normal density (`mvtnorm::dmvnorm`/`stats::dnorm`) over (score_logit, gap_logit) for H1/H2/H3 hypotheses, chi-squared(df=1) Mahalanobis outlier gate, optional MC simulation | Converts a match score into a likelihood ratio for known-species vs. unreferenced-species vs. unreferenced-genus | `match_df`, `model_params` from `train_likelihood_model()`; `alpha`, `ratio_threshold`, `min_match_threshold`, `n_sims`, `min_coverage` | Score well-approximated by logit-Normal; H1 sigma floored at global sigma; H2/H3 means are fixed logit-offsets from H1 mean; independence of MC draws; outlier test uses score-only chi-squared; coverage-driven sigma inflation follows SE(logit(p))∝1/sqrt(N) binomial argument | Somervuo et al. (2017); Efron & Morris (1973) | unvalidated (sigma floor / alpha=0.001 adjusted via "informal inspection of specific real cases" per CLAUDE.md, not a formal study) |
| TaxaLikely | assign_scores | R/assign_scores.R:103 | TaxaLikely::assign_scores::softmax_ratio_normalization | Per-observation ratio-normalization to winning candidate ("probability"), exp(sharpness*score_norm) softmax ("similarity_softmax"), median-of-congener anchoring for H2/H3 | Converts single-best or multi-candidate classifier score into a likelihood ratio comparable across named/unreferenced hypotheses | `hypotheses_df` from `unreferenced_candidates()`; `score_type`, `score_col`, `score_sharpness`; genus/family columns | Winning candidate likelihood always exactly 1.0 by construction; H2/H3 likelihoods approximated by median H1 likelihood of same-genus/-family candidates; score scale auto-detected from global max and assumed constant | none found | unvalidated (CLAUDE.md documents a real scale-autodetection bug found/fixed via informal photo-set comparisons, not a designed study) |
| TaxaLikely | correct_training_bias | R/correct_training_bias.R:202 | TaxaLikely::correct_training_bias::logit_adjustment | Long-tail "logit adjustment": `score_i / n_i^tau`, fixed global-exponent power-law rescaling by training-database representation count | Debiases classifier scores confounded by species' training-set frequency (score_i ∝ L(obs\|species_i) × n_i) | `scored_df`; `count_col` (e.g. iNat n_observations, Xeno-canto n_recordings); `score_col`; `tau` | Classifier output approximates a Bayes posterior under cross-entropy training; n_i (external DB count) is a valid stand-in for true training frequency; one global tau applies uniformly; missing/zero counts get no correction | Menon et al. (2021), "Long-Tail Learning via Logit Adjustment," ICLR 2021 | Explicitly flagged inconclusive: Session 128/129 (image: tau≈0 optimal), Session 133 (acoustic: pooled tau≈0, but 2/8 clusters prefer high tau) — "no current real-data evidence supports tau>0 as a default for either data type" |
| TaxaLikely | detect_suppressed_candidates | R/score_collapse.R:96 | TaxaLikely::detect_suppressed_candidates::purity_threshold_rule | Proportion/purity-threshold rule (fraction exhibiting score-tie uniformity or singleton rate vs. fixed cutoffs) | Diagnoses whether an upstream pipeline silently suppressed lower-scoring candidates (100%-only, tied-top-only, or top-1-only rules) | `match_obj` (score_original, observation_id); `perfect_threshold`, `purity_threshold`, `singleton_threshold` | Fixed purity/singleton proportion (0.99/0.98 default) assumed to reliably distinguish suppression from natural sparsity; exact numeric ties (round(s,8)) treated as evidence of enforced suppression rather than coincidence; observation-level independence assumed | none found | unvalidated |
| TaxaLikely | restore_suppressed_candidates | R/score_collapse.R:274 | TaxaLikely::restore_suppressed_candidates::fixed_offset_imputation | Fixed-offset score imputation: `max_score_in_obs − delta` (auto-scaled), or `1.0 − delta/100` in the synthetic no-score pathway | Estimates a plausible suppressed score for same-genus congeners omitted by a top-hit-only rule | `match_obj`; `reference_df`; `detected` (from `detect_suppressed_candidates()`); `delta`; `max_per_obs` | Single fixed delta gap (0.5pt/0.005) assumed reasonable across all taxa/observations; same-genus congeners assumed the relevant suppressed set (ignores same-family/higher); imputed value deterministic, not drawn from an estimated distribution | none found | unvalidated |
| TaxaLikely | apply_coverage_constraints | R/coverage.R:1442 | TaxaLikely::apply_coverage_constraints::completeness_penalty_weight | Deterministic multiplicative penalty (`score_likelihood * penalty_factor`, default 0) or categorical relabeling of "unreferenced_species" hypothesis, gated on genus/taxon complete/closed status flag | Suppresses/reclassifies the likelihood of "unreferenced species in a represented genus" when independent census evidence indicates the genus is fully sampled | `likelihood_df` from `evaluate_likelihoods()`; `census_result` (from `audit_barcode_coverage()`); `penalty_factor`, `constraint_behavior` | Genus census treated as a hard/binary constraint rather than a probabilistic completeness estimate; `census_result$status` assumed reliable ground truth; default penalty_factor=0 implies certainty no unsampled species exists in a "complete" genus | none found | unvalidated |
| TaxaLikely | build_sequence_matrix | R/build_sequence.R:90 | TaxaLikely::build_sequence_matrix::pairwise_distance_to_match | Pairwise sequence-distance-to-similarity conversion (`p_match = 1 − DECIPHER::DistanceMatrix()` distance) + alignment-overlap "coverage" statistic, sparsified by `max_dist` cutoff | Produces raw within-/cross-species similarity scores + per-pair coverage that `train_likelihood_model()` fits H1 distributions to | `reference_df` (composite_id, sequence, taxonomy); `max_dist`, `min_seq_len`, `max_seq_len`, `filter_unnamed`, `max_seqs_per_taxon` | DECIPHER's MSA-based distance assumed a valid true-similarity proxy; fixed max_dist=0.25 (~75% identity) assumed universal across markers/taxa; coverage (shared aligned length/shorter original length) assumed a meaningful reliability proxy | none found | unvalidated |

## TaxaHabitat (5 components)

| package | function | location | stat_id | statistic_or_model | purpose | inputs | assumptions_embedded | existing_citation | prior_validation |
|---|---|---|---|---|---|---|---|---|---|
| TaxaHabitat | assign_habitat_biological | R/assign_habitat_biological.R:120 | TaxaHabitat::assign_habitat_biological::weighted_consensus_vote | Weighted-sum proportional vote with plurality (argmax) threshold decision rule over per-species categorical habitat-weight vectors | Estimates the most probable habitat category for a spatial sampling point from pooled species habitat affinities | Occurrence data (point_id, taxon_name); habitats_df weights from `parse_hierarchical_habitat_response()`; optional per-point record counts | Species contribute independently/additively (no interaction/competition terms); default equal-weighting assumes record abundance is uninformative of true dominance (sampling-effort bias rationale documented); weights treated as exchangeable across species when weight_by_abundance=FALSE; single dominant category assumed meaningful for mixed assemblages | none found | unvalidated |
| TaxaHabitat | consensus_habitat | R/assign_habitat_biological.R:489 | TaxaHabitat::consensus_habitat::assemblage_consensus_proportion | Equal-weight column-sum normalization to proportions with argmax/threshold decision rule, pooled across an entire assemblage | Estimates a single consensus habitat + modal ecoregion for an entire species assemblage | habitats_df from `parse_hierarchical_habitat_response()` (optional ecoregion_best_guess column) | Each species contributes equally regardless of abundance/detection frequency; independence across species; single plurality category assumed representative of mixed assemblage; modal ecoregion guess assumed most reliable, no confidence weighting | none found | unvalidated |
| TaxaHabitat | flag_habitat_inconsistencies | R/flag_habitat_inconsistencies.R:131 | TaxaHabitat::flag_habitat_inconsistencies::spatial_plausibility_flag | Deterministic distance/depth-threshold classifier assigning a 3-level ordinal confidence label (likely/questionable/unlikely) from point-to-coastline distance + bathymetric depth bins | Estimates whether an occurrence's assigned habitat realm is physically plausible given location, to flag georeferencing errors | Occurrence data with lat/lon + main_habitat (from `assign_habitat_biological()`); Natural Earth coastline polygons; NOAA GEBCO bathymetry | Fixed thresholds (coast_buffer_m=1000, depth_neritic_m=200, depth_oceanic_m=4000) applied uniformly regardless of true habitat-boundary variability; freshwater habitats always given the most lenient flag; hard rule with no propagated uncertainty | none found (roxygen calls thresholds "standard oceanographic" but cites no source) | unvalidated |
| TaxaHabitat | parse_hierarchical_habitat_response | R/parse_habitat_response.R:100 | TaxaHabitat::parse_hierarchical_habitat_response::weight_sum_validation | Categorical (simplex) probability-vector validation + argmax decision — checks each species' elicited habitat-weight vector sums to 1.0 within 0.05 tolerance, assigns argmax as best-guess | Validates/structures an LLM's per-species habitat-weight elicitation into a categorical probability-like distribution + point-estimate label | Raw LLM response to `build_habitat_prompt()`; taxon_list; habitat_scheme | Weights treated as a proper probability simplex despite being LLM-elicited judgments, not fitted/estimated probabilities; no renormalization even when sum deviates from 1 (documented); argmax ties broken by column order (arbitrary) | none found | unvalidated |
| TaxaHabitat | report_habitat | R/report_habitat.R:45 | TaxaHabitat::report_habitat::mean_dominant_habitat | Arithmetic mean of per-species/row habitat weight columns, argmax over column means | Estimates assemblage-level "dominant" habitat category + average weight for reporting | habitat_data — output of `assign_habitat_biological()`/`parse_hierarchical_habitat_response()` | Equal weighting of every row/species in the mean (no detection-effort/abundance accounting); per-column mean weight treated as directly interpretable as a population-level proportion; independence across rows | none found | unvalidated |

## TaxaMatch (4 components)

| package | function | location | stat_id | statistic_or_model | purpose | inputs | assumptions_embedded | existing_citation | prior_validation |
|---|---|---|---|---|---|---|---|---|---|
| TaxaMatch | score_image_inat | R/score_image_inat.R:101 | TaxaMatch::score_image_inat::geo_prior_weight | Ratio decomposition of combined score into vision-likelihood × geographic-prior factors: `combined_score / vision_score` | Estimates the implicit geographic occurrence prior weight iNaturalist's geomodel applies to a candidate taxon at given lat/lon | `combined_score`, `vision_score` from the external iNaturalist CV API; caller/EXIF lat/lng | Assumes combined_score is a multiplicative product of vision likelihood × geographic prior; assumes vision_score>0 (else NA); treats iNat's geomodel as an unspecified black box; independence between per-image API calls | Roxygen: "combined_score approximates a Bayesian posterior by weighting vision_score by the geomodel prior... Neither score is a true likelihood in the Bayesian sense" | unvalidated (docs explicitly caveat this is an approximation) |
| TaxaMatch | blast_sequences | R/blast.R:121 | TaxaMatch::blast_sequences::score_window | Order-statistic proximity filter (retain-within-fixed-range-of-max) applied to percent-identity scores via `.filter_blast_hits()` | Estimates which BLAST hits are plausible taxonomic candidates vs. too dissimilar | Per-hit pident, qcovs, slen; user params score_range, min_score, min_query_coverage, max_hits | Top hit's pident assumed a reliable anchor for a fixed-width tolerance band; flat non-adaptive tolerance (default 2pts) assumed appropriate regardless of absolute identity/marker; each query's window independent (no cross-query pooling) | Roxygen: "the 70% threshold is a conventional cross-genus floor for DNA barcoding; most true species-level matches exceed 95%" | unvalidated (documented as design convention; sibling-package diagnostic — CLAUDE.md Session 147 score_floor_roc_sweep.R — found raw %identity alone cannot discriminate species from closest congener at almost any threshold, a critique performed elsewhere, not inside this function) |
| TaxaMatch | read_animl_output | R/read_image.R:140 | TaxaMatch::read_animl_output::bbox_coverage | Bounding-box area fraction (width×height) used as an image-quality/detection-confidence proxy, analogous to BLAST query coverage | Estimates how much of the frame the detected animal crop occupies, as a quality-weighting signal for downstream filtering | MegaDetector bbox width/height columns (normalized 0-1); consumed by `TaxaLikely::evaluate_likelihoods(min_coverage=...)` | Bbox area fraction assumed a valid proxy for classification confidence (larger=more reliable); assumes distance/pose/lighting-invariance of that relationship; a geometric heuristic, not fitted/distributional | Roxygen: "A small fraction = distant/partial animal = weaker classification evidence. Analogous to BLAST qcovs" | unvalidated |
| TaxaMatch | add_lowest_consistent_rank | R/taxonomy_consistency.R:92 | TaxaMatch::add_lowest_consistent_rank::majority_fraction | Plurality-vote consensus proportion with fixed-threshold decision rule ("majority mode") | Estimates degree of taxonomic agreement across multiple candidate hits per observation, deciding finest trustworthy rank | Per-observation candidate rank-column values from `standardize_match_data()`; `majority_threshold` | Every candidate hit an equally-weighted, exchangeable "vote" with no weighting by match score/sequence quality/evolutionary distance; non-blank values assumed directly comparable categorical labels; one fixed global threshold regardless of candidate count per observation; no independence check between rows | none found | unvalidated |

## TaxaFlag (3 components)

| package | function | location | stat_id | statistic_or_model | purpose | inputs | assumptions_embedded | existing_citation | prior_validation |
|---|---|---|---|---|---|---|---|---|---|
| TaxaFlag | flag_contaminant | R/flag_contaminant.R:105 (`.compute_contaminant_scores()`: 257) | TaxaFlag::flag_contaminant::contaminant_score | Ratio-of-means proportion score: `mean_prop_field / (mean_prop_field + mean_prop_control)`, unweighted two-group relative-abundance ratio | Estimates the probability a field detection reflects true environmental presence rather than lab/reagent/cross-well contamination | Long-format read-count df; `control_samples`/`sample_type_col`/`control_types`; internal within-sample proportion averages across control_ids and field_ids | Independence across samples; within-sample read proportion an unbiased estimate of true relative abundance; unweighted arithmetic mean across replicates (no variance/replicate-count weighting); zero-control taxa get ceiling 1.0, zero-field taxa get floor 0.0, independent of sample size (no small-n shrinkage) | none found | unvalidated |
| TaxaFlag | flag_handler | R/flag_handler.R:58 | TaxaFlag::flag_handler::edge_proximity_score | Piecewise-linear temporal decay: `clamp(minutes_to_nearest_edge / interval_minutes, 0, 1)` | Estimates likelihood a detection is a human/equipment-handling artifact vs. genuine wildlife, based on temporal proximity to deployment start/end | df with datetime_col, optional group_col defining per-station min/max timestamps, interval_minutes, optional handler_taxa whitelist | Linear (not exponential/fit) decay between elapsed time from sampling edge and artifact probability; start/end edges treated symmetrically; one fixed interval_minutes threshold applies uniformly regardless of true handler-activity duration; non-whitelisted taxa within the interval forced to 1.0 | none found | unvalidated |
| TaxaFlag | add_posthoc_assessment | R/add_posthoc_assessment.R:95 | TaxaFlag::add_posthoc_assessment::tier_likelihood_classification | Fixed-threshold classification of ratio-normalized winner-likelihood crossed with categorical prior-establishment tier in a 3×2 lookup table | Classifies each consensus call's plausibility by combining sequence-match evidence strength with a priori taxon expectedness at that location | `consensus_df` from `TaxaAssign::posterior_consensus()` (winner_likelihood, consensus_taxon/rank); `tiers` from TaxaExpect's prior model (tier1/tier2/tier3_undetected); `likelihood_threshold` (default 0.5) | Within-observation best-hypothesis-normalized likelihood ratio treated as directly comparable across all observations; continuous ratio dichotomized at one fixed uncalibrated threshold (0.5); taxa absent from tiers lookup conservatively assumed tier2 rather than flagged missing; tier lookups valid only at one designated finest_rank (species), coarser-rank rows treated as uninformative regardless of proximity to species rank | Roxygen: "winner_likelihood is ratio-normalised within each observation (best hypothesis=1.0). A value below likelihood_threshold (default 0.5) means another hypothesis had more than twice the sequence-match density as the winner" | unvalidated |

## TaxaFetch (2 components)

| package | function | location | stat_id | statistic_or_model | purpose | inputs | assumptions_embedded | existing_citation | prior_validation |
|---|---|---|---|---|---|---|---|---|---|
| TaxaFetch | get_keys_from_context | R/get_keys_from_context.R:61 (`.recover_higherrank()`: 229, lines 260-262) | TaxaFetch::get_keys_from_context::nubkey_consensus | Plurality (mode) vote across `rgbif::name_lookup()` hits (`table()` + `which.max()`) | Estimates the single "correct" GBIF backbone key when initial `name_backbone()` resolution jumped too many rank levels | `rgbif::name_lookup(query=target_name, rank=target_rank)$data$nubKey`; gated on matchType=="HIGHERRANK" and rank-distance check | Exchangeability of checklist-dataset hits (no source-authority weighting); independence between hits (no correlated/duplicate-checklist correction); majority-equals-correct with no minimum-count/margin requirement | none found | unvalidated |
| TaxaFetch | fetch_dataone_occurrences | R/dataone_standardize.R:215 (`.process_one_dataset()`: 1001, lines 1188-1198) | TaxaFetch::fetch_dataone_occurrences::site_centroid_mean | Arithmetic mean coordinate pooling (centroid estimation) | Estimates a representative lat/lon for an EML site code with multiple recorded coordinate entries | `meta$sites$decimalLatitude`/`decimalLongitude` grouped by site_code, from `.extract_eml_sites()` | Duplicate coordinate records treated as independent, equally-weighted replicates of one true site location; planar (Euclidean) averaging despite spherical geometry (no geodesic correction); no retained dispersion measure | none found | unvalidated |

## TaxaTools (0 components)

Confirmed via full read of all 23 R files + NAMESPACE cross-check + two keyword sweeps
(probability/prior/posterior/weight/score/distance/threshold terms; distribution-function
terms). No exported function performs a probability estimate, prior/posterior combination,
distributional assumption, weighting/combination scheme, hypothesis test, or a
probabilistically-used distance/similarity score. Candidates considered and excluded:
`verify_taxon_names()`'s `score` column (pass-through from external Global Names Verifier
API), `census_genus_species()`'s deterministic status classification, `resolve_barcode_lengths()`'s
deterministic min/max union, `token_usage()`'s arithmetic ledger sums, internal
`.summarize_numeric()`/`.summarize_data_frame()` (non-exported, descriptive text formatting only).

## TaxaWizard (0 components)

Confirmed via full read of all 12 R files + NAMESPACE cross-check. All 5 exported functions
(`annotate_script`, `workflow_app`, `workflow_create`, `workflow_engine`, `workflow_fix`) do
LLM-prompt assembly, JSON/graph parsing, script templating, workflow-DAG path search, and
Shiny UI generation — no probability estimation,
prior/posterior combination, distributional assumption, weighting scheme, hypothesis test, or
probabilistic distance/similarity score anywhere in the package or its internal helpers.
`.estimate_scaling()` (R/trial.R, `@noRd`) computes linear/quadratic runtime projections, not a
statistical model of data, and is not exported.
